# Private Ledger decision log

Last reviewed: 2026-07-25

Entries are append-only. A superseding decision must reference the earlier entry rather than rewriting its history.

## D-001 — Local-first synthetic development

- Date: 2026-07-24
- Status: Accepted
- Decision: Complete local implementation and recovery acceptance with invented data before any real statement or hosted resource is used.
- Rationale: Financial data and statement passwords are high-risk; parser and recovery boundaries must be proven without private inputs.
- Evidence: `PRODUCT.md`, `docs/FIXTURE_POLICY.md`, `README.md`.

## D-002 — Canonical integer money

- Date: 2026-07-24
- Status: Accepted
- Decision: Persist authoritative money as PostgreSQL `bigint`; cross JSON boundaries only as canonical signed-int64 decimal strings with explicit currency.
- Rationale: JavaScript numbers cannot preserve all signed-int64 values and binary floating point is unsuitable for authoritative money.
- Evidence: `lib/money.ts`, `lib/backup-contract.ts`, `docs/ARCHITECTURE.md`.

## D-003 — Worker-contained PDF boundary

- Date: 2026-07-24
- Status: Accepted
- Decision: PDF bytes and the document password remain inside a dedicated browser worker. Unsupported geometry fails closed.
- Rationale: The server and main application do not need the original document or password.
- Evidence: `workers/krungthai.worker.ts`, `docs/KRUNGTHAI_CONTRACT.md`.

## D-004 — Strong single-owner access

- Date: 2026-07-24
- Status: Accepted
- Decision: Authoritative access requires a permanently bound owner, `aal2`, and two verified TOTP factors.
- Rationale: The ledger is deliberately single-owner and contains sensitive financial history.
- Evidence: `supabase/migrations/202607240002_security_and_rpcs.sql`, `docs/ARCHITECTURE.md`.

## D-005 — Schema-v2 recovery is the first supported backup contract

- Date: 2026-07-24
- Status: Accepted
- Decision: Restorable backups use schema version 2 with a locked snapshot, exact manifest, canonical strings, authenticated encryption, and acknowledgement only after client custody. Pre-release v1 is unsupported.
- Rationale: Recovery must detect truncation, reordering, mutation races, and tampering before atomic application.
- Evidence: `supabase/migrations/202607240004_backup_and_ledger_hardening.sql`, `lib/backup-contract.ts`, `docs/RECOVERY.md`.

## D-006 — Synthetic preview is not a backup

- Date: 2026-07-24
- Status: Accepted
- Decision: The UI’s `.pldemo` artifact is a non-restorable synthetic preview and never marks the ledger backup-current.
- Rationale: Labeling a partial synthetic payload as schema-v2 recovery would create false confidence.
- Evidence: `app/ledger-app.tsx`, `README.md`.

## D-007 — Compound-row resynchronization requires provenance

- Date: 2026-07-24
- Status: Accepted
- Decision: A one-deposit/one-withdrawal row may resynchronize only when parser provenance explicitly marks the recognized interest/tax ordering anomaly.
- Rationale: Component shape alone cannot distinguish a known bank-layout anomaly from an unexplained balance gap.
- Evidence: `lib/reconcile.ts`, migration 004, `docs/KRUNGTHAI_CONTRACT.md`.

## D-008 — Docker Supabase is the acceptance database

- Date: 2026-07-24
- Status: Accepted
- Decision: Use the project’s Docker-based local Supabase stack for migrations, RLS/RPC behavior, and pgTAP. A standalone Windows PostgreSQL installation is not interchangeable.
- Rationale: Supabase acceptance depends on its extensions, roles, services, and migration environment.
- Evidence: `README.md`, `docs/RECOVERY.md`.

## D-009 — Use Supabase’s default Docker network locally

- Date: 2026-07-24
- Status: Accepted
- Decision: Keep the project on Supabase’s default Docker network. Do not reuse the attempted custom localhost-binding network.
- Rationale: The custom network broke service discovery after `supabase db reset`, causing auth, storage, and realtime restart loops.
- Consequence: Default local services may bind development ports to all interfaces; use only on a trusted machine/network with firewall protection.
- Evidence: `GOTCHAS.md`.

## D-010 — Repository continuity files

- Date: 2026-07-24
- Status: Accepted
- Decision: Maintain `SPEC.md`, `PLAN.md`, `DECISIONS.md`, and `GOTCHAS.md`, enforced through `AGENTS.md` and reconciled with the project-local `$sync-continuity` skill.
- Rationale: Critical state should survive context compaction and new agent sessions without relying on chat history.
- Evidence: `AGENTS.md`, `.agents/skills/sync-continuity/SKILL.md`.

## D-011 — Claude subagent workflow is a lean, tiered adaptation of the Codex one

- Date: 2026-07-24
- Status: Accepted
- Decision: Provide a Claude Code subagent workflow in `.claude/agents/` that mirrors the Codex roles selectively rather than 1:1. Keep only two custom agents — `finance-implementer` (Sonnet) and `finance-reviewer` (Sonnet) — and rely on the built-in `Explore` agent for read-heavy discovery and the `/verify` and `/code-review` skills for validation/review. `CLAUDE.md` is the Claude entry point; `AGENTS.md` remains the tool-neutral routing/invariant authority.
- Rationale: The upstream Codex workflow (`CODEX_AGENT_WORKFLOW.md`, ref `github.com/nsEytgXm/subagents_configs`) optimizes *monetary cost* by delegating-by-default to cheap model tiers, accepting duplicated context because per-token dollar cost drops. On a Claude Pro subscription the scarce resource is the weekly usage limit, which meters cost-weighted tokens — so "delegate by default" burns the limit faster. The transferable value is bias-reduction (independent review/validation) and context hygiene, not cost arbitrage. Therefore: delegate *selectively*, and tier implementation/review subagents down to Sonnet, reserving the Opus parent for decomposition, architecture, migrations, hard debugging, and money/idempotency/migration judgment. Delegating a decision-complete, well-scoped implementation to a Sonnet subagent is the largest weekly-limit saver available, provided the scoping is genuinely complete; the implementer is instructed to return unresolved decisions rather than guess, and the reviewer/validator is the safety net for tiered-down work.
- Evidence: `CLAUDE.md`, `.claude/agents/finance-implementer.md`, `.claude/agents/finance-reviewer.md`, `AGENTS.md`, `CODEX_AGENT_WORKFLOW.md`.

## D-012 — Import payload digest is server-recomputed and bound, never trusted

- Date: 2026-07-24
- Status: Accepted
- Decision: `confirm_import` recomputes the canonical payload digest in PostgreSQL over the statement frame plus the exact rows it receives (`private.sha256_jsonb` over `private.canonical_jsonb`), rejects any mismatch with the caller-supplied digest (`payload digest mismatch`), and uses the recomputed value as the stored/compared idempotency and audit anchor. The client (`app/api/v1/imports/confirm/route.ts` + `confirmationDigest` in `lib/canonical.ts`) computes the identical object — the frame scalars and the `rpcRows` (rows with server `fingerprint`/`sourceIndex` injected) — so the two digests agree byte-for-byte. This is server-authoritative: the caller digest is a verified claim, not the source of truth.
- Rationale: The prior digest was a trusted caller token used as the sole "same payload" discriminator, so materially different rows could be blessed under an unchanged claimed digest (the artifact-reuse path returned the original batch and silently discarded the divergent rows), and a fresh batch could persist rows whose recorded digest did not summarize them — corrupting the audit/backup/restore-equality chain. A digest that anchors idempotency and audit must be a function of the data it claims to summarize.
- Evidence: `supabase/migrations/202607240005_confirm_import_digest_binding.sql`, `supabase/tests/002_security_contracts.sql` (tamper test 19 fails before the fix, passes after), `lib/canonical.ts`, `app/api/v1/imports/confirm/route.ts`.

## D-013 — Restore manifest counts are canonical integers and the sequence range reserves increment headroom

- Date: 2026-07-25
- Status: Accepted
- Decision: `restore_backup` stage validation requires every `tableCounts[kind]` and descriptor `rowCount` to be canonical non-negative integer text (`^(0|[1-9][0-9]*)$`), rejecting fractional/`2.0`/leading-zero/signed forms with `invalid restore manifest descriptor` before any `text::integer` cast. The accepted `snapshotSequence` range is made explicit as `[0, 2^63-2]` (must be `< 9223372036854775807`) so the single post-commit increment (`sequence = restored + 1`) cannot overflow bigint. Restore round-trip integrity is proven by a populated fixture that exports a full ledger, rewrites the owner to a foreign id, wipes, restores, and asserts a re-export reproduces every table byte-for-byte and remaps all rows to the caller.
- Rationale: Counts were validated only as JSON `number`, so a fractional value slipped past the type check and blew up on an uncaught `22P02` cast instead of failing closed; and the sequence range admitted int64 maximum, which overflowed on the commit increment. Both are boundary defects in an authoritative recovery path. The empty-table restore test proved none of the insertion, money-canonicalization, FK-ordering, audit, or owner-remapping logic — a populated round-trip with re-export equality closes that gap.
- Consequences: Restore fixtures that hand-author populated payloads must avoid null row fields, because `restore_request`'s `jsonb_strip_nulls` recursively drops nulls from the chunk and breaks the chunk/aggregate digest binding (see GOTCHAS).
- Evidence: `supabase/migrations/202607240006_restore_count_and_sequence_bounds.sql`, `supabase/tests/003_restore_contracts.sql` (fractional-count and int64-max tests fail pre-006; populated round-trip asserts re-export equality and owner remapping), verified via `supabase db reset` + `supabase test db` with migration 006 held out then restored.

## D-014 — Row fingerprints are server-recomputed and rejected on mismatch, guarded by a source-text charset

- Date: 2026-07-25
- Status: Accepted
- Decision: `confirm_import` recomputes every row's fingerprint with `private.row_fingerprint` and raises `fingerprint mismatch` when the caller's claim differs, rather than silently overriding it. `private.normalize_source_text` mirrors `normalizeSourceText` in `lib/canonical.ts` (NFKC, then collapse the JS whitespace set — including U+FEFF, which NFKC does not fold and PostgreSQL `\s` does not match — then trim). `lib/statement.ts` constrains `transactionLabel`, `description`, `reference`, and `branch` to a fixed charset (ASCII, Latin-1/Extended, combining marks, Thai, general punctuation, currency, fullwidth forms, whitespace controls).
- Rationale: The fingerprint is the ledger deduplication key (`unique(owner_id, account_id, fingerprint)` + `on conflict do nothing`), so a claim that does not match its row could silently drop a real transaction or force a spurious dedup. The digest bound the rows including their fingerprints, but nothing bound a fingerprint to the row it claimed to identify. Reject-on-mismatch was chosen over the server-authoritative override originally proposed in PLAN: under both designs the server recomputes, so a future PostgreSQL major version shipping newer Unicode data could derive a different fingerprint for an already-stored transaction. Under override that silently misses the dedup key and inserts a **duplicate ledger row**; under reject it raises loudly at import time. Silent double-counting of real money is the worse failure. The charset guard makes that skew class unreachable rather than merely unlikely, so a mismatch now means tampering or a genuine bug.
- Consequences: Widening the source-text charset re-opens the NFKC skew risk and must be re-validated against PostgreSQL's Unicode data. pgTAP fixtures that need a specific fingerprint (deliberate collisions, wrong claims) must send it via `pg_temp.confirm(..., p_bind_fingerprints => false)`; overlap fixtures must instead carry identical fingerprint inputs so the collision is derived from row content.
- Evidence: `supabase/migrations/202607240007_fingerprint_functions.sql`, `supabase/migrations/202607240008_confirm_import_fingerprint_binding.sql`, `lib/statement.ts`, `supabase/tests/002_security_contracts.sql` (test 23 fails pre-008 with `caught: no exception`, other 29 pass; 84/84 after `supabase db reset` applying 001–008), `tests/domain.test.ts` (charset boundary tests incl. U+1CCF0), SQL/JS parity harness 0 mismatches over 6 fingerprint cases and 50k realistic-charset strings.

## D-015 — Krungthai geometry is read from the pdf.js text layer against invented fixtures

- Date: 2026-07-25
- Status: Accepted
- Decision: `lib/krungthai-layout.ts` reads the transaction grid from positioned text items rather than PDF bytes, and fixtures (`tests/fixtures/krungthai-layout-v1.ts`) are authored as pdf.js-shaped `{str, x, y}` arrays instead of generated or committed PDFs. Columns are located from a heading line carrying every anchor; each column owns the band from its anchor to the next; a dateless following line is a wrapped detail line for the row above. The worker now extracts rows instead of returning `LAYOUT_V1_UNSUPPORTED_DOCUMENT`, and posts back only rows or a typed error code — never raw page text.
- Rationale: The risk being managed is column and row assignment, not PDF decoding, which pdf.js already handles. Text-layer fixtures add no dependency, keep geometry reviewable in a diff (a column position is a number, not an opaque blob), and keep tests fast and debuggable. Generated PDFs would add a build-time dependency for fidelity to a layer that is not where the defects live; committed binary PDFs cannot be reviewed or adjusted.
- Consequences: The fixture geometry is invented per `docs/FIXTURE_POLICY.md`, so the extractor is proven correct against a synthetic layout only — agreement with a real Krungthai PDF is unverified until the authorized smoke test (PLAN "Later authorization gates" 1). The pdf.js integration itself (transform extraction, page iteration) is not covered by tests. The statement frame is still unextracted, so a PDF cannot yet yield a confirmable payload; the UI reports the row count and stops.
- Evidence: `lib/krungthai-layout.ts`, `workers/krungthai.worker.ts`, `tests/krungthai-layout.test.ts` (13 tests: 6 extraction, 7 fail-closed), `docs/KRUNGTHAI_CONTRACT.md`. Reconciliation assertion confirmed distinguishing by desyncing the opening balance by one satang and observing a blocker.

## D-016 — The statement frame is extracted from labelled pairs and reduced to last four at the parser

- Date: 2026-07-25
- Status: Accepted
- Decision: `extractStatement` reads the frame block above the transaction grid on page one — account type, account number, period, opening balance, closing balance, and an explicit THB marker — taking each value from whatever is printed to the right of its label on the same line. The account number is reduced to its last four digits inside the extractor, so no full account number is ever returned. The extracted period end year anchors every two-digit year in the statement, including its own start date. A final cross-check rejects a statement whose last row balance disagrees with the printed closing balance (`CLOSING_BALANCE_MISMATCH`).
- Rationale: Label-relative reading suits a frame, where fields are printed as label/value pairs rather than in fixed columns, and it degrades to a typed `MISSING_FRAME_FIELD` rather than a mis-parse when a label is absent. Reducing to last four at the point of extraction rather than downstream means no later code path, log line, or error message can leak a full account number — the value simply never exists past the parser, which is stronger than redacting it later. Anchoring years on the extracted period end rather than the current year (the prior shortcut) makes parsing deterministic: the same PDF yields the same dates whenever it is read. The closing-balance cross-check catches dropped or misread rows, which row-level validation cannot detect on its own.
- Consequences: Binding an extracted statement to a ledger account is still unimplemented and deliberately so — which `accounts` row a last-four maps to is a user decision the parser must not infer — so a PDF still cannot produce a confirmable payload without that step.
- Evidence: `lib/krungthai-layout.ts`, `tests/krungthai-layout.test.ts` (27 tests; 11 frame-specific, including one asserting the full printed number does not survive extraction), `tests/fixtures/krungthai-layout-v1.ts`, `docs/KRUNGTHAI_CONTRACT.md`. Gate: 59 passed / 1 skipped, ESLint, tsc, production build.

## D-017 — Account binding is a checked user decision, not a parser inference

- Date: 2026-07-25
- Status: Accepted
- Decision: `lib/import-assembly.ts` turns an extracted statement into a confirmable `ImportPayload`. The target account is an input, never inferred from the PDF, but the binding is verified rather than trusted: the chosen account's last four digits and currency must match what was printed, and the rows must reconcile from the printed opening balance, before `importPayloadSchema` is applied as the final authority.
- Rationale: The parser reads an account's last four digits but cannot know which ledger account they belong to — two accounts can share a suffix, and the mapping is the owner's to make. Taking it as input alone would let a mis-click post one account's transactions into another's ledger, which is silent and expensive in an append-only ledger. Checking the printed last four and currency against the chosen account makes that specific mistake impossible without adding an inference. Reconciling client-side duplicates a server check deliberately: the server is still authoritative, but failing here keeps blockers visible next to the rows that caused them.
- Consequences: An account whose last four is unknown to the ledger cannot be bound until the owner records it. The HTTP path is still unexercised — see PLAN task 3 for the two blockers (local auth env is out of scope to inspect, and owner identity requires Google OAuth, an explicit authorization gate).
- Evidence: `lib/import-assembly.ts`, `tests/import-assembly.test.ts` (6 tests, including account/currency mismatch, a non-reconciling row, and a charset violation, plus one that mirrors the confirm route's fingerprint + digest steps).

## D-018 — Advisory lock serialization is proven with two real connections

- Date: 2026-07-25
- Status: Accepted
- Decision: `tests/advisory-lock.test.ts` verifies the owner mutation advisory lock by contending for it from two separate PostgreSQL connections: a tagged holder session takes `pg_advisory_xact_lock(hashtextextended(owner || ':ledger-mutation', 0))` and sleeps, while a second session attempts the same key under `lock_timeout`. Three properties are asserted — the same owner is blocked (55P03), a different owner is not, and the lock frees when the holding backend ends.
- Rationale: pgTAP cannot cover this. Its tests run in one session inside one transaction, and a session that already holds an advisory lock re-acquires it without blocking, so a single-session assertion passes whether or not the lock does anything — it is not a distinguishing test. The different-owner case is the control that makes the same-owner timeout meaningful: without it, a `tryAcquire` that failed for any unrelated reason would look like correct serialization.
- Consequences: The test needs the local container and skips loudly when it is unreachable, so a skipped run is not evidence of serialization. It exercises the lock key directly rather than through the RPCs, because reaching those needs an authenticated owner (PLAN task 3, blocked).
- Evidence: `tests/advisory-lock.test.ts` (3 assertions, ~5s), GOTCHAS entries on blocked-event-loop stdin starvation and `docker exec` kill semantics, both found while building it.

## D-019 — The recovery chain is proven end to end at scale, non-destructively

- Date: 2026-07-25
- Status: Accepted
- Decision: `tests/backup-roundtrip.test.ts` drives the whole schema-v2 chain over 1,200 source transactions: export, contract validation, encrypt, decrypt, staged restore with all eleven chunks in order, commit, and re-export equality. It exports whatever the database already holds before starting and restores that snapshot in a `finally`, so a full run leaves the ledger exactly as it found it.
- Rationale: pgTAP proves the restore contract with small hand-authored fixtures and the unit suite proves the envelope in isolation; neither exercises the chain at a size where chunk ordering, row counts, and int64-as-text handling actually matter, which is where a recovery path fails in practice. Non-destructiveness matters because the test necessarily commits — without restoring the prior state it would silently wipe the seed and break the pgTAP suites that assume it.
- Consequences: The test needs the local container and skips loudly when unreachable. Because it recomputes digests from whatever snapshot it is given, it proves round-trip fidelity rather than digest tampering — tamper detection stays covered by pgTAP `003`. Restores clear the destination first, matching `restore_backup`'s refusal to overwrite a non-empty ledger.
- Evidence: `tests/backup-roundtrip.test.ts` (1,200 rows, ~3s). Confirmed distinguishing by altering one `post_balance_minor` among 1,200 rows before restore and observing the re-export equality assertion fail. Non-destructiveness confirmed by running `supabase test db` afterwards: still 84/84.

## D-020 — The authenticated import path is proven with a local owner, no hosted resources

- Date: 2026-07-25
- Status: Accepted
- Decision: `tests/import-confirm-e2e.test.ts` signs in as the seeded synthetic owner, enrolls and verifies two TOTP factors to reach aal2, and posts a real `confirm_import` request whose fingerprints and payload digest were computed by `lib/canonical.ts` from a statement read by the real parser. It asserts the import lands, that a tampered fingerprint is rejected with `fingerprint mismatch`, and that the identical request without MFA is refused with `strong owner access required`.
- Rationale: Everything hardened in D-012 and D-014 was previously proven only at the SQL and unit level — the pgTAP wrapper computes fingerprints server-side in `pg_temp`, so it shows the SQL is self-consistent, not that a real client's values are accepted. No hosted Supabase or Google OAuth resources were needed: `private.has_strong_owner_access` checks `auth.uid()`, an aal2 claim, and two verified TOTP factors, and never inspects the auth provider, so a local password session satisfies it exactly as a Google identity would. TOTP codes are generated in-test from the secret Supabase returns at enrollment (RFC 6238), so no authenticator app is involved.
- Consequences: `public.ledger_owners` binds exactly one owner and is immutable, so the test authenticates as the seeded owner rather than creating a second one — a genuinely different owner cannot exist without a database reset. The test targets PostgREST rather than the Next.js route, so the route's zod boundary and cookie handling remain uncovered; the fingerprint and digest computation it performs is mirrored line for line from the route. The suite now runs files serially (`fileParallelism: false`) because several suites mutate the one local database.
- Evidence: `tests/import-confirm-e2e.test.ts` (4 assertions), `vitest.config.ts`. Full gate after the change: Vitest 75 passed / 4 skipped, pgTAP 84/84, ESLint, tsc, production build.

## D-021 — A statement becomes an import through a chooser, not a code path

- Date: 2026-07-25
- Status: Accepted. Closes the open consequence recorded in D-017 (binding had no user-facing step) and, with D-022, the route-coverage consequence recorded in D-020.
- Decision: `GET /api/v1/accounts` lists the owner's accounts, and `app/ledger-app.tsx` gained a bind stage between parsing and review: the owner loads their accounts, picks one, and only then is `assembleImportPayload` applied and the result posted to `/api/v1/imports/confirm`. The endpoint is an RLS-protected select of an explicit column list (`lib/accounts.ts` is the shared wire contract), not a new RPC. The artifact digest is the SHA-256 of the PDF's own bytes, computed on the main thread before the buffer is transferred to the worker. One idempotency key is minted per bound statement, so a failed confirmation can be retried without becoming a second import.
- Rationale: Everything under the UI already worked — extraction, assembly, confirmation — but only a test wired them together, so the app could not turn a PDF into a ledger entry. A plain select suffices where `list_account_transactions` needed an RPC: accounts carry no nested shape to assemble and no column wider than the last four digits exists to withhold, so the existing `strong_owner_select` policy is the whole enforcement. Digesting the bytes before transfer is the only point where they are still readable on the main thread; a digest cannot be reversed into statement content, and it is what makes re-importing the same PDF a detectable conflict rather than a duplicate ledger.
- Consequences: The `.pldemo` export is now offered only on the synthetic path — wrapping real confirmed rows in a file that calls itself synthetic would misdescribe the artifact. The binding chooser is unexercised in a browser: reaching it needs a real PDF, so Playwright still covers only the synthetic review path, and the flow's parts are covered separately by `tests/import-route.test.ts` and `tests/import-assembly.test.ts`. Binding stays a checked user decision (D-017) — a guard in `tests/privacy.test.ts` fails if the UI ever matches an account to the statement automatically.
- Evidence: `app/api/v1/accounts/route.ts`, `lib/accounts.ts`, `lib/canonical.ts` (`sha256HexBytes`), `app/ledger-app.tsx`, `tests/privacy.test.ts` (10 tests, 3 new). Gate: ESLint, tsc, Vitest 87 passed / 5 skipped, pgTAP 84/84, production build, Playwright 4/4.

## D-022 — The route wrapper is tested by invoking the handler with a real cookie session

- Date: 2026-07-25
- Status: Accepted
- Decision: `tests/import-route.test.ts` imports the actual Next.js handler functions and mocks only `next/headers`, backing it with a cookie jar that `@supabase/ssr` itself populates from a real aal2 session via `setSession`. It covers the confirm route's zod boundary (unknown fields, uppercase digest, malformed key, missing key, wrong currency, non-JSON body), its 409 idempotency translation, its route-only `AMBIGUOUS_DUPLICATES` check, and its computed fingerprints and digest, plus the accounts route's shape and both routes' 401/403 refusals. The local-stack harness (`psql`, RFC 6238 TOTP, aal2 sign-in, owner cleanup) moved to `tests/helpers/local-owner.ts` and is shared with the PostgREST suite.
- Rationale: D-020 closed the database half of the import path but left the route wrapper verified only by reading — its fingerprint and digest computation was mirrored by hand into the PostgREST test, and equivalence by inspection is not evidence. Letting `@supabase/ssr` write the session cookie means the test never hardcodes a cookie name or encoding, so it exercises the same storage the running app uses rather than a guess at it. Invoking the handler directly rather than starting a Next server keeps it in the same suite as the database it asserts against.
- Consequences: The negative-auth cases run last on purpose — signing in again at aal1 replaces the stored session, and a token refreshed in that family is no longer aal2, so restoring the strong session mid-file made the suite order-dependent (see GOTCHAS). Route coverage is now real but still bypasses Next's own routing, headers, and middleware; only Playwright reaches those, and it cannot reach the authenticated path without a PDF.
- Evidence: `tests/import-route.test.ts` (9 assertions), `tests/helpers/local-owner.ts`, `tests/import-confirm-e2e.test.ts` (refactored onto the shared harness, still 4 passed / 1 skipped). Gate: Vitest 87 passed / 5 skipped, pgTAP 84/84, ESLint, tsc, production build, Playwright 4/4.

## D-023 — pdf.js gets its own worker, and the browser parse path is tested with a generated PDF

- Date: 2026-07-25
- Status: Accepted
- Decision: `workers/krungthai.worker.ts` hands pdf.js an explicit `GlobalWorkerOptions.workerPort` built from a dedicated entry module (`workers/pdf.worker.entry.ts`, whose only job is to import `pdfjs-dist/build/pdf.worker.mjs`). `tests/fixtures/synthetic-pdf.ts` renders the existing invented geometry into a real PDF, and `tests/e2e/parser.spec.ts` drives the running app with it — one test asserts a full parse and the move to the bind stage, one asserts an unrelated PDF fails closed as `UNSUPPORTED_LAYOUT`. The worker now also returns the caught error's class name, and the UI shows it beside the typed code.
- Rationale: `GlobalWorkerOptions` had never been configured, so pdf.js fell back to loading its worker module inline and threw a bare `Error` before reading any page — every PDF was unparseable, and the app's most load-bearing integration was broken. Nothing caught it because nothing tested it: every parser test feeds `extractStatement` a `PageText` array directly, and the synthetic UI path fetches `/api/v1/demo` without touching the worker, so `getDocument` had never run in a browser. Generating the PDF from the invented fixtures keeps that coverage inside `docs/FIXTURE_POLICY.md` — no real statement is opened to test the reader. The font is Type0/Identity-H with an identity ToUnicode CMap and no embedded glyph program, because pdf.js recovers text from ToUnicode rather than outlines, so Thai extracts exactly without committing a font binary.
- Consequences: `workerSrc` pointed at the package path is not an alternative — the bundler inlines `pdf.worker.mjs` into the parser worker's own chunk, where it runs in that scope, replaces `self.onmessage`, and posts pdf.js protocol messages to the main thread (GOTCHAS). Returning the error class name is a deliberate, bounded disclosure: pdf.js names are library constants, never document content, and without them every failure except an unsupported layout read identically — which is what made the first real-statement attempt uninformative. Encrypted PDFs remain untested by generated fixtures; the generator writes no encryption dictionary, so the password path is still only exercised interactively.
- Evidence: `workers/krungthai.worker.ts`, `workers/pdf.worker.entry.ts`, `tests/fixtures/synthetic-pdf.ts`, `tests/e2e/parser.spec.ts`. Gate: ESLint, tsc, Vitest 87 passed / 5 skipped, Playwright 8/8 across desktop and mobile (was 4/4). Confirmed distinguishing: both new tests fail with `PDF_PARSE_FAILED / Error` on the pre-fix worker.

## D-024 — The column model follows a real statement, corrected by the smoke test

- Date: 2026-07-25
- Status: Accepted. Corrects the invented geometry of D-015 and D-016, whose column model no real statement could satisfy.
- Decision: The reader's columns are `Date/Time`, `Transaction`, `Description/Cheque No.`, `Withdrawal`, `Deposit`, `Balance`, `Branch`. Date and time share one cell, parsed together as `dd/mm/yy` with an optional `HH:MM(:SS)`. The transaction type and the description come from separate columns and map to `transactionLabel` and `description`. Thai wordings stay as alternates, since the contract allows either language. The fixtures in `tests/fixtures/krungthai-layout-v1.ts` now mirror this structure with entirely invented values, and the frame labels are English (`Account Type`, `Account No.`, `Period`, `Opening Balance`, `Closing Balance`).
- Rationale: The authorized smoke test reported `MISSING_COLUMN_ANCHOR`, and the on-device label diagnostic showed why: the statement prints one combined `Date/Time` column and a `Transaction` column that the invented model did not have at all. `findColumns` requires every anchor on one printed line, so the previous seven-anchor set could never match — the mismatch was structural, not a wording difference. Heading words are the bank's boilerplate rather than statement content, so adopting them copies no financial data; matching the real structure is what gives the fixtures any predictive value, which the invented shape demonstrably lacked. Only `Account Type` among the frame labels is confirmed — the others sit on lines the diagnostic redacts because they carry digits — so those remain guesses and both languages stay accepted.
- Consequences: A row whose date/time cell carries digits but does not parse now fails closed with a masked shape. Previously such a row was silently skipped when it was the first on a page, and merged into the preceding row's description otherwise — a dropped or corrupted transaction, found only because the restructure made the old "unreadable time" test pass for the wrong reason. Still unverified against reality: the date format inside the cell, the frame label wording, and every row-level value, because the real statement has not been re-read since this change. `pdf.js` v5 removed `disableCombineTextItems`, so a statement that printed its headings close enough to be merged into one run would fail closed and could not be read by this approach at all.
- Evidence: `lib/krungthai-layout.ts`, `tests/fixtures/krungthai-layout-v1.ts`, `tests/krungthai-layout.test.ts` (27 tests), `tests/e2e/parser.spec.ts`. Gate: ESLint, tsc, Vitest 88 passed / 5 skipped, pgTAP 84/84, Playwright 8/8 against a freshly built server.

## D-025 — The currency marker is searched across page one, not only the frame block

- Date: 2026-07-25
- Status: **Superseded by D-034.** Its rationale was reasoned from a diagnostic corrupted by the `headerY` bug that D-028 later fixed; the currency is in fact printed in the frame block.
- Decision: `extractFrame` tests the currency marker against all of page one rather than only the label block above the transaction grid. The fixtures print the marker below the grid, matching the real statement, and a test asserts the marker sits below the heading line so narrowing the region back fails in the suite.
- Rationale: The smoke test reported `UNSUPPORTED_CURRENCY`, and the allowlisted currency-token diagnostic answered why in one run: wording above the grid `none`, below `THB, Currency`. The statement does state its currency explicitly, satisfying the SPEC invariant — the reader was simply looking in the wrong half of the page.
- Consequences: This is a weaker guard than a frame-only match. A statement denominated in another currency that merely mentioned THB anywhere on page one would now satisfy it. Deliberately not paired with a foreign-currency-code scan: transaction descriptions legitimately contain codes such as `USD`, so that check would reject valid statements — a false rejection of real data is worse here than a theoretical false acceptance, because the hard enforcement is downstream where `parseThb` and the `THB` literal in `importPayloadSchema` refuse anything else.
- Evidence: `lib/krungthai-layout.ts`, `tests/krungthai-layout.test.ts` ("reads a currency stated below the transaction grid"), `tests/fixtures/krungthai-layout-v1.ts`.

## D-026 — The frame contract follows a real statement: no printed balances, two-line rows

- Date: 2026-07-25
- Status: Accepted. Corrects the invented frame of D-015/D-016 and supersedes the closing-balance guarantee recorded in D-024.
- Decision: Frame labels are `Account Number` and `Statement Period`; label matching collapses internal whitespace; a label's value stops at the next known label; and a label occurrence carrying no value no longer ends the search. Opening and closing balances are optional, because a real statement prints neither: when absent, the opening figure is derived as the first row's printed balance less that row's own movement, the closing figure is the last row's printed balance, and `StatementFrame.balancesPrinted` records which case applied. The closing cross-check runs only when a closing balance was printed. Each row's time is printed on its own line beneath its date in the same column and is merged back into the row.
- Rationale: Every item came from one masked structural dump of a real statement, which reports each line's shape and column positions with digits and letters replaced — no name, amount, balance, date, or account number left the device. It showed the frame carries Account Name/Type, Branch, Branch Code, Current Address, Overdraft Limit, Statement Period and Requested Date, and no balance of any kind; that rows occupy two printed lines; and that frame lines carry several label/value pairs on one line.
- Consequences: Three latent defects were fixed rather than shipped. Reading everything right of a label swept the neighbouring `Branch Code` digits into the account number, yielding a wrong but plausible account suffix — the worst class of error in an append-only ledger. The fail-closed guard from D-024 fired on any digit in the date column, so a footer address beginning with a street number aborted a statement that had parsed correctly; the probe is now date-shaped. An anchored label pattern rejected padded internal whitespace, which is invisible in every printed and reported form. The derivation also costs real safety: reconciliation can no longer detect a dropped *first* row, since the derived opening is defined to agree with it, and no printed closing figure remains to check the chain against. Rows two onward stay fully chain-checked. The last page carries a summary block — item count, total withdrawal and total deposit with counts — which would restore a global integrity check stronger than a closing balance; its label wordings are not yet known, so it is not read.
- Evidence: `lib/krungthai-layout.ts`, `tests/krungthai-layout.test.ts` (33 tests, including derived balances, a padded label, the neighbouring-field guard, and a time on its own line), `tests/fixtures/krungthai-layout-v1.ts`. Gate: ESLint, tsc, Vitest 95 passed / 5 skipped, Playwright 8/8.

## D-027 — Browser runs use an isolated Playwright config

- Date: 2026-07-25
- Status: Accepted
- Decision: `playwright.isolated.config.ts` runs the browser suite on port 3100 with `reuseExistingServer: false`, always building first. `playwright.config.ts` is unchanged and stays the convenient one for manual use.
- Rationale: The default config reuses anything already listening on port 3000, so a manually started `pnpm start` silently supplied a stale build. Four consecutive browser runs during the parser work reported identical failures against code that no longer existed, and each false result cost a real-statement read to notice. Isolation also means the owner driving the app by hand no longer has to stop their server before the suite can be trusted.
- Consequences: Two configs to keep in step. The isolated one is the one to trust for verification; a run through the default config is only as current as whatever server it found.
- Evidence: `playwright.isolated.config.ts`, GOTCHAS ("Playwright reuses a server someone else started").

## D-028 — The frame/grid boundary comes from the heading line, and the fixtures print the collision

- Date: 2026-07-25
- Status: Accepted
- Decision: `findColumns` returns the `y` of the line carrying all seven column headings alongside their x positions, and `extractStatement` uses that `y` as `headerY` — the single boundary separating the frame block from the rows. The any-anchor search it replaces is gone, not narrowed. The fixtures print a `Branch` frame label with a branch value on its own line between `Statement Period`/`Account Type` above and `Account Number` below, so the collision and its printed order are part of the default frame every frame test uses.
- Rationale: A real statement prints `Branch` as a frame label above the grid, and `Branch` is also a column heading, so taking `headerY` from the first line matching *any* anchor put the boundary on that frame line; `extractFrame` filters to `y > headerY + LINE_TOLERANCE` and dropped every field printed below it. The fields printed higher up still read, which is why attempt 6 reported `account number (label not found)` and looked like a per-field wording problem rather than a boundary bug. `findColumns` already located the true header correctly — it requires all seven anchors on one line — and merely discarded which line matched, so the fix is to stop throwing that away. `Branch` is not special-cased: any frame label equal to a column heading (`Balance`, `Transaction`, …) reproduces this, and a word-level exception would leave the class open.
- Consequences: `currencyEvidence`'s above/below split now also reports against the true grid line, so the D-025 diagnostic is more accurate than when it was written. Making the collision part of the *default* frame rather than one isolated case means the whole frame suite guards it, at the cost that a future fixture change moving those y values could reintroduce a silent green — the named regression test asserts the printed order explicitly for that reason. Balances remain derived (D-026); this fix restores no protection there, and the summary-block cross-check in PLAN task 8 is still the thing that would.
- Evidence: `lib/krungthai-layout.ts` (`findColumns`, `extractStatement`), `tests/fixtures/krungthai-layout-v1.ts`, `tests/krungthai-layout.test.ts` ("finds the grid header even when a frame label matches a column heading", 32 tests). Gate on 2026-07-25 with the project-local Node 24.18.0 runtime: ESLint, `tsc --noEmit`, Vitest 96 passed / 5 skipped against the live container, pgTAP 84/84, production build, Playwright 8/8 via `--config=playwright.isolated.config.ts`. Confirmed distinguishing: restoring the any-anchor `headerY` fails 26 of the 32 layout tests, each reporting the real statement's message verbatim — `The statement frame has no account number (label not found). Fields that did read: account type, statement period.` **Confirmed against reality 2026-07-25:** a masked structural dump of the real statement shows `Branch` printed as a frame label at `y=717` and the grid heading line at `y=601`, and the account number now reads.

## D-029 — A printed zero money column is no movement, not a rejection

- Date: 2026-07-25
- Status: Accepted
- Decision: A money column printing `0.00` contributes no component to its row. It is not a zero-amount component and not a rejection. A *negative* figure is still refused (`withdrawal column is negative.`), and a row where both columns printed zero fails closed with `no movement in either money column.`
- Rationale: A real statement prints its withholding-tax column as `0.00` on an interest posting where no tax was withheld — Thai withholding only applies above an annual threshold, so this is the common case, not an edge one. Rejecting it stopped a statement that had already read ~118 rows. Nothing moved, so nothing belongs in the ledger and the balance chain is unaffected either way. The sign asymmetry is deliberate: these columns print unsigned, so a sign would mean the statement encodes direction some other way, and reading a credit as a withdrawal would invert a real transaction — the worst failure mode this ledger has.
- Consequences: The interest/tax compound guard no longer applies to a zero-tax interest row, because that row now has one component rather than two — which is correct, but it means the guard covers strictly fewer rows than before. A printed `0.00` is not preserved anywhere in the import: the row is represented by its single component and its printed balance, so the fact that the bank printed a zero column is not auditable after import. Accepted as immaterial, since a zero carries no financial information.
- Evidence: `lib/krungthai-layout.ts` (`toRow`), `tests/krungthai-layout.test.ts` ("reads a printed zero money column as no movement", "rejects a negative money column rather than guessing its direction", "rejects a row whose money columns both printed zero"). Found by the masked cell dump on a real statement: `withdrawal[d.dd]` with no sign, on a row whose deposit had already passed its own positive check.

## D-030 — Columns are assigned by a run's midpoint, using the width pdf.js reports

- Date: 2026-07-25
- Status: Accepted. Corrects the left-edge banding introduced with D-015.
- Decision: `TextItem` carries the run's `width` as pdf.js reports it, and `assign` places a run by its horizontal midpoint rather than its left edge. The `LINE_TOLERANCE` left-overhang allowance is removed. A fixture that omits `width` falls back to a nominal per-character estimate, so there is one assignment rule rather than two code paths.
- Rationale: The text columns are left-aligned but the money and branch columns are right-aligned, so a wider figure starts further left. Measured on a real statement: the balance column is right-aligned to ~518 with a digit width of 4, so a `d,ddd.dd` balance starts at 491 but a `dd,ddd.dd` balance starts at 487 — below the 489 boundary, landing it in the deposit band. The statement crossed 10,000 baht on page 7 and failed with two amounts in `deposit` and an empty `balance`. A midpoint moves half a glyph per extra character where a left edge moves a whole one, and it correctly places every run in the real statement's dump, headings included. A 7-digit branch code was the same latent failure — `dddddd@533` sat exactly on the branch boundary with zero margin.
- Consequences: The reader now depends on pdf.js supplying `width`; it is optional in the type only so hand-written fixtures stay readable, and a fixture's estimate is invented rather than measured. This failed closed rather than silently — all three misfiling paths end in a rejection — but "fails closed" was the only protection, and the margin was 2 units.
- Evidence: `lib/krungthai-layout.ts` (`centreOf`, `assign`), `workers/krungthai.worker.ts`, `tests/krungthai-layout.test.ts` ("assigns a right-aligned amount by its midpoint, not its left edge"). Confirmed distinguishing: restoring the left-edge rule fails that test with `deposit[d.dd dd,ddd.dd] balance[]` — the same shape the real statement produced. Verified through real pdf.js widths by `tests/e2e/parser.spec.ts`.

## D-031 — The statement's calendar era is determined once, from its period end

- Date: 2026-07-25
- Status: Accepted. Replaces `resolveKrungthaiYear`, which assumed Buddhist Era unconditionally.
- Decision: `resolveStatementEra` reads the frame's period-end year in both calendars and keeps whichever lands inside a plausible window for an importable statement (20 years back, 1 year forward). Exactly one can, because the two readings are always 543 years apart and the window is narrower than that; if neither fits it throws and the frame fails closed with `INVALID_FRAME_CONTENT`, and the unreachable both-fit branch throws rather than choosing. The resolved era is threaded to every row through `gregorianYearFrom`, so a statement is never read half in one calendar. The era is returned beside the frame, not inside `StatementFrame`, which is hashed into the import digest.
- Rationale: A real English-language statement prints `26` for 2026. Read as Buddhist 2569 that is 1983, and `2500 + 26 - 543 = 1983` is exactly what the bind screen showed. Every row inherited the same 43-year shift, because rows anchor on the period-end year. **This is the first defect in the sequence that did not fail closed** — the statement parsed cleanly, reached the bind stage, and would have written 1983 dates into an append-only ledger. It was caught only because the bind screen prints the period. Determining the era from the 543-year separation is arithmetic, not a heuristic: a test walks all 100 two-digit years and asserts the ambiguous branch is unreachable (Gregorian admits 06–27, Buddhist 49–70, disjoint).
- Consequences: A statement older than 20 years, or dated more than a year ahead, is now refused outright — deliberate, since neither is importable and the alternative is a silent 43-year error. The window cannot be widened past ~43 years without making the two calendars genuinely ambiguous; the guard makes that failure loud rather than silent. Era detection rests on the period end being present and parseable, which D-026 already requires. The same fix corrected an adjacent defect: the period *start* previously inherited the end's year outright, so a range crossing a new year dated the start a year late — it now resolves its own printed year in the resolved era, which is what revealed the real statement's period to be 13 months (2025-07-01 to 2026-07-31) rather than one.
- Evidence: `lib/dates.ts` (`resolveStatementEra`, `gregorianYearFrom`, `StatementEra`), `lib/krungthai-layout.ts` (`extractFrame`, `toRow`), `tests/domain.test.ts` ("decides the statement era from its period end, refusing an implausible year"), `tests/krungthai-layout.test.ts` ("reads a statement printed in Gregorian years rather than as Buddhist 1983", "resolves a period start that crosses a new year against its own printed year"). Gate: ESLint, tsc, Vitest 106 passed / 5 skipped, pgTAP 84/84, production build, Playwright 8/8 isolated.

## D-032 — Row diagnosis is batched and deduplicated, not serialized one defect per read

- Date: 2026-07-25
- Status: Accepted
- Decision: `extractStatement` no longer returns on the first unreadable row. It parses every row, collects failures, groups them by their masked message with the page/row prefix stripped, and returns up to six distinct classes with counts. The import still fails closed — a statement with any unreadable row returns `ok: false` and no rows — and the reported code is the first failure's, so a single-defect statement reports exactly what it did before.
- Rationale: Reading one real statement cost seven owner-driven runs, each requiring the owner present to type a document password, because each run surfaced one row's failure and stopped. Rows repeat their shapes, so a statement's remaining defects collapse to a handful of classes that one read can return. This is a diagnosis change, not a validation change: what is accepted is unchanged, and the aggregate message is built from the same already-masked per-row messages, so it adds no disclosure.
- Consequences: A statement with many distinct defects reports at most six classes and says how many were omitted. The date-probe failure inside the line loop now records a failure and clears the current row rather than returning, so an unreadable row can no longer be folded into the one above it. Reporting is bounded but a pathological file could still produce a long status line.
- Evidence: `lib/krungthai-layout.ts` (`summarizeFailures`, `RowFailure`), `tests/krungthai-layout.test.ts` ("reports every unreadable row in one result, grouped by shape"), `tests/privacy.test.ts` ("reduces a rejected row to shapes, with no value or wording surviving").

## D-033 — The import is cross-checked against the statement's own printed totals

- Date: 2026-07-25
- Status: Accepted
- Decision: `extractTotals` reads the summary block below the grid on the last page — `Total Page`, `Total Withdrawal`, `Total Deposit`, each a label followed by a count and (for the money labels) a total. `verifyTotals` then compares the page count, the per-kind row counts, and the per-kind absolute totals against the rows actually parsed, and a disagreement fails closed as `SUMMARY_MISMATCH`. A statement printing no such block is accepted with no cross-check; a block that is printed but unreadable fails closed. Summary lines are also excluded from row continuation, so they can never be merged into the final transaction.
- Rationale: this is the only *global* integrity check the reader has — it confirms that no row was dropped and that every amount was read — and D-026 gave up both the dropped-first-row check and the closing-balance chain when it started deriving balances. Confirmed against a real statement on 2026-07-25: its printed counts sum exactly to the number of rows the reader found, and its printed totals close the balance chain from the opening figure derived from the first row to the last row's printed balance. Absence is tolerated rather than fatal because not every layout prints the block, which is the same compromise `balancesPrinted` already makes. Unreadable-but-present is the opposite case: skipping it would let a real mismatch pass unnoticed.
- Consequences: a statement whose summary block the reader cannot parse is now refused even though its rows read cleanly — deliberate, since the alternative is an unverified import. The Thai label wordings are unverified guesses, so a Thai-language statement will most likely fall into the "no cross-check" path rather than being checked. `Total Page`'s second column is a carry-forward marker rather than an amount and is ignored. A mismatch reports counts plainly — a row count is not a financial value — but a total mismatch reports only the masked shape of the gap, so its order of magnitude is diagnosable without the figure. **Not persisted:** whether an import was cross-checked is not recorded in `StatementFrame`, because that type is hashed into the import digest and adding a field would change a committed payload contract. Recording it needs a migration and is left as separate work; until then the ledger cannot distinguish a cross-checked import from an unverified one.
- Evidence: `lib/krungthai-layout.ts` (`SUMMARY_LABELS`, `extractTotals`, `verifyTotals`), `tests/fixtures/krungthai-layout-v1.ts` (the main two-page fixture now prints an agreeing summary block, so the whole suite exercises the check), `tests/krungthai-layout.test.ts` (describe "krungthai summary cross-check": agreement, tolerated absence, count/page/total mismatches, masked gap reporting, unreadable block, and a summary printed within `DETAIL_TOLERANCE` of the last row). Gate: ESLint, tsc, Vitest 114 passed / 5 skipped, production build, Playwright 8/8 isolated.

## D-034 — The currency must be stated in the frame block, restoring the narrow guard

- Date: 2026-07-25
- Status: Accepted. Supersedes D-025.
- Decision: `extractFrame` tests the currency marker against the frame block above the grid only, not the whole of page one. The fixtures print `Currency THB` on its own line in the frame block, and a test asserts the marker sits above the heading line and that moving it below fails as `UNSUPPORTED_CURRENCY`.
- Rationale: D-025 widened the search because the smoke-test diagnostic reported the marker as printed *below* the grid. That reading was an artifact: `headerY` was being resolved to a frame label rather than the heading line (the bug D-028 fixed), so `currencyEvidence` classified a frame line as below the grid. A real statement prints `Currency THB` in the frame block, confirmed directly on 2026-07-25. The wide guard accepted a statement denominated in another currency that merely mentioned THB anywhere on page one — including inside a transaction description, which is exactly where a foreign currency code legitimately appears.
- Consequences: the SPEC invariant that page one must state THB explicitly is now enforced where it was intended. A layout that genuinely prints its currency below the grid would be refused; none is known to. The fixtures gained a footer line beginning with a street number to preserve the coverage the relocated currency marker used to provide for D-026's numeric-footer guard.
- Evidence: `lib/krungthai-layout.ts` (`extractFrame`), `tests/fixtures/krungthai-layout-v1.ts`, `tests/krungthai-layout.test.ts` ("requires the currency in the frame block, not merely somewhere on page one").

## D-035 — Private statements are invoked, never read: the masking harness boundary

- Date: 2026-07-25
- Status: Accepted. Records the access grant referenced in `HANDOFF.md` § Standing authorizations and amends the reach of D-001 and `docs/FIXTURE_POLICY.md`; it does not supersede either.
- Decision: The owner granted direct access to `private-statements/` after offering it three times and being declined twice. The grant is bounded to **invoke, don't read**. An agent may run `scripts/mask-statement.mjs` against a file *or a directory* in that directory and may read the masked dumps the harness writes to the gitignored `masked-dumps/`. An agent may not open, list the contents of, copy, or otherwise read a file under `private-statements/` itself, and no tool call may pass a statement's bytes or page text back into a session. **File names are masked** on the same terms as cell text, which is what makes the no-listing rule cost nothing: directory mode means no real name is ever typed or read, and a dump names its source as `xxxx_dddddddddd_dddddd.pdf`. The owner offered listing access on 2026-07-25 and it was declined in favour of this, because statement names routinely carry an account number or the holder's name. The harness reads the document password from stdin only — never from a chat message, a repo file, an environment variable, or a command-line argument — and needs it once per file, to produce that file's dump. The dump carries only what the on-device diagnostics already emit (`maskShape`, `describeLabelGeometry`, `describeValueLabels`, `describeStructure`), which `tests/privacy.test.ts` holds to shapes, coordinates, and digit-free label wordings.
- Rationale: Task 11 needs the five remaining layouts (SCB, KBANK, three receipt formats), and Krungthai cost ten owner-driven browser reads because each read surfaced exactly one defect and required the owner present to type a password. One dump per format collapses that to one owner-driven invocation, after which the layout can be developed offline. The narrower path — a masked dump rather than the document — keeps the two properties the earlier refusals were protecting: no real transaction, counterparty, amount, or account number enters a session transcript, and "the fixtures are invented" stays falsifiable, because nothing an agent can see carries a value to copy.
- Consequences and cost, stated plainly:
  - **Once an agent holds folder access, "no values reach the agent" is discipline, not structure.** Nothing in the tooling prevents a future session from reading a PDF directly; only this entry and the policy amendment do. That is a real reduction in the guarantee and is the price of the grant.
  - **An agent's reads are transmitted and retained in a session transcript.** The owner's earlier belief that agent reads stay on-device is false. The app's on-device guarantee (D-003, and the "No data left this device" UI string) belongs to the *app*, not to an agent. If a future session finds the owner reasoning from the older belief, correct it before acting on this authorization.
  - **The passwords are identity-grade and non-rotatable** — the owner's date of birth and citizen ID. The owner considers them low-sensitivity; they are the opposite. A password in chat or in a shell argument cannot be recalled: chat is retained, and an argument is visible to every process on the machine and lands in shell history. Hence stdin only.
  - **A dump is not a fixture.** It records a real document's structure, so committing one, or transcribing its coordinates and label wordings into a fixture, would make every fixture written afterwards non-invented. `masked-dumps/` is gitignored and `docs/FIXTURE_POLICY.md` now says this outright.
  - **A dump is masked, not empty of text.** Label wordings are printed unmasked, because a wording is exactly what they exist to reveal. No value can reach them — a run carrying a digit is dropped, so is one over 24 characters, and a label qualifies only when the run to its right is a number, which is why an account holder's name cannot appear. But the filter is structural, not semantic: on an unfamiliar layout, short digit-free text printed left of a number need not be boilerplate, and on a receipt it could be a merchant or recipient name. Verified on the synthetic fixtures, where the section correctly returns the frame labels, the summary labels, and the Thai transaction-type vocabulary and nothing else. The dump labels that section as the one to check, and the harness says so when it finishes. Receipts are the case to watch, since their layout is entirely unknown (task 11).
  - The grant does not extend to `.env*`, which remains off limits to agents under `PLAN.md` task 10.
- Evidence: `scripts/mask-statement.mjs`, `docs/FIXTURE_POLICY.md` § Masked structural dumps, `.gitignore` (`masked-dumps/`), `tests/privacy.test.ts` (the harness may not print raw page text, and the four diagnostics remain value-free). The grant itself is recorded in `HANDOFF.md` § Standing authorizations.

## D-036 — A development sign-in, gated by an opt-in flag rather than by `NODE_ENV`

- Date: 2026-07-25
- Status: Accepted
- Decision: `app/api/v1/dev/session/route.ts` signs in as the seeded synthetic owner, enrols and verifies two TOTP factors, and leaves an `aal2` `@supabase/ssr` cookie session — the session every owner-bound route requires. It is reachable only when all three of these hold, and any one missing gives a 404 rather than a 403, because a 403 confirms the route exists: `NEXT_PUBLIC_ALLOW_DEV_OWNER_SESSION` is exactly `"1"`; the configured Supabase URL is loopback; and the request's own `Host` is loopback. The route takes no request body, so it cannot be asked to sign in as anyone else. The matching UI button is not rendered in a build without the flag, because the bundler inlines the missing variable as `undefined` and the comparison fails. **This is not the login** — the real one is Google OAuth (`docs/PRODUCT_CHARTER.md`, `docs/ARCHITECTURE.md`).
- Rationale: three paths had never run in a browser — the binding chooser, the authenticated import path, and the charset rejection path — because the app has no sign-in at all, and every one of them sits behind an owner-bound route (`PLAN.md` task 10). A local password session satisfies the gate exactly as a Google identity would, because `private.has_strong_owner_access` checks the bound owner, `aal2`, and two verified TOTP factors and never inspects the auth provider (D-020); provider verification happens once, at binding.
- Why not `NODE_ENV !== "production"`, which was the first design: the browser suite must run against `next build && next start`, because the strict CSP forbids the `eval()` React needs in development mode, so under `next dev` the app never hydrates and no click does anything (GOTCHAS, "Strict production CSP can block the Next.js development runtime"). A `NODE_ENV` guard is therefore unreachable in exactly the build that has to exercise it. The alternative — relaxing the CSP with `unsafe-eval` for development — is refused by that same GOTCHAS entry and by the standing CSP invariant. The flag is the weaker guard of the two and it is chosen deliberately, because it is the one compatible with never weakening the CSP.
- Consequences, stated plainly: **this is weaker than "impossible in production".** A deployment that set the flag, pointed at a loopback Supabase, and was reached over localhost could serve it; `NODE_ENV` could not be spoofed that way. What is true is that the flag is absent by default, must be present at *build* time for the button to render, and is set nowhere in the product — only in `playwright.owner.config.ts`. One smaller correction, since an early draft of this entry overstated it: a build without the flag does not render the button, but the string `Dev sign-in` is still present in the JavaScript chunk — the bundler kills the branch, not the literal. Verified by searching a normal production build. The route's 404 is the guard that matters; the button's absence is ergonomics. Secondary: the two TOTP secrets are written to the gitignored `.runtime/dev-mfa.json`, because Supabase returns a factor's secret only at enrolment and a later sign-in cannot otherwise produce a code. Plaintext TOTP secrets on disk would be indefensible in production; here they belong to a synthetic owner on a loopback stack. If the factors exist but their secrets do not, the route says so and prints the one-line SQL to clear them rather than failing obscurely.
- Evidence: `tests/dev-session.test.ts` asserts each guard independently with the other two satisfied — missing flag, non-loopback `Host` (and that a loopback host with a port still passes), non-loopback Supabase URL, unconfigured stack — plus a live-stack case that mints a session and then confirms `aal2` and two verified factors by querying `auth.mfa_factors` directly rather than trusting the route's own report. `tests/e2e/owner-session.spec.ts` covers the three previously unreachable paths in a real browser against a production build: a statement bound through the chooser and confirmed into `confirm_import` (asserted against `import_batches` and `source_transactions`), a refused binding to an account whose last four does not match, and a statement carrying `U+4E00` refused at assembly. The charset case is distinguishing by construction: it is the same statement as the passing case with one character substituted. Gate: ESLint, tsc, Vitest 123 passed / 6 skipped against the live container, Playwright 8/8 isolated plus 3/3 owner-session, production build.

## D-037 — Receipts are images, so they are a separate build behind reviewed OCR

- Date: 2026-07-25
- Status: Accepted. Removes the three receipt formats from `PLAN.md` task 11 and opens task 13.
- Decision: The receipts are JPGs, not PDFs. None of the existing reader applies to them — column bands, the frame/grid boundary, the four masked diagnostics and `scripts/mask-statement.mjs` all operate on a pdf.js text layer, and an image has none. They are deferred as their own task. When built, the approach is **template-region OCR with mandatory per-field review**: fields located by pixel region from a per-bank template descriptor, each region OCR'd under a constrained charset, and every money and date value confirmed by the owner against a crop of the exact pixels that produced it before anything is imported.
- Rationale: The owner reports receipts are consistent within a bank and largely consistent across banks, which changes the problem from document understanding to reading known regions of a known template — the same idea as the column descriptors already planned, with pixel boxes instead of text runs. That makes a constrained charset possible per field (an amount is digits, separators and a point), which eliminates the whole `8`↔`B`, `0`↔`O`, `1`↔`l` class of OCR error, and makes structural validation possible: exactly two decimals, a date inside the window `resolveStatementEra` already enforces, a non-empty region. It does **not** make a digit read trustworthy — `3`↔`8` and `5`↔`6` survive all of it, and a misread there is a valid-looking wrong number. That is the D-031 failure shape: parses clean, looks right, silently wrong, in an append-only ledger.
- Consequences: The review step is the actual protection and is non-skippable, so the automation saving over typing four fields is modest for a one-transaction receipt — accepted deliberately, because the owner wants many receipts handled at once. Reviewing a whole receipt would be theatre; showing one field's crop beside its extracted value is a real check, and it is possible only because the templates are fixed. A masked dump for an image is the OCR boxes with the recognized text put through `maskShape`, so the same invoke-don't-read boundary as D-035 holds: shapes and coordinates reach an agent, values do not.
- Open before any of it starts, and none of it assumed: whether tesseract.js runs under this CSP at all (`'wasm-unsafe-eval'` is allowed and `connect-src` is `'self'`, so training data must be self-hosted rather than CDN-loaded — unverified); whether the dependency and Thai training data can be installed offline from the existing pnpm store; and whether the receipts are screenshots (clean, axis-aligned) or photographs (skew, glare, fading), which decides whether preprocessing is needed at all. One masked receipt dump comes before any design claim, per the same discipline `PLAN.md` already applied to layouts.
- Evidence: none yet, by design — this records a scope correction and an intended approach, not an implementation. `PLAN.md` task 13.

## D-038 — What may appear in a masked dump is decided structurally, not by density

- Date: 2026-07-26
- Status: Accepted. Tightens the mechanism D-035 relies on; does not supersede it.
- Context: the first real use of the masking harness put **real counterparty names into a session transcript** — merchant names from an SCB statement, eight of them. No amount, balance, date, or account number escaped; the structural dump was sound throughout. The leak came from `describeLabelGeometry`, the section D-035 had described as safe, while the section its own warning pointed at was clean.
- Decision, in two rules that a reported line must both satisfy:
  1. **No item anywhere on the line may contain a digit** — the whole line is dropped, not just the digit-bearing items. A transaction row always carries a date or an amount; a heading row never does. This is the rule that holds.
  2. The same items must appear at the same vertical position on at least two pages, which is what a running header structurally is. Defence in depth, since a wrapped description could in principle be digit-free. A single-page document therefore reports nothing and is diagnosed from `describeStructure`, which is masked throughout.
  Separately, `maskShape` now masks to `?` everything outside an allowlist of format-bearing punctuation, rather than keeping whatever was neither letter nor digit.
- Rationale: the original rule was "a dense line is a heading" — at least three short digit-free items. A real SCB statement prints every transaction as `<code> | DESC : | <merchant>`, which is exactly that. The first replacement used position alone, reasoning that rows move down a page while a header does not; that was wrong, because rows sit on a fixed pitch, so the same `y` recurs on every page and a frequent merchant lands in the same slot twice — two of the eight survived it. The `maskShape` change came from the same round: a KBANK glossary embeds subset fonts with no usable `ToUnicode`, so Thai and Chinese decoded to arbitrary symbols like `⤎ ⁄ ⏟ ® £ ¤`, which are neither letters nor digits and so passed through verbatim. Unreadable, but a deterministic remapping of real content that anyone holding the font's cmap could undo.
- Consequences: two design lessons worth more than the fix. **One bank's evidence made an unsafe rule look safe** — Krungthai never tripped the density rule, and it survived a privacy review, a decision entry, and a published claim before a second layout broke it. And **a test fixture that is easier to satisfy than the document proves nothing**: the first regression test omitted the date and amounts a real row necessarily carries, which is exactly why it passed against a rule that did not hold. The fixture now carries them.
- Evidence: `lib/masked-diagnostics.ts` (`describeLabelGeometry`, `maskShape`, `FORMAT_CHARACTERS`), `tests/privacy.test.ts` ("never reports a transaction row, however heading-shaped it looks", "masks a character that decoded to a symbol rather than letting it through"). Confirmed against the real statements: re-running the harness on the same 12 SCB documents reports only the two heading lines, English and Thai, and no counterparty. Gate: ESLint, tsc, Vitest 125 passed / 6 skipped against the live container, Playwright 10/10 isolated and 3/3 owner-session, production build.

## D-039 — A slash-joined money heading is two right-aligned columns, and direction is arithmetic

- Date: 2026-07-26
- Status: Accepted. Corrects `docs/SCB_CONTRACT.md` and `docs/KBANK_CONTRACT.md` as first written on 2026-07-26; does not supersede D-035, which produced the dumps both were derived from.
- Context: the first drafts of both layout contracts were wrong about the single most consequential field, in opposite ways. SCB's said *"One signed money column, not two. `Debit/Credit` is a single column; direction is not encoded by which column the figure sits in."* KBANK's listed `Withdrawal / Deposit` as one of five heading anchors and said nothing about a split at all. Building either as written would have read **every deposit as a withdrawal** — a clean parse, a plausible ledger, and an inverted sign on every credit, which is the D-031 failure shape in an append-only ledger.
- What the dumps actually show, on three independent lines of evidence:
  - Across all 12 SCB statements every row carries **exactly one** run in the money region — never none, never two — and those 361 runs right-align into two tight clusters with nothing between them. KBANK's 89 rows do the same, with the two clusters closer together.
  - SCB's summary block right-aligns `TOTAL AMOUNTS (Debit)` on one cluster and `(Credit)` on the other, and prints the two `TOTAL ITEMS` counts on those same edges. The document labels its own columns.
  - The balance chain agrees: a left-cluster run always decreases the printed balance and a right-cluster run always increases it. `kbank-03` shows it at its cleanest — one right-cluster run takes the balance from tens to tens of thousands and the next row's left-cluster run, **the same printed shape**, takes it back to single digits.
- Decision, in three parts:
  1. **Direction is derived from the balance chain**, not from geometry: the delta from the previous printed balance is either `+amount` or `-amount` and cannot be both, since a zero amount is already refused. This is arithmetic on printed values rather than an inferred coordinate.
  2. **Geometry must agree, and disagreement fails closed.** Each derived direction must occupy one tight cluster of right edges and the two clusters must be distinct, with withdrawals left of deposits as both headings name them. Redundant with the chain by construction and kept deliberately: it is the check that catches the reader picking the wrong run as the balance, which is the shape of D-030.
  3. **Right edges, not midpoints.** For a right-aligned column the right edge is the invariant; a midpoint moves with the value's width, which is what a long figure exploits.
- A second correction in the same pass, for the same reason: **a layout is identified by its heading anchor set, never by a bank name.** A masked dump masks every letter, so it cannot reveal what name a statement prints — the claim would be unfalsifiable against the only evidence available. It would also be wrong: both KBANK statements print `Internet/Mobile SCB` as an ordinary channel, so a signature matching `SCB` routes every KBANK statement to the SCB reader. A bank's name appears on other banks' statements because that is what a transfer is. Krungthai keeps its own name signature, which is proven against a real statement, and is tried only after the heading sets fail to match.
- Consequences: the heading anchors do **not** bound the data columns in either new layout, so `lib/krungthai-layout.ts`'s midpoint banding could not be reused and `lib/statement-layout.ts` reads an ordered row grammar instead. That is why the Krungthai reader was left alone rather than generalized — the abstraction that would have fitted one layout does not fit three.
- Evidence: `lib/statement-layout.ts` (`assignDirections`, `verifyMoneyColumns`, `matchLayout`), `tests/statement-layout.test.ts` ("reads its credit as a deposit", "refuses a figure printed in the wrong money sub-column" for both layouts, "keeps a KBANK statement whose rows name another bank on the KBANK reader"). Distinguishing: disabling `verifyMoneyColumns` fails exactly the two misfiled-column tests and nothing else. Gate: ESLint, tsc, Vitest 154 passed / 6 skipped, pgTAP 90/90, production build, Playwright 14/14 isolated and 5/5 owner-session.

## D-040 — The currency guard moves to the column heading, per layout, rather than being dropped

- Date: 2026-07-26
- Status: Accepted. Extends D-034 to two further layouts; does not supersede it — Krungthai still requires `Currency THB` in its frame block.
- Decision: neither SCB nor KBANK states a currency in its frame block, so D-034's guard as written cannot be satisfied by either. It is relocated per layout rather than relaxed: the marker must appear in the **heading block**, which SCB prints as `Balance/Baht` and KBANK as `(THB)` on the line beneath its balance heading. A statement whose headings do not state THB is refused as `UNSUPPORTED_CURRENCY`.
- Rationale: the guard exists because a statement denominated in another currency must not import as THB. Dropping it for a layout that happens not to print the marker where Krungthai does would reintroduce exactly that. The heading block is a stronger place than the frame, not a weaker one — the marker is attached to the very column whose figures it denominates.
- Consequence worth noting: for SCB the marker sits **inside a required heading anchor**, so a statement that reads at all has stated its currency and the failure is unreachable. KBANK's is separable and is tested.
- Evidence: `lib/statement-layout.ts` (`currencyMarker`, `extractFrame`), `tests/statement-layout.test.ts` "refuses a statement whose heading block does not state the currency".

## D-041 — Migration 009 admits three banks, and takes the fingerprint's bank from the bound account

- Date: 2026-07-26
- Status: Accepted. Applied locally and tested; **not deployed** — no hosted resource exists.
- Decision: `supabase/migrations/202607260009_multi_bank_layouts.sql` widens `accounts.bank_code` and `import_artifacts.contract_version` to the three supported values, adds `private.contract_bank_code`, and changes `confirm_import` in two ways: the bank code passed to `private.row_fingerprint` now comes from the **bound account** instead of the literal `KTB`, and a contract version may only be confirmed into an account at the bank that layout reads.
- Rationale: widening the CHECK constraints was the easy half and not the half that mattered. `confirm_import` recomputed every row fingerprint with a hard-coded `KTB` while the client computes it from the statement's own bank code, so every SCB and KBANK import would have failed with `fingerprint mismatch` — a message that points at tampering for what was in fact a constant. Once the bank comes from the account, the pairing becomes enforceable, so it is enforced: binding an SCB statement to a Krungthai account is a different mistake from a tampered fingerprint and is named as one.
- Consequences: `lib/statement.ts` pins `contractVersion` to `bankCode` in the payload schema, and `assembleImportPayload` refuses a bank mismatch before it checks the last four — necessary because `public.accounts` is unique on (owner_id, bank_code, last_four), so one owner may legitimately hold three accounts ending in the same digits and matching on digits alone would post one bank's transactions into another's ledger. The seed gains an SCB and a KBANK account, because the app has no account-creation surface and without them neither new reader could be exercised end to end.
- Evidence: `supabase/tests/002_security_contracts.sql` tests 31–36. Two red proofs, both run: against the pre-009 `confirm_import` the SCB import dies with `invalid import contract`; with the CHECK constraints widened but the fingerprint's bank left as the literal, the same import dies with `fingerprint mismatch`. `tests/e2e/owner-session.spec.ts` carries an SCB statement through the chooser into `confirm_import` in a real browser and refuses one bound to a Krungthai account sharing its last four. Gate: pgTAP 90/90 across migrations 001–009 after a clean reset.

## D-042 — A reader must say how much of the parse it managed to verify

- Date: 2026-07-27
- Status: Accepted. Makes visible the compromise D-033 accepted; does not change it.
- Context: the first real SCB and KBANK reads both succeeded, and in doing so showed that the owner cannot tell a verified import from an unverified one at the moment of confirming it. The status line reported a row count, an account suffix and a period. It did not say which of the three layouts had read the statement, and — the part that matters — it did not say whether the rows had been checked against the statement's own printed counts and totals.
- Why that is not cosmetic: `extractTotals` returns nulls when no summary label matches, `verifyTotals` skips every null, and the statement is then accepted with **no global check at all**. D-033 chose that deliberately, because not every layout prints such a block and refusing one that does not would reject a readable statement. The consequence is that a 55-row import whose count the bank itself confirmed, and a 55-row import where nothing would have caught a dropped row, produced identical screens.
- Decision: `StatementFrame` carries `crossChecked`, true only when a summary block was read in full — **all four** per-direction counts and totals — and agreed with the rows. A partially readable block is false: it confirms part of the parse, and calling that cross-checked overstates it by exactly the amount that matters. Krungthai's `Total Page` does not count towards it, because a page count confirms nothing about rows. The bind stage names the layout that read the statement and states plainly whether it was cross-checked, with the unverified case rendered as an alert — that screen is the last point at which the owner can decline.
- Why on the frame rather than beside it: `balancesPrinted` is already exactly this kind of flag — how much verification the document permitted — and lives there. Adding a second one changes no committed contract: `assembleImportPayload` copies named fields out of the frame rather than spreading it, and `confirmationDigest` hashes only the account, contract version, currency, period, balances and rows. (Older wording in `PLAN.md` and `HANDOFF.md` said "`StatementFrame` is hashed into the import digest". It is not; the payload built from it is. Corrected in the same pass.)
- What this deliberately does **not** do: persist it. Whether an import was cross-checked is still recorded nowhere in the ledger, so a committed batch cannot be distinguished after the fact. That is `PLAN.md` task 12, it needs a migration and a payload-contract change, and it needs an owner decision first on what a missing cross-check should mean for an already-committed import.
- Evidence: `lib/statement-frame.ts`, `lib/statement-layout.ts`, `lib/krungthai-layout.ts`, `app/ledger-app.tsx`. Tests: `tests/statement-layout.test.ts` ("reports whether the rows were checked against the statement's own totals", "does not call a partially readable summary block a cross-check") and `tests/krungthai-layout.test.ts` ("still reads a statement that prints no summary block at all, and says it was not checked"). Distinguishing: forcing the flag true fails exactly those three tests and nothing else. Gate: ESLint, tsc, Vitest 156 passed / 6 skipped against the live container, production build, Playwright 14/14 isolated and 5/5 owner-session. pgTAP not re-run — no SQL, migration, or database code changed; last run 2026-07-26, 90/90 across migrations 001-009.

## D-043 — An import the bank's arithmetic did not confirm is refused, not labelled

- Date: 2026-07-27
- Status: Accepted by the owner. Closes `PLAN.md` task 12 and settles the question D-033 left open; supersedes the part of D-042 that rendered the unverified case as a warning the owner could proceed past.
- Decision: `assembleImportPayload` refuses a statement whose `crossChecked` is false, with `NOT_CROSS_CHECKED`, before it checks anything about the binding. Nothing is persisted and no migration is needed, because nothing unverified can reach the ledger to be labelled.
- What decided it, and it is not what it first looked like. The cross-check appeared to be the main defence against a dropped row; it is not. `reconcileRows` and `confirm_import` both require `previous + movement = printed` on every row, so a dropped **middle** row surfaces as a gap at the next row and is already refused. What the balance chain cannot see is narrow, and differs per layout:
  - a dropped **first** row is invisible on Krungthai, whose opening is derived from row one (D-026), and caught on SCB and KBANK, which print a brought-forward balance;
  - a dropped **last** row or trailing page is invisible on Krungthai and SCB, neither of which prints a closing balance, and caught on KBANK, which prints `Ending Balance`.
  So the cross-check earns its keep precisely where the printed balances run out — and most of all on Krungthai, the layout with 233 real rows already read through it.
- Why refusing costs almost nothing: **all three known layouts print a summary block** — Krungthai's confirmed on the real statement (D-033), SCB's in all twelve dumps, KBANK's in both. A gate therefore never fires on a document that has been seen. Its whole cost is future variants, and that cost is recoverable: the reader learns the wording and the statement imports. The alternative's cost is not — an unverified row in an append-only ledger can only be removed by restoring from a backup taken before the import.
- Rejected alternatives, and why:
  - *Persist a label and import anyway.* The ledger could then answer "which imports were verified?", but the default path would write unverifiable rows permanently, against D-033's own reasoning that an append-only ledger cannot take back a dropped row.
  - *Refuse with a recorded override.* Better, and the natural upgrade if a real statement is ever refused — but designing an override today means designing it for a document nobody has seen. Wait for the example.
  - Both alternatives share a flaw worth recording: the server cannot recompute `crossChecked`, because it never sees the PDF, so a persisted flag would be a **caller's attestation** — weaker than everything around it, since D-012 rebinds the payload digest server-side and D-014 recomputes every fingerprint. Making it a fact rather than a claim would mean sending the statement's printed counts and totals in the payload and having `confirm_import` re-verify them against the rows it is storing. That is the shape any future option C should take.
- Where the refusal lives, and why not in the reader: the reader still reads such a statement. What a document contains is a fact, and the diagnostic that fixes a wording mismatch depends on the parse succeeding. Putting the gate at the import boundary also keeps a dozen fixtures that never print totals readable, instead of coupling every layout test to this policy.
- Consequence, and the one that makes the refusal actionable: the worker now sends `describeValueLabels` with a **successful** parse when `crossChecked` is false, so the bind screen can show the candidate summary wordings the statement actually prints. Without it the owner would meet a refusal with nothing to act on, since that diagnostic previously existed only on the failure path. It crosses on the same terms as before — reduced to a local before the post, never `pages`, and `tests/privacy.test.ts` holds it there.
- Evidence: `lib/import-assembly.ts` (`NOT_CROSS_CHECKED`), `workers/krungthai.worker.ts`, `app/ledger-app.tsx`, `tests/import-assembly.test.ts` ("refuses a statement whose printed totals never confirmed its rows", "names the missing cross-check before anything about the binding"). Distinguishing: disabling the guard fails exactly those two tests and nothing else. Gate: ESLint, tsc, Vitest 158 passed / 6 skipped against the live container, production build, Playwright 14/14 isolated and 5/5 owner-session. pgTAP not re-run — no SQL, migration, or database code changed; last run 2026-07-26, 90/90 across migrations 001-009.

## D-044 — Portable recovery is rehearsed into a second, separately bound local project

- Date: 2026-07-27
- Status: Accepted. Implemented and passing locally; discharges the *local* half of `PLAN.md` § Later authorization gates item 3. Nothing hosted has been tested, and no real data has been imported.
- Decision: a second Supabase project, `private-ledger-recovery` (`recovery/supabase/config.toml`), runs alongside the primary one on ports 5433x, bound to a different invented owner. `scripts/recovery-destination.mjs` brings it up, applies every committed migration and binds that owner; `tests/recovery-portability.test.ts` carries a ledger from the primary project into it and asserts it arrived whole and wholly rebound.
- Why a second **project** and not a second database or a reset: the ledger schema depends on `auth` in 24 places across 8 migrations, including `ledger_owners.owner_id references auth.users(id)`. A `createdb` inside the same container has no `auth` schema at all, so migration 001 fails on its eighth line — the cheap option does not exist. And a real aal2 session can only be minted by a GoTrue pointed at the destination's own database, so anything short of a second project leaves the restore driven by hand-set `request.jwt.claims`, which is the fidelity gap the rehearsal exists to close. Resetting the primary project to a new owner would have worked, but it perturbs the seed that `tests/helpers/local-owner.ts`, the pgTAP fixtures and the Playwright owner config all assume.
- What this proves that `tests/backup-roundtrip.test.ts` cannot. That suite restores into the project the snapshot came from, as the owner it came from — so every id in the payload already exists in that project's `auth.users` and an unremapped actor column still satisfies its foreign keys; its "empty destination" is manufactured by deleting rows; and it drives the RPC through psql. Here the destination has never seen the source's auth users, `restore_backup` never disables replication triggers so those foreign keys are live throughout, and both sides are driven over HTTP with real aal2 sessions from their own GoTrue instances.
- The finding that justified the exercise: **a restore has to rebind an owner id that lives inside jsonb, and no column-level check can see it.** `overlay_revisions.snapshot` is `to_jsonb` of the whole overlay row, so it embeds `owner_id`; `restore_backup` merges `jsonb_build_object('owner_id', v_owner)` over it for exactly that reason. Red proof: stripping that merge from the destination's function leaves the ledger arriving intact, every owner and actor **column** correctly rebound, and the source owner's id sitting inside a jsonb payload — the suite fails on that assertion alone. The first fixture written for this test embedded no owner id and so could not have caught it; the fixture now builds the snapshot the way the overlay RPC does.
- Consequence — `lib/restore-plan.ts`: the manifest, chunk digests and ordering previously existed only inside `tests/backup-roundtrip.test.ts`. Recovery is the one path that has to work on a machine that has lost everything else, and a builder that lives in a test means a real restore begins by reimplementing eleven chunk digests, an aggregate payload digest and the count bindings from memory, against a server that recomputes all of them and refuses without saying which was wrong. It now ships in `lib/`, and the round-trip suite consumes it rather than duplicating it — which also exercises it at 1,200 rows.
- What this deliberately does not cover: there is still **no restore surface in the app**, and its only export button produces the non-restorable `.pldemo` preview. The rehearsal drives PostgREST directly, so the thin Next.js route wrapper at `app/api/v1/backups/restores/[action]/route.ts` is not exercised by it. Nothing hosted has been rehearsed at all.
- Evidence: `recovery/supabase/config.toml`, `recovery/destination-seed.sql`, `scripts/recovery-destination.mjs`, `lib/restore-plan.ts`, `tests/recovery-portability.test.ts`, `tests/helpers/local-owner.ts` (`psqlAt`, `sessionAt`), `docs/RECOVERY.md` § Portable recovery rehearsal. Gate: ESLint, tsc, Vitest 159 passed / 7 skipped against both live containers, production build, Playwright 14/14 isolated and 5/5 owner-session. pgTAP not re-run — no SQL, migration, or database code changed; last run 2026-07-26, 90/90 across migrations 001-009.

## D-045 — The owner can create an account, through an RPC and nothing else

- Date: 2026-07-27
- Status: Accepted. Migration `202607270010_account_creation.sql` applied locally and tested; **not deployed** — no hosted resource exists.
- Context, and it was a dead end rather than a missing feature: every account in existence came from `supabase/seed.sql`, because `public.accounts` grants `authenticated` select only and has no insert policy. A real statement prints an account suffix, binding matches on bank **and** last four (D-017, D-041), and both statements read on 2026-07-27 printed suffixes no seeded account carried. So a statement could be opened, parsed, cross-checked, and then bound to nothing — with no action available inside the app that would change that. The recovery gate (D-044) was never what stood between this project and its first real import; this was.
- Decision: `public.mutate_account(p_action, p_id, p_bank_code, p_label, p_account_type, p_last_four)`, security definer, modelled directly on `mutate_category` — strong-access gate, ledger-mutation advisory lock, audit row, mutation-sequence bump. `POST /api/v1/accounts` calls it; the bind stage offers a form exactly when the loaded accounts contain none matching the statement's bank and last four, with those two fields taken from the statement rather than typed.
- Create and relabel only, and this is the substantive part. `label` is the only column on an account that is not identity: `bank_code` is hashed into every row fingerprint and `confirm_import` recomputes each one from the bound account's bank (D-041), so changing it would invalidate every fingerprint stored under that account; `last_four` is what binding matches against, so changing it silently re-scopes imported history. A relabel that is sent an identity field is therefore **refused rather than ignored** — a caller that sends `bank_code` believes it is changing it, and returning success for something that did not happen is worse than an error. Deletion is not offered at all: `source_transactions.account_id` references the account, and an append-only ledger has no story for removing what its rows hang from.
- The bank list is deliberately not restated in the function. Migration 009 exists because `confirm_import` restated `'KTB'` as a literal and silently gated two banks out of the product (D-041, GOTCHAS). Here the table's own CHECK constraints stay the single authority: the RPC catches `check_violation` and names it `invalid account`, and `unique_violation` becomes `account already exists`, which the route maps to 409 because a duplicate is something the owner can act on rather than a malformed request. Adding a fourth bank remains a one-line change to one CHECK plus `BANK_CODES`.
- Consequence: creating an account advances the mutation sequence, so it marks the backup stale — correctly, since `accounts` is one of the eleven tables a backup carries. The UI reflects that.
- Evidence (D-045): `supabase/migrations/202607270010_account_creation.sql`, `lib/accounts.ts`, `app/api/v1/accounts/route.ts`, `app/ledger-app.tsx`. Tests: pgTAP 001 (the RPC exists; `authenticated` holds no insert, update or delete on `public.accounts`) and 002 (create, audit row, sequence bump, duplicate refused, same digits at another bank allowed, unsupported bank refused by the constraint, relabel, identity change refused, single-factor session refused); `tests/import-route.test.ts` § POST /api/v1/accounts; `tests/e2e/owner-session.spec.ts` "creates the account a statement needs, from the bind stage". Distinguishing: `grant insert on public.accounts to authenticated` fails exactly one test of 28 in pgTAP 001 and nothing else. Gate: ESLint, tsc, Vitest 164 passed / 7 skipped, production build, pgTAP 104/104 across migrations 001–010 after a clean reset, Playwright 14/14 isolated and 6/6 owner-session.

## D-046 — Recovery is something a person can do, not something a developer can reconstruct

- Date: 2026-07-27
- Status: Accepted. Closes `PLAN.md` task 14. Implemented and tested locally; nothing hosted.
- Context: D-044 proved the restore contract portable and moved the request builder into `lib/restore-plan.ts`, but left the surface untouched — the app had no restore control at all, and its only export button produced the `.pldemo` synthetic preview, which is explicitly not restorable. `GET /api/v1/backups/export` had no caller in the product. So the honest description of recovery was "it works, if you write code", which is the wrong answer for the one path that has to work on a machine that has lost everything else.
- Decision: a Recovery section in `app/ledger-app.tsx`, deliberately not tied to a parsed statement, because the moment it is needed there is no statement in hand. **Export** fetches the snapshot, validates it against `backupSnapshotSchema`, encrypts it in the browser, writes a `.plbak` file, and only then acknowledges custody. **Restore** takes that file and its password, decrypts locally, builds the sequence with `lib/restore-plan.ts`, and posts stage, eleven chunks and commit through the existing route.
- Ordering, which is the part that carries meaning: custody is acknowledged **after** the file has been handed to the download flow, and the database records it only if the ledger has not moved since the snapshot. A failed acknowledgement is therefore reported as "backup written, custody not recorded" rather than as a failed backup — the file exists, and telling someone their backup failed when it did not is its own kind of harm.
- `encryptedBackupSchema` (`lib/backup.ts`) checks the file's shape before any decryption is attempted, so "this is not a Private Ledger backup" is answered as such. Without it that case surfaces from WebCrypto as the same opaque failure as a wrong password — and a wrong password is the recoverable one, so conflating them is the worst available diagnosis.
- The empty-ledger requirement is presented as the point rather than as a limitation: a restore rebinds every row to whoever is signed in, so permitting it over live data would be an overwrite wearing a recovery's clothes. It is enforced at commit, after all eleven chunks are accepted.
- Consequence, and a latent trap this surfaced: adding a second `<input type="file">` and a second `<select>` to the page broke every browser spec at once, because the suites located both by tag. Named form controls now: `statement-pdf`, `ledger-account`, `restore-file`, `restore-password`, `ledger-backup-password`, `new-account-label`, `new-account-type`. A bare tag locator was never a contract; it only looked like one while the page held exactly one of each.
- Evidence (D-046): `app/ledger-app.tsx`, `lib/backup.ts` (`encryptedBackupSchema`), `app/globals.css`. Tests: `tests/e2e/owner-session.spec.ts` "backs up a confirmed ledger and restores it after the ledger is destroyed" — import four rows, export, **destroy the ledger with a targeted wipe**, restore, and assert the rows, the batch and the bound account all return — and "refuses a restore into a ledger that still holds rows". The wipe deliberately avoids `resetOwnerImportSurface`, which also drops the owner's TOTP factors and so would remove the session the restore needs. Gate: ESLint, tsc, Vitest 164 passed / 7 skipped, production build, pgTAP 104/104 (unchanged; no SQL touched), Playwright 14/14 isolated and **8/8** owner-session, and `tests/recovery-portability.test.ts` re-run against a live destination because `lib/backup.ts` changed.

## D-047 — The first real import, and the guard it forced

- Date: 2026-07-28
- Status: Accepted. The local ledger now holds real financial records. This supersedes the "committed data remains invented only" line in `SPEC.md`, which is corrected in the same pass.
- What happened: the owner imported one real KBANK statement through the app — 55 rows across 2 pages, six-month period, read on the first attempt, **cross-checked** against the bank's own printed counts and totals. The account it needed did not exist and was created from the bind stage (D-045). The import was immediately backed up (D-046), and the backup was then **proven** rather than assumed: the ledger was deliberately destroyed — all 175 rows across eight tables — and restored from the encrypted file alone. All 175 returned, 55 distinct fingerprints, one owner, the account back under its original id, and the mutation sequence advanced 2 → 3 with the restored ledger correctly marked backup-stale.
- Why the rehearsal was worth the risk: an untested backup is a hope. The cost of it failing was re-running the import from a PDF that had already read first time; the cost of discovering it was worthless later is the ledger. That asymmetry only holds while the ledger is small, which is why it was done on day one rather than deferred.
- **The consequence nobody had accounted for: `pnpm test` became a command that destroys real financial records.** `resetOwnerImportSurface` deletes every transaction, component, provenance row, batch, artifact and audit event **the owner has** — owner-scoped, not test-scoped — and both the Playwright owner config and `tests/import-route.test.ts` call it in setup and teardown. `tests/backup-roundtrip.test.ts` wipes every row unscoped between an export and a restore. None of that was wrong while the database held nothing but a seed; all of it became dangerous the moment a real row existed, and nothing in the repository would have said so.
- Decision: `assertOnlyDisposableLedgerData` in `tests/helpers/local-owner.ts` refuses to proceed when `public.accounts` holds an account created by neither the seed nor the calling suite, and `resetOwnerImportSurface` calls it, so every caller is covered without opting in. The two suites that wipe outside that helper call it directly. The escape hatch is `ALLOW_DESTRUCTIVE_TESTS=1`, deliberately awkward, and the refusal names the backup path before it names the override.
- Why an unrecognised **account** is the signal: suites always know their own account ids and the seed's are fixed, so anything else was created by a person. It also trips on a leftover account from a crashed run, which is correct rather than a false positive — something is present that no test is entitled to delete.
- What this does not solve: `pnpm supabase:reset` is the Supabase CLI and cannot be guarded from here, so it remains able to destroy everything. The real fix is separating the ledger that holds real data from the one the tests own — chosen in principle, not yet built, and this guard is what makes the interval survivable rather than a substitute for it.
- Honest limitation, recorded because it will be met again: with real data present, **the full suites can no longer be run to green on this database** — the guard refuses, which is it working. Verification of the guard itself was therefore done both ways without wiping anything: with only seed ids recognised one account is unrecognised and every destructive suite refuses; with the real account declared recognised the count is zero and the path opens.
- Evidence (D-047): `tests/helpers/local-owner.ts` (`assertOnlyDisposableLedgerData`, `SEEDED_ACCOUNT_IDS`), `tests/backup-roundtrip.test.ts`, `tests/recovery-portability.test.ts`, `tests/e2e/owner-session.spec.ts` (the KBANK cleanup moved ahead of the reset, since a UI-created account has a database-assigned id the guard cannot recognise). Also `.gitignore`: the first real backup was written into the repository root and was untracked but stageable, so `*.plbak` is now ignored.

## D-048 — The real ledger gets its own project; the test project goes back to being disposable

- Date: 2026-07-28
- Status: **Complete and verified 2026-07-28.** The data moved by restoring the backup into the empty live project, exactly as designed. Verification after the move: all eleven table counts equal the backup's own `tableCounts` (175 rows); the md5 digests of the sorted account ids, transaction ids and fingerprints are byte-identical between the two projects, so every row returned under its original id rather than being re-created; 55 distinct fingerprints across 55 rows; every row rebound to the live owner `4444…` rather than the test owner `1111…`. `private-ledger-local` was then reset to migrations + seed and holds only the three seeded accounts. Full validation went green on it afterwards — Vitest 164/7, pgTAP 104/104, ESLint, `tsc`, build, Playwright 14/14 isolated and 8/8 owner — closing the "not yet verified" note below. Live's digests were unchanged by all of that, which is the separation working rather than an assumption about it.
- Context: D-047 put a guard in front of the destructive suites, and a guard is a fence around a hazard rather than the removal of one. It also cannot cover `pnpm supabase:reset`, which is the Supabase CLI. And it left the project in a state where a green suite run and a populated real ledger could not both exist on one database — an unstable equilibrium that would eventually be resolved by someone typing `ALLOW_DESTRUCTIVE_TESTS=1`.
- Decision: a third local project, `private-ledger-live` (`live/supabase/config.toml`), on ports 5434x, holding real records and nothing else. `private-ledger-local` returns to being purely synthetic and disposable, and remains what every suite, seed and pgTAP fixture targets — so no test, helper, fixture or CI assumption changes. Real data moves by **restoring a backup into the empty live project**, which is the portable-recovery path of D-044 used for its actual purpose rather than as a rehearsal.
- Which project moves, and why this way round: moving the *tests* would have meant rewriting the hardcoded container name, ports and publishable key in `tests/helpers/local-owner.ts` and every suite that reaches for them. Moving the *data* costs one restore — an operation already proven end to end, twice, including once on these very rows.
- `down` behaves differently per project, and deliberately: the recovery destination passes `--no-backup` because a rehearsal reusing a dirty destination proves less than it appears to, while the live ledger stops without it, preserving the volume. **No script deletes real records.**
- The enforcement that makes this more than a convention: `playwright.owner.config.ts` now pins `NEXT_PUBLIC_SUPABASE_URL`, the publishable key and `OWNER_GOOGLE_EMAIL` in its `webServer.env`. `next start` loads `.env.local`, so once that file points at the live ledger an unpinned browser suite would run its owner-scoped wipes against real financial records. The pinned values are the CLI's fixed local defaults, so pinning costs nothing and removes the path entirely.
- The owner identity is a placeholder, recorded as such: the live project binds the same local password account the test seed uses, because the development sign-in has that address compiled into it and Google OAuth is still unbuilt behind the hosted gate. `public.ledger_owners` is immutable, so replacing it later means a fresh project and a restore into it — a known, tested operation, which is precisely the value of having proved it. The owner *id* differs from the test project's so a stray row's provenance is never ambiguous.
- Consequence: `scripts/recovery-destination.mjs` and `scripts/live-ledger.mjs` are now thin declarations over `scripts/lib/local-supabase.mjs`, which carries the migrate-and-seed machinery both need. The `db push` limitation D-044 recorded is documented there once instead of twice.
- Evidence: `live/supabase/config.toml`, `live/live-seed.sql`, `scripts/live-ledger.mjs`, `scripts/lib/local-supabase.mjs`, `scripts/recovery-destination.mjs`, `playwright.owner.config.ts`, `docs/LOCAL_DEV.md`. Verified: the live project starts, applies all ten migrations, binds its owner and reports an empty ledger; the refactored recovery script still reports correctly; all three projects coexist on their own ports. ESLint and `tsc --noEmit` pass. **Not yet verified, and cannot be until the data moves:** a full green suite run, because the test project still holds the real rows and the D-047 guard correctly refuses.

## D-049 — Agents may read real statements, but only re-passworded copies, and only in one directory

- Date: 2026-07-28
- Status: Accepted, at the owner's explicit direction after the trade-off was stated and restated.
- The problem it solves: the bank password is derived from the owner's **date of birth and citizen ID**. It is identity-grade and cannot be rotated — a leaked one is leaked for life, and it unlocks far more than a PDF. Every previous route around this (masked dumps, D-035) existed to keep an agent from ever needing that password. The owner wanted to widen access without widening *that* exposure.
- Decision: `scripts/repassword-pdfs.py` re-encrypts statements under a disposable password. Copies go to `shared-statements/`, which agents may read directly. `private-statements/` is untouched, still closed, and every existing "never inspect `private-statements/`" instruction stays literally true — which is why the copies got their own directory rather than a carve-out in the old one.
- What the owner accepted, in his words rather than inferred: the transaction rows are not sensitive to him. He was told, and confirmed after being told, that a statement's *header* — name, account number, branch — is the identifying part, and that **agent file reads are not local**: contents go to Anthropic's servers. That correction mattered; earlier sessions had assumed on-device reads. The acceptance covers statement contents only, and does not extend to passwords, keys, `.env*`, backups or the citizen ID.
- The rule that had to survive intact: **read access is not write permission.** An agent may now look at a real value; it still may not put one into a fixture, a doc, a commit or a transcript. D-035 kept those two things fused because no agent could see a value anyway; separating them is the actual risk this decision introduces, so `docs/FIXTURE_POLICY.md` states it twice and names screenshots specifically.
- Masked dumps remain first resort, deliberately. Permission is not a default. Most reader work needs structure, and the cheaper route staying the habitual one is what keeps this narrow.
- Tool design worth keeping: the password is read by `getpass` and never accepted as an argument, env var or file — the protocol that held all through D-047. Files the supplied password does not open are reported and left alone rather than failing the run, so KBANK/SCB (one password) and Krungthai (another) are handled by running twice with no bank detection anywhere. Archive mode writes and *verifies* a replacement before `os.replace` touches the original.
- **Where the protection actually lives, since this is the part most likely to be misread later:** `archive` mode, run first, is the security step — it takes the owner's own files off the DOB/citizen-ID password permanently, and the two bank passwords collapse into one he chose. The shared copies are then written **plain** (`copy --decrypt`), because encrypting them would have protected nothing: an agent given the password to read them is not meaningfully walled off by it, and the round trip would have added a decryption step to every read. `--decrypt` is rejected in archive mode, where stripping the password from the owner's originals would be the opposite of the intent.
- Both steps completed 2026-07-28. The archive rotation is unverifiable from inside the agent boundary — `private-statements/` is closed — so the `copy` run was its own proof, and it is worth recording *why* it counts as one: 16 files reported `decrypted` under the new password and **no file reported `not opened by the supplied password`**, with all three banks present in the output. An absent file is invisible from the agent's side, so the meaningful evidence is the missing failure line rather than the success count. The per-file page numbers the tool printed sum to 76, matching an independent count taken by opening the copies.
- Evidence: `scripts/repassword-pdfs.py`, `docs/FIXTURE_POLICY.md` § Shared working copies, `docs/LOCAL_DEV.md`, `.gitignore`. Verified on generated PDFs, never on real ones: `self-test` passes, and it was **red-proved three times** — sabotaging the rotation to keep the old password, dropping a page, and making `--decrypt` silently encrypt, each make it fail. Also confirmed: a file under a third password is reported and left byte-identical with no destination written; an already-rotated file is a no-op; a failed archive rotation leaves the original byte-identical and no temp file behind. pikepdf 10.10.0 (`cp314` wheel, no toolchain needed) is a **local owner-tool dependency, not a project dependency** — it is not in `package.json` and nothing in the app or the suites imports it.

## D-050 — Slips are provisional entries the statement confirms, and their images are not stored

- Date: 2026-07-28
- Status: Accepted as direction. Nothing built; this records a design the next session should follow rather than re-derive.
- Context: the owner wants daily capture from bank-app transfer slips (K PLUS, K NEXT, SCB Easy) with statements imported weekly or monthly, and cash purchases entered by hand. Three real samples were assessed.
- What the samples established: they are **app screenshots, not thermal paper**, so the hard OCR case does not apply — crisp, high-contrast, fixed layouts. All three QR codes decoded first try with no upscaling. The payloads are EMVCo TLV (the Thai slip-verification format): bank code, a unique transaction reference and a CRC, and **no amount**. Three banks meant three entirely different layouts, and the SCB sample was a bill payment, so transfer variants are unseen — assume more layouts exist, as `KBANK-01.pdf` taught the same day.
- Decision: a slip is a **provisional entry**, not an authoritative row. The statement remains the authority and reconciles against slips already captured. This preserves the invariant the whole ledger rests on — every authoritative import is cross-checked against the bank's own printed totals, and a slip has no arithmetic to check against, so promoting slips to authoritative would invert the trust model.
- Decision: **the QR supplies identity, OCR supplies values, and the owner confirms.** The QR is exact and free of OCR risk, so it carries the dedup key and the statement-matching key — the part that is catastrophic to get wrong. Amount, date and merchant are proposed and never auto-confirmed, reviewed against the image with region-targeted OCR and a digit whitelist rather than whole-image recognition.
- Decision: **slip images are not stored.** Storing them either bloats the backup from 14 KB to hundreds of megabytes, or sits outside it in Supabase Storage and costs the property that one file restores the whole ledger. The images already exist in the owner's Google Photos, better backed up than the app would manage. What is stored is the QR payload, the extracted values, and optionally an image hash — "store what you cannot reconstruct". A thumbnail is the fallback if browsing ever demands one; it is easy to add and painful to remove at 3,000 rows.
- Rejected: a paid slip-verification API (Thunder 150/month free then ~199 ฿/month; SlipOK from 210 ฿; EasySlip from 350 ฿). Its only benefit is same-day authority, which the statement supplies free a week later, and it would send every transaction reference to a third party — awkward for an app premised on the data staying the owner's.
- Rejected: syncing the owner's three Google Photos albums. Google removed the `photoslibrary.readonly` scope on 2025-03-31; apps now reach only media they created, and anything else returns `403 PERMISSION_DENIED`. The Picker API replaces it and requires explicit per-session selection. **Share-to-app is preferred anyway** — an installable web app with a share target captures each slip at the moment of payment rather than from a roll at midnight, needs no Google scope, and retires the albums as a pipeline.
- Build order, and why: **manual capture first, OCR second.** The form is identical either way; OCR only changes where the initial values come from. Building the cheap half first means a bank whose slips read badly simply stays manual, and nothing is wasted.
- Watch for: Buddhist-era dates in two shapes across the samples (`24 ก.ค. 69` and `23 ก.ค. 2569`) with Thai month abbreviations. The 543-year shift already parsed cleanly into 1983 dates once (D-031) and must fail closed here too.

## D-051 — Hosting is accepted, and Google OAuth is the gate in front of it

- Date: 2026-07-28
- Status: Accepted as direction, not yet authorized to execute. Creating hosted resources still needs explicit approval at the time (`PLAN.md` § Later authorization gates item 2).
- The owner's reasoning, recorded because it reverses a founding premise: he already trusts third-party finance apps with this data, so a hosted app he controls is a strict improvement rather than a new exposure. Phone access from outside the house is the goal, and it is what makes daily slip capture realistic at all.
- The ordering constraint that decides sequencing: the development sign-in refuses any request that is not from loopback, so a hosted deployment today would have **no way to sign in**. Google OAuth is a hard prerequisite for hosting, not a companion to it.
- Decision: **do OAuth and hosting as one move, and early.** Replacing the owner identity means a fresh project and a restore because `public.ledger_owners` is immutable, and that cost only grows — today it is 175 rows and an operation proven twice. Splitting the two would mean paying for two restores instead of one.
- Decision: the transactions view is built **local-first**, because it is unblocked by all of the above and will expose data problems while the ledger is small. It was previously argued for as a test of whether the owner would use the app; he stated he will regardless, so that argument is void and only the data-scale one survives.
- Target: Vercel Hobby plus the Supabase free tier, both free. Vercel because this is a Next.js app with a strict CSP, web workers and WASM, where the Next.js team's own platform is the least surprising host. Cloudflare Zero Trust Access (free to 50 users) optionally in front, needing only a domain (~$10/year) with DNS at Cloudflare; Vercel's own production protection needs Pro. Access is defence-in-depth — the primary control stays Google OAuth, the bound-owner check, `aal2` with two verified factors, and RLS.
- Recorded so it is not mistaken later: **locking the app does not lock the database.** The hosted Supabase endpoint is separately reachable by design and is protected by RLS and JWT validation alone. A real security review belongs in the hosting step rather than treating it as a deploy.
- Free-tier facts that shape design: 500 MB database (a non-issue at this scale), **1 GB file storage** (~2 years of slip images at ~10/day, which is part of why D-050 stores none), a 7-day inactivity pause that daily use avoids, and **no backups** — which matters less here than anywhere, because the app's own encrypted backup is stronger than the paid tier's and has been proven by destruction and restore.

## D-052 — `HANDOFF.md` owns mutable homeless facts, and the rule is now testable per edit

- Date: 2026-07-28
- Status: Accepted and applied. `HANDOFF.md` trimmed from 102 lines to 39; both copies of the `sync-continuity` skill amended.
- Context: `HANDOFF.md` opened by calling itself a thin entry point and had become a 3,363-word duplicate of `PLAN.md`. The skill already forbade exactly that — *"keep it a thin index plus a short current headline. Never recreate a long duplicate snapshot"* — so the rule was not missing and was not wrong. It was unenforceable.
- The measurement that decided it: across its 20 commits `HANDOFF.md` grew from 60 lines / 1,352 words (2026-07-25) to 102 / 3,363 (2026-07-28), rising on 18 touches and falling on 2, by one and three lines. Median growth is roughly +100 words. **No single commit is a violation.** A rule phrased as a property of the finished file can only be broken in aggregate, so at the moment of each edit the honest answer to "am I recreating a long duplicate snapshot?" is no.
- Second cause, found while trimming: the rule said what the file should look like and never what it uniquely owns. That is why the drift was specifically *duplication* — every paragraph cut also existed in `PLAN.md`. Step 3 of the skill directs an agent to update `PLAN.md` with the verified checkpoint and `HANDOFF.md` with a headline, so an agent syncing after a milestone creates the duplicate **by following the skill correctly**.
- Decision: `HANDOFF.md` owns facts that are **mutable and homeless** — state that changes (authorizations granted or spent, what is uncommitted or unpushed, machine-specific hazards) and that no other maintained doc can hold, because `DECISIONS.md` is append-only and `PLAN.md` owns project state. A fact true in both files is a defect in `HANDOFF.md`, not redundancy.
- Rejected: enumerating the sections `HANDOFF.md` owns, which the earlier assessment proposed. It would import this repo's structure into a skill whose own text says it is repo-agnostic, and the list would go stale as the sections change. The criterion behind the sections generalizes where the list does not, and it is the test an agent can apply to a single edit — which is the failure being fixed.
- Consequence: a sixth validation step asks whether a move orphaned anything. It earned its place immediately. `GOTCHAS.md` carried a cross-reference to a `Get-Content`/`Set-Content` mojibake entry that did not exist; the only copy of that warning was in `HANDOFF.md`, so cutting it blind would have deleted a trap that already had a dangling pointer aimed at it. It is now a real `GOTCHAS.md` entry.
- Retained from the earlier assessment, and still right: cut duplication, keep specificity. A lean continuity doc's failure mode is a future session repeating a mistake, which costs far more than a few hundred words. The Vitest 164/7 skip warning and the em-dash trap survive the trim for that reason, in `PLAN.md` and `GOTCHAS.md` respectively.
- Evidence: `HANDOFF.md` (102 lines / 3,363 words before, 39 lines after; a word count is deliberately not recorded as current, since it moves with every edit and the line above is the durable measurement), `GOTCHAS.md` § `Get-Content`/`Set-Content` mangles every em dash in a Markdown file, `~/.claude/skills/sync-continuity.md` lines 30 and 50, `.agents/skills/sync-continuity/SKILL.md` lines 47 and 78. The `~/.claude/commands/sync-continuity.md` symlink resolves to the amended file, so no `/sync-skills` repair was needed; `agents/openai.yaml` carries interface metadata only and needed none.

## D-053 — The slip sample is four layouts rather than nine, and the QR does not always decode

- Date: 2026-07-28
- Status: Accepted. Supersedes two sample-derived claims in D-050; every design decision in D-050 — provisional entries, QR for identity, no image storage — stands unchanged.
- Context: D-050 was written from three slips. The owner gathered twenty more into `receipts_sample/` (23 total, gitignored) and said variants within a bank are few and broadly patterned. Assessed structurally from JPEG headers and QR geometry; no pixel content was read.
- What the sample shows: 23 slips across three banks — KBANK (code 004) five, Krungthai (006) nine, SCB (014) nine. QR placement and size are near-constant within a bank and separated between banks: KBANK centres at (0.81, 0.79) of the image at 0.26 of its width, Krungthai at (0.87, 0.25) at 0.17w, SCB at (0.84, 0.77) at 0.19w. Krungthai prints its QR at the top right where the other two print it at the bottom. **Bank identity is therefore derivable from QR geometry before anything is decoded.**
- Decision: treat the sample as **four layout variants, not nine** — KBANK one, SCB one, Krungthai two. The owner's expectation is confirmed. The only genuine sub-variant is Krungthai's two reference lengths (17 and 21 characters), which tracks a capture-height split of 1409 against 1442 and most likely marks two transaction types. SCB shows five distinct capture geometries with constant QR placement and constant reference length, so it is one layout variously cropped rather than five layouts. This narrows D-050's "assume more layouts exist" caution without retiring it: SCB transfer variants remain unseen, and the sample is one owner's three banks.
- Correction to D-050: its claim that "all three QR codes decoded first try with no upscaling" does not survive the larger sample. **Three of 23 fail to decode at native resolution.** OpenCV locates the finder pattern in all three and cannot read the payload; all three decode at 2x cubic upscale, and their payload length identifies them as SCB. Capture must upscale and retry rather than read a decode failure as "no QR present" — otherwise 13% of this sample falls through to whatever the no-QR path does.
- Scope of the evidence, recorded because it is easy to overread: this establishes how many slip families exist and that each is internally consistent. It says nothing about where the amount, date and counterparty sit on the page, which is what task 21's region-targeted OCR depends on and which requires reading pixels. Task 20 does not need it, consistent with D-050's manual-capture-first ordering.
- Evidence: `receipts_sample/` (23 files, gitignored, never fixtures). A throwaway probe in the session scratchpad read JPEG SOF dimensions and decoded QR payloads with OpenCV, printing only bank code, per-field lengths and QR geometry. Not committed: it reads real slips, so it cannot be a test. Recreate it when a new bank or slip type needs checking. The two traps it produced are recorded in `GOTCHAS.md`.

## D-054 — Fourteen statements imported through the app's own routes; the fifteenth is refused and stays refused

- Date: 2026-07-29
- Status: Accepted and executed. The import is done and verified; the Krungthai defect it exposed is open as `PLAN.md` task 23.
- Authorization: the owner approved this batch explicitly, and approved an agent performing it. The standing rule is unchanged — an import is never a standing permission and the next one must be asked for again (`HANDOFF.md`).
- Result: the live ledger went from 55 rows to **1,232** across 14 batches — KBANK 89 rows from two statements, SCB 1,143 from twelve. Verified by querying `public.source_transactions` afterwards rather than by trusting the importer's own log.
- Method, recorded because it is new and would otherwise be re-derived: a throwaway Node harness under `.runtime/` (gitignored) drove the **real** routes — `POST /api/v1/dev/session` for an aal2 cookie, then `POST /api/v1/imports/confirm` exactly as the bind stage does. It reimplements no server-side check: `confirm_import` still recomputed every row fingerprint against the bound account's bank code and the payload digest, and would have refused a mismatch. What it skipped is the UI, not the enforcement. `readStatement` ran under Node against pdf.js with extraction copied line-for-line from `workers/krungthai.worker.ts`; that it reproduced the previously recorded 1,465-row total exactly is the evidence the copy is faithful. Vitest was the TypeScript runner because no `tsx` or `ts-node` is installed offline, with a scratchpad config whose `include` points into `.runtime/` — an absolute include cannot be used, because vitest resolves `include` against `root` and the scratchpad is on another drive.
- **`KRUNGTHAI-01` is refused at assembly** with `BALANCE_RECONCILIATION_FAILED`, three blockers among 233 rows, all on page 10. Its printed totals agree, so it passes the D-033 cross-check; the row-by-row balance chain is what fails. **The two checks are independent and this is the first document to show it.**
- Diagnosis, from the repo's own value-free masker: page 10 has three money column right edges (≈377, ≈439, ≈516 for the balance). A normal row carries one amount plus a balance. One blocking line carries amounts in **both** money columns plus a balance — the interest-plus-withholding-tax compound of D-029 — and the reader produced a single withdrawal component with `anomaly` unset. `reconcileRows` tolerates a compound row only when `parserFields.anomaly === 'interest-tax-order'`, so it counts as unexplained. The direction confirms it: with the deposit half dropped, `expected` is too low and `printed > expected`, which is the sign measured.
- **Deliberately not concluded:** the other two blocking rows are unexplained. One has geometry identical to a neighbouring row that reconciles cleanly. Either something non-geometric differs, or the diagnostic's page-line indexing does not align with the reader's row numbering. Recorded as unknown rather than folded into the one explanation that was found, because a fix aimed at a cause that was assumed rather than established moves the symptom.
- **A mistake worth keeping:** an account was created for Krungthai *before* the import was proven, so a permanently empty account now exists. `public.accounts` has no delete path by design — `source_transactions.account_id` references it — so it cannot be removed. Create the account only after `assembleImportPayload` returns ok. Recorded in `GOTCHAS.md`.
- A second, smaller defect left behind: the harness mishandles the response shape of `POST /api/v1/accounts`, so the run that *creates* an account fails after creating it and the retry succeeds through the list path. It blocked nothing because both accounts exist, and the harness is deleted, but the shape mismatch is real if anyone rebuilds it.
- Evidence: `public.source_transactions` 1,232 rows, `public.import_batches` 14, `public.accounts` 6 (three seeded and empty, one Krungthai empty, KBANK 89, SCB 1,143). Harnesses deleted after use; they read real statements and so cannot be committed (`docs/FIXTURE_POLICY.md`).

## D-055 — A statement's printed row order is not always its balance order, and reconciliation recovers the one order that is

- Date: 2026-07-29
- Status: Accepted by the owner, who chose an order-tolerant check over keeping the statement refused. Corrects the diagnosis recorded in D-054; the import result recorded there stands.
- **D-054's diagnosis of `KRUNGTHAI-01` was wrong, and how it was wrong matters more than that it was.** It attributed the three blockers to the D-029 interest/tax compound being read as a single component with `parserFields.anomaly` unset. Measured against the reader's own output: the statement contains **zero** two-component rows, no reader sets that marker at all, and the blocking row's printed direction is `printed < expected`, not the `printed > expected` recorded. Every part of the diagnosis was an inference from the page-line masker rather than an observation of the reader, and each part was wrong.
- Decision: `reconcileRows` may repair the order of a **maximal run of consecutive rows printed under one date**, and only there. It is attempted only where the function would otherwise refuse, and it accepts a repair only when **exactly one** ordering of that date's rows reproduces every printed balance from the entry balance. Nought or several orderings refuse exactly as before, so ambiguity is never resolved by choosing. Every row in a searched run must balance exactly — a reordering is not licence to also tolerate a gap. `reconcileRows` returns rows twice: `rows` in **printed order**, for display, and `applied` in the order the balances were applied, which is the order the import **submits**. Each row keeps its printed page and row in `provenance`, so the printed position survives the reordering; only `source_index` follows the applied order.
- Rationale: the statement is correct and the reader is correct. `KRUNGTHAI-01` prints an end-of-day interest posting first among its date's entries and applies it last, so the printed chain breaks at that row, at the row after it, and again at the first row of the next date — the three blockers, one cause. Two independent facts establish that nothing is missing: the D-033 printed-totals cross-check passes, so every amount and per-direction count agrees with the bank's own summary; and the blocking row's geometry is ordinary, its three money runs landing mid-371 / mid-433 / mid-506 against right edges every neighbouring row shares, with the withholding column printing `0.00` and correctly contributing no component (D-029). Refusing the document would have been refusing the bank's own arithmetic. The order is not recoverable from any printed key — date/time order is already monotonic in printed order, so sorting changes nothing — which is why a search against the printed balances is the only route.
- Consequences: a fail-closed guard on an append-only ledger now has a path that accepts what it used to refuse, and that is the risk to watch. Three things bound it: the search never crosses a date boundary, so a statement with wrong dates is not silently absorbed by a reordering; the uniqueness requirement means a coincidental ordering cannot be preferred over another; and a run longer than `MAX_REORDER_RUN` (10) refuses rather than settling, with `SEARCH_BUDGET` (50,000 nodes) scoped to the **whole statement** rather than to each run, so total cost is bounded by one budget however many dates a document has. Reordered rows are not silent: `reconcileRows` returns an `out-of-order-run` warning naming the recovered order, the rows carry `status: "reordered"`, and the review table marks each one and breaks the balance rail at them.
- **Why the applied order is submitted rather than the printed one, which is the whole reason this works.** `public.confirm_import` performs its own balance walk in PL/pgSQL (`202607260009_multi_bank_layouts.sql:130-135`), raising `balance reconciliation failed` on any unbalanced row without the compound marker and then requiring the running total to equal the declared closing balance. It is the authoritative check (D-041) and it knows nothing of this repair. Submitting printed order would have produced a client that accepts and a database that refuses — and the route's error mapping does not match that exception, so the owner would have seen a bare 400 with no blocker list. Nothing about the database's guarantee is weakened by submitting the applied order, because that guarantee was always order-relative: *these rows, in this order, form a valid chain ending at this closing balance.* The uniqueness requirement is an additional client-side check on top of it, not a substitute for it. `assembleImportPayload` therefore carries `reconciliation.applied` and `reconciliation.closingBalance`, and refuses outright when a statement printed its own closing figure and the reconciled one disagrees — a recovered order contradicting the bank's own total is a reason to refuse, not to prefer either figure.
- **A limitation to state rather than to hide:** the search is arithmetic-only. It ignores `sourceTime`, and for `KRUNGTHAI-01` the accepted order *contradicts* the printed clock — the interest posting carries the earliest time in its date and is applied last. Rejecting orderings inconsistent with printed times was considered and would refuse this document, so it is not a free strengthening. What that leaves open: a statement with a genuinely misread amount whose rows happen to admit exactly one balance-satisfying permutation would be repaired rather than refused. The printed-totals cross-check (D-033) is what stands between that case and the ledger, since a misread amount changes a per-direction total.
- **Left deliberately unfixed, and recorded because it looks like a bug and is not the one that mattered:** no layout reader ever sets `parserFields.anomaly = "interest-tax-order"` — only `lib/synthetic.ts` does — so the compound-row tolerance in `reconcileRows` is unreachable for every real statement. It was not the cause here and setting the marker for every compound row would weaken a guard to fit a document nobody has seen. An unbalanced unmarked compound row still fails closed, which is the correct default.
- **Imported 2026-07-29, with the owner's explicit authorization and a confirmed backup beforehand.** `public.source_transactions` went **1,232 → 1,465**, `import_batches` **14 → 15**, and the Krungthai account went from 0 to **233** rows, closing the empty-account gap GOTCHAS records. Verified by querying the database, not by reading the importer's status line. The applied order persisted exactly: within page 10, `source_index` 206…209 carry printed rows 18, 19, 20, **17** — the end-of-day interest posting stored last in its date. The printed position lives in `import_batch_rows.page`/`row_number`; `public.source_transactions` carries neither provenance nor a source index, so "the row keeps its printed page and row" is true of the batch-rows table rather than of the transaction itself.
- **A defect this shipped with, found by the owner at the confirm screen and worth more than the fix.** On a successful bind the app replaces the parsed statement with the assembled payload, whose rows are now in *applied* order — so the review table re-reconciled rows that already chain, correctly found nothing, and rendered no warning and no badges. The reordering became invisible at exactly the moment it was being confirmed, which is the inverse of this decision's stated intent that "reordered rows are not silent". Two lessons rather than one. First, a claim that something is surfaced has to be checked *in the surface*, not in the function that produces it — every test passed while the UI showed nothing. Second, the owner asking "where do I see the warning?" was the only thing that caught it; two wrong theories (font metrics, pdf.js build) were investigated and disproved with measurements before the real cause was found in code I had written that day. `assembleImportPayload` now returns its `warnings` with the payload, and `tests/import-assembly.test.ts` covers it — confirmed distinguishing.
- Reviewed before any of it reached the ledger, and the review is why this entry says what it says. It caught that `confirm_import` would refuse the payload — an omission that would have surfaced as an unexplained 400 during a live import — and that an earlier draft of this entry claimed a server-side guarantee that did not exist. Two of its findings did not survive checking and are recorded so they are not re-raised: the closing balance *is* cross-checked in SQL (line 135), and rejecting orderings that contradict `sourceTime` would refuse this very document.
- Evidence: `lib/reconcile.ts` (`sameDateRuns`, `findUniqueClosingOrder`, `applied`), `lib/import-assembly.ts` (submits `applied` and the reconciled closing balance, refuses a printed-closing disagreement), `app/ledger-app.tsx` (warning branch, rail and row marking), `tests/domain.test.ts` — four invented-fixture tests: recovery of the one closing order, refusal when two orderings close, refusal to reorder across a date boundary, refusal of a run too long to settle. Confirmed distinguishing: the recovery test fails against the pre-change `reconcileRows`; the other three pass either way by design, since they assert the guard still refuses. Gate on 2026-07-29 with the project-local Node 24.18.0 runtime: ESLint clean, `tsc --noEmit` clean, Vitest **194 passed / 7 skipped across 14 files**, `tests/krungthai-layout.test.ts` 48/48 unchanged — `lib/krungthai-layout.ts` was not modified, because it had no defect. Against the real document: 233 rows, cross-checked, **3 blockers to 0**, one `out-of-order-run` warning recovering rows 207, 208, 209, 206, and four rows marked reordered. `confirm_import`'s balance rule, transcribed line-for-line into the harness and run over the assembled payload, returns `balance reconciliation failed` on printed order and **passes on applied order** — which is the end-to-end proof the import can actually succeed, short of performing it. The pathological shape the review measured at 4,437 ms (500 date-runs of 10, 5,000 rows) now completes in **10 ms** with the budget scoped statement-wide and movements precomputed once per row.

## D-056 — Slips are a twelfth table, identified by a CRC-checked QR, and the backup reads two versions

- Date: 2026-07-30
- Status: Accepted and built. Implements `PLAN.md` task 20 on the design D-050 set and the sizing D-053 measured; neither is revised, and both are confirmed in every measurable part. Task 19 still gates *use* — the development sign-in refuses non-loopback requests, so slips cannot be shared from a phone yet.
- **D-053 is confirmed, and the grammar behind it is now written down.** A structural probe over the 23 slips in `receipts_sample/` reproduced its counts exactly: KBANK 5, Krungthai 9, SCB 9; Krungthai's two reference lengths (17 and 21); and exactly **3 of 23 decoding only at 2× upscale**, all three SCB. Beyond confirming it, the probe determined the format: nested EMVCo TLV, outer tags `00` (identity block) / `51` (`"TH"`, constant) / `91` (four hex digits), inner `00` (`"000001"`, constant) / `01` (three-digit bank code) / `02` (transaction reference). Reference lengths by bank: KBANK 20, Krungthai 17 or 21, SCB 25.
- **Tag `91` is a CRC-16/CCITT-FALSE over the whole payload preceding it — 23 of 23.** This is the decision's most useful finding, because it converts the dedup key from "whatever the detector returned" into something tamper-evident. A misread QR is refused rather than stored against a reference no statement can ever reconcile, which is the failure D-050 called catastrophic. `readSlipQr` was then run over all 23 real payloads: **23 accepted, 0 refused, and each rebuilt byte-identically** from the bank and reference it extracted.
- Decision: **per-bank reference lengths are recorded as evidence and deliberately not enforced.** D-053's sample is one owner's three banks and Krungthai alone already prints two lengths, so pinning the observed set would refuse a legitimate slip the day a bank adds a third. The CRC already proves the reference was read correctly, which is what a length pin would otherwise be standing in for. Charset and a 64-character bound are enforced.
- Decision: **a slip carries no `account_id`.** The QR names a bank, not an account, and only the statement will eventually say which account it was (task 22). Asking the owner to pick one at capture time would invite a wrong answer for no benefit.
- Decision: **capture is idempotent on `(owner_id, bank_code, slip_reference)`**, and a re-share returns the stored row unchanged rather than erroring or overwriting. Share-to-app makes double capture the *expected* accident rather than an unlikely one. A second share carrying a different amount changes nothing — the table is append-only, and silently replacing a value the owner already confirmed would be worse than declining to.
- Decision: **the backup moves to schema v3 and `restore_backup` accepts both 2 and 3.** The owner holds exactly one backup covering the whole ledger and it is a v2 file, so a hard bump — the way pre-release v1 was dropped (D-018) — would have stranded it the moment migration 011 applied. The table list and its length are now derived from the payload's declared version and bound into the restore run at stage time, so a v2 payload restores exactly as before and cannot acquire a twelfth chunk mid-restore. `slips` is **appended** to the table order rather than slotted in alphabetically, so indices 0..10 keep meaning what they meant; a manifest binds each chunk to its index, and reordering would invalidate every existing descriptor in a way no digest could distinguish from tampering.
- **The table list was written down twice, and only one copy was obvious.** Migration 004 enumerated the eleven kinds as a CHECK on `restore_chunks` as well as inside `restore_backup`, so the first v3 restore failed as a raw row-level constraint violation naming `chunk_index 11` rather than as a contract error. This is the D-041 shape exactly — one restated list gating something out of the product — and it is worth expecting a third copy before adding a thirteenth table.
- Decision: **share-to-app needs a service worker, not a POST route.** A Web Share Target with `method: POST` delivers the image as a real network request; letting it reach the server would upload the slip image, which D-050 forbids and which is why a backup is 14 KB rather than hundreds of megabytes. `public/share-slip-sw.js` intercepts the POST, keeps the file in a local Cache, and redirects; the app reads it back, decodes it, and deletes it. The worker caches **nothing else** — no app shell, no assets — because a stale-cache service worker is among the hardest failures to diagnose and task 19 is about to rewrite routing.
- **Residual exposure, stated rather than buried:** `app/share-slip/route.ts` exists for the window before the worker controls the page. It does not read the request body, so nothing is parsed, stored or logged — but on a hosted deployment the bytes still travel to the server before the redirect is written. Task 19 has to confirm this when hosting lands (D-051).
- Decision: **the native `BarcodeDetector`, not a bundled decoder.** No new dependency, nothing for the strict CSP to admit, and the slip is decoded on the same device that photographed it. The cost is that capture needs a Chromium browser, which the UI states plainly rather than failing silently.
- Consequences, both deliberate: a captured slip is **append-only with no correction path** — the confirm form is the review step, and adding `transaction_overlays`-style revisions would have tripled migration 011 for a case the confirm step exists to prevent — and the Buddhist-era guard lives in `capture_slip` rather than in a CHECK, because a CHECK constraint cannot call `current_date`. A `2569` typed through unconverted lands 543 years ahead and is refused, which is D-031's rule applied to the same 543-year shift.
- Evidence: `lib/slip-qr.ts`, `lib/slip-scan.ts`, `lib/slips.ts`, `supabase/migrations/202607300011_slip_capture.sql`, `app/api/v1/slips/route.ts`, `app/slip-capture.tsx`, `public/manifest.webmanifest`, `public/share-slip-sw.js`, `app/share-slip/route.ts`. Tests: `tests/slip-qr.test.ts` (15), `tests/slip-scan.test.ts` (6), `tests/slips.test.ts` (9), `supabase/tests/004_slip_capture.sql` (24), five specs in `tests/e2e/owner-session.spec.ts`. **Every fixture is built by `buildSlipQrPayload` from an invented reference** — the builder lives in `lib/` precisely so no test input is ever copied off a real slip. Gate on 2026-07-30 with the project-local runtime: ESLint, `tsc --noEmit`, Vitest **227 passed / 7 skipped across 17 files**, pgTAP **129/129 across 4 files**, production build, Playwright owner **15/15** and isolated **14/14**. The 23-slip verification ran through a throwaway harness against `lib/slip-qr.ts` itself; it reads real slips, so it is not committed — recreate it when a new bank or slip type needs checking.
- A defect this work introduced into existing tests, caught by running them: the capture section rendered its "no QR reader in this browser" notice as `role="status"`, which put a second status live region on the page and broke all ten pre-existing owner specs at their `getByRole("status")` sign-in step. The slip specs hid it by stubbing `BarcodeDetector` — headless Chromium has none, so the notice rendered for every other test and not for those. It is static explanatory text and is no longer a live region. A live region is for what just happened, not for what is true.

## D-057 — The platform QR reader does not exist on the owner's desktop, so the app carries its own and only downloads it when it must

- Date: 2026-07-30
- Status: Accepted and built. Supersedes D-056's "the native `BarcodeDetector`, not a bundled decoder" on the point of *availability*; every other decision in D-056 — provisional entries, QR identity, CRC-checked references, idempotent capture, no stored image — stands unchanged.
- **The measurement that forced this.** `BarcodeDetector` is absent from every browser this machine can drive: bundled Chromium and the installed Chrome, headless and headed, and still absent with `--enable-features=ShapeDetection` and `--enable-experimental-web-platform-features`. Chrome implements the Shape Detection barcode backend on Android, macOS and ChromeOS and **not on Windows or Linux desktop**. D-056 called the limitation "needs a Chromium browser", which was wrong in the way that mattered: capture could not run *at all* on the owner's own computer.
- **Why that was worse than a missing feature.** It made slip capture unverifiable. The browser specs stubbed the detector, so they proved the wiring and nothing about decoding; the only device that could have exercised the real path is a phone, which needs task 19. The position was "we find out whether the one path that writes to the ledger works after we deploy it", which is not a position this repo takes anywhere else.
- Decision: **native where it exists, bundled fallback everywhere else, and the fallback is `import()`ed.** The phone — the device this feature is actually for — keeps the platform decoder and never downloads the ~1.1 MB WebAssembly reader. Desktop pays for it once. `lib/slip-scan.ts` is untouched: it already took the detector as a callback, so the retry ladder did not move.
- Decision: the fallback is **`barcode-detector`**, chosen on measurement rather than reputation. All three candidates were run in a real browser over all 23 real slips, through the same upscale ladder the app uses, with every payload validated by `readSlipQr` including its CRC:

  | decoder | accepted | needed 2× upscale | size |
  | --- | --- | --- | --- |
  | jsQR | 23/23 | 1 | 252 KB, pure JS, unmaintained since 2021 |
  | zxing-wasm | 23/23 | **0** | 1.1 MB wasm + 44 KB |
  | barcode-detector | 23/23 | **0** | 1.1 MB wasm + 82 KB (wraps zxing-wasm) |

  `barcode-detector` implements the `BarcodeDetector` interface, so the native and fallback paths are the same code. It reads every sample at native resolution.
- **A finding that revises D-053 rather than contradicting it:** OpenCV needed the 2× upscale on 3 of 23 slips; ZXing needs it on none. The retry ladder is therefore no longer load-bearing for this sample. It is kept — a worse photograph is the case it exists for, and `tests/slip-scan.test.ts` still covers it — but "3 of 23 fail at native resolution" is a property of the decoder that measured it, not of the slips.
- **The WebAssembly binary is served from our own origin, and neither committed nor fetched from a CDN.** `zxing-wasm` resolves the binary relative to its own module URL, which after bundling is `/_next/static/chunks/…`; the fetch 404s and **every decode silently returns nothing**. `scripts/copy-zxing-wasm.mjs` copies it into `public/` from `prebuild`, and `app/slip-capture.tsx` overrides `locateFile`. Not a CDN, because `default-src 'self'` is deliberate and a finance app fetching executable code from a third party at runtime has handed that party the page. Not committed, because a 1.1 MB binary in git is a review surface nobody can read and would drift from the package version it must match. **The CSP needed no change** — `'wasm-unsafe-eval'` was already there for pdf.js, and the binary is same-origin.
- Consequence, and the real payoff: **the browser specs no longer stub anything.** They render a real QR to a real PNG (`tests/fixtures/synthetic-slip.ts`, from an invented reference) and let the app's own decoder read it, so they now cover image bytes through to a stored row. The stubbed upscale spec was removed rather than kept green artificially — a generated QR is read at native size by this decoder, so the ladder is covered where it belongs, in `tests/slip-scan.test.ts`.
- **What is still not covered, stated so it is not assumed away:** a generated QR has no camera noise, glare or JPEG artefacts. That is exactly why the 23 real samples were measured separately, and it is still true that no real slip has been through the app's UI. That remains an owner-present step.
- Evidence: `app/slip-capture.tsx` (`resolveDetector`), `scripts/copy-zxing-wasm.mjs`, `package.json` (`prebuild`, `barcode-detector` dependency, `zxing-wasm` devDependency for fixtures), `tests/fixtures/synthetic-slip.ts`, `.gitignore`. The availability probe and the three-decoder benchmark were throwaway harnesses under `.runtime/diag/`, deleted after use — they read real slips and so cannot be committed; recreate them when a new bank or decoder needs checking. Gate on 2026-07-30 after the change: ESLint, `tsc --noEmit`, Vitest **227 passed / 7 skipped across 17 files**, pgTAP **129/129 across 4 files**, production build, Playwright owner **15/15 with no stubbed detector** and isolated **14/14**.

## D-058 — `connect-src` is derived from the configured Supabase origin, and an unconfigured build gets a narrower policy rather than a wider one

- Date: 2026-07-30
- Status: Accepted and built. First owner-free piece of `PLAN.md` task 19; creates no hosted resource and needs no authorization.
- Context: the CSP hard-coded `connect-src 'self' http://127.0.0.1:54321 ws://127.0.0.1:54321`. That is correct for exactly one machine. A hosted deployment would have built, loaded, and then failed every request as a CSP violation — a failure that reads as "the app is broken" rather than "the policy names the wrong origin", and whose obvious fix under pressure is to widen the policy.
- Decision: the origin is **derived** from `NEXT_PUBLIC_SUPABASE_URL` at build time, which is when the same variable is inlined into the client — so the header and the client can never disagree about which project a build talks to. Only the origin is taken: a configured value carrying a path, query or fragment contributes scheme, host and port and nothing else, because a path in `connect-src` does not restrict what a reader might assume it does.
- Decision: the WebSocket origin is derived from the page origin rather than configured separately — `https:` gives `wss:`, `http:` gives `ws:`. Two values that must agree are one value; an `https` origin with a `ws://` socket is blocked as mixed content and nothing in the config would say why.
- Decision: **an unconfigured or malformed URL yields `'self'` alone.** This is the fail-closed direction and it is worth stating explicitly because the tempting fallback is the opposite. An app that cannot reach its database is broken and says so — `strongOwnerClient` already answers 503 in exactly that state — whereas a policy widened to cover an unvalidated value is broken and does not. `javascript:`, `file:`, a bare hostname and a `ws://`-only URL all take this path.
- Consequence: the CSP moved out of `next.config.ts` into `lib/security-headers.ts` so it could be tested at all. It had **no test coverage whatsoever** despite being a stated invariant of the project, which is how it stayed a hard-coded loopback origin without anyone noticing.
- Evidence: `lib/security-headers.ts`, `next.config.ts`, `tests/security-headers.test.ts` (17 tests). **Confirmed distinguishing** — four sabotages each turn the suite red and the restored file green: a wildcard fallback instead of fail-closed (12 tests), keeping the whole URL instead of its origin (2), hard-coding the socket scheme to `ws:` (3), and dropping `'wasm-unsafe-eval'` (1). Verified in the surface as well as the module: a build with `NEXT_PUBLIC_SUPABASE_URL=https://…supabase.co` serves `connect-src 'self' https://…supabase.co wss://…supabase.co`, read off a real response from `next start`. Gate on 2026-07-30: ESLint, `tsc --noEmit`, Vitest **245 passed / 7 skipped across 18 files**, pgTAP **129/129**, production build, Playwright owner **15/15** and isolated **14/14**.

## D-059 — Two of the four slip layouts carry the transaction date inside the QR, so it is read rather than typed

- Date: 2026-07-30
- Status: Accepted and built. A partial, exact answer to `PLAN.md` task 21 that arrives without any OCR; it does not close task 21, which still owns the amount and the counterparty.
- Context: the owner ran a real slip through the capture form for the first time and asked why the amount, time and counterparty were not filled in. The answer for those is D-050's build order — manual first, OCR second. The answer for the **date** turned out to be different.
- **The measurement.** Across the 23 real samples, the transaction reference embeds a `YYYYMMDD`: SCB **9/9** at the start, Krungthai's 21-character variant **5/5** after its single leading letter, Krungthai's 17-character variant **0/4**, KBANK **0/5**. That is 14 of 23 slips whose date needs no reading of pixels at all. Confirmed independently on the owner's own slip, whose reference began with the day the slip prints.
- Decision: **pre-fill the date from the reference when one is there, and fall back to today when it is not.** This is strictly better than OCR for this field and not merely cheaper: the reference is covered by the QR's CRC, so the date is the bank's own rather than an inference from an image, and it cannot be misread. It is still shown to the owner and still editable — D-050's "never auto-confirming" is unchanged.
- **It removes the Buddhist-era hazard rather than warning about it.** The QR date is Gregorian, so for these 14 slips the 543-year shift that D-031 fought cannot arise at all. The warning still shows for the slips that carry no date, which is exactly where it is still needed.
- Decision: match on the *shape*, not on the reference length. Offsets 0 and 1 are tried and a candidate is accepted only if it parses as a real calendar date **and** falls inside the window the form would accept. Keying on per-bank lengths would repeat the mistake D-057 named, and the plausibility window is what makes the shape match safe: KBANK's leading `01620511` reads as year 0162 and falls out, and Krungthai's hex variant is not digits at all.
- **The guarantee that makes pre-filling safe:** the function can never propose a value the server would refuse, because it is handed the same window the form enforces and returns null outside it. A Buddhist-era `2569…` cannot reach the field.
- Consequence, and worth stating because it is a trust question: a pre-filled date looks identical to a typed one. The help text now says which it is — "read from the slip's QR code" against "carries no date, so today is filled in" — so a value the owner did not enter never passes silently as one they did.
- **A second finding from the same session, smaller and entirely the owner's:** the amount field's placeholder was `1250.00`, a plausible number in grey. It read as a pre-filled value that was simply wrong, which is what prompted "it doesn't autofill price". Replaced with text that cannot be mistaken for an amount. No test would have found this; using the thing did.
- Evidence: `lib/slips.ts` (`slipDateFromReference`), `app/slip-capture.tsx`, `tests/slips.test.ts` (6 new tests: both dated shapes, both undated ones, date-shaped non-dates, window bounds including a Buddhist year, and offset precedence), `tests/e2e/owner-session.spec.ts` (a browser spec covering SCB, the dated Krungthai variant and the undated KBANK fallback, each asserting the help text matches the source of the value). The 23-sample measurement came from a throwaway probe, deleted after use — it reads real slips and cannot be committed. Gate on 2026-07-30: ESLint, `tsc --noEmit`, Vitest **251 passed / 7 skipped across 18 files**, pgTAP **129/129**, Playwright owner **16/16** and isolated **14/14**.

## D-060 — Three real slip references reached the test fixtures, and the entries claiming otherwise are wrong

- Date: 2026-07-31
- Status: Accepted by the owner as an exposure he is content with; **not remediated**. Corrects three earlier entries rather than superseding their decisions — every design decision in D-056, D-057 and D-059 stands. What is wrong in them is a factual claim, not a judgement.
- **The false claims, named so they are not re-read as true.** D-056's evidence line says "Every fixture is built by `buildSlipQrPayload` from an invented reference — the builder lives in `lib/` precisely so no test input is ever copied off a real slip". D-057 repeats "from an invented reference". `tests/e2e/owner-session.spec.ts` said "No real slip reference exists in this repo." All three are false. `DECISIONS.md` is append-only, so they stay as written and this entry is the correction; the source comments, which are not append-only, have been fixed in place.
- **What is actually in the repository:** three real transaction references, copied from `receipts_sample/` — one in `tests/fixtures/synthetic-slip.ts` (`KTB_SLIP_DATED`) and the same one plus two others in `tests/slips.test.ts` (a Krungthai 17-character reference and a KBANK one). They are committed and pushed. The other fixtures are genuinely invented.
- **How it happened, which is the part worth keeping.** The structural probe in D-056 was written to be value-free, but its reportable-tag whitelist assumed a flat TLV and the payload nests everything inside tag `00` — so it printed whole references (already recorded in GOTCHAS). At the time this was noted as a read rather than a write, with the explicit remark that nothing derived from it would reach a fixture. Hours later, writing tests that needed reference *shapes* per bank, three of those shapes were reproduced from what had been printed. The gap was not carelessness about the rule; it was that **the rule was applied at the moment of the leak and not at the moment of reuse**, by which point the values looked like ordinary knowledge of the format.
- Consequence for `docs/FIXTURE_POLICY.md`: its central rule — "No real value becomes a fixture, ever" — is intact as a rule and is now **violated in practice**. The policy is not amended; a breach that is recorded is better than a policy quietly narrowed to fit what happened.
- Decision (the owner's): the exposure is accepted and the references stay. Recorded because the alternative — swapping three strings — is a few minutes' work, so leaving them is a choice rather than an obstacle. **Still open, and cheap, whenever it is wanted:** replace the three with invented values of the same shape. Note that a forward commit does not remove them from history or from the remote; only a history rewrite and force-push would, which is a separate decision nobody has asked for.
- **The generalisable lesson, which is not "be careful":** a value-free reporting rule protects the moment a value is *printed*. It does nothing at the moment that value is *reused*, because by then it is indistinguishable from format knowledge the agent legitimately has. Where a fixture must reproduce a real *shape*, generate the shape from the grammar — `buildSlipQrPayload` already existed for exactly this and was used for the payloads while the references it was given were pasted.
- Evidence: `tests/fixtures/synthetic-slip.ts`, `tests/slips.test.ts`, both now carrying comments that say which references are real. Found when the owner captured a Krungthai slip through the UI and its printed reference was visibly identical to the "invented" fixture — that is, by using the product, not by any check in the suite. No automated check exists that would have caught it, and none is added here.

## D-061 — The app becomes four routes, and the shell owns everything that is not one surface's business

- Date: 2026-07-31
- Status: Accepted and built. The routing half of `PLAN.md` task 19; it creates nothing hosted and needed no authorization. The OAuth half of that task is untouched.
- Context: the app was one long page. That had already been worked around once — on 2026-07-29 the ledger view was moved above the import bench because reaching it meant scrolling past everything else (task 17), which is a plaster over the fact that four unrelated surfaces shared one scroll position and one component.
- Decision: **four routes — `/ledger`, `/import`, `/slips`, `/recovery` — and `/` redirects to `/ledger`.** One canonical URL per surface, so nothing has to be kept in step, and the ledger is what an ordinary day opens on. `app/ledger-app.tsx` split into `app/import-bench.tsx` (select → unlock → bind → review → the `.pldemo` preview) and `app/recovery-bench.tsx` (export and restore); the transactions view and slip capture were already components and moved unchanged.
- Decision: **the shell owns the header, nav, footer and the session control**, in `app/site-header.tsx`. The development sign-in used to live inside "Import / 01", which was never true of it — a session is not a property of the import bench, and when OAuth lands it belongs in exactly the place this now is.
- **The session line is a polite live region, not a second `role="status"`.** Every route already has one status line, and adding a second to the shell would make `getByRole("status")` resolve to two elements on every page — breaking assertions in specs that have nothing to do with the header. Deliberate, and the kind of thing that is only cheap to decide before the shell exists.
- Decision: **no state crosses a route.** The one piece that did was `backupStale`, which the import bench set and the recovery band read. Splitting it revealed the coupling was wrong anyway: it made a *synthetic preview* go stale because a *real ledger* was exported. The import route now tracks only its own preview, and the confirmation message points at Recovery in words. Nothing is lost, because the authoritative staleness check is the snapshot sequence `confirm_backup_custody` compares server-side — the flag was always a hint.
- Decision: **service-worker registration moves from the capture form to the shell**, and this one is a privacy argument rather than a tidiness one. A Web Share Target POST can only be kept on the device by a worker that is *already* registered. With one page, any visit armed it; with four, a worker installed only by `/slips` would let the first share ever made reach the server's fallback handler — which is precisely what D-050 forbids. It remains exactly one registration site, and the worker still caches nothing but the shared image. The share redirect and the manifest's `start_url` were retargeted to `/slips` and `/ledger` accordingly.
- Consequence: the `section-index` ordinals ("Ledger / 05", "Slips / 06") named a position on a page that no longer exists, so single-section routes carry the label alone; the import flow keeps 01/02/03 because there the numbers are stages.
- Consequence for the suite: every spec that navigated to `/` had to be told which route it means — including `parser.spec.ts` and `statement-pdf.spec.ts`, whose subject is the PDF reader and which failed for a reason unrelated to anything they test (GOTCHAS). Four browser specs are new: the root redirect, nav across all four routes with `aria-current`, and an axe pass on each route, which is the first automatic accessibility coverage the ledger, slips and recovery surfaces have had without the import bench above them.
- Evidence: `app/layout.tsx`, `app/site-header.tsx`, `app/page.tsx`, `app/{ledger,import,slips,recovery}/page.tsx`, `app/import-bench.tsx`, `app/recovery-bench.tsx`, `lib/download.ts`, `app/globals.css`, `public/share-slip-sw.js`, `public/manifest.webmanifest`, `app/share-slip/route.ts`, `tests/privacy.test.ts` (the file lists it greps), `tests/e2e/{ledger,parser,statement-pdf,owner-session}.spec.ts`. Gate on 2026-07-31, all re-run: ESLint, `tsc --noEmit`, Vitest **251 passed / 7 skipped across 18 files**, pgTAP **129/129 across 4 files**, production build, Playwright owner **16/16** and isolated **18/18** (up from 14/14 by the four new specs across two viewports).

## D-062 — A captured slip belongs in the ledger view, as a provisional entry that stays out of every confirmed figure

- Date: 2026-07-31
- Status: Accepted and built, at the owner's direction. Supersedes the read-side assumption in D-050/D-056 — that slips would stay invisible until reconciliation (task 22) gave them a meaning — while leaving every write-side decision in those entries untouched.
- **How it was found, which is the pattern by now:** the owner captured a real slip through the UI, opened the ledger, and it was not there. The capture had worked — `public.slips` held the row. Nothing in the app displayed it, and `GET /api/v1/slips` had existed since migration 011 **with no caller at all**. Task 20 shipped a write path with no read path, and every test passed because each one asserted against the database directly.
- His stated intention, which the build had quietly narrowed: *slips should show in the ledger and be verified against the statement once in a while.* That is a strictly better product than "slips are invisible until task 22", and nothing in the invariants forbids it — the invariant is that `public.source_transactions` is written only by a confirmed import, not that a *view* may show one kind of row.
- Decision: **the ledger view renders one sequence of two kinds.** `LedgerEntry` is a discriminated union of a confirmed transaction and a captured slip; the same date/time/id comparator orders both, so a slip sits where it happened. The owner reads "what happened, in order", and a slip waiting for its statement happened.
- **Decision: provisional money is totalled separately and never folded in.** This is the load-bearing one. A slip and the statement row for the same payment are two records of one transaction, and until reconciliation matches them nothing can tell that they are a pair — so one combined total would overstate spending by exactly the amount of every slip that was captured *correctly*. Two true numbers beat one wrong one. The row count, deposits, withdrawals and net movement remain the statement's figures; provisional slips get their own line saying why they are apart.
- Decision: **a provisional row has no balance, in either money column.** A printed balance is the bank's statement of the account after a row; a slip is not in that chain, and deriving a figure there would invent one. Both cells read `—`.
- Decision: **slips appear in the all-accounts view only.** A slip carries no account by design — its QR names a bank, and only the statement says which of that bank's accounts the money moved through (D-056) — so filing one under a selected account would be the parser-guesses-the-binding mistake wearing different clothes. Selecting one account hides them and says that, rather than letting them silently vanish.
- Decision: a failure to load slips reports beside the rows instead of replacing them. The confirmed ledger is the authority and must not be hidden by an outage in the provisional half.
- Consequence: the section heading is now `Transactions` rather than `Confirmed transactions`, because it no longer shows only confirmed ones.
- Consequence, and worth stating: this makes the **missing correction path** visible. A capture is append-only with no way to fix a mistyped amount (task 20's recorded limitation), and until now that was invisible too. Seeing a wrong provisional row and being unable to correct it is the next thing the owner will hit.
- What this does **not** do: it does not reconcile. A slip whose statement has already been imported now appears twice — once provisional, once confirmed — and the app cannot tell. That is task 22, and this entry is what makes the pair visible enough to reconcile.
- Evidence: `lib/slips.ts` (`capturedSlipSchema`, `slipListSchema`), `lib/transactions.ts` (`LedgerEntry`, `toLedgerEntries`, `compareEntries`, `summarizeSlips`, `matchesSlipQuery`), `app/transactions-view.tsx`, `app/globals.css`, `tests/transactions.test.ts` (8 new tests: interleaved ordering, nulls-last on an untimed slip, totals kept apart, balances untouched, exactness past 2^53, filtering, and the strict wire contract), `tests/e2e/owner-session.spec.ts` (a slip captured then read back beside four confirmed rows, asserting the strip still counts 4, both balance cells read `—`, and one-account mode hides it). Gate on 2026-07-31: ESLint, `tsc --noEmit`, Vitest **259 passed / 7 skipped across 18 files**, production build, Playwright owner **17/17** and isolated **18/18**. pgTAP unchanged at 129/129 — no migration was needed, because the endpoint and the table were already there.
- **Superseded within the hour by D-063**, on the owner's direction, in the two places it hedged: slips are now counted in the totals, and a matched pair is one row rather than two. The reason it hedged — that nothing knew which records were pairs — was true when written and stopped being true when matching was built. The read-side decisions it took that still stand: slips belong in the ledger view, a provisional row carries no balance, and the difference between the bank's record and the owner's is stated in words rather than by colour.

## D-063 — Slips are reconciled against statement rows, so the ledger shows one row per payment and one total

- Date: 2026-08-01
- Status: Accepted and built, at the owner's direction, with both open choices put to him and answered. First half of `PLAN.md` task 22. **Read-time only so far** — the owner's overrides and the undo are the second half, and are not built.
- Context: D-062 put slips in the ledger but kept their money out of the totals, because a slip and its statement row would otherwise be counted twice. The owner's answer was that the totals should include them, that a payment should appear once, and that each row should say which records it rests on. That is reconciliation, and it was already task 22.
- **The fact that shapes everything here: there is no identifier to join on.** No supported layout prints the transaction reference the slip QR carries — Krungthai and KBANK print none, and SCB's is a short channel code such as `SIPI`. Checked in the layout descriptors before any of this was designed. So a match can only be inferred from bank, amount and date, and must therefore be visible, reversible, and willing to refuse.
- Decision: **match on bank, amount equal to the minor unit, and a date within three days; consider only the candidates nearest in date; and pair only when that is mutually unique** — the slip has exactly one nearest candidate row and that row has exactly one slip nearest to it. No tolerance on money, ever. The mutual-uniqueness clause is what makes the result independent of the order rows arrive in; a greedy matcher would pair two identical transfers with whichever came first and look confident doing it.
- **The nearest-date clause was added on measurement, not taste, and the measurement is the most useful thing in this entry.** Over the owner's real ledger — 1,465 rows, read directly after he widened the read grant to cover slips and statements — the share of rows sharing a bank and an exact amount with another row is **6.5% on the same day, 11.5% within one day, 16.3% within three, and 27.8% within seven**. The window that buys tolerance for a late posting therefore also doubles the ambiguity it must resolve. Restricting to the nearest date takes 16.3% back to **6.5%**, the same-day floor: the tolerance becomes free, and the residue is irreducible — two identical payments on one day, where no date rule can help. Competition between *slips* for one row is resolved the same way, so a slip three days out cannot block one sitting on the row's own date.
- **Validated against real slips, not only fixtures.** Six real slips read across the four layouts were checked against the live ledger: three matched a single same-day row, two had no candidate at all (their statements are not imported), and one — a Krungthai payment — found **two identical same-day rows**. That last case is exactly the coin toss the mutual-uniqueness clause refuses, and it appeared in the first six slips looked at rather than in a contrived test.
- Decision (the owner's, from two offered): **a matched pair collapses onto the statement row**, which keeps the printed balance and the immutable source facts and takes the slip's counterparty as detail, labelled `(from slip)`. The alternative — showing the slip — was rejected because a slip has no printed balance, and a row with no balance puts a hole in the running-total column.
- Decision (the owner's): **match automatically when the rule is satisfied**, badged and reversible, rather than requiring a confirmation click per slip. The click would be the same judgement the rule already encodes, and ambiguity never auto-matches.
- Consequence, and the point of the whole change: **the totals can now include slips**, because a matched slip is not a row. Rows, deposits, withdrawals and net movement count every payment exactly once, and the row count discloses how many of them are still provisional.
- Decision: **four statuses, in words**: `Verified by slip`, `Awaiting statement`, `Needs review · several rows match`, `Statement only`. The owner asked for a status on rows with no slip too, so the fourth exists rather than being left as the absence of a badge.
- Decision: **a slip is shown against an account when the owner holds exactly one at that bank**, and against none when they hold two. This is not a reversal of D-056 — the QR still names only a bank — it is the observation that with one account at that bank there is no choice to make, so nothing is being guessed. Derived at read time, never stored: a stored account id would be a guess written down that a later statement could contradict silently.
- Decision: **reconciliation runs over the whole ledger, before any filter.** A match is a fact about two records, not about what is on screen — reconciling the filtered subset would let choosing an account or typing in the search box silently unmatch a pair and move the totals.
- **What is deliberately not built yet, and what it costs:** the owner cannot override a match. Auto-matching is applied on every load and there is no stored decision, so a new statement arriving can change what matches, and a wrong match cannot be undone. That needs a column on `public.slips`, a mutation RPC with audit, and a **backup schema v4** keeping v2 and v3 restorable — real work that is expensive to reverse, which is why the read-time half shipped first for the owner to judge the rule against his own data.
- Evidence: `lib/slip-reconcile.ts` (the whole rule, pure and dependency-light), `app/transactions-view.tsx`, `app/globals.css`, `tests/slip-reconcile.test.ts` (24 tests: the window's exact edge, one-satang and crossed-sign refusals, the wrong bank, both ambiguity directions, the nearest-date preference and the equidistant tie it must still refuse, a far slip not blocking a near one, order independence, account attribution with one and two accounts at a bank, collapse and status, and totals counting a matched payment once and staying exact past 2^53). The ledger measurement came from read-only SQL against `private-ledger-live` and a throwaway QR probe under `.runtime/diag/`, deleted after use — it reads real slips and so can never be committed (D-060), `tests/e2e/owner-session.spec.ts` (two specs: a slip that collapses onto its statement row leaving four rows and one `Verified by slip`, and an SCB slip against a Krungthai statement that stays its own row, is counted in the total, shows no balance and reports its account as unknown because the owner holds two SCB accounts). Gate on 2026-08-01: ESLint, `tsc --noEmit`, Vitest **279 passed / 7 skipped across 19 files**, production build, Playwright owner **18/18** and isolated **18/18**. pgTAP unchanged at 129/129 — nothing in the database moved, which is exactly what makes this half cheap to change.
