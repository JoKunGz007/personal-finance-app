# Private Ledger decision log

Last reviewed: 2026-08-09

Entries are append-only. A superseding decision must reference the earlier entry rather than rewriting its history.

This file carries **D-060 onward**. Entries **D-001 … D-059** are settled and were relocated to [`docs/decisions/ARCHIVE-D-001-D-059.md`](docs/decisions/ARCHIVE-D-001-D-059.md) on 2026-08-09 — moved unchanged, not rewritten. The index below covers both files, so a reader can find any entry without opening either body.

## Index

### Archived 
—
 `docs/decisions/ARCHIVE-D-001-D-059.md`

- **D-001** — Local-first synthetic development
- **D-002** — Canonical integer money
- **D-003** — Worker-contained PDF boundary
- **D-004** — Strong single-owner access
- **D-005** — Schema-v2 recovery is the first supported backup contract
- **D-006** — Synthetic preview is not a backup
- **D-007** — Compound-row resynchronization requires provenance
- **D-008** — Docker Supabase is the acceptance database
- **D-009** — Use Supabase’s default Docker network locally
- **D-010** — Repository continuity files
- **D-011** — Claude subagent workflow is a lean, tiered adaptation of the Codex one
- **D-012** — Import payload digest is server-recomputed and bound, never trusted
- **D-013** — Restore manifest counts are canonical integers and the sequence range reserves increment headroom
- **D-014** — Row fingerprints are server-recomputed and rejected on mismatch, guarded by a source-text charset
- **D-015** — Krungthai geometry is read from the pdf.js text layer against invented fixtures
- **D-016** — The statement frame is extracted from labelled pairs and reduced to last four at the parser
- **D-017** — Account binding is a checked user decision, not a parser inference
- **D-018** — Advisory lock serialization is proven with two real connections
- **D-019** — The recovery chain is proven end to end at scale, non-destructively
- **D-020** — The authenticated import path is proven with a local owner, no hosted resources
- **D-021** — A statement becomes an import through a chooser, not a code path
- **D-022** — The route wrapper is tested by invoking the handler with a real cookie session
- **D-023** — pdf.js gets its own worker, and the browser parse path is tested with a generated PDF
- **D-024** — The column model follows a real statement, corrected by the smoke test
- **D-025** — The currency marker is searched across page one, not only the frame block *(superseded by D-034)*
- **D-026** — The frame contract follows a real statement: no printed balances, two-line rows
- **D-027** — Browser runs use an isolated Playwright config
- **D-028** — The frame/grid boundary comes from the heading line, and the fixtures print the collision
- **D-029** — A printed zero money column is no movement, not a rejection
- **D-030** — Columns are assigned by a run's midpoint, using the width pdf.js reports
- **D-031** — The statement's calendar era is determined once, from its period end
- **D-032** — Row diagnosis is batched and deduplicated, not serialized one defect per read
- **D-033** — The import is cross-checked against the statement's own printed totals
- **D-034** — The currency must be stated in the frame block, restoring the narrow guard
- **D-035** — Private statements are invoked, never read: the masking harness boundary
- **D-036** — A development sign-in, gated by an opt-in flag rather than by `NODE_ENV`
- **D-037** — Receipts are images, so they are a separate build behind reviewed OCR
- **D-038** — What may appear in a masked dump is decided structurally, not by density
- **D-039** — A slash-joined money heading is two right-aligned columns, and direction is arithmetic
- **D-040** — The currency guard moves to the column heading, per layout, rather than being dropped
- **D-041** — Migration 009 admits three banks, and takes the fingerprint's bank from the bound account
- **D-042** — A reader must say how much of the parse it managed to verify
- **D-043** — An import the bank's arithmetic did not confirm is refused, not labelled
- **D-044** — Portable recovery is rehearsed into a second, separately bound local project
- **D-045** — The owner can create an account, through an RPC and nothing else
- **D-046** — Recovery is something a person can do, not something a developer can reconstruct
- **D-047** — The first real import, and the guard it forced
- **D-048** — The real ledger gets its own project; the test project goes back to being disposable
- **D-049** — Agents may read real statements, but only re-passworded copies, and only in one directory
- **D-050** — Slips are provisional entries the statement confirms, and their images are not stored
- **D-051** — Hosting is accepted, and Google OAuth is the gate in front of it
- **D-052** — `HANDOFF.md` owns mutable homeless facts, and the rule is now testable per edit
- **D-053** — The slip sample is four layouts rather than nine, and the QR does not always decode
- **D-054** — Fourteen statements imported through the app's own routes; the fifteenth is refused and stays refused
- **D-055** — A statement's printed row order is not always its balance order, and reconciliation recovers the one order that is
- **D-056** — Slips are a twelfth table, identified by a CRC-checked QR, and the backup reads two versions
- **D-057** — The platform QR reader does not exist on the owner's desktop, so the app carries its own and only downloads it when it must
- **D-058** — `connect-src` is derived from the configured Supabase origin, and an unconfigured build gets a narrower policy rather than a wider one
- **D-059** — Two of the four slip layouts carry the transaction date inside the QR, so it is read rather than typed

### Current 
—
 this file

- **D-060** — Three real slip references reached the test fixtures, and the entries claiming otherwise are wrong
- **D-061** — The app becomes four routes, and the shell owns everything that is not one surface's business
- **D-062** — A captured slip belongs in the ledger view, as a provisional entry that stays out of every confirmed figure
- **D-063** — Slips are reconciled against statement rows, so the ledger shows one row per payment and one total
- **D-064** — The match window narrows to one day, the chip that said nothing comes off the row, and the rule turns out to be unverifiable until task 21
- **D-065** — The live ledger moves to migration 011, so slip capture and reconciliation finally reach real records
- **D-066** — The one-day match window is measured rather than judged, and the measurement caught a wrong match it would have made
- **D-067** — The owner's say over a match is stored, and a statement row is something only one slip can claim
- **D-068** — The match decision becomes reachable, and the decisions arrive with the slips they are about
- **D-069** — The table is the chooser, because a dropdown cannot describe a row this app's rows repeat
- **D-070** — What the review of the match UI found, and the two of them that were about money
- **D-071** — A fee is its own statement row, so the amount-equality guard stays; what is left open is the slip, not the ledger
- **D-072** — The fee residual is closed, and the slip layout already said so
- **D-073** — The live ledger reaches migration 012, so the owner's override is finally about real records
- **D-074** — A backup's own description must come from the backup, not from the newest constant
- **D-075** — A capture leaves a record, and a pairing can be inspected
- **D-076** — The statements are reimportable, so a backup protects the typed layer rather than the ledger
- **D-077** — The three real slip references are gone from the working tree, and so is the real date inside one
- **D-078** — The owner's real backup is proven to restore, and a v3 file is proven to land in a v4 ledger
- **D-079** — The routing table stops naming agents, because only each harness knows which ones exist
- **D-080** — The decision log gets an index and an archive, because the cost was in the reads nobody chose
- **D-081** — The traps get sections and an index, and three of them were found to be lying
- **D-082** — The continuity documents get a checker, because every way they drifted was mechanical
- **D-083** — Hosting migrates by restoring the backup, not by reimporting the statements

## D-060 — Three real slip references reached the test fixtures, and the entries claiming otherwise are wrong

- Date: 2026-07-31
- Status: Accepted by the owner as an exposure he is content with; **not remediated**. Corrects three earlier entries rather than superseding their decisions — every design decision in D-056, D-057 and D-059 stands. What is wrong in them is a factual claim, not a judgement.
- **The false claims, named so they are not re-read as true.** D-056's evidence line says "Every fixture is built by `buildSlipQrPayload` from an invented reference — the builder lives in `lib/` precisely so no test input is ever copied off a real slip". D-057 repeats "from an invented reference". `tests/e2e/owner-session.spec.ts` said "No real slip reference exists in this repo." All three are false. `DECISIONS.md` is append-only, so they stay as written and this entry is the correction; the source comments, which are not append-only, have been fixed in place.
- **What is actually in the repository:** three real transaction references, copied from `receipts_sample/` — one in `tests/fixtures/synthetic-slip.ts` (`KTB_SLIP_DATED`) and the same one plus two others in `tests/slips.test.ts` (a Krungthai 17-character reference and a KBANK one). They are committed and pushed. The other fixtures are genuinely invented.
- **How it happened, which is the part worth keeping.** The structural probe in D-056 was written to be value-free, but its reportable-tag whitelist assumed a flat TLV and the payload nests everything inside tag `00` — so it printed whole references (already recorded in GOTCHAS). At the time this was noted as a read rather than a write, with the explicit remark that nothing derived from it would reach a fixture. Hours later, writing tests that needed reference *shapes* per bank, three of those shapes were reproduced from what had been printed. The gap was not carelessness about the rule; it was that **the rule was applied at the moment of the leak and not at the moment of reuse**, by which point the values looked like ordinary knowledge of the format.
- Consequence for `docs/FIXTURE_POLICY.md`: its central rule — "No real value becomes a fixture, ever" — is intact as a rule and is now **violated in practice**. The policy is not amended; a breach that is recorded is better than a policy quietly narrowed to fit what happened.
- Decision (the owner's): the exposure is accepted and the references stay. Recorded because the alternative — swapping three strings — is a few minutes' work, so leaving them is a choice rather than an obstacle. **Still open, and cheap, whenever it is wanted:** replace the three with invented values of the same shape. Note that a forward commit does not remove them from history or from the remote; only a history rewrite and force-push would, which is a separate decision nobody has asked for.
- **The generalisable lesson, which is not "be careful":** a value-free reporting rule protects the moment a value is *printed*. It does nothing at the moment that value is *reused*, because by then it is indistinguishable from format knowledge the agent legitimately has. Where a fixture must reproduce a real *shape*, generate the shape from the grammar — `buildSlipQrPayload` already existed for exactly this and was used for the payloads while the references it was given were pasted.
- Evidence: `tests/fixtures/synthetic-slip.ts`, `tests/slips.test.ts`, both now carrying comments that say which references are real. Found when the owner captured a Krungthai slip through the UI and its printed reference was visibly identical to the "invented" fixture — that is, by using the product, not by any check in the suite. No automated check exists that would have caught it, and none is added here.

## D-061 — The app becomes four routes, and the shell owns everything that is not one surface's business

- Date: 2026-07-31
- Status: Accepted and built. The routing half of `PLAN.md` task 19; it creates nothing hosted and needed no authorization. The OAuth half of that task is untouched.
- Context: the app was one long page. That had already been worked around once — on 2026-07-29 the ledger view was moved above the import bench because reaching it meant scrolling past everything else (task 17), which is a plaster over the fact that four unrelated surfaces shared one scroll position and one component.
- Decision: **four routes — `/ledger`, `/import`, `/slips`, `/recovery` — and `/` redirects to `/ledger`.** One canonical URL per surface, so nothing has to be kept in step, and the ledger is what an ordinary day opens on. `app/ledger-app.tsx` split into `app/import-bench.tsx` (select → unlock → bind → review → the `.pldemo` preview) and `app/recovery-bench.tsx` (export and restore); the transactions view and slip capture were already components and moved unchanged.
- Decision: **the shell owns the header, nav, footer and the session control**, in `app/site-header.tsx`. The development sign-in used to live inside "Import / 01", which was never true of it — a session is not a property of the import bench, and when OAuth lands it belongs in exactly the place this now is.
- **The session line is a polite live region, not a second `role="status"`.** Every route already has one status line, and adding a second to the shell would make `getByRole("status")` resolve to two elements on every page — breaking assertions in specs that have nothing to do with the header. Deliberate, and the kind of thing that is only cheap to decide before the shell exists.
- Decision: **no state crosses a route.** The one piece that did was `backupStale`, which the import bench set and the recovery band read. Splitting it revealed the coupling was wrong anyway: it made a *synthetic preview* go stale because a *real ledger* was exported. The import route now tracks only its own preview, and the confirmation message points at Recovery in words. Nothing is lost, because the authoritative staleness check is the snapshot sequence `confirm_backup_custody` compares server-side — the flag was always a hint.
- Decision: **service-worker registration moves from the capture form to the shell**, and this one is a privacy argument rather than a tidiness one. A Web Share Target POST can only be kept on the device by a worker that is *already* registered. With one page, any visit armed it; with four, a worker installed only by `/slips` would let the first share ever made reach the server's fallback handler — which is precisely what D-050 forbids. It remains exactly one registration site, and the worker still caches nothing but the shared image. The share redirect and the manifest's `start_url` were retargeted to `/slips` and `/ledger` accordingly.
- Consequence: the `section-index` ordinals ("Ledger / 05", "Slips / 06") named a position on a page that no longer exists, so single-section routes carry the label alone; the import flow keeps 01/02/03 because there the numbers are stages.
- Consequence for the suite: every spec that navigated to `/` had to be told which route it means — including `parser.spec.ts` and `statement-pdf.spec.ts`, whose subject is the PDF reader and which failed for a reason unrelated to anything they test (GOTCHAS). Four browser specs are new: the root redirect, nav across all four routes with `aria-current`, and an axe pass on each route, which is the first automatic accessibility coverage the ledger, slips and recovery surfaces have had without the import bench above them.
- Evidence: `app/layout.tsx`, `app/site-header.tsx`, `app/page.tsx`, `app/{ledger,import,slips,recovery}/page.tsx`, `app/import-bench.tsx`, `app/recovery-bench.tsx`, `lib/download.ts`, `app/globals.css`, `public/share-slip-sw.js`, `public/manifest.webmanifest`, `app/share-slip/route.ts`, `tests/privacy.test.ts` (the file lists it greps), `tests/e2e/{ledger,parser,statement-pdf,owner-session}.spec.ts`. Gate on 2026-07-31, all re-run: ESLint, `tsc --noEmit`, Vitest **251 passed / 7 skipped across 18 files**, pgTAP **129/129 across 4 files**, production build, Playwright owner **16/16** and isolated **18/18** (up from 14/14 by the four new specs across two viewports).

## D-062 — A captured slip belongs in the ledger view, as a provisional entry that stays out of every confirmed figure

- Date: 2026-07-31
- Status: Accepted and built, at the owner's direction. Supersedes the read-side assumption in D-050/D-056 — that slips would stay invisible until reconciliation (task 22) gave them a meaning — while leaving every write-side decision in those entries untouched.
- **How it was found, which is the pattern by now:** the owner captured a real slip through the UI, opened the ledger, and it was not there. The capture had worked — `public.slips` held the row. Nothing in the app displayed it, and `GET /api/v1/slips` had existed since migration 011 **with no caller at all**. Task 20 shipped a write path with no read path, and every test passed because each one asserted against the database directly.
- His stated intention, which the build had quietly narrowed: *slips should show in the ledger and be verified against the statement once in a while.* That is a strictly better product than "slips are invisible until task 22", and nothing in the invariants forbids it — the invariant is that `public.source_transactions` is written only by a confirmed import, not that a *view* may show one kind of row.
- Decision: **the ledger view renders one sequence of two kinds.** `LedgerEntry` is a discriminated union of a confirmed transaction and a captured slip; the same date/time/id comparator orders both, so a slip sits where it happened. The owner reads "what happened, in order", and a slip waiting for its statement happened.
- **Decision: provisional money is totalled separately and never folded in.** This is the load-bearing one. A slip and the statement row for the same payment are two records of one transaction, and until reconciliation matches them nothing can tell that they are a pair — so one combined total would overstate spending by exactly the amount of every slip that was captured *correctly*. Two true numbers beat one wrong one. The row count, deposits, withdrawals and net movement remain the statement's figures; provisional slips get their own line saying why they are apart.
- Decision: **a provisional row has no balance, in either money column.** A printed balance is the bank's statement of the account after a row; a slip is not in that chain, and deriving a figure there would invent one. Both cells read `—`.
- Decision: **slips appear in the all-accounts view only.** A slip carries no account by design — its QR names a bank, and only the statement says which of that bank's accounts the money moved through (D-056) — so filing one under a selected account would be the parser-guesses-the-binding mistake wearing different clothes. Selecting one account hides them and says that, rather than letting them silently vanish.
- Decision: a failure to load slips reports beside the rows instead of replacing them. The confirmed ledger is the authority and must not be hidden by an outage in the provisional half.
- Consequence: the section heading is now `Transactions` rather than `Confirmed transactions`, because it no longer shows only confirmed ones.
- Consequence, and worth stating: this makes the **missing correction path** visible. A capture is append-only with no way to fix a mistyped amount (task 20's recorded limitation), and until now that was invisible too. Seeing a wrong provisional row and being unable to correct it is the next thing the owner will hit.
- What this does **not** do: it does not reconcile. A slip whose statement has already been imported now appears twice — once provisional, once confirmed — and the app cannot tell. That is task 22, and this entry is what makes the pair visible enough to reconcile.
- Evidence: `lib/slips.ts` (`capturedSlipSchema`, `slipListSchema`), `lib/transactions.ts` (`LedgerEntry`, `toLedgerEntries`, `compareEntries`, `summarizeSlips`, `matchesSlipQuery`), `app/transactions-view.tsx`, `app/globals.css`, `tests/transactions.test.ts` (8 new tests: interleaved ordering, nulls-last on an untimed slip, totals kept apart, balances untouched, exactness past 2^53, filtering, and the strict wire contract), `tests/e2e/owner-session.spec.ts` (a slip captured then read back beside four confirmed rows, asserting the strip still counts 4, both balance cells read `—`, and one-account mode hides it). Gate on 2026-07-31: ESLint, `tsc --noEmit`, Vitest **259 passed / 7 skipped across 18 files**, production build, Playwright owner **17/17** and isolated **18/18**. pgTAP unchanged at 129/129 — no migration was needed, because the endpoint and the table were already there.
- **Superseded within the hour by D-063**, on the owner's direction, in the two places it hedged: slips are now counted in the totals, and a matched pair is one row rather than two. The reason it hedged — that nothing knew which records were pairs — was true when written and stopped being true when matching was built. The read-side decisions it took that still stand: slips belong in the ledger view, a provisional row carries no balance, and the difference between the bank's record and the owner's is stated in words rather than by colour.

## D-063 — Slips are reconciled against statement rows, so the ledger shows one row per payment and one total

- Date: 2026-08-01
- Status: Accepted and built, at the owner's direction, with both open choices put to him and answered. First half of `PLAN.md` task 22. **Read-time only so far** — the owner's overrides and the undo are the second half, and are not built.
- Context: D-062 put slips in the ledger but kept their money out of the totals, because a slip and its statement row would otherwise be counted twice. The owner's answer was that the totals should include them, that a payment should appear once, and that each row should say which records it rests on. That is reconciliation, and it was already task 22.
- **The fact that shapes everything here: there is no identifier to join on.** No supported layout prints the transaction reference the slip QR carries — Krungthai and KBANK print none, and SCB's is a short channel code such as `SIPI`. Checked in the layout descriptors before any of this was designed. So a match can only be inferred from bank, amount and date, and must therefore be visible, reversible, and willing to refuse.
- Decision: **match on bank, amount equal to the minor unit, and a date within three days; consider only the candidates nearest in date; and pair only when that is mutually unique** — the slip has exactly one nearest candidate row and that row has exactly one slip nearest to it. No tolerance on money, ever. The mutual-uniqueness clause is what makes the result independent of the order rows arrive in; a greedy matcher would pair two identical transfers with whichever came first and look confident doing it.
- **The nearest-date clause was added on measurement, not taste, and the measurement is the most useful thing in this entry.** Over the owner's real ledger — 1,465 rows, read directly after he widened the read grant to cover slips and statements — the share of rows sharing a bank and an exact amount with another row is **6.5% on the same day, 11.5% within one day, 16.3% within three, and 27.8% within seven**. The window that buys tolerance for a late posting therefore also doubles the ambiguity it must resolve. Restricting to the nearest date takes 16.3% back to **6.5%**, the same-day floor: the tolerance becomes free, and the residue is irreducible — two identical payments on one day, where no date rule can help. Competition between *slips* for one row is resolved the same way, so a slip three days out cannot block one sitting on the row's own date.
- **Validated against real slips, not only fixtures.** Six real slips read across the four layouts were checked against the live ledger: three matched a single same-day row, two had no candidate at all (their statements are not imported), and one — a Krungthai payment — found **two identical same-day rows**. That last case is exactly the coin toss the mutual-uniqueness clause refuses, and it appeared in the first six slips looked at rather than in a contrived test.
- Decision (the owner's, from two offered): **a matched pair collapses onto the statement row**, which keeps the printed balance and the immutable source facts and takes the slip's counterparty as detail, labelled `(from slip)`. The alternative — showing the slip — was rejected because a slip has no printed balance, and a row with no balance puts a hole in the running-total column.
- Decision (the owner's): **match automatically when the rule is satisfied**, badged and reversible, rather than requiring a confirmation click per slip. The click would be the same judgement the rule already encodes, and ambiguity never auto-matches.
- Consequence, and the point of the whole change: **the totals can now include slips**, because a matched slip is not a row. Rows, deposits, withdrawals and net movement count every payment exactly once, and the row count discloses how many of them are still provisional.
- Decision: **four statuses, in words**: `Verified by slip`, `Awaiting statement`, `Needs review · several rows match`, `Statement only`. The owner asked for a status on rows with no slip too, so the fourth exists rather than being left as the absence of a badge.
- Decision: **a slip is shown against an account when the owner holds exactly one at that bank**, and against none when they hold two. This is not a reversal of D-056 — the QR still names only a bank — it is the observation that with one account at that bank there is no choice to make, so nothing is being guessed. Derived at read time, never stored: a stored account id would be a guess written down that a later statement could contradict silently.
- Decision: **reconciliation runs over the whole ledger, before any filter.** A match is a fact about two records, not about what is on screen — reconciling the filtered subset would let choosing an account or typing in the search box silently unmatch a pair and move the totals.
- **Superseded in part by D-067 (2026-08-05)**, which built the second half and corrected the storage design named below: it cannot be a column on `public.slips`, because migration 011 made that table append-only.
- **What is deliberately not built yet, and what it costs:** the owner cannot override a match. Auto-matching is applied on every load and there is no stored decision, so a new statement arriving can change what matches, and a wrong match cannot be undone. That needs a column on `public.slips`, a mutation RPC with audit, and a **backup schema v4** keeping v2 and v3 restorable — real work that is expensive to reverse, which is why the read-time half shipped first for the owner to judge the rule against his own data.
- Evidence: `lib/slip-reconcile.ts` (the whole rule, pure and dependency-light), `app/transactions-view.tsx`, `app/globals.css`, `tests/slip-reconcile.test.ts` (24 tests: the window's exact edge, one-satang and crossed-sign refusals, the wrong bank, both ambiguity directions, the nearest-date preference and the equidistant tie it must still refuse, a far slip not blocking a near one, order independence, account attribution with one and two accounts at a bank, collapse and status, and totals counting a matched payment once and staying exact past 2^53). The ledger measurement came from read-only SQL against `private-ledger-live` and a throwaway QR probe under `.runtime/diag/`, deleted after use — it reads real slips and so can never be committed (D-060), `tests/e2e/owner-session.spec.ts` (two specs: a slip that collapses onto its statement row leaving four rows and one `Verified by slip`, and an SCB slip against a Krungthai statement that stays its own row, is counted in the total, shows no balance and reports its account as unknown because the owner holds two SCB accounts). Gate on 2026-08-01: ESLint, `tsc --noEmit`, Vitest **279 passed / 7 skipped across 19 files**, production build, Playwright owner **18/18** and isolated **18/18**. pgTAP unchanged at 129/129 — nothing in the database moved, which is exactly what makes this half cheap to change.

## D-064 — The match window narrows to one day, the chip that said nothing comes off the row, and the rule turns out to be unverifiable until task 21

- Date: 2026-08-05
- Status: Accepted, at the owner's direction. Answers both questions D-063 deliberately left open. Read-time only, like D-063 — no migration, no database change, so it stays as cheap to revise as the half it amends.
- Context: D-063 shipped the read-time half first precisely so the owner could judge the rule against his own ledger before a migration made changing it expensive. That is what happened, and this is the judgement.
- Decision (the owner's): **the window narrows from three days to one.** His banks post same-day, and the evidence that exists agrees — of the six real slips ever checked against the live ledger, four produced a candidate at all and every one of those sat on the slip's own day. So one day is **tolerance for the clocks, not for the bank**: the slip's date is the owner's local date and the row's is the bank's posting date, so a payment made late in the evening can fall either side of midnight between them with no lag whatsoever. Zero would leave no room for that. One does.
- **Why narrowing is not the mirror image of widening.** By D-063's own measurement the nearest-date clause collapses even a three-day window back to the 6.5% same-day floor, so a wider window was never costing ambiguity. What it cost was the chance of a *wrong* match in the case that measurement cannot see: the true row not yet imported, and a decoy of the same bank and exact amount sitting two or three days away. Narrowing removes that. The risk it adds is the opposite one — a genuinely late posting failing to match, which does not merely miss a pairing but **counts the payment twice** in the totals, once as the slip and once as the row. That asymmetry is the whole reason the question was worth asking rather than defaulting.
- Decision (the owner's, revising D-063's fourth status): **the `Statement only` chip comes off the row.** It is the ledger's default state — on this ledger essentially every one of 1,465 rows — so as a badge it carried no information while making the three statuses that do mean something harder to find. The status *value* in `lib/slip-reconcile.ts` is unchanged and still exhaustive; only its presentation moved. It remains the cell's accessible name for a screen reader, and becomes a **Status filter** in the ledger controls, so the question "which rows have no slip?" is asked rather than answered 1,465 times unprompted. D-063 recorded this as the one part of that design chosen without evidence; this is that admission acted on.
- **The finding that matters most, and it is a blocker: the match rule cannot be validated across the sample until PLAN task 21.** The rule needs three facts — bank, exact amount, date. The QR carries only bank and reference (`SlipIdentity` in `lib/slip-qr.ts`), the date is derivable from the reference for 14 of 23 (D-059), and **the amount exists solely on the printed face of the slip**; `docs/SLIP_CONTRACT.md` records that no OCR has been run. So checking one slip against the ledger costs reading its image by eye, which is why the evidence stops at six of 23 samples and not because six was thought sufficient. A full-sample measurement was designed this session and then **abandoned rather than run by hand**: doing it manually means transcribing 23 real amounts, which is the D-060 pattern — a value read legitimately, written down, and permanent thereafter — for a result that task 21 will produce as a script for free.
- Consequence for the plan: **task 21 is a prerequisite for validating task 22, not merely adjacent to it.** When OCR lands, the window should be re-derived from a run over all 23 samples rather than from this entry. Recorded here because nothing in the continuity docs said it, and the next agent to attempt the measurement would hit the same wall in the same order.
- Deliberately not built: the ledger table has **no row detail view**, so the owner's "keep the status in detail" lands as the status cell's accessible name plus the filter. Building a detail panel is new UI beyond this change and was not attempted under it.
- Superseded in part by D-065, which put the live ledger on migration 011, so this rule now runs against real records rather than the synthetic project only.
- Evidence: `lib/slip-reconcile.ts` (`MATCH_WINDOW_DAYS = 1`, with the reasoning and the task 21 dependency at the constant), `app/transactions-view.tsx` (status filter, chip removed, the slips status line's copy), `app/globals.css` (`.status-chip.statement-only` retired for `.status-none`), `tests/slip-reconcile.test.ts` and `tests/e2e/owner-session.spec.ts`. **Two unit tests had to be rebuilt, not merely re-expected**: "prefers the nearest date" and "does not let a far candidate block a pairing it could never win" both placed their far candidate three days out, which a one-day window excludes outright — each would have kept passing while silently testing the window instead of the clause it is named for. Gate on 2026-08-05: ESLint, `tsc --noEmit`, Vitest **279 passed / 7 skipped across 19 files**, pgTAP **129/129**, production build, Playwright owner **18/18**, isolated **18/18**.

## D-065 — The live ledger moves to migration 011, so slip capture and reconciliation finally reach real records

- Date: 2026-08-05
- Status: Accepted and applied, on the owner's explicit authorization given at the time. **The request was disambiguated before anything ran** — "do the migration first" could equally have meant migration 012 (task 22's persistence half, synthetic project only) or 011 onto the live ledger (real financial records), and those differ by everything that matters. It was put back to him as a choice rather than resolved by inference.
- Context: slip capture shipped 2026-07-30 and reconciliation 2026-08-01, but `private-ledger-live` was still on 001–010, so both existed only against the synthetic project. Three days of work touched nothing real. This is what changes that.
- **What was established before applying it, in this order.** The migration was read end to end: it is **purely additive** — `create table public.slips` with its index, immutability trigger, RLS and grants, two CHECK constraints *widened* (`restore_runs.schema_version` to `in (1,2,3)`, and `slips` appended to `restore_chunks`' permitted kinds), and three functions replaced. No dropped table, no dropped column, no `UPDATE` or `DELETE` against ledger data, and the whole file is one `begin`/`commit`, so it lands atomically or not at all.
- **Pre-flight on the two tables whose constraints get rewritten**, because a re-added CHECK validates existing rows: live held 1 `restore_run` at schema_version 2 and 11 `restore_chunks` across 11 kinds. Both sets are subsets of the widened constraints, so neither rewrite could fail on existing data. Widening cannot strand a row; that was confirmed rather than assumed.
- **The backup was proven current from the database, not from the handoff line that claimed it.** `mutation_sequences.sequence` = 19, `last_exported_sequence` = 19, the newest `backup_records` row at sequence 19, and the last `audit_events` row dated 2026-07-29 — four independent readings agreeing that the ledger has not moved since the 2026-07-29 export. A fresh backup would therefore have covered exactly the same state, and taking one requires the owner at the `/recovery` route, so the standing backup was relied on **deliberately and with evidence** rather than by omission. Migration 011 does not touch `mutation_sequences`, so it stayed current across the change.
- **The seed hazard was checked in the script before running it.** `live-ledger.mjs up` calls `migrate()` and then applies `live/live-seed.sql` **only when no owner is bound**. An owner is bound, so the seed did not run — read out of `scripts/lib/local-supabase.mjs` first, and confirmed afterwards by the command's own `Owner already bound.` line.
- Result, verified after the fact against the live database: 11 migrations recorded; `public.slips` present and empty; **1,465 rows, 15 batches, 6 accounts, 1,465 components and 18 audit events — every figure identical to the pre-migration reading**; sequence still 19; `export_backup_snapshot` now declares schema version 3; `restore_backup` accepts both '2' and '3', so the owner's v2 file is not stranded; and `authenticated` holds no INSERT, UPDATE or DELETE on `public.slips`, leaving `capture_slip` the only write path. The least-privilege and append-only invariants hold on real records.
- **The new hazard this creates, and it is the point of writing this entry.** An app pointed at `.env.local` now reaches a real `slips` table. Capturing a slip there is a **real mutation of real financial records**: it is append-only and cannot be deleted, it writes an audit event, and it bumps the mutation sequence, which **invalidates the standing backup**. It is never a standing permission and must be asked for every time, exactly like an import. The old protection — that live simply had no such table — is gone, and it was doing more work than it looked like.
- What this does **not** do: it does not let a phone share a slip, which is task 19's hosted half and remains unauthorized; and it does not validate the match rule, which is blocked on task 21 (D-064).

## D-066 — The one-day match window is measured rather than judged, and the measurement caught a wrong match it would have made

- Date: 2026-08-05
- Status: Accepted. Closes the measurement D-064 deferred, earlier than D-064 expected — it assumed task 21 had to ship first, and it did not.
- **What made it possible without shipping OCR.** D-064 recorded the rule as unvalidatable until task 21 because the amount is not machine-readable. That was right about the amount and wrong about the ordering: a *local throwaway harness* can read the amounts without any of it shipping, being bundled, or costing the phone a byte. The in-app pre-fill and the measurement were never the same job, and separating them turned a blocked task into an afternoon's run.
- **Nothing about the method let a real value escape.** The harness read the images, turned each amount into one SQL query, and printed only counts and date *distances*. No amount, date, reference or counterparty was printed, written to a file, or shown to an agent — which makes this strictly safer than the alternative the owner declined on 2026-08-05, where the slips would have been read by eye and 23 real amounts would have passed through a transcript. Worth stating as a pattern: **when a measurement needs real values, compute the answer where the values already are and emit only the aggregate.**
- **The QR half reproduced three independently recorded measurements exactly**, which is what makes the rest of the run trustworthy: 23/23 decoded, banks SCB 9 / KTB 9 / KBANK 5, **14 carrying a usable date** (SCB 9/9, Krungthai's 21-character variant 5/5), and reference lengths KBANK 20, KTB 17 or 21, SCB 25. Those match D-053, D-059 and `lib/slip-qr.ts` — figures taken weeks earlier by a different path.
- **The result: of 9 slips with a ledger candidate, 8 posted on the slip's own day and one sits 11 days away.** Two of the 11 amounts read had no candidate at all, their statements not being imported. No case had more than one row at the nearest distance.
- **The outlier is a coincidence, not a late posting, and saying so is a judgement worth showing.** No Thai interbank transfer posts eleven days late. D-063 measured that 27.8% of this ledger's 1,465 rows share a bank and an exact amount with another row within seven days, so a stray same-amount row at that range is the expected behaviour of chance whenever the true row is absent — and absent rows are known to be present in this sample, since two slips had no candidate for exactly that reason. The measurement cannot *prove* which it is; the arithmetic makes coincidence overwhelmingly the better explanation.
- **The finding that matters most: a wide window would have made a wrong match inside the very sample used to test it.** At the three-day window D-063 shipped, that slip is unaffected — but at anything reaching eleven days it collapses onto an unrelated statement row, taking a real payment off the ledger as a duplicate and moving the totals. At one day it stays unmatched, which is correct. **The narrowing was right, and this is the evidence, not the reasoning that motivated it.**
- Consequence: `MATCH_WINDOW_DAYS = 1` stands, now on measurement rather than judgement. **Same-day posting is confirmed for this owner's banks**, and the residual case a one-day window buys — a payment straddling midnight between the owner's clock and the bank's — remains unobserved in 9 samples and is cheap enough to keep.
- **What the run also measured, which task 21 wanted and did not have** (recorded in `docs/SLIP_CONTRACT.md`): tesseract.js 7.0.0 with `tha+eng`, at native resolution and default settings, recognises Thai on 14 of 14 images at a mean of 187 words, finds the amount's label on **11 of 14 (79%)**, and yields a usable amount on **all 11** where the label is found. So the policy layer works wherever the engine reads the label, and 79% is the number an in-app pre-fill would have to live with — comfortable for a value the owner must review anyway, and useless for anything automatic.
- **Honest limits.** n is 9. The outlier is inferred rather than resolved. The 3 unread labels were not retried at 2× upscale, and D-053 found exactly 3 of 23 samples needing that upscale for the QR — whether they are the same 3 images is unmeasured and is the first thing to check next.
- Evidence: the harness under `.runtime/diag/`, **deleted after use** — it reads real slips and so can never be committed (D-060), the same rule the statement read-through harness followed. No repository code changed, so the 2026-08-05 gate stands.

## D-067 — The owner's say over a match is stored, and a statement row is something only one slip can claim

- Date: 2026-08-05
- Status: Accepted and built. Migration 012, the second half of `PLAN.md` task 22. D-063 held this back deliberately so the rule could be judged against real data before a migration made it expensive; that judgement happened (D-064, D-066) and the rule now rests on measurement, so this stores it.
- **The design the docs specified was impossible, and the docs disagreed with each other.** `PLAN.md` said the override needs "a column on `public.slips`". Migration 011 put `slips_immutable before update or delete` on that table, so **no** column on it can ever be updated, whatever it holds — the design would have failed on its first write, at run time rather than at design time. `GOTCHAS.md` had recorded exactly this on 2026-08-01 while the same half was being designed; PLAN was never corrected and the two sat in contradiction for four days. Both are now reconciled. **The lesson is about which document to believe:** GOTCHAS records what was discovered by reading the schema, PLAN records what was intended, and where they disagree the schema wins.
- Decision: the established **`transaction_overlays` + `overlay_revisions` shape**, not a new one. `public.slip_match_overlays` holds the current decision and is mutable with a revision counter; `public.slip_match_revisions` holds the history and is append-only under `private.reject_change()`. `public.set_slip_match` is the only write path, with `authenticated` holding no insert, update or delete on either table, and optimistic concurrency through an expected revision exactly as `update_transaction_overlay` does it. Inventing a second shape for a problem this ledger had already solved would have been the more expensive choice, not the cheaper one.
- Decision: **three states, one of them the absence of a row.** `matched` names the statement row, `unmatched` says this slip is none of them, and no row at all means no decision and the automatic rule applies — which is where every slip starts. The absent state is what keeps this additive: nothing changes for a ledger whose owner never disagrees with anything.
- **The guard nothing asked for, and the reason it exists.** A partial unique index on `(transaction_id, owner_id)` makes it unrepresentable for two slips to claim one statement row. `lib/slip-reconcile.ts` already enforces mutual uniqueness, but only as a property of a *proposal* recomputed on every read; a stored decision is a fact, and two facts claiming one row would double-count the very payment reconciliation exists to count exactly once. Partial because `unmatched` decisions all carry a null transaction and must not collide with each other. Red-proved: dropping the index fails exactly one contract of 24 and nothing else.
- **Decisions are applied before the rule runs, and the ordering is load-bearing.** The reader removes decided slips and claimed rows from the pool *first*, so the automatic rule only ever sees what is left. Layering decisions over the rule's output instead would let an automatic pairing consume the row the owner had already assigned, and the owner's explicit decision would then lose to a guess depending on iteration order — the same class of bug mutual uniqueness was introduced to kill.
- Decision: **a decision naming a row this ledger no longer holds is ignored, not obeyed.** A restore or a re-import can remove the statement row a decision pointed at. Obeying it would pair the slip with nothing and drop it out of the totals silently; ignoring it returns the slip to being its own visible provisional row. Fail visible, not fail quiet.
- **Decision, and the one most worth revisiting: a manual match is held to the same two facts the automatic rule uses** — same bank, and an amount equal to the minor unit. What the override buys is resolution of *ambiguity* and rejection of a *wrong* pairing, which are precisely the two gaps D-063 recorded. It is not a way to declare that two unrelated sums are the same payment. The reason is asymmetric harm: a refused override costs the owner a manual entry, while an accepted mismatched pairing **collapses two records into one row and takes a real payment off the ledger**, with no audit row that makes the loss visible afterwards. This is the conservative end and the reversible one; loosening it later is a decision to write down, not a default to drift into.
- Consequence: **backup schema v4**, with v2 **and** v3 still restorable. Each version keeps its own kind list rather than the newest list being applied leniently to all, so a v3 manifest does not pass as v4 and a v2 payload carrying a `slips` key is still refused — widening the union is not the same as relaxing it. The reason v2 must keep working has not changed since D-056: the only backup covering this ledger whole is still a v2 file (D-065 re-verified it at sequence 19).
- **Deliberately not built, and named so it is not mistaken for done:** `set_slip_match` has no HTTP route and no UI, so the decision cannot yet be made from the app. That is the next step. It is called out because it is the exact shape of D-063's origin — task 20 shipped a write path with no read path, and it took the owner opening the ledger and finding nothing to notice.
- Evidence: `supabase/migrations/202608050012_slip_match_decisions.sql`, `supabase/tests/005_slip_match.sql` (24 contracts: least privilege on both tables, RLS forced, the revision counter, the audit event, the mutation-sequence bump, optimistic-concurrency conflict, undo, the decision vocabulary and the null that must travel with it, the bank and amount guards, unowned slip and unowned transaction, the double-claim refusal, append-only history under both `update` and `delete`, and the aal1 refusal last so nothing above runs on a downgraded session), `lib/slip-reconcile.ts` and `tests/slip-reconcile.test.ts` (7 new), `lib/backup-contract.ts` and `tests/backup.test.ts`. Gate 2026-08-05: ESLint, `tsc --noEmit`, Vitest **313 passed / 7 skipped across 20 files**, pgTAP **154/154 across 5 files**. The recovery destination received 012 before the portability suite ran — it is a separate project and does not receive a migration otherwise — and that suite passed on its named line.

## D-068 — The match decision becomes reachable, and the decisions arrive with the slips they are about

- Date: 2026-08-07
- Status: Accepted and built. The step D-067 named as the next one and deliberately did not take: `public.set_slip_match` had no HTTP route and no caller. No migration — the database is exactly as 012 left it, and pgTAP is unchanged at 154/154 because the contracts this reaches are the ones already written.
- Decision: **one write route, `PUT /api/v1/slips/[id]/match`, and no second write path.** It mirrors `app/api/v1/transactions/[id]/overlay/route.ts` in shape and in method: the decision is one row per slip, so sending it twice must land in the same place rather than accumulate. The route is a boundary, not an implementation — zod checks the shape, the RPC checks the money, and `authenticated` still holds no insert, update or delete on either decision table.
- **Decision, and the one with a real alternative: the stored decisions come back on the `GET /api/v1/slips` response rather than through a route of their own.** A separate endpoint reads cleaner and is wrong in the failure case. Two responses can half-arrive, and the dangerous half is slips loading while their decisions do not — which shows the owner a pairing they have already overruled and presents it as the automatic rule's, silently and with the totals to match. One response cannot fail that way: the view's existing degradation is "no slips shown", which stays honest only while a decision cannot go missing on its own. The cost is a wider response and a strict schema that now requires the key.
- **Decision: the manual candidate list is not the automatic rule's candidate set.** It re-checks the two facts `set_slip_match` re-checks server-side — same bank, amount equal to the minor unit — and **drops the date window entirely**, because reaching past that window is the entire reason an override exists. A control that could only offer what the rule had already found would resolve nothing the rule refused. Rows already claimed by another slip's stored decision are left out, since the partial unique index would refuse that write; a row the rule merely paired **is** offered, because taking it is a legitimate overrule and the slip that loses it falls back to a visible provisional row.
- Consequence: **the list is capped at 25, nearest in date first, and says how many it left out.** Bank plus exact amount is not selective on a round figure — a monthly transfer of the same amount produces one candidate per month — and a cap that silently hid the right row would be worse than a long list. The ordering is total (distance, then date, then id) so the option under the pointer does not move between reading it and clicking it.
- Consequence: **an undo stores `unmatched`; it does not delete the decision.** The RPC has no way to return a slip to the automatic rule, so the UI does not offer one — a control promising something the database cannot do would be a lie that only shows up later. The three states remain what D-067 defined: `matched`, `unmatched`, and no row at all.
- **A defect found and fixed on the way, in the test harness rather than the product:** `resetOwnerImportSurface` deleted slips without deleting the decisions referencing them. It runs under `session_replication_role = replica`, which disables foreign-key triggers along with the append-only ones, so the delete succeeded and left orphans instead of failing. Nothing had written a decision from a test before, so the gap arrived with 012 and could only ever have surfaced later, as a slip that appears already decided or a restore refusing an apparently empty ledger. In `GOTCHAS.md`.
- **Also recorded, because it makes an accessibility row read greener than it is:** the isolated suite's axe pass on `/ledger` runs against a route nobody has loaded, where the table and every per-row control are absent. The D-068 controls are covered by two axe passes inside the owner suite instead, one per state. In `GOTCHAS.md`.
- Evidence: `app/api/v1/slips/[id]/match/route.ts`, `app/api/v1/slips/route.ts`, `lib/slips.ts` (`slipMatchDecisionSchema`, `slipMatchRequestSchema`, `slipMatchResponseSchema`, and `SlipMatchDecision` now defined once where it is parsed), `lib/slip-reconcile.ts` (`matchCandidates`), `app/transactions-view.tsx`, `app/globals.css`, `tests/slip-match-route.test.ts` (12), `tests/slip-reconcile.test.ts` (7 new), `tests/transactions.test.ts` (1 new), `tests/e2e/owner-session.spec.ts` (1 new), `tests/helpers/local-owner.ts`. Gate 2026-08-07, synthetic project only: ESLint, `tsc --noEmit`, Vitest **335 passed / 7 skipped across 21 files**, pgTAP **154/154 across 5 files**, production build emitting `/api/v1/slips/[id]/match`, Playwright owner **19/19** and isolated **18/18**, portable recovery passing on its named line. **The candidate control described here was replaced the same day — see D-069.**

## D-069 — The table is the chooser, because a dropdown cannot describe a row this app's rows repeat

- Date: 2026-08-07
- Status: Accepted and built. Supersedes the candidate control in D-068 only; everything else there — the route, the response shape, the guards, the three decision states — is unchanged.
- **The owner found it by using it, which is the third time that has decided a design here** (D-062, D-063 were the first two). D-068 offered the candidate rows as `<option>`s labelled with their date and description. On the seeded ledger the two options differed only because the seed's descriptions said "first of two" and "second of two". **On a real statement they would have been identical**: same bank, same amount to the satang, same day, same wording. The dropdown was describing rows using exactly the fields that make them indistinguishable, and omitting the two that do not — the printed time and the running balance.
- Decision: **the ledger table becomes the chooser.** `Choose a statement row` filters the table to the slip and every row it could be; each candidate carries a `This is it` button on the row itself, beside the time and balance; a banner names what is being chosen and offers `Cancel`. A row is identified by being read, not by being re-described somewhere else.
- Decision: **while choosing, the Account, Status and search controls are suspended, and so is Reload.** A filter left on from earlier could otherwise hide the row being looked for, and the owner would conclude it is not there — the same failure as a silently capped list, arrived at differently. Reload is included because it would drop a half-made choice.
- Decision: **the totals strip is hidden while choosing.** What is on screen is a slip and its candidates, which is not a view of the ledger and has no total worth printing; a three-row subtotal in the place the ledger's own figures normally sit would read as a figure about the ledger.
- Consequence: **the 25-row cap and its "N further rows are not listed" note are gone.** They existed because a `<select>` cannot hold a long list; table rows can, and a cap that hides the right row is worse than a long list. `matchCandidates` now returns every eligible row. It keeps its nearest-date-first ordering as a property of the function even though the view re-sorts into the ledger's own order, because a caller that does not re-sort must not be quietly non-deterministic.
- **A real defect this flushed out, worth more than the redesign:** both new buttons carried an `aria-label` that *replaced* their visible words rather than extending them — `This slip is not the row dated…` on a button reading `Not this slip`. That breaks WCAG 2.5.3 (label in name): a voice-control user saying the words on the button reaches nothing. axe does not catch it, and it surfaced only because a Playwright locator that asked for the button by its visible name timed out. Both labels now begin with the visible text and add the distinguishing detail after it. **Read a name-based locator timing out as an accessibility signal, not a test-selector problem** — in GOTCHAS.
- Evidence: `app/transactions-view.tsx`, `app/globals.css`, `lib/slip-reconcile.ts`, `tests/slip-reconcile.test.ts`, `tests/e2e/owner-session.spec.ts` (the D-068 spec now drives the picking mode and asserts the two-row filtered table, the absent totals strip, and the disabled Status control). Gate re-run in full 2026-08-07 after the change; figures in `PLAN.md`. **Five defects in this UI were found by the review that followed it — see D-070.**

## D-070 — What the review of the match UI found, and the two of them that were about money

- Date: 2026-08-07
- Status: Accepted and fixed. A `/code-review` pass over the uncommitted D-068 / D-069 work, run by the owner before any commit. Six findings; five were in this session's code and are fixed here, and the sixth is in a deliberately local config file and is the owner's to take (recorded below, not acted on).
- **The two that were about money, and neither was a crash.** *A candidate row already paired with another slip lost its status chip.* Picking mode *replaced* the Status cell with the `This is it` button, so a row the rule had paired with some other slip showed no chip while being offered — and taking it silently returned that other slip to provisional. `matchCandidates` offers such rows on purpose and the database accepts the write; the defect was that the only remaining cue was the green left edge on `.verified-row`, which is colour alone — the thing this file's own comment rules out. The chip now stays, and a line names the slip that would lose the row and what happens to it. *And two decisions could be in flight at once*: `deciding` gated only the row it belonged to, so a second undo could be pressed while the first was still going; the first to resolve re-enabled the second's button, and a further press sent a revision the database had already moved past — surfacing as "changed in another session" to an owner with one tab open, while the shared error line could not say which decision it was even about. Every decision control is now gated on any write being in flight: one at a time.
- **One that could have stored a wrong attribution.** With one account selected, the table shows a Reference column instead of an Account column — but candidates are filtered by **bank**, so with two accounts at one bank the offered rows can belong to different accounts and nothing on screen would say which. `set_slip_match` checks bank and amount only, so the wrong one stores a mis-attributed match. Picking mode now forces the account column, which is exactly the ambiguity `slipAccount` returns null rather than guess at (D-056).
- **Two that were about being able to use it at all.** The button that opens the mode disables itself in the same update, and a browser blurs a disabled element — dropping a keyboard or screen-reader user to `<body>` with the announcement read and nowhere to be; focus now moves to `Cancel`. And the mode was gated on two different values — the banner on the slip, the row filter and the disabled controls on the id — which left a dead end where the only `Cancel` disappeared while everything stayed disabled; one derived `picking` value now decides all of it.
- **What the review changes about how this UI is tested.** Four of the five are invisible to axe and to every assertion that only reads text: a focus destination, a chip that is present rather than absent, a column choice, and a second click during a request. The browser suite gained a focus assertion and a whole spec for the row-already-taken case, because that one ends with a real payment quietly off the ledger.
- **Raised, then fixed on the owner's instruction the same day: `playwright.config.ts` had `reuseExistingServer: !process.env.CI`.** Its `webServer.env` pins the test project, but a server already listening on 3000 is reused instead — `webServer.command` and its pins never run — and that config has no `testIgnore`, so it collects `owner-session.spec.ts`, which now writes through the app. If the owner leaves a server running from `.env.local` (which points at the real ledger, D-048) and then runs that config, the browser suite writes to real records. Both sibling configs set `false` for this reason (D-027). It now reads `false`. **The file is one of the three deliberately local-only ones `HANDOFF.md` protects, so the fix exists on this machine only** — a fresh clone still reuses, and still has no pins at all, which is the same residual `PLAN.md` task 16 already records. Whether these three files should be committed is a question for the owner, not a decision to drift into.
- Evidence: `app/transactions-view.tsx`, `app/globals.css`, `tests/e2e/owner-session.spec.ts` (focus assertion plus the new "already another slip's" spec, which drives the swap and asserts both sides of it in the UI and the single stored decision in the database). Gate re-run in full afterwards; figures in `PLAN.md`.

## D-071 — A fee is its own statement row, so the amount-equality guard stays; what is left open is the slip, not the ledger

- Date: 2026-08-07
- Status: Accepted, measured. Answers the question D-067 called "the one most worth revisiting", and answers it from the real ledger rather than from judgement. No code changed.
- **Method, and why it was safe.** The owner granted a read of `private-ledger-live` for this one question. It was answered with counting queries run inside the database, emitting **only counts** — the same shape as D-066's measurement, and for the same reason: the answer needed real values, so it was computed where the values already live and nothing but aggregates crossed into a transcript.
- **The finding: fees are posted as their own rows, and nothing in this ledger bundles two amounts into one.** All **1,465 rows carry exactly one component** — so no transfer row anywhere in the real ledger silently includes a fee inside its printed amount. Fee-shaped rows exist separately: **15 of them**, SCB 11, KTB 3, KBANK 1, spread over 6 months and 3 accounts, with 9 distinct amounts among them, so they are per-event charges rather than one repeating monthly fee. **7 of the 15** sit on the same day and account as a transfer-looking withdrawal; the ledger holds **163** such withdrawals, so at most about 4% of transfers carry a same-day fee at all.
- Decision: **`MATCH_WINDOW_DAYS`'s companion guard — amount equality to the minor unit — stays exactly as it is,** in the automatic rule and in `set_slip_match` alike. The case that would have broken it is a fee rolled into the transfer row, and this ledger does not contain one.
- **What is *not* settled, and it moved from the ledger to the slip.** The owner's recollection is that a slip's printed amount **includes** any fee. If that is right, then for the ~4% of transfers that carry one, the slip's amount is the transfer *plus* the fee while the statement's transfer row is the transfer alone — so those slips will not match automatically, **and the manual override will refuse them too**, since it enforces the same equality. The failure is visible and safe (the slip stays a provisional row of its own; nothing is mis-paired), but it is unfixable from inside the app.
- **If it ever needs fixing, fix the manual path only.** The owner asserting a pairing and the rule inferring one are different acts: a person can see the fee on the slip and the fee row on the statement, and the rule cannot. Loosening the automatic rule would let a near-match on money become a match, which is the thing this ledger must never invent. So the change, if it is ever wanted, is to let `set_slip_match` accept a pairing whose amounts differ **when the owner names both rows** — the transfer and its fee — and never a tolerance.
- Consequence: nothing to do now. Recorded so the next session does not re-open a question that has been measured, and so the residual is not mistaken for an unknown.
- Evidence: counting queries against `private-ledger-live` on 2026-08-07 under an explicit read grant; no value, date, counterparty or reference was printed, retained or committed. The ledger was not written to. **The residual left open here was closed the same day — see D-072.**

## D-072 — The fee residual is closed, and the slip layout already said so

- Date: 2026-08-07
- Status: Accepted. Closes the one thing D-071 left open, which was the last open question about the matching rule.
- **The owner's answer: a transaction that carries a fee never produces a slip.** Fees on this ledger belong to transactions that are statement-only — so the case D-071 worried about, a slip whose printed amount is transfer-plus-fee meeting a statement row carrying the transfer alone, needs a slip that does not exist.
- **And the slip layout independently says the same thing, which is the stronger evidence.** `docs/SLIP_CONTRACT.md`, measured over the 23 real samples, records `Amount` (`จำนวนเงิน` / `จำนวน:`) and `Fee` (`ค่าธรรมเนียม`) as **two separately labelled fields** on both the Krungthai and KBANK layouts. So a slip's amount field is the transfer alone whatever the fee is — the owner's earlier recollection that a fee would be "already included in the amount" was the pessimistic reading, and the layout contradicts it. Both paths reach the same conclusion, and this one is measured rather than recalled.
- Consequence: **amount equality to the minor unit is now unconditionally correct for this owner's data**, not merely correct-for-now with a residual attached. Nothing to loosen, and the D-071 sketch for loosening the manual path is a design for a case that does not arise here — keep it as a record of what *would* be safe, not as pending work.
- Consequence, and it was already built: `lib/slip-ocr.ts` finds the amount by its own label and **refuses rather than falling back to the nearest number**, precisely because the fee is money on a nearby line (D-064's successor work, red-proved by a test that fails if the fallback is added). That refusal was written before this question was asked, and it is what keeps a future OCR pre-fill from reading a fee as an amount.
- Evidence: `docs/SLIP_CONTRACT.md` § per-bank label inventory; `lib/slip-ocr.ts` and its fee test; the D-071 counts. No code changed.

## D-073 — The live ledger reaches migration 012, so the owner's override is finally about real records

- Date: 2026-08-09
- Status: Accepted and applied, on the owner's explicit authorization given at the time and after a **fresh backup he took himself**. Same procedure as D-065, deliberately: read the migration, pre-check the constraints it rewrites, prove the backup current from the database, apply, verify every figure against the pre-migration reading.
- **The backup was taken rather than relied on, and that was the owner's call.** D-065 relied on the standing file with evidence; this time he asked for a new one first. The evidence said it was unnecessary — sequence 19, last exported 19, newest `backup_records` row at 19, last audit event dated 2026-07-29, four readings agreeing the ledger had not moved — and taking one anyway cost a few minutes and produced a **second complete file plus a re-proven password**, which is the assurance a currency check cannot give. **The password was typed by the owner and never seen, requested or held by an agent**, which is why the export cannot be delegated.
- **The file was verified without being opened.** Backup files are never inspected. What was checked instead: `backup_records` 5 → 6 with the newest row at sequence 19, `last_exported_sequence` catching up to 19, every table count unchanged by the export, and the reported row total of **4,451** matching the sum of the twelve tables live actually held. That arithmetic is what proves the file complete — no table silently missing — from outside it.
- **Pre-flight on what 012 rewrites.** Live held `restore_runs` at schema version 2 only and 11 distinct `restore_chunks` kinds; both are subsets of the widened CHECKs, so neither rewrite could fail on existing rows. The migration is purely additive otherwise — two tables, an index, a trigger, RLS, grants, one function, and three functions replaced — in one `begin`/`commit`.
- Result, verified against the live database afterwards: **12 migrations recorded**; both decision tables present and **empty**, with RLS forced on each and `authenticated` holding **no insert, update or delete**; `set_slip_match` present; the double-claim index present; `export_backup_snapshot` now declaring **v4** while `restore_backup` accepts **2, 3 and 4**, so the v3 file taken minutes earlier and the older v2 one are both still restorable; and **1,465 rows, 15 batches, 6 accounts, 1,465 components, 18 audit events, 1 category, 15 artifacts and 1,465 batch rows — every figure identical to the pre-migration reading**, with the mutation sequence still 19. **The fresh backup therefore survived the migration current**, exactly as the 2026-07-29 file survived 011.
- **One operational step that is not in the migration**: PostgREST caches the schema, so the live project's `rest` container was sent a reload and restarted. Without it the API layer cannot see the new tables and `GET /api/v1/slips` fails — which the ledger view reports as "Captured slips could not be loaded". Confirmed by the owner loading the ledger against live afterwards: 1,465 rows and **no slips error**, which is the read path D-068 added working against real records.
- **The hazard this creates, and it is the point of the entry.** An app pointed at `.env.local` now reaches real decision tables. Storing an override there — pressing `Not this slip` or `This is it` — is a **real mutation of real financial records**: audited, append-only in its history, impossible to delete, and it **bumps the mutation sequence, making the standing backup stale**. Never a standing permission; ask every time, exactly as for a slip capture or an import. It also means the owner's first real use of this feature needs slips captured into live first, which is its own separate ask.
- Evidence: `node scripts/live-ledger.mjs up` output (`applied 202608050012…`, `Migrations: 12 committed, 1 applied now.`, `Owner already bound.`); the verification queries above, run directly against `private-ledger-live`; `PLAN.md` § Live ledger schema.

## D-074 — A backup's own description must come from the backup, not from the newest constant

- Date: 2026-08-09
- Status: Accepted and fixed. Found by taking the D-073 backup and reading what the screen said about it.
- **The defect.** The recovery bench announced `N rows across M tables`, where `N` was summed from the snapshot the server had just returned and `M` was `BACKUP_TABLE_KINDS.length` — this repo's **newest** table list, which went to 14 when migration 012 was written. Live was still on 011, so the export it produced was a **v3 snapshot of twelve tables**, and the screen said fourteen. Half the sentence was evidence and half was a constant, and nobody could tell which by looking.
- **Nothing was wrong with the file, and that is the uncomfortable part.** `backupSnapshotSchema` is a discriminated union on the declared version, so the v3 payload was validated *as v3* and written correctly; the row total of 4,451 matched the twelve tables exactly. The only broken thing was the sentence — on the one screen whose entire job is to tell the owner what they now hold, before they close the tab and rely on it.
- Decision: **`describeBackupSnapshot` derives both figures from the snapshot and names the schema version.** A person checking a backup needs to know which version they hold, because v2, v3 and v4 are all still restorable here and all still in circulation.
- **The general rule, which is why this is a decision and not a bug fix:** a status line that mixes measured values with module constants reads as one claim and is two. Where a sentence describes an artifact, every figure in it must come from that artifact. The same shape has bitten this repo before from the other direction — a source-grep test that kept passing after the thing it named became false (`GOTCHAS.md`).
- Evidence: `lib/backup-contract.ts` (`describeBackupSnapshot`), `app/recovery-bench.tsx`, `tests/backup.test.ts` — the new test asserts a v3 snapshot is described as twelve tables at version 3 **and explicitly not as the newest list's length**, which is the assertion that fails against the old code.

## D-078 — The owner's real backup is proven to restore, and a v3 file is proven to land in a v4 ledger

- Date: 2026-08-09
- Status: Accepted, rehearsed, torn down. The first time a **real** backup file of this ledger has been restored anywhere.
- **What was actually unknown before this.** The export → encrypt → decrypt → stage → chunk → commit chain is exercised on every gate run by `tests/recovery-portability.test.ts`, so the *mechanism* was never in doubt. What no test could speak to is whether **this file, with this password**, comes back — a property of one artifact, not of the code. A backup that has never been restored is a hope with a filename.
- **The result.** The 2026-08-09 export — 4,451 rows across 12 tables at schema v3 — decrypted and restored into `private-ledger-recovery`, a project that had been discarded and recreated so it had never held this ledger and is bound to a **different owner**. Verified independently against the destination database afterwards: **6 accounts, 1,465 transactions, 1,465 components, 15 batches, 1,465 batch rows, 15 artifacts, 1 category, 18 audit events** — every figure equal to the live ledger's. **Zero rows** still owned by the live owner id, and **zero** occurrences of it inside `overlay_revisions.snapshot`, which is the jsonb hiding place D-044 exists for.
- **A path nothing had tested: v3 into v4.** The file predates migration 012 and the destination carries it, so this exercised a **cross-version restore on real data** — the plan built per the file's own version (12 chunks), the RPC accepting an older version into a newer schema, and the re-export coming back as v4 with the two new tables empty. `SPEC.md` gate 6 has claimed since D-056 that a version the owner still holds a backup in stays restorable; until now that claim rested on same-version rehearsals and pgTAP. It is now demonstrated end to end with the owner's own file.
- **Staleness, made concrete rather than warned about.** The restored copy holds **0 slips and 0 match decisions**, because the file was exported minutes before the first real slip was captured (sequence 19; the capture took it to 20). So the rehearsal shows exactly what D-076 argues in the abstract: the statement layer came back whole, and the one thing the owner had *typed* was not in the file at all.
- **How the password was handled, and why the script existed at all.** The owner offered it; it was refused, as it always is. Instead a throwaway harness under the gitignored `.runtime/diag/` read it **from a prompt with the echo suppressed** — never an argument, never an environment variable, never a message — decrypted in memory, and never wrote the decrypted ledger to disk. It imported the app's own `decryptBackup`, `backupSnapshotSchema` and `buildRestorePlan` rather than reimplementing them, because a rehearsal of a *copy* of the restore path proves nothing about the real one. It printed counts only. Both the harness and the restored copy are deleted; the destination project was discarded with `down`, which drops its volume.
- **Two traps met while building it**, both already recorded and both worth the confirmation. The harness was handed over as a bare `node` command and hit **system Node 20**, which cannot read TypeScript — the exact trap `GOTCHAS.md` opens with, invisible to whoever has the project runtime on PATH. And a second run failed at `insufficient_aal`: the first run left two verified TOTP factors, so a fresh password session starts at aal1 and Supabase refuses to enrol a new factor without aal2, while the old secrets are gone — the leftover-factor lock-out, met rather than read about. The harness cleared the destination's factors before signing in.
- Consequence: `SPEC.md` gate 6 is discharged **with a real artifact** rather than a synthetic one. The next thing that would strengthen recovery is unchanged and unaffected: **nothing hosted has been rehearsed**.
- Evidence: the run's own output (counts only), the destination verification queries above, and `scripts/recovery-destination.mjs down` discarding the copy. No file, password or row content was retained.

## D-077 — The three real slip references are gone from the working tree, and so is the real date inside one

- Date: 2026-08-09
- Status: Accepted and done. Closes the standing violation D-060 recorded on 2026-07-31 and left visible on purpose.
- **What was wrong.** Three references in `tests/` were copied from real slips while writing the D-059 date tests: Krungthai's dated 21-character variant, Krungthai's undated 17-character one, and a KBANK one. `docs/FIXTURE_POLICY.md` says fixtures are invented; these were not. D-060 chose to record the breach rather than quietly fix it, on the grounds that a policy is only worth something if a breach of it is legible — and the fix then waited a week, which is the cost of that choice.
- **A fourth value nobody had counted.** The dated Krungthai reference embeds a **real slip's transaction date**, which the test asserted as an expected value. A date is a real value even when its digits are not money, so the replacement invents the date too and the browser spec now **derives** the expected date from the fixture instead of writing it out — a hard-coded date beside an invented reference would have quietly re-introduced the same problem at the next edit.
- **What "same shape" had to mean.** Each replacement preserves the grammar the test exercises, not the digits: the leading letter and eight-digit date of the 21-character variant, the letter-plus-sixteen-hex of the 17-character one, and the digits-then-letters block of the KBANK one. The hex case is the sharpest — from its second character it *is* eight digits, so it reaches the date parser and must be refused on the calendar rather than on the shape, which an invented value has to keep true.
- **What this does not undo.** The originals are in the git history and on GitHub, permanently; nothing in this entry changes that. What changes is that the working tree stops breaching the policy, every future clone and fork carries invented values, and the next person editing these files is not shown a real reference as an example of what a fixture looks like.
- Evidence: `tests/fixtures/synthetic-slip.ts`, `tests/slips.test.ts`, `tests/e2e/owner-session.spec.ts`; a repository-wide search for all three originals now returns nothing outside `.git`.

## D-076 — The statements are reimportable, so a backup protects the typed layer rather than the ledger

- Date: 2026-08-09
- Status: Accepted, the owner's position, recorded because it changes how loudly staleness should be reported and because half of it is not true.
- **The position:** when this goes hosted, every statement gets imported again from the files he holds, so the live ledger is not a unique artifact and a stale backup is not the emergency the warnings imply. That is right about the bulk of it: **1,465 rows across 15 batches are reconstructible**, and `confirm_import` recomputes every fingerprint from the statement itself, so a rebuild is a rebuild rather than a copy.
- **What a reimport does not bring back**, and this is the part worth knowing before relying on it:
  - **Captured slips.** The images survive in `receipts_sample/`, but the amount, date, counterparty and note on each are **typed by hand** — no OCR yet (task 21), and the QR carries none of them except the date on some layouts. Recapturing 23 slips is 23 acts of transcription, each with its own chance of a wrong figure that nothing can catch.
  - **Match decisions.** An override is pure judgement about two records; nothing in a statement implies it.
  - **Categories, descriptions and notes** on overlays — the same, and none of it printed anywhere.
  - **Audit history and the mutation sequence**, which are the ledger's account of itself.
- **The sharp edge: overrides do not survive a reimport, and they fail quietly on purpose.** A reimport mints new `source_transactions` ids, so a stored decision points at a row that no longer exists. D-067 decided that case deliberately — *ignored, not obeyed* — so the slip returns to being its own provisional row rather than pairing with nothing and dropping out of the totals. Visible, not silent, but the decision is gone and nothing announces its loss. **A backup taken before the rebuild is the only way those come back.**
- Consequence: staleness warnings stay, because they are cheap and the mutation sequence is what makes them precise, but their meaning is narrower than "your ledger is at risk" — it is *the part you typed is not covered*. The statement layer looks after itself; the human layer does not.
- Consequence for the hosted move, when it comes: **export before the reimport, not after.** The rebuilt ledger will not accept the old decisions, so if the typed layer is to be carried across, it needs a plan of its own rather than a restore.
- Evidence: `supabase/migrations/202608050012_slip_match_decisions.sql` (the decision tables key on `transaction_id`), `lib/slip-reconcile.ts` (a decision naming a missing row is skipped), D-067, D-055 (a reimport is a fresh confirm, not a restore).

## D-075 — A capture leaves a record, and a pairing can be inspected

- Date: 2026-08-09
- Status: Accepted and built. Found the way the last two gaps of this shape were found: the owner captured his **first real slip into the live ledger** and asked where the confirmation was.
- **Two gaps, one cause.** The capture form clears itself on success, prints one status line, and leaves nothing behind, so the only place a captured slip could be seen was the ledger — a different route. And in the ledger a matched pair collapses onto the statement row, so the slip it was matched *to* was on screen nowhere at all: the row said `Verified by slip` and offered no way to ask *by which one*. Both are the same shape as D-062 and D-063 — a write path whose read path lives somewhere else, or nowhere — and this is the third time that pattern has been caught by the owner using the app rather than by a test.
- Decision: **the slips route lists what it has captured.** Date, bank, reference, counterparty, note, amount, and a mark where the owner has stored a decision about that slip. It deliberately does **not** say whether a slip matched a statement row: that is a fact about two records and belongs where both are visible. This answers only the question the form leaves open — *is it stored, and is it what I typed*.
- Decision: **a verified ledger row can be expanded to show the slip beside it** — reference, bank, printed date and time, **its own amount**, counterparty, note, and whether the pairing is the rule's proposal or a stored decision. Showing the slip's amount next to the row's movement is the point: they are equal to the minor unit or the pairing could not exist, so the claim becomes checkable instead of asserted. For a rule match it also states that no layout prints a slip's reference, so this is a proposal from three facts and nothing is stored until the owner decides something.
- **Where the refresh lives, and why it is not an effect.** A capture must refresh the list, and a capture is an **event**. Reacting to it from a `useEffect` keyed on a counter is a render reacting to a render — React's own lint rule refuses it, and it is harder to follow. So the fetch sits in `app/slips-bench.tsx`, the wrapper the route needed anyway because a server component cannot hand a callback to a client one, and the list is presentational. Nothing loads until asked, as everywhere else that reaches real records; once asked, a capture keeps it current.
- **The same accessible-name trap, twice in one day.** The new `Show slip` control was first labelled `Show the slip matched to…`, which does not begin with the words on the button — so the name is unspeakable to voice control and unfindable by a name-based locator. Caught by a Playwright timeout again, exactly as `GOTCHAS.md` predicts. The entry stays as written; it is evidently not obvious even to someone who wrote it that morning.
- **A testing trap worth recording separately:** `locator.click().catch(() => {})` does **not** skip a control that is absent. It waits the full test timeout, swallows the failure, and lets the *next* action report a closed page — so the spec fails somewhere unrelated to the cause. In `GOTCHAS.md`.
- Evidence: `app/captured-slips.tsx`, `app/slips-bench.tsx`, `app/slip-capture.tsx` (an `onCaptured` callback), `app/slips/page.tsx`, `app/transactions-view.tsx`, `app/globals.css`, `tests/privacy.test.ts` (both new client components enumerated), `tests/e2e/owner-session.spec.ts` (the capture spec now asserts the list, including that a second capture refreshes it unasked; the reconciliation spec opens the pairing and reads the slip's reference and amount out of it). Gate 2026-08-09: ESLint, `tsc --noEmit`, Vitest **336 passed / 7 skipped across 21 files**, pgTAP **154/154**, production build, Playwright owner **20/20** and isolated **18/18**.

## D-079 — The routing table stops naming agents, because only each harness knows which ones exist

- Date: 2026-08-09
- Status: Accepted. Documentation and agent instructions only; no application code changed.
- **What prompted it was a model-behavior audit, and that audit mostly came back clean.** The instruction files were read against the published Opus 5 guidance for prompts written for earlier models — aggressive tool language (`CRITICAL:`, `YOU MUST`), self-verification scaffolding, forced progress narration, and delegate-more encouragement. `CLAUDE.md`, `AGENTS.md` and both `.claude/agents/` definitions contain none of them. The defects worth fixing turned out to be **stale pointers, not model-era tuning** — which is itself the finding, and the reason this entry exists rather than a larger rewrite.
- **The defect: § Routing contradicted the decision that governs it.** D-011 records `AGENTS.md` as "the tool-neutral routing/invariant authority", but the section named the Codex roster (`finance-explorer`, `finance-validator`) and Codex model tiers (Sol/low, Sol/medium). `.claude/agents/` holds only `finance-implementer` and `finance-reviewer`. So a Claude session — which `CLAUDE.md` tells to start at `AGENTS.md` — opened its first document to a routing table where two of the four named agents do not exist for it, selected by tier names that do not apply to it.
- Decision: **§ Routing now routes by role — discovery, implementation, validation, review, with the parent owning decomposition — and names no agents and no tiers.** Each harness already maps roles to concrete agents in its own entry point (`CODEX_AGENT_WORKFLOW.md` for Codex, `CLAUDE.md` § Subagents for Claude). Removing the names from the shared file means the mapping exists once per harness instead of twice, so the two cannot drift apart again. This is D-052's rule applied to routing rather than to `HANDOFF.md`.
- **Three smaller changes rode along.** § Coordination gained an explicit ceiling — delegate only for genuinely independent, sizeable work, prefer one agent to several, never more than three at once unnoted — because Opus 5 reaches for subagents more readily than its predecessor and nothing in the tool-neutral file capped it (the existing "maximum three children" rule was a Codex harness limit, recorded only in `CODEX_AGENT_WORKFLOW.md`). § Continuity documents gained writing rules for entries: one term per concept, active voice with the actor named, one bounded action per numbered step, no nominalizations or stacked-noun compounds. And `finance-reviewer` now states confidence and severity per finding and reports everything inside its subject scope, rather than silently dropping what it judges minor.
- **Why the reviewer change is not a scope change.** Its material-risk scope is deliberate and D-011 explains why, so it stays exactly as it was. What changed is *who filters*: an instruction to avoid speculative findings is followed literally, so the agent was suppressing findings it had already reached, and the parent never learned they existed. Subject-matter filtering stays in the agent; importance filtering moves to the parent, which is the only side that can see the whole change.
- **Deliberately not changed, and recorded so it is not "fixed" later:** `finance-reviewer` § How keeps *"Do not trust a green label or a 'confirmed' comment — re-derive whether the cited test is actually distinguishing."* A model-behavior audit reads that as verification scaffolding of the kind now worth deleting. It is not. The guidance is about a model re-checking **its own** work; this line is about refusing **someone else's** claim, which is the reviewer's entire purpose and the reason the role exists at all (D-011).
- Consequence: no behavior in the application changes. A Claude session no longer starts by reading routing it cannot act on, and neither harness's agent names live in a file that the other harness maintains.
- Evidence: `AGENTS.md` §§ Routing, Coordination, Continuity documents; `.claude/agents/finance-reviewer.md` § How; cross-checked against `.claude/agents/` (two agents present), `CLAUDE.md:9`, `CODEX_AGENT_WORKFLOW.md:7,201`, D-011 and D-052. `/verify` and `/code-review` were confirmed to be built-in Claude Code commands, not user or plugin skills — an earlier reading of this audit wrongly called `CLAUDE.md:9`'s `/verify` reference a dead pointer, and it is correct as written.

## D-080 — The decision log gets an index and an archive, because the cost was in the reads nobody chose

- Date: 2026-08-09
- Status: Accepted and applied. Documentation structure only. No entry was edited and no application code changed.
- **What prompted it.** The owner asked whether the log should become one file per decision, reasoning that consulting one entry should not mean loading seventy-eight others. The premise was right about the waste and wrong about the mechanism: a grep plus a ranged read already fetches a single entry for about 530 tokens, so per-decision files would have saved roughly a fifth of an already cheap path.
- **Where the cost actually was.** The file had reached **244,589 characters, about 61,000 tokens** — because entries grew from one-clause bullets into paragraphs, not because there were many of them. A recent entry costs five times an early one. `/sync-continuity` reads this file unconditionally and in full, after every substantive change, and that read is the one nobody chooses per-entry. It was the whole bill.
- Decision: **split hot from cold and index everything.** `DECISIONS.md` keeps **D-060 onward**; **D-001 … D-059** move unchanged to `docs/decisions/ARCHIVE-D-001-D-059.md`; an index of every entry sits at the top of the maintained file. The maintained file dropped to **91,300 characters, a 63% cut to the default read**, and the index tells a reader what all eighty entries are for about 1,800 tokens — which is the selective-reading property the sharding proposal was really after.
- **Why not one file per decision.** **183 cross-references inside entry bodies cite 52 of the 79 entries**, so this log is a linked graph and its common read follows a chain — which sharding turns from a grep in one buffer into three file opens. It also makes rewriting an entry's history a diff indistinguishable from appending one, which is precisely the invariant this file's header asserts, and turns an append from one edit into three. The duplicate below was minted while the whole sequence sat visible in a single file; scattering it across eighty would not have helped.
- **The duplicate that was found on the way.** Two entries were numbered **D-073** — the live ledger reaching migration 012, and the agent-routing change from `bd4330f`. The routing one is renumbered **D-079**. Nothing referenced it; all five external citations of D-073, in `PLAN.md` and `HANDOFF.md`, mean the migration entry and still resolve.
- **Archiving is not rewriting, and that was checked rather than asserted.** The original body was reconstructed from the two files and diffed against a copy taken beforehand: **79 entries, numbered 1 to 79, no gaps and no duplicates**, with no differing line except the renumbered heading and two trailing blank lines. A superseding decision still goes in this file and references the archived entry where it now lives.
- **The rule going forward.** Archive by range when the maintained file passes roughly 1,200 lines, never per entry, and regenerate the index from the headings rather than maintaining it by hand — an index that drifts from its entries is D-074's defect in another costume.
- Evidence: `DECISIONS.md` § Index; `docs/decisions/ARCHIVE-D-001-D-059.md`; `HANDOFF.md` § read order. Character counts measured from the file before and after.

## D-081 — The traps get sections and an index, and three of them were found to be lying

- Date: 2026-08-09
- Status: Accepted and applied. Documentation only; no application code changed. Extends D-080's reasoning to `GOTCHAS.md`, and deliberately does not apply its remedy.
- **The hot/cold split does not transfer, and that is the finding.** A decision is settled once made, so position in an append-only log predicts whether anyone still needs it. A trap goes stale only when the thing it warns about changes, and **no position in the file predicts that** — an entry written on day one may be more live than one written this week. So there is no prefix that can be safely archived, and `GOTCHAS.md` keeps all 91 entries.
- Decision: **group into eight sections and index every trap.** Environment and shell (12), Docker and the local Supabase projects (8), database and pgTAP (10), backup and restore (8), statement and slip parsing (17), real data and masking (8), tests and the gate (17), app, auth and accessibility (11). The index is worth more here than in the decision log: this file is consulted to answer "am I about to step in one of these", which titles alone answer, so the common read drops from about 24,000 tokens to about 1,600.
- **`Last reviewed` was deliberately left at 2026-07-31.** Moving entries is not reviewing what they claim, and advancing that date on the strength of a reorganisation would be D-074's defect exactly — a line that reads as one claim and is two.
- **Three traps were found stating something false, all in the same family: the entry outlived the code.**
  - *"Only the owner Playwright config pins its Supabase target; the other two inherit `.env.local`"* — false since 2026-07-28, and the entry's own `Avoid:` line already said so, so the heading contradicted its body. Retitled to what is true: two of the three pin **in git**, and the third's pin is uncommitted.
  - *"Playwright reuses a server someone else started"* — the entry blamed `reuseExistingServer: !process.env.CI` in `playwright.config.ts`. All three configs in this working tree read `false`, but only two carry it in git; `playwright.config.ts` is one of the deliberately uncommitted files (D-070), so `git show HEAD:playwright.config.ts` still reads `!process.env.CI`. **The trap is fixed on this machine and live for any fresh clone**, and the entry now says so. A related trap for whoever reads it next: `playwright.isolated.config.ts:5` quotes `!process.env.CI` inside a comment describing the *default* config while its own setting on line 27 is `false` — reading the comment as the code inverts the conclusion.
  - **`app/ledger-app.tsx` had outlived D-061's route split** and was still named in two `Cause:` lines as the file to look in. Both now name `app/import-bench.tsx`, where `confirmSynthetic` actually lives. Its remaining mention is left alone: that entry narrates a past incident, and rewriting history to satisfy a checker is the wrong direction.
- **What this pass did not do.** It was mechanical and targeted — every file path the file names was resolved against the tree, and only the entries that flagged were read and judged. The other traps were not re-verified against current behaviour, and **61 of 91 carry no date on their `Verify:` line**, so their staleness cannot be queried at all. Backfilling those dates is the outstanding work, and D-082 makes it measurable rather than remembered.
- Evidence: `GOTCHAS.md` §§ Index and the eight sections; the three corrected entries; `playwright.config.ts` against `git show HEAD:playwright.config.ts`; `app/import-bench.tsx`.

## D-082 — The continuity documents get a checker, because every way they drifted was mechanical

- Date: 2026-08-09
- Status: Accepted and built. `pnpm check:docs`, wired into `pnpm verify` between `typecheck` and `test`. Tooling and documentation only; no application code changed.
- **Every rule exists because the mistake had already been made by hand.** Decision ids unique and contiguous — two entries were numbered **D-073**. An index matching its entries one for one — the D-080 index line read "the cost **is** in the reads" while its own heading read "**was**", drift introduced within minutes of writing both, which is the entire case against a hand-maintained index. Every `D-0NN` citation resolving — `HANDOFF.md` advertised the log as running to D-069 when it ran to 79. Every backtick-quoted repo path existing — `app/ledger-app.tsx` outlived D-061 in four documents. Plus dead relative links, and a line budget per file, which is what turns archiving into a trigger rather than a chore somebody has to remember.
- **Retired paths are recorded, not scrubbed.** An append-only log and a plan that narrates what happened both must name files that no longer exist; `PLAN.md` says "`app/ledger-app.tsx` is gone, split into…", which is correct prose a checker must not fight. So a removed path is declared once in `RETIRED_PATHS` with its successor, and the check fails only for a path that died with **nobody noticing** — which is the actual defect. A retirement whose file reappears also fails, because a stale exemption is how a check quietly stops checking (`GOTCHAS.md`, the source-grep test that kept passing after the thing it named became false).
- **Trap staleness warns rather than fails, and the asymmetry is deliberate.** The only queryable proxy for a stale trap is the date on its `Verify:` line, and 61 of 91 have none (D-081). Failing on that today would plant a red check in the gate that nobody can clear, which trains people to ignore it. So it warns, and `--strict` makes warnings fatal once the backfill lands.
- **It was proved to fail before it was trusted.** Five defect classes — duplicate id, index drift, dangling citation, vanished source path, sequence gap — were each introduced into throwaway fixtures under `.runtime`-style scratch, and each produced the specific message naming the specific defect, against a clean baseline that passed. Exit codes alone would have shown only that something failed, not that it failed for the right reason.
- Consequence: `pnpm verify` now fails when a continuity document contradicts the tree or itself. **This is not a substitute for `/sync-continuity`**, which reconciles meaning against evidence; the checker covers only the mechanical half — which is the half that was going uncaught.
- Evidence: `scripts/check-docs.mjs`; `package.json` (`check:docs`, and `verify` now running it); `docs/LOCAL_DEV.md` § Validation order. Current run: 80 decisions, 91 traps, indexes matching, citations and paths resolving, one warning.

## D-083 — Hosting migrates by restoring the backup, not by reimporting the statements

- Date: 2026-08-09
- Status: Accepted, the owner's decision, taken when the sequencing of tasks 19, 21 and 22 turned out to rest on it. Supersedes the **premise** of D-076; that entry stays exactly as written, and what changes is which of the two futures it described is now the plan. No code changed.
- Decision: **when the ledger goes hosted (task 19), the hosted project is a restore destination for the owner's encrypted backup.** The fifteen statements are not reimported.
- **Why this was available to choose, and it is already proved rather than proposed.** D-078 rehearsed precisely this move on 2026-08-09: the owner's real backup was decrypted and restored **whole** into a freshly recreated project **bound to a different owner**, every table count equal to live's, with no trace of the live owner id anywhere including inside jsonb, and then discarded. A hosted project is another destination for that same path. The migration is not new work — it is the rehearsal aimed somewhere else.
- **What it changes, and this is the point of the entry.** D-076 reasoned that the 1,465 statement rows are reconstructible and so are not what a backup protects, and that the **typed layer** — slip amounts, counterparties, notes, categories, match decisions — is what a reimport silently drops, because a rebuild mints new transaction ids that stored decisions no longer reference. Under a restore none of that holds: **transaction ids survive**, so match decisions keep pointing at the rows they were made about and the typed layer travels intact. **Backup staleness goes back to being read broadly** — the file is again the thing protecting the whole ledger, not merely the half that could not be rebuilt.
- **Consequence for sequencing, which is why the question was asked.** Anything typed into the ledger before hosting is durable, so tasks 21 and 22 no longer risk producing throwaway data by being built first. That risk was the only argument for taking task 19 ahead of them, and it is gone. Recommended order is now task 22's remainder (manual cash entry and the slip correction path, where the migrations are), then task 21, then task 19 — schema settled before the database is relocated, and one security review rather than two.
- **What it does not change, recorded so it is not read as a relaxation.** Capturing a slip into live and storing a match decision there remain real mutations of real records, still bump the mutation sequence and still make the standing backup stale — under a restore plan **more sharply**, because the backup is now the entire migration rather than half of it. A hosted restore still needs its own authorization, its own fresh backup taken first, and the OAuth, identity and security-review work task 19 already carries; none of that is discharged here. The current file is also still a dummy-password export, stale by one capture.
- Evidence: D-078 (the rehearsed restore into a differently bound project), D-076 (the premise superseded), `docs/RECOVERY.md`, `PLAN.md` task 19.