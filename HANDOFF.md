# Private Ledger continuity handoff

Last updated: 2026-08-12 — synced after **the app went live and `PLAN.md` task 19 closed** (D-096). A Vercel project serves the ledger from Singapore on Node 24, the owner signed in with Google and his TOTP factor for the first time, all 1,465 rows loaded, and he drove a share-to-app slip capture from his own phone — which is what task 20 had been waiting on since 2026-07-30. The hosted surface was reviewed from the public internet and nothing was found above the bar; the check that mattered ignored the app entirely and confirmed the hosted database refuses an anonymous caller at the grant layer. One thing had to be fixed first: `pnpm-workspace.yaml` was local-only, so a clone of `HEAD` could not install and a hosted build could not have started (D-095, committed). **Still open and deliberately named**: the hosted project's backup is owner-reported and never verified from the database, and `.next` here is still pinned to the hosted project. A new task came out of the first phone use — **task 28**, mobile and desktop layout comfort, unscoped. Before that, 2026-08-10 — synced after **the real sign-in surface was built** (D-091), which is task 19's first two steps and authorises nothing: Google sign-in, a callback that exchanges the code, and the **TOTP enrolment and challenge screens no task had listed** — without which a hosted deployment would sign the owner in and then refuse every page, since Google returns `aal1` and every owner-bound route wants `aal2` with two factors. Full gate green: Vitest 417/7 across 24 files, owner browser suite 25/25, isolated 18/18, and `.next` back to its unpinned resting state. Google itself is untested and untestable here. **What remains in task 19 is entirely owner-gated**: the Google Cloud credentials, the hosted Supabase project, the identity restore and the security review. Before that, a documentation-only pass that wrote **no code**: two new tasks are recorded in `PLAN.md` (**25**, automatic categorisation by local LLM, and **26**, item-level records, deferred by the owner until the app is usable), the choice behind 25 is **D-090**, two `playwright.config.ts` traps that had only ever lived in a session handoff are now in `GOTCHAS.md`, and two stale `PLAN.md` claims were corrected — a "still unbuilt" line D-084 had superseded, and a `check:docs` row whose counts went stale inside the same day. **Task 19 (hosting) is still next and still unauthorized.** Before that, synced after **task 21 was finished** (D-088): the OCR amount finder reached the app, so `PLAN.md` tasks 20, 21 and 22 are all done and **task 19 (hosting) is next and unauthorized**. It ships as a *finder* rather than the pre-fill the task was named for — the engine locates the amount and the owner reads it off a cropped enlargement, so no machine-read digit can reach a stored value. Full gate green against the synthetic project, portable recovery included this time, with a new browser spec that is the only check of the CSP against the engine. Before that, after **task 22 was finished** (D-084): cash entry and the slip and cash correction paths reached the app, so migration 013's three RPCs have callers and migration 014's corrected-amount guard is what the ledger view reads. Full gate green against the synthetic project, with one honest gap — portable recovery **skipped**, destination down. Before that, after **the live ledger reached migration 012** (D-073), which is what puts the owner's override in front of real records, taken after a fresh schema-v3 backup and verified unchanged on every count; and before that after **the match decision became reachable from the app** (D-068: `PUT /api/v1/slips/[id]/match`, the decisions returned beside the slips, and the control on the ledger row), which closes the gap D-067 named and needed no migration, **and after the owner drove it and replaced its chooser** (D-069: the table filters to the slip and its candidate rows instead of listing them in a dropdown, because on a real statement those rows differ only by time and balance — neither of which an option label carried); and before that after **the live ledger moved to migration 011** (D-065), which is the first change to real financial records since the imports and is what finally puts slip capture and reconciliation in front of the real 1,465 rows; and after the match window narrowed to one day and the `Statement only` chip came off the row (D-064), which answered both questions slip reconciliation left open (task 22 half done, D-063) and the slips-in-the-ledger change it superseded (D-062), following the routing split (task 19, D-061), slip capture (task 20), the decoder fallback (D-057), the CSP change (D-058) and the QR date pre-fill (D-059). No commit sha here on purpose (a2efdc7's whole point): read `git status -sb`.

Thin entry point. It carries only what is **mutable and current**: live authorizations, the destructive-operation state of this machine, and where to start reading. Project state lives in `PLAN.md` — if you are about to add a status paragraph here, add it there instead. Why the rule is ownership rather than length, and how this file drifted to 102 lines despite it: D-052.

**Headline.** **Cloud Vision reads a card at 96% where the local engine reads 70%, measured 2026-08-16 and NOT adopted** (D-118, `PLAN.md` task 35, **nothing built**). Through the same grammar and the same 12 screenshots: **88 fields filled against 70**, with amount, balance and printed account digits at **100%** — and it reads KBank Live's balance and account digits, which are 0 of 5 locally under every mode, variant and scale tried. So the KBank rows were never a grammar or contrast problem; they were beyond the local engine. **Every remaining failure is in this repository**: `findCards` requires a line to *start* with the direction word and Vision breaks Thai runs differently, so three images yielded no card — a fragility latent on the local path too — and KBank's timestamp is refused by the grammar rather than unread, which is the month-name form built and removed the same day. **The boundary this crossed is recorded rather than assumed**: twelve real screenshots went to Google under an explicit authorization, the first time financial data has left the device, and it does not generalise to statements. **A Google Cloud project and a restricted Vision key now exist and are staying**; the key is in the owner's Windows user environment and in no file, and no agent has seen it. Before that: **A card is now read enlarged, and that is the only lever that moved** (D-117). Reading a card screenshot at a capped 2× fills **70 of 100** digit-bearing fields against 62 at native size, over 12 real screenshots and 25 cards, with no card lost. Page-segmentation mode and every contrast variant were measured first and changed nothing. **This is D-087's ladder bounded rather than reversed** — the scale is applied by the card form at its own call site, `lib/slip-ocr-engine.ts` is untouched, and slip capture still reads at native size, which is what D-087 measured. **That is the fourth inherited slip measurement not to govern cards** and it is now a trap rather than a surprise. **The open problem is KBank Live**: 0 of 5 on balance, timestamp and account digits under every mode and variant tried, amount 4 of 5, unexplained. `jimp` was added dev-only to measure and removed after; `package.json` and the lockfile are untouched. Before that: **`PLAN.md` task 34 is COMPLETE and the pre-fill trial is live** (D-114, D-115, D-116). All four parts shipped: the strict module, the form that fills a card's four digit-bearing boxes and remembers what it offered, migration 019, and the route that carries the two lists of **field names** into the audit row. **Every project including the hosted one is on 019** — pushed and verified against the remote, with the owner's backup verified from the database first (sequence 27, last-exported 27, equal, and equal again afterwards because DDL mutates no owner data). **The ordering rule was honoured**: 019 reached the hosted project before part 4 reached `main`. **What is now the owner's, and it is not code**: whether pre-fill stays. Read the **pairing rate of pre-filled cards against typed ones**, never the change rate — a low change rate is consistent with the engine being right *and* with him having stopped looking, and only the statement can tell those apart (D-114, D-102). Gate: Vitest **567 / 7 skipped across 29**, pgTAP **263 across 8**, Playwright owner **29/29** and isolated **18/18**, build clean at seventeen `/api/v1/` routes. Before that: **Migration 019 recorded the pre-fill as field names** (D-116, part 3). Two arrays of field names join the audit detail the capture RPC already writes — offered, and changed before submitting. **No table, no column, so the backup stays at v7**, verified by portable recovery passing on all four named lines against a 019 ledger. **It adds two optional payload keys rather than a parameter**, which is the decision that matters operationally: the signature was already a single `jsonb`, absent means an empty list, and the deployed app keeps working unchanged — so 019 can reach the hosted project **before** part 4 ships, which is the order D-109 requires. The closed set is enforced in the database as well as at the route, `changed` must be a subset of `offered`, and a pgTAP test asserts neither stored list carries a digit. pgTAP **263 across 8** (up from 252). **Local projects are on 019, the hosted one on 018, and that push is not authorized.** Before that: **The pre-fill trial was half built** (D-115, task 34 parts 1 and 2). `lib/notification-card-prefill.ts` offers a card's four digit-bearing figures and the form fills them, names on screen which boxes the card filled, and holds two lists of **field names** — offered, and changed — which currently go nowhere, because that is migration 019 and it is gated. **The module is a port and proves it**: it reproduces D-113's four rates exactly. **Three things came out of building it that the plan did not predict.** The harness it was ported from preferred a token fragment over the rejoined figure and would have offered a **tenth of a printed balance** — a wrong-but-plausible value, the one thing D-112 measured as absent, and the harness could not have caught it because it never graded its own accepted values. A **month-name date reader for KBank Live** looked like the obvious way to lift the date rate, was built, filled **0 of 2** real cards because the month token does not survive recognition at all, and was removed with a test holding the refusal in place. And the change that actually moved the number was **in the reader, not the pre-fill**: SCB's timestamp label `วันที่/เวลา` carries a slash, OCR reads it as `|` or `!`, so the *label* was unfindable and the field refused as `LABEL_NOT_FOUND` — repairing the separator before matching took date and time **10/19 → 12/19 (53% → 63%)**, everything else unchanged. **7 of the 19 date refusals are the reader declining to locate the field against 2 that reach the pre-fill**, so this field's remaining constraint is upstream. `SPEC.md`'s "all four are typed" invariant was stale the moment part 2 landed and is corrected; `tests/privacy.test.ts` was **reversed** — it used to assert no card figure could be pre-filled and now asserts a figure may reach a box only through the strict module. Full gate green: Vitest **561 / 7 skipped across 29**, Playwright owner **29/29** and isolated **18/18**, production build clean at seventeen `/api/v1/` routes, `.next` returned to its unpinned resting state. **Nothing is committed.** Before that: **Pre-fill was on trial rather than decided** (D-114, task 34, nothing built). Asked to keep or reverse D-087, the owner chose neither: build OCR pre-fill, collect statistics on how it is actually used, decide later. **The trap is written down before the data exists**: a low edit rate is consistent with the engine being right *and* with the owner having stopped looking, so the independent check is the **statement** — a wrongly pre-filled figure fails to pair and surfaces as `AWAITING STATEMENT` without anyone auditing anything. It needs **migration 019** (a parameter on `capture_notification_card` carrying field names, no table, backup stays v7). What made a trial reasonable: **D-112 and D-113 measured the engine on 19 real cards** and found it fails *visibly* rather than plausibly — with value-free repair (`|`→`/`, `=`→`-`, digit sequence compared before and after and the repair discarded if it changed) a strict pre-fill would fill the amount **17/19**, balance **15/19**, date **10/19**, account **11/19**, and never offer a figure whose digits the engine did not read. **The crop now sits inside its own field** (`dd6f7e9`), which is what D-087 described and the form had never done. Before that: **The owner captured his first real notification card in production, 2026-08-16, and it worked** — the card is in the ledger `AWAITING STATEMENT` with `Choose a statement row`, `Not a payment` and `Correct what you typed` all live, which is task 29 running against migration 018. **The first hour of real use found a bug no test had**: both the card and cash forms defaulted their date with `toISOString()`, which is UTC, so they offered **yesterday** between midnight and 07:00 Bangkok — and a card matches on a one-day window (D-110, fixed and red-proved, Vitest **522 / 7 skipped across 28**). **The owner diagnosed it himself** by asking whether the date auto-filled; it does not, so the default was the only value present. **LINE automation was investigated and is closed** (D-111): a bot cannot read a 1:1 Official Account chat, OA messages have **no Forward**, and the owner is on **iPhone**, which has no notification-reading API — so the screenshot is the only path. LINE's **Capture** is a better input than a hand screenshot and batches several cards, which raises the value of **task 32**, `findCards` finding only the first card on a multi-card image. Before that: **EVERY PUSH TO `main` IS A PRODUCTION DEPLOYMENT, confirmed in the dashboard 2026-08-15** (D-109). The Vercel project builds on every push, so the app has been redeploying itself all along and the redeploy the owner authorised had already happened. **Nothing in this repository shows this** — no `.vercel`, no `vercel.json`, no workflow — so a search here reads as "deployment is manual" and is wrong. Two consequences. **The ordering rule binds the push**, not a later button: migrations reach the hosted database before the commit needing them reaches `main`, and **a documentation-only push still deploys the code beside it**. And **the trap already fired**: the card work went live on 2026-08-14 against a 015 database and stayed there until the migrations were pushed on 2026-08-15, the symptom being card capture failing while the ledger degraded gracefully. **Now consistent** — production serves `943e143` against a project on 018. **Remedy if it slips again is Instant Rollback, not a revert push**, which would be another deployment. **Read the Overview panel's Production Deployment block, never the deployment list**, which shows older builds beside the current one and misled this session once. Before that: **The hosted project took 016, 017 and 018 and every project is now level, 2026-08-15** (D-108), on the owner's explicit authorization, with agent access to the hosted project widened for the checks. The backup he reported was **verified from the database before the push** — sequence 25, last-exported 25, equal — and is **still current after it**, since DDL mutates no owner data. Tables 23 → 28, `restore_runs` accepts v7, `anon` holds nothing anywhere and `authenticated` holds `select` and nothing else, all read back rather than assumed. **The app was not redeployed**; that is the next decision and it is the owner's. Before that: **Migration 018 closes the inherited-privilege gap, 2026-08-15** (D-107, `PLAN.md` task 31). `anon` now holds nothing anywhere in `public` and `authenticated` holds `select` and nothing else, on future tables as well as current ones — the `alter default privileges` half is what makes that last clause true, and is what migration 002 lacked. Verified by re-running the attack rather than re-reading the catalog: `truncate` as `anon` succeeded before and is refused after, both in rolled-back transactions. Gate after a full reset: pgTAP **252 across 8 files**, Vitest **521 / 7 skipped across 28**, portability passing on all four named lines. **The local projects are on 018 and the hosted one is on 015 — three apart now, not two**, and 018 travels with the pending push on the owner's instruction. **Not verified on the hosted project**, where the read grant covers counts and migration versions only; 018 closes the gap there unconditionally when pushed. Before that: **`anon` held `truncate` on thirteen tables, found 2026-08-15 while red-proving a test** (D-106). Every `slips`, `cash_entry` and `notification_card` table — everything added after migration 002, whose `revoke all ... from anon, authenticated` only reached the tables existing when it ran, while Supabase's own bootstrap keeps granting the set to each new one. **`truncate` bypasses RLS.** It is **not reachable from the internet** — PostgREST maps no HTTP verb to it, nothing here runs caller-influenced SQL, and the publishable key is a PostgREST token rather than a database credential — so it is latent rather than live. Proven by running it as `anon` in a rolled-back transaction on the synthetic project; **not checked on the hosted project**, where the standing read grant covers counts and migration versions only. Closing it needs its own migration, and whether that travels with the pending hosted push is the owner's call. **This corrects the review below**, which had reasoned from a grep of the migrations to a claim about the database. Before that: **Migration 017 and its routes passed a security review with no findings, 2026-08-15** (D-105) — the step `PLAN.md` and the previous handoff both named as the next action, now spent. Commits `911ef3d` and `b2e9585`, read by a fresh session rather than by the one that wrote them. **Code review only: nothing was run against a database**, so the row-level-security and grant conclusions come from the migration text, not from a live instance. It found no defect in the code and one stale claim in a document — `SPEC.md` gate 6 still declared the backup at v6 where 017 made it v7 — which is now fixed and carried as a `GOTCHAS.md` trap, since it is the second `SPEC.md` invariant to contradict the database with every gate green. Two observations are recorded rather than fixed: **`PLAN.md` task 30**, widening the pgTAP privilege assertions to all four new tables (a proof gap, not a protection gap), and a pre-existing property of `restore_backup` that predates 017 and crosses no privilege boundary. **What this does and does not unblock**: the review no longer stands in front of the hosted push, and the push itself still needs its own authorization and a fresh backup taken first. Before that: **Task 29 is complete: a card can be corrected, decided and retired, 2026-08-14** (D-104, migration 017). Four tables and the backup at **v7**, with v2 … v6 still restorable. A card's typed figures are correctable including the balance; the binding is not, and the overlay has no column for it. A balance disagreement is refused by default and storable only with an explicit acknowledgement, which is what gets stored rather than a comparison that would go stale. A wrong card is retired out of the ledger and the totals, reversibly, and the ledger lists retired cards so that reversal is real on screen. Full gate green: Vitest 521/7 across 28 files, pgTAP 247 across 8, Playwright owner 29/29 and isolated 18/18, seventeen `/api/v1/` routes, and the v6-into-v7 restore pair proven before anything depends on it. **Two consequences to hold**: the local projects are on **017** while the hosted one is on **015**, so they are now two migrations apart; and pushing 017 there needs its own fresh authorization and a fresh backup taken first, exactly as 011 and 012 each did on live. Before that: **Task 27 is complete: a captured card meets its statement row, 2026-08-13** (D-102). The ledger view and the reconciliation were its last two parts. A card matches on the account it was bound to, the exact amount, a one-day window and **the balance it printed** — which breaks the tie between two same-amount rows that the slip rule refuses outright, and refuses to pair when it disagrees with every row that otherwise fits. It is a tie-breaker and a fail-closed cross-check, not identity. Full gate green: Vitest 502/7 across 28 files, Playwright owner 27/27 and isolated 18/18, the production build emitting the same fifteen `/api/v1/` routes because this adds none. **Those two gaps are now designed rather than open**: `PLAN.md` task 29 and D-103 carry a card's correction overlay, its stored match decision and a way to retire a wrong one, as one migration moving the backup v6 → v7. Nothing of it is built, and it needs its own authorization when it is. Before that: **A notification card can now be captured, 2026-08-12** (D-101). The form is on the slips page and `POST /api/v1/notification-cards` is behind it, closing the fourth case of an RPC shipping with no caller. The route carries the one check the database only *stores* — that the digits the card printed resolve to the chosen account, per layout — and because `notification_cards` is append-only it is the last place that check can be made. Full gate green, both browser suites included; the build now emits fifteen `/api/v1/` routes. **What remains in task 27**: the ledger view and the reconciliation that uses the printed balance. Before that: **task 27's reader is built and measured, 2026-08-12** (D-100): it splits a screenshot into its cards and returns a **box per field**, and it reads no digit — the owner types the amount, the balance, the timestamp and the account digits. The read grant over `receipts_sample/line/` was **granted again and exercised a third time** that day, and the measurement earned it: **a screenshot carries two cards**, so an unsplit read refuses nearly every field, and a Krungthai `ประเภท` row carries a free-text phrase **containing the direction word**, which invented a card and aimed a crop at the wrong row until every anchor was pinned to the start of its row. **Still nothing can capture a card** — the form, its route, the ledger view and the reconciliation are all ahead. Before that: **task 27 has its printed grammar, 2026-08-12** (D-099): where every field sits on a card, looked up per layout *and per direction*, with direction read from two signals that must agree. The read grant over `receipts_sample/line/` was given and exercised **twice** that day and is **not inherited by a new session** — ask again. The second pass matters: three screenshots asked for by name arrived and **overturned two conclusions the first pass had recorded as settled**, so the folder now holds eight screenshots and twelve cards, and every layout is measured in both directions. The one screenshot still worth having is a card captured while a hold was outstanding; the owner is unsure one exists for these accounts. Before that: **task 27's migration landed and the backup writes v6, 2026-08-12** (D-098, migration 016). `public.notification_cards` exists, its identity is a fingerprint the database computes and the printed balance makes unique, and `export_backup_snapshot` / `restore_backup` moved v5 → v6 in the same migration because a table the export does not carry breaks `SPEC.md` gate 6 immediately. v2 through v5 all stay restorable, and the v5-into-v6 pair — the one the owner's newest file will use — is proven end to end. **Nothing can capture a card yet**: reading a screenshot, the form and the ledger view are all still ahead. **Two consequences to hold.** The hosted project is on **015** and this migration has not been pushed there, so the local projects and the hosted one are no longer on the same migration; and pushing it needs its own fresh authorization and a fresh backup first, exactly as 011 and 012 each did on live. Before that: **the ledger is hosted and the owner uses it from his phone, 2026-08-12** (D-096) — `PLAN.md` task 19 is complete, which was the last thing standing between this project and ordinary use. The real records live in the hosted Supabase project (D-094); `private-ledger-live` is historical. The full gate is green against the synthetic project, both browser suites included. **Statement work is finished: all fifteen statements are imported and the ledger holds 1,465 rows across 15 batches** (D-054, D-055). **Slip capture is built (task 20, D-056)**: the QR grammar was verified against all 23 real samples, `public.slips` stores a slip as a provisional entry, and the backup now carries twelve tables at schema v3 **while still restoring v2** — which is what keeps the owner's existing whole-ledger backup usable. Tasks 17, 18, 20 and 23 are done. **The app is now four routes** — `/ledger`, `/import`, `/slips`, `/recovery`, with `/` redirecting to the ledger (D-061, 2026-07-31). That was the owner-free half of task 19; what remains in it is the OAuth sign-in path (writable, unverifiable without Google credentials) and then hosting, the identity restore and a security review, none of which is authorized. Until that lands slips can be captured on this machine but not shared from a phone.

**Slips are reconciled into the ledger as of 2026-08-01** (D-063, task 22 half done): a slip matched to a statement row collapses onto it, unmatched slips are their own rows, ambiguous ones say so, and the totals count each payment once. The rule itself is still recomputed on every read; what is stored is the owner's disagreement with it — migration 012 as of 2026-08-05 (D-067), reachable from the ledger row as of 2026-08-07 (D-068). Project state for both lives in `PLAN.md` task 22.

**All three local projects are on migration 012 — the live one as of 2026-08-09** (D-073), having taken 011 on 2026-08-05 (D-065). Each step was authorized on its own and each left the ledger unchanged on every count: 1,465 rows, 15 batches, 6 accounts, 18 audit events, mutation sequence still 19. **The next backup taken from live will be a v4 file**, and `restore_backup` there accepts 2, 3 and 4, so every file the owner holds stays restorable. **Capturing a slip into live and storing a match decision there are both real mutations that invalidate the standing backup.**

**Both questions that half left open are answered as of 2026-08-05** (D-064). The window is now **one day, not three** — the owner's banks post same-day, so it is tolerance for his clock and the bank's straddling midnight rather than for a late posting — and the `Statement only` chip is off the row, surviving as the status cell's accessible name and a new Status filter. Still read-time only: no migration moved, so both remain free to change until 012 stores them.

**And the window is now measured, not judged** (D-066, 2026-08-05). D-064 said this had to wait for task 21; it did not, and the correction is worth carrying. The amount is unreadable *in the app* without OCR, but a **local throwaway harness** read it with nothing shipped and no real value printed — only counts and date distances. Of 9 slips with a ledger candidate, **8 posted same-day and 1 sat 11 days out**, judged a coincidental same-amount row rather than a late posting. The finding that matters: **at a wide window that slip collapses onto an unrelated statement row and moves the totals; at one day it correctly stays unmatched.** Limits: n is 9, and the outlier is inferred rather than resolved. The harness is deleted — it reads real slips and can never be committed.

**Two things about slip capture that are not obvious from the code.** The QR decoder is bundled as a fallback because `BarcodeDetector` does not exist on Windows desktop (D-057) — its WebAssembly binary is copied into `public/` by `prebuild`, so **a tree that has not been built cannot decode anything**, silently. **Two real slips have now been through the app's UI** (2026-07-30/31), an SCB bill payment and a Krungthai one, both decoding correctly — which closes the last verification gap in task 20. That produced D-059 (the date is inside the QR for 14 of 23 slips, and pre-fills), a misleading placeholder, and the discovery that **three real slip references are committed as fixtures** (D-060).

Read in order: [SPEC.md](SPEC.md) (scope, invariants, gates) → [PLAN.md](PLAN.md) (checkpoint and next actions) → [DECISIONS.md](DECISIONS.md) (append-only; indexed at the top, carrying D-060…D-084 in full, with D-001…D-059 relocated unchanged to [docs/decisions/ARCHIVE-D-001-D-059.md](docs/decisions/ARCHIVE-D-001-D-059.md)) → [GOTCHAS.md](GOTCHAS.md) (traps worth reading before touching tests or the database).

Claude Code starts at `CLAUDE.md`; Codex at `AGENTS.md`. Product, design, parser, fixture and recovery contracts are in `PRODUCT.md`, `DESIGN.md` and `docs/`, including the three per-bank layout contracts ([Krungthai](docs/KRUNGTHAI_CONTRACT.md), [SCB](docs/SCB_CONTRACT.md), [KBANK](docs/KBANK_CONTRACT.md)). Local setup and the validation order are in `docs/LOCAL_DEV.md`.

## Read this before running anything destructive

**The real ledger lives in `private-ledger-live` (5434x)** as of 2026-07-28 (D-047, D-048). Three local Supabase projects: `private-ledger-local` (5432x, synthetic and disposable, what **every** suite targets), `private-ledger-recovery` (5433x, disposable rehearsal destination), and `private-ledger-live` (5434x, real records, Studio on 54343). See `docs/LOCAL_DEV.md`.

**The trap is `.env.local`, not the test database.** It points at the live project, and `NEXT_PUBLIC_*` are inlined at build time — so a browser config that runs `pnpm build` without pinning its target builds against real financial records. Since 2026-07-30 the **CSP is inlined from the same variable** (D-058), so a mis-pinned build now also serves a `connect-src` naming the wrong project — which is a louder symptom of the same mistake, not a new one. All three Playwright configs now pin, but `playwright.config.ts` is one of the uncommitted files below, so a fresh clone does not. GOTCHAS carries the variable list and the Windows `$env:VAR = ""` deletion trap that makes a pin look broken.

`pnpm supabase:reset` cannot be guarded and destroys whatever project the working directory names — the repo root names the test project, `live/` names the real one. **Back up through the `/recovery` route before anything destructive, and never set `ALLOW_DESTRUCTIVE_TESTS=1` merely to get a green run.**

## Standing authorizations and their conditions

Mutable by nature — granted, spent, re-granted — which is why they live here and not in append-only `DECISIONS.md`. Nothing here is inherited by a new session.

- **Importing a real statement: never standing, ask every time.** Fourteen statements have now been imported — one on 2026-07-28 (D-047) and thirteen more on 2026-07-29, the latter by an agent the owner explicitly approved to do it (D-054). The fifteenth, `KRUNGTHAI-01`, was imported on 2026-07-29 after its own explicit ask and a confirmed backup (D-055). All fifteen are now in; the next import is a new statement and needs a new ask. None of that is a standing permission, and the scale of the last batch makes re-asking more important rather than less. Note what the first one cost in vigilance: it silently turned `pnpm test` into a destructive command, and the fix took a whole second project (D-048). Assume the next import creates a hazard nobody has thought of yet, and go looking for it.
- **Real-PDF smoke tests: approved 2026-07-25, exercised twelve times, conditions unchanged.** The owner enters the document password interactively; no value is logged or retained; nothing derived is committed. Requires the owner present, so it cannot run unattended.
- **Reading `receipts_sample/line/`: granted again 2026-08-12 and exercised a third time that day.** Two exercises, both counts-only: a tesseract sweep over all eight screenshots through a throwaway harness under `.runtime/` since deleted, which printed no recognised text, amount, balance, date, account digit or name; and **two screenshots opened directly** to settle a count the harness disagreed with — the reader reported three cards where the record said two, and looking is what showed the third was invented (D-100). Both readings stayed in codes and counts; nothing became a fixture, a doc line, a quotation or a commit. **The same grant also covers commit and push, given the same day and NOT yet spent** — see that bullet. Not inherited by a new session: ask again.
- **Reading real slips: re-granted 2026-08-10 and exercised twice that day.** First to settle the month vocabulary by eye across four slips (D-086), then for a 23-sample OCR sweep whose only output was counts — and again on the shipped engine version at the end of the day (D-088), which is what confirmed the amount can be *located* on 16 of 23. Each harness lived under `.runtime/`, was deleted with its output, and printed no amount, date, reference or file name; `.runtime/diag` is empty again and no `*.traineddata` was left behind (GOTCHAS). **Nothing observed became a fixture, a doc line or a commit.** Not inherited by a new session — ask again.
- **Reading real slips: granted 2026-08-01, re-granted 2026-08-05 and not exercised.** The 2026-08-05 grant was for a sweep of all 23 samples against the live ledger to settle the match window; it was returned unused once the amount turned out not to be machine-readable without task 21's OCR (D-064), so no slip was opened that day. Nothing here is inherited by a new session — ask again.
- **Reading real slips: the original grant, 2026-08-01, and what it unblocked.** The owner widened the read grant to cover `receipts_sample/` (23 real slips) as well as statements, explicitly and unprompted. It discharged the one step `PLAN.md` task 21 had marked owner-present — the slip layouts are now measured and recorded in `docs/SLIP_CONTRACT.md` — and it let the task 22 match rule be checked against the real ledger before a migration was written for it, which changed the rule (D-063). **Same boundary as every other read grant**: reading a real value never licenses writing one into a fixture, a doc, a commit or a screenshot, and that limit is not about the owner's own privacy preference — this repository pushes to GitHub, where a committed value is permanent (D-060 is that mistake, already made once). Passwords and the citizen ID stay excluded under any grant.
- **Reading real statements: granted 2026-07-28 (D-049), and narrower than it sounds.** `private-statements/` stays closed. What is open is `shared-statements/` — password-free copies the owner made with `scripts/repassword-pdfs.py`. Scope is **statement contents only**: not passwords, keys, `.env*`, backups or the citizen ID. Subagents stay closed; reads route through the parent. He was told again that agent reads are transmitted and retained in a transcript, and confirmed — respect that rather than re-litigating it each session. Masked dumps stay the first resort, and reading a real value never licenses writing one into a fixture, doc, commit or screenshot.
- **Passwords: never, under any authorization.** Not in chat, not in a repo file, not as a CLI argument — the harnesses read stdin only. Statement passwords derive from the owner's date of birth and citizen ID, so they are identity-grade and non-rotatable.
- **Commit and push: granted on 2026-07-30, 2026-07-31, 2026-08-01 and repeatedly through 2026-08-05, and spent every time.** 2026-08-05 was the long one, and its work is listed in `git log` rather than counted here — the match window and status chip (D-064), the live ledger reaching migration 011 (D-065), the OCR policy layer, the measurement that settled the window (D-066), and the owner's say over a match (migration 012, D-067), each on its own ask, with pushes asked for separately. **This paragraph deliberately no longer carries a commit count**: it was rewritten three times in one day purely because the count went stale, which is the same lesson as a2efdc7's missing sha. Every grant that day was commit-only until that last one; push was asked for separately and given once. **2026-08-07: commit and push were both granted and both spent** on that day's work (D-068 … D-071) after a `/code-review` pass and a full gate. **2026-08-10: granted again and spent on the day's earlier work** — the five commits ending `8b3039e`, all pushed, which is why `main` is level with `origin/main`. **A grant covering task 21's work and the v4-into-v5 rehearsal was given later the same day and is spent** — both are committed and pushed. **A further grant covering the sign-in surface was given at the end of the same day and is also spent**, on `81569d9`, which was pushed; the tree is back to the three local-only config files alone. **2026-08-11: granted and spent three times** — twice on the day's earlier work, then again on the `pnpm-workspace.yaml` fix that unblocks a hosted build (D-095), which was committed and pushed. **2026-08-12: granted and spent repeatedly across the day** — the last two were task 27's migration and the v6 backup (D-098), then the printed grammar and its second measurement pass (D-099), each committed and pushed on its own ask with the two local-only config files preserved. **2026-08-12, later: commit and push were granted twice more and both are SPENT** — first on the card reader and its `/code-review` fixes (D-100), then on the capture form, its route and their `/code-review` fixes (D-101), each committed and pushed after a full green gate with the two local-only config files preserved unstaged. **2026-08-13: the owner granted every authorization the session had named at once**, in one sentence covering commit, push, reading `receipts_sample/line/`, and hosted changes. **What was actually used is narrower than what was given**: task 27's ledger view and reconciliation touch no SQL, no hosted resource and no screenshot, so **the read grant and the hosted permissions were not exercised and are returned unspent**, and only commit and push were used. Record the shape of that grant as well as its scope: a blanket "all of the above" is still one session's grant and is **not inherited by a new session** — ask again, and prefer asking for the one thing the work needs over accepting the set. **2026-08-15: commit and push to `main` were granted at the start of the session as standing for that session's work, and spent** on the security review's continuity edits (D-105) — the `SPEC.md` v7 correction, the two new `GOTCHAS.md` traps, `PLAN.md` task 30 and this file. The same grant named **read-only queries against the hosted project, counts and migration versions only**, and **it was returned unspent**: a code review needs no database, and the migration-version reading from 2026-08-14 was still current and about to be superseded by the reading that must follow the fresh backup. That is the third session running in which the read grant was given and not needed — prefer asking for the one thing the work needs over accepting the set. **The next commit and the next push each need a fresh ask.** **Read `git status -sb` and `git log` rather than trusting either sentence, and treat every grant as spent at the start of a new session.** Read `git status -sb` rather than trusting this sentence — it is the line most likely to be stale, which is what a2efdc7 and two rewrites of this paragraph on 2026-08-05 were all about. Treat any earlier grant as spent at the start of a new session. Committed straight to `main`, matching this repo's history; nobody has asked for a branch-and-PR flow, so raise it rather than assume it. Deliberately no commit sha here — one written into this file is stale the moment the file is committed; read `git status -sb` instead. Treat as spent at the start of a new session and ask again.
- **Backups: the newest was taken by the owner on 2026-08-11 at 1,465 rows, it is a schema-v4 file, and it is CURRENT.** Exported through `/recovery` against the live project with the password typed by the owner and never seen by an agent; the app reported **4,453 rows across 14 tables at schema version 4**. Verified afterwards from the database rather than from the file, which is never opened: **`sequence` is 20 and `last_exported_sequence` is 20**, so the ledger has not moved since the export, and `backup_records` holds 8 rows all with custody confirmed, the newest at sequence 20. **The file's completeness was checked by arithmetic, not by opening it**: 6 accounts, 1 category, 15 artifacts, 15 batches, 1,465 transactions, 1,465 components, 1,465 batch rows, 0 overlays, 0 overlay revisions, 19 audit events, 1 slip, 0 match overlays, 0 match revisions and 1 sequence row sum to exactly the 4,453 the app reported, across exactly 14 tables. Nothing was written by the export — every ledger count is unchanged. **This is the v4 half of the v4-into-v5 restore that task 19 moves the ledger by**, and that version pair is already proven (D-089). Two records sit at sequence 20 rather than one, which is a repeated export and is harmless; the same is true of the two at sequence 19, where earlier notes below record only one. **It goes stale again the moment anything is confirmed, a slip is captured or a match decision is stored.** The previous one follows, and is superseded by this paragraph rather than corrected.
- **Superseded — the 2026-08-09 backup, kept for the restore evidence it carries:** taken by the owner on 2026-08-09 at 1,465 rows, a schema-v3 file. Exported through the `/recovery` route against the live project, with the password typed by the owner and never seen by an agent, and **custody recorded** — `backup_records` went 5 → 6 with the newest row at mutation sequence 19. Verified from the database rather than from the file, which is never opened: sequence 19, last exported 19, and every table count unchanged by the export. **It is proven to restore, as of 2026-08-09** (D-078): decrypted with the owner's password typed at a prompt and restored whole into a freshly recreated `private-ledger-recovery` bound to a different owner, every table count equal to live's, no trace of the live owner id anywhere including inside jsonb — then the copy was discarded and the harness deleted. Its 4,451 rows are exactly the twelve tables live held at the time (6 accounts, 1 category, 15 artifacts, 15 batches, 1,465 transactions, 1,465 components, 1,465 batch rows, 18 audit events, 1 sequence row, no overlays or slips), which is how the file's completeness was checked without reading it. **Migration 012 landed immediately afterwards and did not move the sequence, so this backup is still current** — and `restore_backup` on live now accepts 2, 3 and 4, so both it and the older v2 file remain restorable. **The next backup taken from live will be a v4 file.** It goes stale the moment anything is confirmed, a slip is captured **or a match decision is stored**, since all three bump the mutation sequence — **it went stale on 2026-08-09** when the owner captured his first real slip (sequence 20, last exported 19). **Read staleness broadly again** (D-083, which supersedes D-076's premise): hosting will migrate by **restoring this backup into the hosted project**, not by reimporting the statements, so the file protects the whole ledger rather than only the half a rebuild could not reconstruct. Transaction ids survive a restore, so match decisions keep pointing at the rows they were made about and the typed layer — slip amounts, counterparties, notes, categories, decisions — travels intact. D-076's narrow reading followed from the reimport plan, and that is no longer the plan; the restore path itself is already rehearsed end to end (D-078). Check before anything destructive rather than trusting this line. The previous one — 2026-07-29, schema-v2 — was **re-verified against the live database on 2026-08-05, before and after migration 011 landed there**: `mutation_sequences.sequence` is 19, `last_exported_sequence` is 19, the newest `backup_records` row sits at sequence 19, and the last audit event is dated 2026-07-29 — so the ledger has not moved since the export and this backup covers it exactly. Migration 011 does not touch `mutation_sequences`, which is why it stayed current across the migration. It still restores: `restore_backup` on live was checked after the fact and accepts **both** '2' and '3' (D-056), so the v2 file was not stranded. The next backup taken from live will be a **v3** file, because `export_backup_snapshot` there now declares version 3 — also verified on the live database rather than inferred from the migration text. It goes stale the moment anything is confirmed *or a slip is captured*, since capture bumps the mutation sequence too. Check before anything destructive rather than trusting this line.
- **Reading the live ledger: granted 2026-08-07 for one question, and exercised.** The owner asked whether a bank posts a transfer's fee as its own row or inside the transfer, and granted a look at `private-ledger-live` to settle it. Answered by counting queries run inside the database with **only counts emitted** — no amount, date, counterparty or reference crossed into the transcript, and nothing was written (D-071). Not standing: the next such look needs its own ask.
- **The hosted Supabase project now EXISTS, as of 2026-08-11 — the first hosted resource this project has ever had.** Created by the owner: ref `rnjwxpurrvabecbgzsol`, Singapore region, free plan. **THE REAL LEDGER NOW LIVES IN THE HOSTED PROJECT, as of 2026-08-11** (D-094). The owner's fresh v4 backup was restored into it and verified against the destination database: 1,465 transactions, 1,465 components, 1,465 batch rows, 6 accounts, 15 batches, 15 artifacts, 19 audit events, 1 slip — every figure equal to what live held — with **zero** occurrences of live's owner id in any table, searched by casting whole rows to jsonb text rather than by comparing a column. **`private-ledger-live` is historical**: it keeps migration 012 and receives nothing further. **A backup was taken from the hosted project immediately afterwards, on the owner's own report** — it is the first backup of this ledger in its new home, and the older file backs up `private-ledger-live` rather than this project. **VERIFIED FROM THE DATABASE 2026-08-12, and the owner's report was accurate**: `public.mutation_sequences` reads sequence **21** and last-exported **21** — equal, so the ledger has not moved since the export — with exactly **one** `backup_records` row, sitting at sequence 21. It had been owner-reported and agent-unverified for a day, which is why the check was worth making rather than assuming. **The fallback trap was ruled out rather than hoped past**: `supabase db query --linked` silently answers from the *local* database when its SQL spans multiple lines, so the query was passed as a single line with `--linked` before it, and the destination was then proved three independent ways — the Docker daemon was not running at all, so no local project could have answered; `inet_server_addr()` returned a public Singapore address rather than a loopback or Docker one; and the row counts matched the hosted ledger. **This goes stale the moment anything is confirmed, a slip is captured, a cash entry is written or a match decision is stored**, since each bumps the mutation sequence. Re-read it rather than trusting this line. Free-plan projects **pause after 7 days idle**, and this is now the only copy that matters.
- **The owner is BOUND, as of 2026-08-11, and that step is spent and irreversible.** He signed in with Google, enrolled one TOTP factor, and `bind_ledger_owner` was run from the SQL editor against his real identity — `public.ledger_owners` holds one row carrying his Google address, plus the `Uncategorized` category and the mutation-sequence row that RPC always creates. **The table is immutable, so re-binding is impossible; a wrong identity would have meant a fresh project.** The address and owner id are deliberately not repeated here — both are readable from the database, and this file is committed. **Migration 015 is on the hosted project too**, and it is functionally proven there rather than merely applied: the owner is through the gate holding **one** verified factor, which the pre-015 rule would have refused. **The restore was authorized separately and is done** (D-094). **Migrations 001–014 are on it and nothing else is** — pushed on his explicit instruction after he ran `supabase login` and `supabase link` himself, because both need credentials no agent accepts. Verified against the remote afterwards: 14 migrations recorded and matching local on every one, **23 tables all reporting 0 rows**, and `public.ledger_owners` **empty**, so the one-shot owner binding is still unused and still available for his real Google identity. An anonymous request carrying only the publishable key is refused `42501 permission denied` on all seven tables probed — the grant layer, not RLS (D-092). **The repository is UNLINKED again**, so anything further needs a fresh `supabase link` and therefore his database password. **All four of the things this line once listed as unauthorized — the Google provider, the owner binding, the restore and the deployment — were each granted separately and are each spent** (D-092, D-094, D-096). Free-plan projects pause after 7 days idle and must be resumed from the dashboard.
- **Do not run `live/live-seed.sql` against the hosted project.** It binds the synthetic placeholder identity, `public.ledger_owners` is immutable, and the binding is one-shot — doing so would burn it on a fake account and the only remedy is a fresh project and a restore into it.
- **Hosted OAuth / Vercel: authorized, executed and spent, 2026-08-11 and 2026-08-12.** Accepted as direction by D-051, which reversed the 2026-07-25 "not needed" position; each hosted resource was then asked for separately and granted separately — the Google client and the Supabase project (D-092, D-094), then the Vercel project, its deployment and the hosted security review (D-096). **`PLAN.md` task 19 is complete.** None of those grants is standing and none generalises: **creating any further hosted resource — another project, a custom domain, a second region, any new external service — needs explicit approval at the time.**
- **2026-08-16: four grants were given and every one is SPENT.** Recorded together because the day's shape matters as much as its scope. **Commit and push to `main`**, granted at the start as standing for the session's work and spent across eleven commits. **`supabase db push` to the hosted project**, asked for and granted explicitly, spent on 016/017/018 after the backup was verified from the database (D-108). **Full read access to the hosted Supabase project**, widened from the counts-and-migration-versions grant when the owner said so in one sentence — used for the backup check, the migration list, the pre-push privilege audit and the post-push verification, and **used for nothing else**; no financial value was read or written anywhere. **Reading `receipts_sample/line/`**, granted by name when the owner asked for `IMG_7411.png` to be looked at, and exercised by an OCR harness over all ten images (D-112, D-113). **Nothing observed there became a fixture, a doc line, a commit or a quotation** — every figure in the continuity docs is a count or a percentage. **None of these is inherited by a new session; ask again.**
- **`.runtime/` holds a throwaway harness and a file of real values that MUST be deleted.** `card-ocr-accuracy.harness.ts`, `vitest.harness.config.ts` and **`card-ocr-readings.tsv`** — the last carries real amounts, balances, dates and account digits read off the owner's cards. Gitignored, never committed, and to be deleted exactly as D-088's and D-100's harnesses were. **It is the owner's to delete or to mark first**: marking each row y/n against the card is the only way to establish whether an accepted figure was *correct*, which is the one number D-112 and D-113 could not produce. The harness also drops `tha.traineddata` into the repository root each run (GOTCHAS); it was removed on 2026-08-16 and will come back if the harness runs again.
- **A GOOGLE CLOUD PROJECT AND A CLOUD VISION API KEY NOW EXIST, as of 2026-08-16** (D-118) — the second hosted resource this project has ever had, and **nothing in this repository calls it**. Created by the owner: project `ledger-ocr-test`, Vision API enabled, an API key **restricted to the Cloud Vision API**, on a trial account with its own credit and a ฿50 alerts-only budget. **The key is stored in his Windows user environment and is not in this repository, in any file, or in any transcript** — no agent has seen its value; the measurement harness read it from the process environment and reported only its length. **The key is staying** on his instruction. **Twelve real card screenshots were sent to Google** under an explicit authorization that day, which is the first time financial data has left the device; it does not generalise, and a statement is a much larger disclosure than a card. **Whether the app ever calls Vision is undecided** (`PLAN.md` task 35). If the answer is ever no, revoking the key means deleting it in the Console as well as clearing the variable.
- **The Vercel project deploys on every push to `main`, VERIFIED IN THE DASHBOARD 2026-08-15** (D-109), and production serves **`943e143`** against a hosted project on migration **018**. Treat `git push origin main` as a deployment, because it is one — including for a commit that changes only documentation. **This supersedes the claim, carried here for three days and never checked, that production served the 2026-08-12 build.** Read the Overview panel's Production Deployment block rather than the deployment list.
- **The deployment's own configuration is the mutable part worth keeping here.** The Vercel project sets three environment variables plus `ENABLE_EXPERIMENTAL_COREPACK`, and **`SUPABASE_SERVICE_ROLE_KEY` is deliberately not among them** — nothing in this repository reads it and it bypasses RLS, so `.env.example` must never be copied wholesale into a hosted environment. The Supabase redirect allowlist names **exactly one** production callback and no wildcard, so preview deployments cannot complete a sign-in; that is intended, not a defect to fix. The two `NEXT_PUBLIC_*` values are inlined at build time, so changing either needs a rebuild rather than a redeploy (GOTCHAS).

## Before you touch anything

- **EVERY project is on migration 019 as of 2026-08-16, hosted included, VERIFIED AGAINST THE REMOTE** (D-116). `supabase migration list --linked` shows `202608160019` with local and remote matching. `private-ledger-live` stays frozen on 012. The recovery destination needed `node scripts/recovery-destination.mjs down` then `up`, as it does for every migration. **The backup contract stays at v7** — 019 adds no table and no column, checked rather than assumed, since portable recovery passes on all four named lines against a 019 ledger. **The owner's backup was verified from the database before the push and is still current after it**: sequence **27**, last-exported **27**, equal both times, with the newest `backup_records` row at 27 and 4 rows in total. **It goes stale the moment a card, slip, cash entry or decision is written** — and he uses the app daily, so assume it is stale and re-read rather than trusting this line. The hosted ledger holds **1 card** as of that reading. Before that: **the gap was reopened with the local projects on 019 and the hosted one on 018**, for a few hours on the same day. Before that: **EVERY project was on migration 018 as of 2026-08-15, hosted included, VERIFIED AGAINST THE REMOTE** (D-108). `supabase migration list --linked` shows 001 … 018 with matching local and remote on every one. The gap open since 2026-08-12 is closed and **the next migration is the first one that will reopen it.** The owner's backup was verified from the database before the push and **is still current after it** — sequence 25, last-exported 25, equal both times, because DDL mutates no owner data. **It is a v5 file and the hosted ledger now writes v7**; that pair is proven by `restores a v5 file into the current ledger`. **The app was NOT redeployed** and still serves the 2026-08-12 build against this newer schema, which is the safe direction of that mismatch. **The hosted ledger holds 1,552 transactions across 19 batches, 6 accounts, 23 audit events, 1 slip and 0 cards** — read 2026-08-15, and it supersedes the 1,465-across-15 figure the docs carried; the difference is the owner's own imports. Before that: **the local projects were on migration 018 and the hosted one on 015, THREE apart.** `private-ledger-local` and `private-ledger-recovery` both took 018 that day (D-107); the recovery destination needed `node scripts/recovery-destination.mjs down` then `up`, as it does for every migration. **The hosted reading below is from 2026-08-14 and was not re-taken**, because a fresh backup must precede any push and the reading that matters comes after it. **Taking 016, 017 and 018 to the hosted project needs its own fresh authorization and a fresh backup taken first.** Migration 018 adds no table, so it moves no backup version — the contract stays at v7. Before that: **the local projects were on migration 017 and the hosted one on 015, VERIFIED AGAINST THE REMOTE 2026-08-14** — two apart then. Read with `pnpm exec supabase migration list --linked` under a read grant given that day: **001 … 015 are applied on the hosted project and match local on every one**, and **016 and 017 carry an empty remote column**, so exactly two are pending. The command also proves the free-plan project is **awake**, since a paused one refuses the connection — which is a cheaper check than the dashboard. **The hosted backup state was deliberately NOT re-read in the same pass**: a fresh backup must be taken before any push, and that reading would supersede whatever this one said, so the grant was left unspent rather than used on a number about to be replaced. Re-read it *after* the fresh backup, not before. `private-ledger-local` and `private-ledger-recovery` both took 017 that day (D-104); the recovery destination needed `node scripts/recovery-destination.mjs down` then `up`, because `up` alone leaves an earlier run's rows behind and a non-empty destination makes portable recovery *fail* rather than skip. **Taking 016 and 017 to the hosted project needs its own fresh authorization and a fresh backup taken first.** `private-ledger-live` stays frozen on 012. Before that: **the local projects were on 016 and the hosted one on 015, as of 2026-08-12.** `private-ledger-local` and `private-ledger-recovery` both took 016 that day (D-098); the recovery destination needed `node scripts/recovery-destination.mjs down` then `up`, because `up` alone leaves an earlier run's rows behind and a non-empty destination makes portable recovery *fail* rather than skip (GOTCHAS). **Taking 016 to the hosted project needs its own fresh authorization and a fresh backup taken first**, exactly as 011 and 012 each did on `private-ledger-live` — and the backup taken before that push is the last **v5** file this ledger will produce, so it is also the file the newly proven v5-into-v6 restore is for. `private-ledger-live` stays frozen on 012.
- **The local-only `playwright.config.ts` will time out before running a single test, found by `/code-review` on 2026-08-12 and NOT fixed.** Its `webServer.command` is a full `pnpm build && pnpm start`, which on a cold Next build takes longer than Playwright's 60-second default, and no `timeout` was set beside it. `playwright.owner.config.ts` runs a byte-identical command with `timeout: 180_000`, which is the in-repo evidence that 60 seconds is not enough. **Left alone deliberately**: this file is local-only, never committed, and was already modified before the session that found this — changing it would silently rewrite someone else's uncommitted state. Fix it by hand before using that config, or use the isolated one, which `docs/LOCAL_DEV.md` recommends anyway.
- **The tree holds task 27's last two parts alongside the local-only pair, 2026-08-13.** Seven modified files and two new ones — `lib/notification-card-reconcile.ts` and `tests/notification-card-reconcile.test.ts` — plus the continuity edits for D-102. `eslint.config.mjs` and `playwright.config.ts` are the local-only pair and stay unstaged; **`git status --short` is what tells the two kinds apart**, and the untracked files will not appear in an ordinary `git diff` at all (GOTCHAS). Before that: **the tree was back to the local-only pair alone, end of 2026-08-12.** The reader, the capture form, its route and every continuity edit are committed and pushed; `eslint.config.mjs` and `playwright.config.ts` remain deliberately unstaged and are never committed. Telling the two kinds apart is what the next bullet is for. **Two stale claims were found in `SPEC.md` during that sync and fixed**: it said nothing could capture a card, and — wrong since 2026-08-11 — that strong access needs *two* verified TOTP factors, where D-093 and migration 015 made it one. The second is the one worth learning from: an invariant in `SPEC.md` contradicted the database for a day and no gate noticed, because `check:docs` reads structure rather than meaning.
- **Docker Desktop was down at the start of the capture-form work and was restarted from this session.** All seventeen containers came back healthy, both Supabase projects included, and the route suite then ran for real rather than skipping. It stops on this machine often enough that any document saying it is up is a reading rather than a fact — check `docker ps` before trusting a database-backed row, because a stopped daemon makes those suites *skip* and the totals read the same either way.
- Run `git status --short`. The tree holds **two** deliberately local-only config files — `eslint.config.mjs` and `playwright.config.ts` — which are preserved and never committed. **`pnpm-workspace.yaml` left this list on 2026-08-11 and is now committed** (D-095): hosting builds from a fresh clone, and a fresh clone could not install without it. Do not re-add it to the local-only set. As of the end of 2026-08-10 the two were all the tree held: the sign-in work and its continuity edits were committed and pushed as `81569d9`, seventeen files. Anything else appearing there is ordinary work awaiting a commit, and telling the two kinds apart is the point of this bullet. **`playwright.owner.config.ts` and `playwright.isolated.config.ts` are committed files** — only `playwright.config.ts` is local-only, and the three named above are the whole list. The day's five pieces of code work (D-084, D-085/D-086, D-087, D-088, D-089) are all committed and pushed; nothing since is code.
- **Docker Desktop is UP and all three projects have containers running, verified 2026-08-10 after the handoff that recorded it down.** It stops on this machine often enough that the state in any document is a reading rather than a fact — check `docker ps` before trusting a database-backed suite, because a stopped daemon makes those suites *skip* rather than fail and the totals read the same either way.
- **`main` is level with `origin/main`** as of the end of 2026-08-12 — the day's work was committed and pushed in several separately authorized steps. Read `git status -sb` rather than trusting this sentence; it is the line most likely to be stale, which is the whole point of a2efdc7.
- **`public/zxing_reader.wasm` is generated, not committed.** `prebuild` copies it from `node_modules`, so any `pnpm build` produces it and a fresh clone has it after the first build — but a checkout that has only been installed, never built, has a slip capture that silently decodes nothing (D-057, GOTCHAS).
- **All three projects are on migration 012 as of 2026-08-09** — the live one reached it that day (D-073) on explicit authorization and after a fresh backup, having taken 011 on 2026-08-05 (D-065). An app pointed at `.env.local` therefore has a real `public.slips` table **and real match-decision tables**, so slip capture, reconciliation and the owner's override all work against the **real ledger** — which is what the changes were for, and also the hazard: **capturing a slip into live, and storing a match decision there, are real mutations and are never standing permissions.** Both bump the mutation sequence and make the standing backup stale; the decision tables also write an audit event and an append-only revision. `private-ledger-live` is still migrated separately, from the `live/` directory, by `node scripts/live-ledger.mjs up`; that command re-runs the owner seed only when no owner is bound, and one is. The next migration to reach it needs its own fresh authorization.
- Check `git status -sb` and `git log --oneline -3` for where `main` stands against `origin/main`. Nothing in this file records that, on purpose.
- `shared-statements/` holds 16 password-free files — 12 SCB, 1 Krungthai and 2 KBANK statements, plus `KBANK-01.pdf`, which is a bank-code reference sheet rather than a statement and correctly refuses with `UNSUPPORTED_LAYOUT`. **`masked-dumps/` no longer exists** — verified absent 2026-08-01; it was working material that had served its purpose, and `scripts/mask-statement.mjs` recreates a dump when one is needed. `shared-statements/` is gitignored working material, never a fixture, and contains no receipt: the slips live in `receipts_sample/` (23 files, also gitignored), which is what tasks 20–22 measure against. **They have now served their purpose**: all fifteen statements are imported, so these password-free copies of real statements are working material with no remaining job. Deleting them is the owner's call and has not been made.
- The recovery destination is a **second Supabase project** and may be left stopped. `node scripts/recovery-destination.mjs up` starts and migrates it, `down` discards it. `tests/recovery-portability.test.ts` skips without it, the Vitest totals read the same either way, and a skipped run proves nothing about recovery. **It does not receive a new migration automatically** — run `up` again after adding one, or the portability test fails on a missing relation rather than on anything real.
- **Docker Desktop shut down mid-session on 2026-07-30 and was restarted.** All three projects came back healthy together and the live ledger was re-verified afterwards at 1,465 rows / 15 batches. A stopped Docker makes the DB-backed suites *skip* rather than fail — 15 green-looking skips is what that looks like, so read the counts, not the colour.
- **Restarting the database container is not enough.** After `supabase db reset` or a `docker restart` of a `supabase_db_…` container, restart that project's `auth`, `rest`, `realtime`, `storage`, `pg_meta` and `kong` containers too. They stay `(healthy)` while holding dead connections, and the symptom is a CLI that cannot connect, a browser suite failing at sign-in with `fetch failed`, or a socket error against the recovery project's port — none of which look like what they are. The recovery project has its own set. GOTCHAS carries the full entry, including why an in-container `psql` check misleads here.
- Never inspect `private-statements/`, `.env*`, or backup files. Preserve the exact-money, currency, idempotency, append-only, audit and least-privilege invariants, and do not weaken the CSP.
- **`.next` is built against the live project** as of the end of **2026-08-10**, re-confirmed after the D-088 gate rebuilt it twice — the browser suites build pinned to the test project, and a final unpinned `pnpm build` put it back to the documented resting state. The signature was read rather than assumed: **two chunks carry `127.0.0.1:54341`**, and the test project's port appears **only as a key in the dev-session route's port→project map** (`{54321: "private-ledger-local", …}`), which is the benign case that bullet below describes and not a mis-pin. `prebuild` also repopulated `public/tesseract/` — 4 assets, 3.78 MB, gitignored — which the OCR amount finder cannot run without. Earlier: **2026-08-07**, re-confirmed the same way after that day's browser suites rebuilt it against the test project and a final unpinned `pnpm build` put it back — one chunk names the test port and two name the live one, which is the documented signature and not a mis-pin. Same state on **2026-08-05**: grepping `.next/server` finds **two chunks naming the live port and one naming the test port**, the latter being the dev-session route's port→project map described below. Confirmed by grepping `.next/server` for the API URL: two chunks carry the live project's port. **One chunk also carries the test project's port and is not a target** — `app/api/v1/dev/session/route.ts` holds a port→project map naming all three, inlined by every build, so read the match's context rather than counting files (GOTCHAS). Note that driving the app by hand against slip capture needs the **test** project instead, because the live one has no `slips` table. Re-confirm before trusting it; the test project's URL lives on in the stale `.next/dev` turbopack cache, which `next start` does not serve.
- **No app server is running** as of the end of 2026-08-10, re-confirmed after the D-088 gate: both browser suites start and stop their own on 3100 and 3200, and port 3000 is free. All three Supabase projects were up and healthy throughout that gate, the recovery destination included, so no row in it rests on a skip. Earlier that day one had been left running on port 3000 against the **live** project since 2026-08-09, started by the owner to export a backup; it was stopped on his instruction that day, before the browser suites — which rebuild `.next` and would have left it serving a half-replaced build. `.next` is now the unpinned, **live-targeted** resting state that the production-build row in `PLAN.md` describes. Earlier: none running at the end of 2026-08-07 — the browser suites start and stop their own on 3100 and 3200 — and `.next` is back to the live-targeted resting state the line above describes. One ran on port 3000 on 2026-08-01 against the *test* project so the owner could drive the four routes and slip capture; it was stopped and the tree rebuilt unpinned. To drive it by hand against the *test* project, pin `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `OWNER_GOOGLE_EMAIL` and `NEXT_PUBLIC_ALLOW_DEV_OWNER_SESSION` for **both** `pnpm build` and `next start`. **The old reason for pinning is gone and the new one is stronger.** Until 2026-08-05 slip capture *had* to use the test project because live had no `slips` table (D-065 changed that); now an unpinned build reaches a real one, so an unpinned server driven by hand can write a real, undeletable slip into the real ledger and invalidate the standing backup. Pin deliberately rather than relying on live to refuse. **The test project is empty after every browser-gate run** — 3 seeded accounts, no transactions, no slips: the owner's hand-captured slip was deleted twice on 2026-07-31/08-01 by the owner suite's own cleanup (GOTCHAS), so anything to be looked at by hand must be re-created afterwards. The Playwright configs use ports 3100 and 3200 and never reuse a server.
- **Docker Desktop stopped twice on 2026-07-31** and was restarted each time. A stopped Docker makes the database-backed suites **skip rather than fail**, which reads as a green run — check the counts, not the colour. It was up for the routing gate later that day: Vitest reached both live containers and pgTAP ran, so those rows are real. **Docker Desktop stopped again on 2026-08-05** — the fourth time in a week — and was restarted mid-session. It was caught by the owner browser suite reporting **`18 skipped` at exit 0**, having printed all eighteen test names first; the tell was `docker ps` failing to reach the daemon. **Read the last word of a browser run, not the numeral** (GOTCHAS carries the entry). All three projects came back healthy together, `auth` lagging the database by a few seconds, and the **entire gate was re-run afterwards** so no row predates the restart. `tests/recovery-portability.test.ts` passed on its named line rather than skipping, and `private-ledger-live` answered the migration and row-count queries D-065 rests on. Same all-up state on 2026-07-31. **On 2026-08-07 all three projects were up and healthy for the whole gate**, the recovery destination included, and nothing was stopped or restarted mid-run — the live project was not queried or written that day, and the whole gate ran against the synthetic one.

**The three projects are no longer on the same migration.** `private-ledger-local` carries **001–014** as of 2026-08-10, `private-ledger-recovery` is **up and carries 001–014**, and `private-ledger-live` is on **012**. Migrations 013 and 014 (cash entries and the correction path) have **never touched live**, and taking them there needs its own fresh authorization and a fresh backup taken first, exactly as 011 and 012 each did. The recovery destination was brought up with `node scripts/recovery-destination.mjs up` earlier on 2026-08-10 and was still up for the end-of-day gate, where the portability suite passed on its **named line** rather than skipping — read that line, never the totals, which are identical either way. It is a separate project and receives no migration automatically, so run `up` again after adding one.

**All three projects carried migrations 001–012: the test and recovery ones since 2026-08-05, the live one since 2026-08-09** (D-073). The test project was reset twice on 2026-08-05, and the recovery destination was migrated with `node scripts/recovery-destination.mjs up` — it is a separate project and receives nothing automatically, and until it had 012 the portability suite failed on a missing relation rather than on anything real. **The next migration to reach live needs its own fresh ask and a confirmed backup**, exactly as 011 and 012 each did. Live is migrated from the `live/` directory by `node scripts/live-ledger.mjs up`, which re-runs the owner seed only when no owner is bound, and one is.
- After substantive changes, run `/sync-continuity` to reconcile these docs against verified evidence.
