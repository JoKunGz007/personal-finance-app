# Private Ledger execution plan

Last verified: 2026-08-29

## Current checkpoint

**Task 47's ledger date filter is built, reviewed and locally verified, not yet committed, 2026-08-31** (D-178). `/ledger` gets two date inputs beside the account select; unlike Account, Order, Status and Filter — all client-side narrowings of rows already fetched — a date window has to bound the *fetch itself*, so it takes effect only on the next Reload. Fixed a real correctness risk found by design rather than by testing: `loadMore` must walk its cursor under the range the currently-loaded window was actually fetched with, not whatever is live in the date inputs, so a new `appliedRange` state (set only where `ledgerWindow` is) is what `loadMore` reads — an edited-but-not-yet-reloaded date field cannot reach a deeper page ahead of Reload. `/code-review high` found the ledger route's new window-validation duplicated `lib/date-range.ts`'s `isUsableRange` instead of calling it, and that the identical duplicate already existed in the already-shipped `/api/v1/statistics` route (planted by D-174); both now delegate, re-verified by request afterward — 200 for an ordered range, 400 for a transposed one, on both routes, unchanged. `tests/date-range.test.ts` is new (9 tests, the shared module's first direct coverage) along with 5 for `ledgerPageSearch`/`cursorAfter`. Gate on the isolated commit: `tsc`, `eslint .`, `check:docs --strict` (177 decisions, 191 traps), `pnpm build`, Vitest **906 passed / 7 skipped across 42 files**, all clean. Verified in a real browser against `next build && next start`: a transposed range shows the refusal and disables Reload; a corrected one fires `?from=…&to=…` on every account's request and the page states the applied window. **Not committed, not exercised against real confirmed rows** — the local seed carries accounts but no confirmed transactions.

**Task 46's account filter has its control, reviewed, tested, committed, pushed and deployed, 2026-08-31** (D-177, `4f51a7e`). The window picker (D-170) and the account filter now both render on `/statistics`: a `<select>` populated from `GET /api/v1/accounts`, narrowing `windowSearch`/`pickerSearch`'s existing `accountId` field into the request key so an account change invalidates in-flight figures the same way a date change already did. **`lib/statistics.ts`, `app/api/v1/statistics/route.ts` and `lib/date-range.ts` were already uncommitted in the working tree when this session started** — task 46's SQL-and-route half, authored by earlier, unrecorded work; `lib/date-range.ts` turned out to be a genuine compile-time dependency of `lib/statistics.ts` rather than task-47-only groundwork, so it went into this commit too. **The accounts-transactions route and `lib/transactions.ts` were checked for a dependency (none found) and deliberately left uncommitted** — task 47's unrelated backend, unreviewed this session. `/code-review high` ran before the commit ask, as D-125 requires, and found two real defects in the first draft: the account `<select>` could show "All accounts" as selected while the page was genuinely narrowed to an account (its "unknown account" fallback only rendered once the separate accounts request had resolved), and the whole control was a near-copy of `/ledger`'s own account select. Both are fixed by extracting `app/account-select.tsx`, shared by both pages now, with the loading-vs-unknown fallback collapsed into one condition and opt-in via `showUnknown` so `/ledger`'s behaviour is unchanged — verified in a real browser on both pages, not inferred from the type check. Two tests were added for the account-id round trip through `pickerSearch`/`pickerStateFromSearch`/`windowSearch`, which the review also flagged as missing. Gate, run against the isolated commit (`git stash push --keep-index`, not the full working tree): `tsc`, `eslint .`, `check:docs --strict` (177 decisions, 191 traps), `pnpm build` and Vitest **892 passed / 7 skipped across 41 files** all clean. **Committed as `4f51a7e`, pushed to `origin/main`, and confirmed live on the hosted deployment** — the owner opened it in a real signed-in browser session, the account `<select>` listed his three real accounts, and choosing one correctly narrowed every figure and switched the balance-source sentence, against real confirmed rows. **No figures from that verification are reproduced in this file or anywhere else in the repository**, per the real-financial-data rule. Task 47's ledger date range remains entirely unbuilt: `lib/date-range.ts` is committed but nothing under `app/` imports it for that purpose.

**Migration 024 is on hosted and the three synthetic accounts are gone, 2026-08-30** (D-175, D-176). The push was an agent's, authorized by the owner in the turn it ran, after a backup verified from the database at **39 / 39** and a `--dry-run` naming only `202608290024`; `supabase migration list --linked` reads back **24 for 24, local matching remote**. Task 50 is closed — the production picker offers three accounts and `IMPORTED ACCOUNTS: 3 OF 3`. **The explanation task 50 had been investigated under was wrong**: not a `seed.sql` leak, since hosted never held the synthetic owner. **Two things are owed**: the function grants have not been read back from hosted, and the two controls migration 024 exists for — task 46's account filter and task 47's ledger date range — are **unbuilt**, which is now the only thing standing between the database and the feature.

**The phone audit is a committed test, the window picker lives in the URL, and migration 024 is written but not pushed, 2026-08-29** (D-172, D-173, D-174). **Committed and pushed as `676a8ea`, the seventh deployment of the day.** The CSS fix and the URL state **change what renders**, so both join the list of things nobody has looked at on the deployment. `tests/e2e/owner-phone-audit.spec.ts` asserts the 44px standard and no sideways pan on all five routes, twice each — disclosure shut and open — and a third time on `/statistics` with the Custom tick on. **Its first run found `/ledger` panning sideways at 390px**: the account filter `<select>` measured 404px in a 390px viewport, from a CSS rule older than the audit that missed it. Migration 024 gives `ledger_statistics` an account and `list_account_transactions_page` a date range; **it is applied to `private-ledger-local` only and needs a verified backup and the owner's own `db push`** before either control can be built. **`/code-review high` found seven defects and all seven are fixed — eight for eight now on finding a real one**, and the two that mattered were a stale pgTAP signature that would have aborted `009` under `supabase db reset`, and the real statement mailbox left inherited by both committed browser configs.

Gate at this checkpoint, every exit code read on its own because a PowerShell chain does not propagate them: Vitest **890 passed / 7 skipped across 41 files**; Playwright owner **34/34**, isolated **38 passed / 4 skipped**; pgTAP **009: 33, 011: 35, 012: 31** run directly against the local project; `tsc`, `pnpm exec eslint .`, `check:docs --strict` at **174 decisions and 191 traps**, and the production build — all clean.

**The tenth archive boundary is taken and `DECISIONS.md` is at 61%, 2026-08-29** (D-171). Six entries moved — **D-153 with D-164 … D-168** — to `docs/decisions/ARCHIVE-D-153-D-168.md`, stepping over D-158 and D-161 for the second boundary running. **D-153 is what made the cut available**: D-169 answered the default face, so the question that had fenced the file from below since the eighth boundary stopped fencing it, which is the third time running that a closing question rather than the calendar bought a boundary its depth. **It stops below D-169 and D-170 on purpose** — both shipped today, both change what renders, and nobody has looked at either on the deployment. That is a fence the first nine boundaries never met: an *unverified rendering* rather than an unanswered question, weaker than the other kind, and it expires the moment either page is seen. `check:docs --strict` clean at **171 decisions and 191 traps**; nothing outside `docs/` and the continuity files moved, so nothing else was re-run. **Committed and pushed as `9a97f70`, the sixth deployment of the day** — documentation only, so nothing the app serves changed and there is nothing to verify in the running app.

**The statistics window is a control, the default face is Pixelify Sans, and six controls reached the phone tap standard, 2026-08-29** (D-168, D-169, D-170). **`main` is at `77c0d6b` and `origin/main` matches it.** Five commits went out today and all five are deployed: `de70c9e` (the continuity sync that could not record its own commit), `3b1205d` (five tap targets raised at phone width), `24b894a` (the default face, a sixth control found only on the real ledger, and the docs catching up), `0b88ea2` (task 46 first half) and `77c0d6b` (the sync that recorded them). **Two of them change what renders, and that is why the tenth boundary stopped where it did.**

**Task 48's own control was 8px under this repo's phone standard.** `.match-control .secondary-button` is 36px and `ledger-statement-row.tsx` wraps Exclude/Include in it, so the control that shipped on 2026-08-27 went out short, on the surface D-138 was found on. Four others sat with it. **Why nothing saw it is the finding**: `.runtime/mobile-audit.spec.ts` is the only instrument that measures tap targets at 390px and it clicks Dev sign-in directly — which D-157 hid behind the Settings disclosure below 700px on 2026-08-26. The audit had timed out at phone width for three days unnoticed, because a gitignored throwaway only fails when someone asks it to run. **The change that introduced small controls and the change that blinded the audit shipped the same day.**

**The 1603-versus-1604 question is CLOSED, by measurement rather than by decision.** Both surfaces read **1604** and agree to the satang: ฿470,002.63 in, ฿459,037.96 out, ฿10,964.67 net. Three independent partitions each reconcile — fourteen months, seven weekday buckets, and the whole-window total — on both the row count and the money. The 1603 was a correct reading taken *during* the exclude/include round trip, while one row was out of reporting; the round trip restored it. Nothing needs rewording.

**Previously, 2026-08-27** (D-164, D-165, D-166, D-167): two archive boundaries, and both authorized tasks built, committed, pushed and verified in the running app.

**`DECISIONS.md` took three boundaries in three days, and the rate is the finding rather than the percentage.** The eighth moved **D-154 … D-156** — three entries, the shallowest of the eight, because it was the first range **fenced by an open question on both sides**: D-153 below and D-157 above. The cheaper cut was priced at 71% and 54% and refused. Two decision entries and a review's findings put the file straight back to **96%**, so the **ninth** was taken the same afternoon (D-167): **D-157, D-159, D-160, D-162 and D-163**, stepping over **D-158** and **D-161**, for **96% → 73%**. Four entries on 2026-08-29 took it to **86%**, and the **tenth** (D-171) moved **D-153 with D-164 … D-168** for **86% → 61%**. **What bought each of the last three was a question closing, not the calendar** — task 49 settled D-157's metrics for the ninth, and D-169 settled the default face for the tenth, exactly as D-164 predicted both times. What the maintained file holds now is three open questions and two settled-but-unlooked-at entries, which is the intended shape.

**Task 48 — `include_in_reporting` has a control.** The hazard was a *silent success*: the endpoint takes the whole overlay and the RPC writes every column, so a body sending the rest as null is **accepted** and erases what the owner typed. `overlayWriteBody` derives the body from the row and a change may only narrow it, so the erasure is unrepresentable rather than remembered — red-proved in Vitest and end to end, where a seeded populated overlay survives a toggle with only the flag and revision moved. The control sits in the **Status cell**, which is already an em dash on exactly the statement-only rows that need it: measured **148px of content in a 148px cell in all four faces**, and that check was proved able to fail. `withOverlay` corrects the account totals with it, because those came from SQL that honours the flag.

**Task 49 — the prescribed remedy was measured and not built.** `ascent-override` and its two siblings would have pinned nothing: every line height here is an explicit ratio, so a font's own metrics never reach layout, and three of the four faces were identical to the pixel on both routes. **The one thing that moved was the header**, and the cause was text capped by *width* occupying a face-dependent number of lines — the picker's standing note, folded behind the `(i)` on D-156's rule, and the phone's privacy chip. The header is now **148px in every face**, 23px shorter than it was. The guard is committed rather than throwaway, and it caught both that the phone case existed at all and that its own font wait was racy.

**Two defects of this session's own making, both found by the work rather than by the gate**: `resetOwnerImportSurface` never cleared `transaction_overlays` — the fourth table in that function needing the comment the other three carry — and the overlay route was returning the owner's uuid to the browser.

**`/code-review high` ran before asking to commit and found four defects, all fixed** — six for six now on finding a real one. The serious one: the ledger totals honoured `include_in_reporting` on the server branch and **not** on the fallback the search box reaches, so excluding a row and typing one character counted it again while the row still wore its “Excluded” chip. Three more were the typeface disclosure written inside its `<label>`, which joins a `<label>`’s accessible name, loses a specificity contest it did not know it was in, and had a comment claiming axe had verified something axe structurally cannot see.

**Verified in the running app on 2026-08-27, not merely deployed** — the first time an agent has done that here, driven through the owner’s own signed-in browser session with no credential handled. The exclude control works end to end on a real internal transfer, `/statistics` agrees to the satang, the round trip is the identity, and the database shows the audit trail. **It also corrected this plan**: the excludable set is about **eighteen** rows, not the 248 that carry a transfer label — most name other people and are ordinary spending. See task 48.

**Gate green at `0b88ea2`**: Vitest **881 passed / 7 skipped across 41 files** (+8, the window arithmetic); Playwright owner **33/33**, isolated **38 passed / 4 skipped** — including *switching the typeface moves nothing on the page* and axe on every route, both on the **mobile** project, which are what hold D-166's invariant across this change; `tsc`, `pnpm exec eslint .` and `check:docs --strict` clean at **170 decisions and 191 traps**; production build clean. **pgTAP deliberately not re-run — no SQL has moved since migration 023.** Phone audit at 390px: **all five routes plus a disclosure-open pass report tap targets ≥ 44px**, no route pans sideways, and task 48's Status cell measures 324 against 324.

**Two suites were red for a while and neither was a regression.** Both failed in `assertOnlyDisposableLedgerData`, refusing to wipe a ledger holding two accounts no suite claimed — leftovers from a run that accidentally collected every spec under `.runtime/`. The guard was right; deleting exactly those two rows restored both suites to their baselines. **That guard is D-048's, and this is the first time it has fired on anything.**

## Current review blockers

All four original review blockers are now resolved with red→green pgTAP evidence (2026-07-24), and the one carried follow-up (fingerprint binding) is also resolved (2026-07-25, D-014). No open review blockers remain.

The one open **task** blocker — the parser's `headerY` resolution — is fixed as of 2026-07-25 (D-028) and the fixtures now reproduce it. No blocker of either kind is open.

1. ~~`confirm_import` trusts caller-supplied payload digests.~~ **Resolved** — migration `202607240005_confirm_import_digest_binding.sql` recomputes the canonical digest server-side and rejects mismatches (`payload digest mismatch`); the client computes the identical object (`confirmationDigest`). `002_security_contracts.sql` now uses real computed digests via a `pg_temp` wrapper and includes a tamper test (test 19) that fails on the pre-005 schema and passes after. See DECISIONS D-012. The fingerprint half of the finding is now closed by migrations 007–008 and the source-text charset guard — see DECISIONS D-014.
2. ~~`003_restore_contracts.sql` commits only empty ledger tables.~~ **Resolved** — `003` now populates every ledger table, exports a canonical snapshot, rewrites the owner to a foreign id, wipes, restores, and asserts a re-export reproduces every restored table byte-for-byte (schemas, FKs, money-as-text, audit rows) and that every row is remapped to the caller. See DECISIONS D-013.
3. ~~Restore manifest counts accept fractional JSON numbers.~~ **Resolved** — migration `202607240006_restore_count_and_sequence_bounds.sql` requires `tableCounts[kind]` and each descriptor `rowCount` to be canonical non-negative integer text, failing closed with `invalid restore manifest descriptor` instead of an uncaught `22P02` cast. Red proof: `003` fractional-count test fails on the pre-006 schema.
4. ~~A staged snapshot sequence at signed-int64 maximum overflows on the commit increment.~~ **Resolved** — migration 006 makes the accepted range explicit: `snapshotSequence` must be `< 2^63-1`, reserving headroom for the single post-commit increment. Red proof: `003` int64-max test fails on the pre-006 schema.

## Completed hardening

- Database-locked, count-checked backup snapshot with canonical text bigint boundaries.
- Manifest-bound schema-v2 restore with ordered table kinds, counts, chunk digests, aggregate digest, and exact mutation-sequence checks.
- Persisted statement frame and PostgreSQL reconciliation enforcement.
- Category mutation RPC with audit and backup-staleness sequencing.
- Backup freshness acknowledgement separated from snapshot retrieval.
- Synthetic `.pldemo` explicitly non-restorable and unable to clear authoritative backup staleness.
- Unknown deposit/withdrawal pairs fail closed unless parser provenance carries the recognized interest/tax anomaly marker.
- Strict per-table restore schemas and canonical signed-int64 string validation.
- Pre-release schema version 1 explicitly unsupported.

## Next local tasks

1. ~~Add true multi-session concurrency coverage for the owner mutation advisory lock.~~ **Done** (D-018) — `tests/advisory-lock.test.ts` contends for the key from two real connections and asserts same-owner blocking, different-owner independence, and release on backend termination. Not yet covered: contention through the RPCs themselves, which needs an authenticated owner (task 3).
2. ~~Add a real schema-v2 export → encrypt → decrypt → stage → chunk → commit → re-export equality integration test, including more than 1,000 rows.~~ **Done** (D-019) — `tests/backup-roundtrip.test.ts` over 1,200 rows, non-destructive, confirmed distinguishing.
3. ~~Drive a real authenticated request into `confirm_import`.~~ **Done** (D-020) — `tests/import-confirm-e2e.test.ts` signs in as the seeded owner, reaches aal2 with two verified TOTP factors, and posts fingerprints and a digest computed by `lib/canonical.ts` from a parser-extracted statement; asserts the import lands, a tampered fingerprint is rejected, and the same request without MFA is refused. No hosted resources were needed. The follow-up gap it left — the Next.js route wrapper — is now closed by `tests/import-route.test.ts` (D-022).
4. ~~Repeat privacy, browser-storage/network, accessibility, and interface-guideline audits.~~ **Done 2026-07-25** — re-audited against this round's new code. No storage APIs (`localStorage`/`sessionStorage`/`indexedDB`/`document.cookie`) anywhere in `app/`, `lib/`, `workers/`; one client `fetch`, same-origin to `/api/v1/demo`; no `console.*` in shipped code; CSP and Permissions-Policy unchanged and strict. Two regression guards added to `tests/privacy.test.ts` for the new parser surface: the worker may not post the password or raw page text, and the extracted frame may not carry a full account number. Accessibility re-verified by the axe checks in the Playwright run.
5. ~~Build the account-binding UI: an accounts-list endpoint plus a chooser, so an extracted statement can be bound and confirmed from the app rather than from a test.~~ **Done** (D-021) — `GET /api/v1/accounts` plus a bind stage in `app/ledger-app.tsx`; the bound payload is posted to `/api/v1/imports/confirm` with the PDF's own SHA-256 as the artifact digest and one idempotency key per bound statement. Route coverage came with it (D-022). Still uncovered: the chooser in a browser, which needs a real PDF to reach.
6. ~~Re-run Playwright.~~ **Done 2026-07-25** — 4/4 across desktop and mobile, re-run after the binding UI landed. Still unexercised in a browser: the charset rejection path, the binding chooser, and the authenticated import path — all three sit behind a real PDF.

7. **Read a real statement end to end.** Six attempts on 2026-07-25, all productive, none complete. Each was driven by the owner in their own browser with the document password entered interactively; every finding came from on-device masked diagnostics, so no statement content was read by an agent or written to the repo:
   - Attempt 1 failed with `PDF_PARSE_FAILED` before pdf.js read a page — `GlobalWorkerOptions` had never been configured, so no PDF could be opened at all (D-023). Not a geometry result.
   - Attempt 2 reached `MISSING_COLUMN_ANCHOR`, and the on-device label diagnostic showed the real column model: one combined `Date/Time` column and a separate `Transaction` column, neither of which the invented geometry had. The reader and fixtures are corrected (D-024).
   - Attempt 3 reached `UNSUPPORTED_CURRENCY`: the currency is printed below the grid, not in the frame block (D-025).
   - Attempts 4 and 5 reached `MISSING_FRAME_FIELD`. A masked structural dump of the whole statement identified the real frame contract in one pass (D-026): labels are `Account Number` and `Statement Period`, no opening or closing balance is printed anywhere, each row's time sits on its own line, and frame lines carry several label/value pairs. Three latent defects were fixed as a result — a neighbouring field's digits being read as the account suffix, a footer aborting a valid statement, and an anchored label pattern rejecting padded whitespace.
   - Attempt 6 reached `MISSING_FRAME_FIELD` again, this time reporting `account number (label not found)` while account type and statement period read. Root cause: `headerY` was taken from the first line containing *any* column anchor, and a real statement prints `Branch` as a frame label above the grid, which matches the `branch` column anchor — so the frame/grid boundary landed on that frame line and `extractFrame` dropped every field printed below it. **Fixed 2026-07-25** (D-028): `findColumns` now returns the `y` of the line carrying all seven headings, and that is the boundary. Attempt 6 also returned the last page's summary-block label wordings.

   - Attempt 7 (D-028 landed) reached `INVALID_ROW_CONTENT` at page 6 row 14 on a zero withholding-tax column, fixed as D-029.
   - Attempt 8 reached page 7 row 6, where the balance first exceeded 10,000 and a right-aligned figure misfiled one column left, fixed as D-030 along with batched failure reporting (D-032).
   - **Attempt 9 read the whole statement: 233 rows across 12 pages, through to the bind stage.** It exposed the one defect in the sequence that did not fail closed — two-digit years read as Buddhist when the statement prints Gregorian, dating everything 1983 (D-031). Fixed, and attempt 10 reads the period as 2025-07-01 to 2026-07-31.

   **The reader now works against a real statement end to end.** Confirmed against reality: the file opens and decodes, the signature matches, all seven column headings match, the currency is found, the frame reads (account type, account number, statement period), all 233 rows parse, and the app reaches the account-binding stage. The date format is confirmed as `dd/mm/yy` with the time on its own line.

   Verified by the owner against the document on 2026-07-25, closing every parser question this task opened:
   - **The row count is confirmed.** Page 12 prints 131 withdrawal rows and 102 deposit rows; 131 + 102 = 233, exactly the number the reader found. Now enforced in code (task 8, D-033).
   - **The balance chain is confirmed.** The first row is a withdrawal leaving a zero balance, so the opening figure derives cleanly; opening + printed deposits − printed withdrawals lands exactly on the last row's printed balance. This is the first independent evidence that D-026's derived opening is correct.
   - **The 13-month period is confirmed.** The document prints `Statement Period 01/07/25 to 31/07/26` and `Requested Date 24/07/26` — Gregorian, and a genuinely 13-month range.
   - **The account is confirmed** as the right one by the owner, against the last four the bind screen shows.
   - **The currency position is confirmed** as the frame block, above the grid; the guard is restored (task 9, D-034).

   The counts summing to *exactly* 233 also confirms D-029 independently: the bank does not count the `0.00` withholding-tax column of a zero-tax interest posting as a withdrawal either.

   Still unreached behind a real PDF: the binding chooser, the authenticated import path, and the charset rejection path — all three blocked on task 10, not on the parser.

8. ~~Read the last page's summary block as a global integrity check.~~ **Done** (D-033) — `extractTotals` reads `Total Page`, `Total Withdrawal` and `Total Deposit` from below the grid on the last page, and `verifyTotals` checks the page count, the per-kind row counts and the per-kind absolute totals against the rows parsed. A disagreement fails closed as `SUMMARY_MISMATCH`; an absent block is tolerated as "no cross-check"; a block printed but unreadable fails closed. The open product question is resolved in favour of failing closed, because an append-only ledger cannot take back a dropped row.

   Verified against the real statement on 2026-07-25 by the owner reading page 12: the printed counts sum exactly to the 233 rows the reader found, and the printed totals close the chain from the opening figure derived from the first row to the last row's printed balance. That is the first independent confirmation that the parse is complete and that D-026's derived opening is correct.

   Left undone: whether an import *was* cross-checked is not **persisted**. It is now shown at the bind stage (D-042), so the owner can see it before confirming, but the ledger still cannot distinguish a cross-checked batch from an unverified one afterwards. Recording it needs a migration and a payload-contract change: the confirmation digest covers the account, contract version, currency, period, balances and rows, so a new persisted field has to be added on both sides at once.

9. ~~Restore the currency guard to the frame block.~~ **Done** (D-034, supersedes D-025) — confirmed directly against the real statement, which prints `Currency THB` above the grid.

10. ~~**Configure the app's local Supabase env so binding can run**, then reach the last three paths in a browser.~~ **Done 2026-07-25.** Two halves, and the second was not anticipated when this task was written.

    The owner set `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` and `OWNER_GOOGLE_EMAIL` in `.env.local` (the third was missing from this task's original wording; `lib/server/supabase.ts` compares it and answers 403 without it). Verified without reading `.env*`: `GET /api/v1/accounts` moved from `503 Local Supabase is not configured` to `401 Sign in to continue`.

    That exposed the real blocker: **the app had no sign-in at all.** `createServerClient` appears only in `lib/server/supabase.ts`; there is no `createBrowserClient` and no `signInWithPassword` anywhere in `app/`, and only the test helpers ever authenticated. So configuration alone could never have reached the three paths — they all sit behind an owner-bound route needing `aal2` and two verified TOTP factors. Closed by D-036: a flag-gated development sign-in route, and `tests/e2e/owner-session.spec.ts` now covers all three in a real browser against a production build — the binding chooser, a statement confirmed through `/api/v1/imports/confirm` into `confirm_import`, a refused binding to a non-matching account, and an out-of-charset statement refused at assembly.

    The real login remains Google OAuth, unbuilt and behind the hosted authorization gate (`docs/PRODUCT_CHARTER.md`). Nothing in this task built product auth surface.

11. ~~**Extend the reader to the remaining statement and receipt layouts.**~~ **Done for both statement layouts (2026-07-26); receipts moved to task 13.** Two further statement formats (SCB, KBANK) and three receipt formats were in scope. Most of the eleven defects found on the Krungthai layout were reader-architecture faults rather than bank-specific ones — the worker, the frame/grid boundary, midpoint column assignment, zero money columns and the calendar era all carry over — so the per-format cost should be far below Krungthai's ten reads.

    **The masking harness is built and its two authorization gates are satisfied** (2026-07-25, D-035): `docs/FIXTURE_POLICY.md` now carries the amendment, and the `private-statements/` grant is recorded with its boundary and its cost. `scripts/mask-statement.mjs` opens one PDF on this machine and writes a masked structural dump to the gitignored `masked-dumps/`, so a layout can be developed offline from one dump instead of one owner-driven read per defect. The four diagnostics moved to `lib/masked-diagnostics.ts`, which imports nothing so the harness can run them under plain Node and so the privacy surface reads in one file; `lib/krungthai-layout.ts` re-exports them, and every existing call site is unchanged.

    Verified end to end on the invented fixtures: `tests/fixtures/synthetic-pdf.ts` renders `validStatement` into a real PDF, the harness reads it, and the dump reproduces the column grid, the frame block, the summary block, the `dd/dd/dd` date shape and the right-aligned money bands with no value surviving. **Not yet run against a real statement** — that is one owner-driven invocation per format, and each needs the owner present to type a password once.

    **Scope corrected 2026-07-25: the three receipt formats are JPGs, not PDFs**, so they are not in this task at all — see task 13. What remains here is SCB and KBANK, both confirmed PDFs. Each needs one owner-driven `scripts/mask-statement.mjs` run; nothing else is blocked.

    **Done 2026-07-26 for both statement layouts.** `lib/statement-layout.ts` reads `scb-layout-v1` and `kbank-layout-v1` from per-layout descriptors; `lib/read-statement.ts` dispatches across all three readers by heading anchor set; migration 009 lets the results reach the ledger (D-039, D-040, D-041). `lib/krungthai-layout.ts` was left alone apart from carrying its contract version, exactly as the design decision required — and re-reading the dumps proved that right, since the heading anchors do not bound the data columns in either new layout and its midpoint banding could not have been reused.

    The descriptor question this task left open is answered by having built it: **descriptors as data, but a hand-written row grammar rather than a column model.** What varies across the three layouts is not parameters of one shape — date and time packing, the number of heading lines, where the summary sits, and how counts are encoded are three different shapes each. The descriptor names the encoding; it does not pretend one fits all.

    **Both readers have now met a real document, each on the first attempt (2026-07-27).** One real SCB statement read 94 rows across 5 pages, one calendar month; one real KBANK statement read 55 rows across 2 pages, covering six months. Both reached the account-binding stage with every fail-closed check passing: the row grammar on every row, the balance chain closing row by row, the per-page carry-forward, and the two money sub-columns staying distinct and correctly ordered.

    The KBANK read is verified rather than merely successful. The masked dump of that document covers both its pages in full and contains exactly 55 transaction rows, and its summary block sits where the reader looks — so the D-033 cross-check ran and the bank's own printed counts and per-direction totals agreed with all 55. The row count is confirmed by the document's arithmetic, not only by the reader agreeing with itself. The SCB dump truncates middle pages so its row count cannot be checked the same way, but the page count matches and reading 5 pages proves the heading line repeats on the middle ones, which `docs/SCB_CONTRACT.md` had only inferred.

    Ten owner-driven reads were needed for Krungthai and one each for these. That difference is what reading the masked dumps first bought.

    **What this still does not establish:** neither statement was imported. Binding, the authenticated import path, and reconciliation against real rows remain unexercised on real input, and are gated by § Later authorization gates item 3 below. Both statements print account suffixes no seeded account matches, so binding refuses — correctly.

Tasks 1–12, 14, 15 and 16 are complete and verified for statements. Remaining build work, in the order settled 2026-07-28 (D-050, D-051):

17. **Transactions view.** ~~Front-end only~~ **Built 2026-07-28; browser-verified since, and complete.** The "not yet browser-verified" clause stood here until **2026-08-21** and had been false since routing gained its own spec: `tests/e2e/ledger.spec.ts` carries five tests over this view, and the owner suite drives it end to end including the reconciliation that collapses a slip onto its statement row. `list_account_transactions` already returned everything needed and `GET /api/v1/accounts/[id]/transactions` already served it authenticated and `no-store`, so this added **no migration, no RPC and no route** — `lib/transactions.ts` (wire contract and pure derivations), `app/transactions-view.tsx` (the section), `lib/wire.ts` (`readError`, extracted from `app/ledger-app.tsx` so both callers share one copy), and styles in `app/globals.css`.

    Decisions taken while building, each recorded because the alternative looks equally reasonable from outside:

    - ~~**Nothing loads until asked.**~~ **Superseded 2026-08-26 by task 43** (D-155). The reason it recorded — every other read surface here is driven by an explicit action, so a section fetching on page load would be the one place that stopped being deliberate — was a **consistency argument and not an invariant**, which is what made it reversible. The ledger now loads on arrival. Left in place rather than deleted, because a decision that was reversed is worth finding at the place it was taken.
    - **The balance column appears only for a single account.** The printed balance belongs to one account's statement chain, so a merged list would show it stepping between unrelated running balances — a number that looks like a ledger balance and is not one. The merged view shows the account instead.
    - **Money never becomes a number.** The RPC casts every bigint with `::text`; the schema accepts canonical integer strings only, and every sum is BigInt. A test asserts precision past 2^53 — and asserts the loss is real by round-tripping the same value through a double.
    - **The overlay schema is strict.** A migration adding an overlay column should fail the parse loudly rather than have the view silently ignore a field the ledger now considers part of an overlay.
    - Category *names* are not shown: overlays carry `category_id` and resolving it needs a second endpoint. Deliberately out of scope for this pass.

    **Refined 2026-07-28 after the owner drove it against the real ledger** — the first feedback this app has had from its own data. He confirmed the rows track the statement, and asked for three changes, all made: an **Order** control (newest-first or oldest-first, as a select beside Account and Filter); **both balance columns in the merged view** rather than none, `Account balance` beside `All accounts`; and the partial-coverage caveat demoted from a warning banner to a quiet `Imported accounts: N of M` status line, because on this ledger most accounts are empty most of the time and a banner that fires on nearly every load is read as noise. His point on the balances corrected the original design: with an explicit combined column present, a per-account balance can no longer be misread as a total, which was the whole objection to showing it.

    The combined figure is exact rather than approximate. An account's balance before its first imported row is derivable from that row (`post_balance − movement`), so every account has a known balance at every point in the merged list, including dates before its statement begins. It is computed over the whole account scope and never over the text-filtered subset, since a running total of whatever a search matched would not be a balance. An account with no rows at all contributes nothing and cannot, which is what the status line discloses.

    Verified 2026-07-28 with the project-local runtime: ESLint, `tsc --noEmit`, production build, and Vitest **190 passed / 7 skipped** (up from 164/7 — the 26 tests in `tests/transactions.test.ts`), with `tests/privacy.test.ts` extended to cover the new component. **Verified in a browser, both ways.** The owner drove the real view against the live ledger and confirmed the rows track the statement — stronger evidence than any fixture, and the source of the three refinements above. And two owner-session Playwright specs now cover it automatically (**10/10**, up from 8): one reads a confirmed import back and switches between merged and per-account, asserting the column set changes with the mode and that an account holding nothing shows the empty state; the other imports into **two** accounts and checks the all-accounts figure against a SQL sum of each account's latest printed balance. The two-account case is the point — with one account the combined column is trivially that account's own balance and a broken derivation would pass. It also asserts the combined cell differs from that row's account balance, which is what a regression collapsing the two columns would break. The isolated suite still passes 14/14 with the new section mounted, accessibility checks included.

    Writing those specs found a real defect in the test, worth recording because it is a trap rather than a typo: the existing import helper asserted only that the status line did *not* say the confirm failed, which is true immediately — before the confirm has completed. Navigating to a second import then aborted the first one's in-flight request, and the lost rows surfaced much later as a puzzling count in a different assertion. The helper now polls the database until the rows have actually landed. A negative assertion on a status line passes for a page that rendered nothing at all. The section rendered last on the page, after Recovery / 04. **Daily use decided it: moved above the import bench on 2026-07-29** at the owner's request, having found that reaching it meant scrolling the whole page. **Superseded 2026-07-31**: it has its own route, `/ledger`, and is what `/` opens on (D-061).
18. ~~**Import the remaining statements.**~~ **Done 2026-07-29 for fourteen of fifteen** (D-054). The ledger went from 55 rows to **1,232** across 14 batches: KBANK 89 rows from two statements, SCB **1,143 rows from twelve**, every one cross-checked against its own printed totals with every fingerprint recomputed server-side. Verified by querying the database, not by reading the importer's own output.

    **`KRUNGTHAI-01` was refused, then reconciled and imported the same day** — see task 23. Its printed totals agreed but its balance chain did not, at three rows on page 10 of twelve; the cause was printed order differing from balance order, not a reader defect (D-055).

23. ~~**Fix the Krungthai compound-row drop, then import that statement.**~~ **Diagnosed and fixed 2026-07-29 (D-055); the import itself landed with the other fourteen, and this task is complete.** The "open remainder" line stood here until **2026-08-21**, by which time it had been false for weeks — all fifteen statements are in the ledger, as this same file says a few paragraphs below. D-131 is about exactly this shape: a line that was true when typed, never re-read, and left contradicting its own neighbours. There was no compound-row drop. The premise this task was written on came from D-054 and was wrong in every part: the statement holds **zero** two-component rows, no reader ever sets the `interest-tax-order` marker, and the blocking row's gap runs `printed < expected`, the opposite of what was recorded. `lib/krungthai-layout.ts` was **not modified** — it had no defect, and its 48 tests pass unchanged.

    What was actually wrong: the statement prints an end-of-day interest posting first among its date's rows and applies it last, so the printed order is not the balance order. That single fact produced all three blockers — at that row, at the row after it, and at the first row of the next date. `reconcileRows` now repairs the order of a same-date run, but only where it would otherwise refuse, and only when exactly one ordering reproduces every printed balance; nought or several still refuse, and the search never crosses a date boundary. Rows are **submitted in the recovered order**, because `public.confirm_import` runs its own balance walk in PL/pgSQL and would otherwise refuse what the client accepted; each row keeps its printed page and row in `provenance`. Real document: 3 blockers to 0, one `out-of-order-run` warning, and the transcribed SQL rule passing on the applied order having failed on the printed one.

    **Imported 2026-07-29 and verified in the database:** 1,232 → **1,465** rows, 14 → **15** batches, the Krungthai account 0 → **233**. Driven by the owner through the app's own review screen after a confirmed backup, so the reordering was seen before it was committed. **All fifteen statements are now in the ledger.**

    Left behind by it: the review screen shipped with the reordering invisible (GOTCHAS), found by the owner at the confirm button rather than by the suite — every test passed while the surface showed nothing. Backup state is mutable and lives in `HANDOFF.md`, not here.
19. **Google OAuth + hosted Supabase + Vercel, as one move** (D-051). **Started 2026-07-30 on the parts that create nothing hosted.** `connect-src` is no longer a hard-coded loopback origin — it is derived from `NEXT_PUBLIC_SUPABASE_URL` at build time and falls back *narrower*, never wider (D-058), which removes one hard blocker and gives the CSP its first test coverage. **The routing split is done, 2026-07-31** (D-061): `/ledger`, `/import`, `/slips`, `/recovery`, `/` redirecting to the first, and the header, nav, footer and session control moved into the shell. That closes the half this task carried for the single page — on 2026-07-29 the ledger view had been moved above the import bench because reaching it meant scrolling past everything else, which was a plaster over exactly this. The gate was re-run in full; `app/ledger-app.tsx` is gone, split into `app/import-bench.tsx` and `app/recovery-bench.tsx`. **How the data moves is decided as of 2026-08-09 (D-083): by restoring the owner's encrypted backup into the hosted project, not by reimporting the fifteen statements.** That path is already rehearsed end to end (D-078), and it means transaction ids survive, so the typed layer — slip values, categories, match decisions — travels intact. It also settles the ordering: **this task goes after 22 and 21**, because work typed into the ledger before hosting is now durable, 22's remainder is where the migrations are, and a schema that has stopped moving is what should be relocated and security-reviewed once. **Both are now done — 22 on 2026-08-10 (D-084) and 21 the same day (D-088) — so this is the next task**, and the condition it was sequenced on is met: nothing has moved in the database since migration 014, and D-088 added no migration and no route. **One dependency of it is now discharged ahead of time** (D-089): the restore this task moves the ledger by is a **v4-into-v5** one, since live writes v4 on migration 012 and a hosted project starts on 014 — and that version pair is exercised end to end as of 2026-08-10. It needed no access to live, and it is **not** an argument for migrating live first: the hosted project takes 013 and 014 from this repository either way. Nothing in it is authorized: the OAuth sign-in path, hosting, the identity restore and a security review each need explicit approval at the time.

    **The "OAuth sign-in path" is four pieces, not one, and this was found by reading the source on 2026-08-10 rather than from any earlier plan.** Grepping `app/` and `lib/` for `createBrowserClient`, `signInWithOAuth`, `exchangeCodeForSession` and `mfa.enroll` returns **exactly one hit**: the enrolment call inside `app/api/v1/dev/session/route.ts`, which answers 404 unless the flag is set *and* the Supabase URL is loopback *and* the request's own Host is loopback. So the app has **no browser Supabase client, no OAuth call, no callback route, and no MFA surface of any kind** outside a route that cannot run hosted. What is needed: a browser client; `signInWithOAuth` plus a callback that exchanges the code for a session; **a TOTP enrolment screen**; and **a TOTP challenge screen** for every later sign-in. The last two are not optional and not small — `lib/server/supabase.ts` refuses any owner-bound route below `aal2` with two verified TOTP factors, Google returns aal1, and the only code that has ever enrolled a factor is the development route's. **The useful consequence was an ordering:** the two MFA screens depend on Google for nothing and are fully verifiable against the local stack, while only the Google button itself is blocked on real credentials — so the MFA half was built first.

    ~~**Still to do here, and needing no authorization:** the OAuth sign-in code path~~ — **built 2026-08-10 (D-091), and verified in a browser except for Google itself.** `lib/owner-access.ts` decides which of four states applies and knows nothing about React or Supabase; `lib/browser/supabase.ts` is this app's first browser client; `app/owner-access.tsx` renders Google sign-in, TOTP enrolment and the code challenge; `app/auth/callback/route.ts` exchanges the code for the cookie session, with a **constant** redirect target because that URL is handed to a third party by construction. The development sign-in stays exactly as it was and the browser suites still use it — the two are not alternatives, and its new `?stop=aal1` is what makes the enrolment and challenge screens reachable in a browser at all. **The rule worth not re-deriving:** the factor *count* is checked before the assurance level, because one verified factor already reaches Supabase's `aal2` while `lib/server/supabase.ts` still refuses every route for want of a second. Nothing hosted was created and nothing in the database moved.

    **The hosted Supabase project exists as of 2026-08-11 and carries migrations 001–014** (D-092), created by the owner and pushed on his instruction after he ran `supabase login` and `supabase link` himself. It is empty on every one of its 23 tables, no identity is bound, and an anonymous caller is refused at the **grant** layer rather than by RLS — which is what turning off "automatically expose new tables" bought, and it matters because migrations 011–014 create their tables *after* 002's blanket revoke and none of them revokes `anon` select. The repository was unlinked afterwards. Backup state and authorization state live in `HANDOFF.md`.

    ~~**What that leaves undone here, and it is all owner-gated:**~~ **All of it is now done — this task is complete as of 2026-08-12** (D-096), and the paragraph is kept for the ordering it records rather than as outstanding work. Google itself — the Cloud project, the OAuth client, the registered redirect URL and the first real sign-in — then the **one-shot owner binding** (`public.ledger_owners` is immutable, so a wrong identity means a fresh project), the identity restore, and the security review. `signInWithOAuth` hands the browser to a third party, so no test can reach past it. **The binding must come after the first Google sign-in**, because `bind_ledger_owner` refuses unless the `auth.users` row has a confirmed email and `provider = google`. Then the parts that do need authorization: creating the hosted Supabase project, the identity restore, and a real security review. OAuth gates hosting because the development sign-in refuses non-loopback requests. **It also gates task 20**: share-to-app means sharing from a phone, and the development sign-in refuses non-loopback requests — so 20 can be *built* locally but not *used* until this lands. Routing did move one piece of 20 forward: the share service worker now registers in the shell, so any visited route arms the interceptor rather than only `/slips`.

    **The deployment landed 2026-08-12** (D-096), and one thing had to be fixed before it could build at all: `pnpm-workspace.yaml` was one of the deliberately local-only files, so a clone of `HEAD` — which is exactly what a hosted build starts from — failed its install. It is committed as of 2026-08-11 (D-095). The hosted surface was then checked from the public internet rather than from this machine: the served CSP names the hosted Supabase origin and its WebSocket counterpart, an anonymous owner-bound route answers 401, the development sign-in answers 404, and a request made directly against the hosted Supabase REST endpoint with the publishable key — bypassing the app entirely — is refused at the **grant** layer on every table tried, before RLS is consulted. **What that check is for:** locking the app does not lock the database, and the two are separately reachable by design.
20. ~~**Slip capture via share-to-app**~~ **Built 2026-07-30 (D-056), and usable from a phone as of 2026-08-12**, once task 19 gave it a non-loopback origin — the owner drove a capture from his own phone against the hosted app (D-096). It had been unusable for the thirteen days between, which is the cost of the ordering D-083 chose. QR for identity, owner confirms the amount, categorise. No OCR — that is task 21, and the form is identical either way, which is why D-050 ordered the manual half first.

    What landed: `lib/slip-qr.ts` (the EMVCo TLV grammar, its CRC, and a payload *builder* so every fixture is invented rather than copied), `lib/slip-scan.ts` (the retry ladder), `lib/slips.ts` (the wire contract), migration 011 (`public.slips`, `capture_slip`, and the backup version move), `app/api/v1/slips/route.ts`, `app/slip-capture.tsx`, and the share target in `public/manifest.webmanifest` plus `public/share-slip-sw.js`.

    **The grammar was derived structurally and then verified against reality.** D-053's sizing is confirmed in every part: three banks (KBANK 5, Krungthai 9, SCB 9), Krungthai's two reference lengths, and exactly 3 of 23 decoding only at 2× upscale. Beyond it, the payload is a nested TLV — outer `00`/`51`/`91`, inner `00`/`01`/`02` — whose tag `91` is a **CRC-16/CCITT-FALSE over everything preceding it, matching 23 of 23**. That is what licenses treating a CRC mismatch as a refusal, and it makes the dedup key tamper-evident. `readSlipQr` accepted all 23 real payloads and rebuilt each byte-identically. The probe was a throwaway; it reads real slips and cannot be a test.

    Three design points worth not re-deriving. **A slip carries no `account_id`** — the QR names a bank, and only the statement will say which account, so binding at capture time would invite a wrong answer for no benefit. **Capture is idempotent on (bank, reference)**, because share-to-app makes double capture the expected accident rather than an unlikely one; a re-share is reported plainly and never overwrites a confirmed amount. **The image is never uploaded**, which is why share-to-app needs a service worker rather than a plain POST target — see D-056 for the residual exposure that leaves for task 19.

    **The decoder was reopened the same day (D-057) and the result matters.** `BarcodeDetector` does not exist on Windows or Linux desktop — measured across bundled Chromium and installed Chrome, headless and headed, with the relevant flags — so capture could not run, or be verified, on the owner's own machine. The app now prefers the native reader and **dynamically imports a bundled one when it is absent**, so the phone never downloads the 1.1 MB WebAssembly build. Three candidates were benchmarked in a real browser over all 23 real slips: all read 23/23, and ZXing needed the 2× upscale on **none** where OpenCV needed it on three. The browser specs consequently **stub nothing** — they render a real QR to a real PNG from an invented reference and let the app's own decoder read it.

    **Verified against reality 2026-07-30/31.** The owner put two real slips through the form on this desktop — an SCB bill payment and a Krungthai one — and both decoded correctly, with the Krungthai date pre-filling from its QR (D-059). That closes the last verification gap in task 20; generated fixtures carry no camera noise, and until this nothing had proved the path end to end on a real image.

    **A captured slip is visible in the ledger as of 2026-07-31** (D-062). Task 20 shipped a write path with no read path — the owner captured a slip, opened the ledger, and found nothing, while `GET /api/v1/slips` had existed since migration 011 with no caller. The ledger view now interleaves provisional slips with confirmed rows, keeps their money out of every confirmed total (a slip and its statement row are two records of one transaction until task 22 matches them), gives them no balance, and shows them in all-accounts mode only, since a slip carries a bank and not an account.

    ~~One limitation remains, deliberate and recorded, and D-062 makes it visible rather than fixing it: a captured slip is **append-only with no correction path**~~ — **closed 2026-08-09/10** by migration 013 and D-084, using exactly the overlay pattern this line named. The slip stays append-only; the correction is a separate overlay with an append-only history, and `app/correction-form.tsx` is the way to make one. And one defect is open rather than fixed: **three real slip references are committed as test fixtures** (D-060), which breaks `docs/FIXTURE_POLICY.md`. The owner accepted the exposure; replacing them with invented values of the same shape is a few minutes' work whenever it is wanted.
24. ~~**Backfill dates onto the undated traps in `GOTCHAS.md`.**~~ **Done 2026-08-10** (D-085). All 95 traps now carry a date and **`pnpm check:docs --strict` passes with no warnings**, which is what closes this rather than a judgement that enough had been done; `--strict` is now in the documented validation order, so a new undated trap fails the gate instead of starting another backlog. 53 dates were recovered from the commit that introduced the code each trap is about and say so in the form `Dated <date> from <sha>`, which the file's header explains is weaker than an ordinary date — it marks when a trap became true, not when it was last confirmed still true. **The last two were verified live instead of backfilled**, because the history could not answer them: the project root's ACL owner is the logged-in user, so that trap's condition is not currently present at all, and the untracked-file one was demonstrated with a probe file that `git diff` could not see. **No date was invented** — the rule this task was written on held, and the two that had no commit evidence got a real check rather than a plausible number.

21. ~~**OCR pre-fill.**~~ **Done 2026-08-10** (D-088) as a *finder*, and **superseded on 2026-08-18 by task 36** (D-129), which finally makes it the pre-fill its title always named. **Everything below describes a shipped state that no longer exists** and is kept for the reasoning rather than the facts: the local engine, its self-hosted assets, the `Enlarge the amount` control and the assertion that `setAmount` never appears in the reader were all deleted or reversed when slip capture adopted Cloud Vision. `app/slip-capture.tsx` now reads through `POST /api/v1/ocr/read` and fills the box from `proposeAmount`. **What carried over unchanged is the part that mattered**: `parseThb`, the two-fractional-place rule and blank-on-failure still sit downstream of the engine, so no machine-read digit reaches a stored value except through the strict grammar. The record of the task as it shipped follows.

    ~~It ships as an amount **finder**, not a pre-fill, because D-087 measured that a machine-read digit cannot be trusted into a stored value. `lib/slip-ocr-engine.ts` is the browser module and the only file that names tesseract.js; `app/slip-capture.tsx` gained an **Enlarge the amount** control that locates the amount, crops it with padding and shows it enlarged beside the field, and **never fills the field in** — `tests/privacy.test.ts` fails if `setAmount` ever appears in that function. All four assets are served from `/tesseract/` on this origin, verified in a real browser along with the CSP; `cacheMethod: "none"` keeps the language model out of IndexedDB; the QR retry ladder is deliberately not reused.~~ **Re-measured on the installed tesseract.js 7.0.0**, which is what D-087 left open: the amount was **located on 16 of 23** real samples with `LABEL_NOT_FOUND` on 7 and nothing ambiguous, each match under exactly one distinct anchor — so D-087's figures hold on the shipped version. What remained unmeasured on that engine was **accuracy**, deliberately: nothing there read a figure, so nothing there could be wrong about one. That is what D-128 went and measured on the engine that replaced it.

    **The month blocker is cleared and the printed-date reader is built, 2026-08-10** (D-086). All three layouts print the abbreviated Thai month with periods, measured by eye across four real slips under a fresh read grant, so one token table serves every layout — and three of its twelve tokens (`มี.ค.`, `เม.ย.`, `มิ.ย.`) defeat the obvious two-consonant pattern, which is the finding most likely to be re-derived the hard way. `readPrintedDate` reads the four-digit layouts and **refuses KBANK's two-digit year with its own code**, because completing it means assuming a century (D-031); that refusal is now the only thing between KBANK and a printed date, and it is a decision left open rather than an oversight. **Still no engine is installed and nothing new ships to the browser** — what follows is unchanged by any of this:

    **The engine question is settled, 2026-08-10** (D-087): tesseract.js ships **Thai only**, self-hosted, loaded on demand, and it is used to **locate** the amount rather than to read it — the owner reads a cropped enlargement, so no machine-read digit reaches a stored value. Measured payload **3,962,749 bytes**. Three measurements decided it, all over the 23 real samples: the ceiling is the Thai label at `LABEL_NOT_FOUND=7` **either way**, so English buys 2 slips for ~5 MB and cannot lift the ceiling; the QR retry ladder **does not transfer** and 2× upscaling broke 3 working reads while recovering 1; and digits are unstable about **1 time in 15**, with at least one figure passing the money grammar while wrong. Built so far: `locateAmount`/`paddedCrop` and their tests, the dependency, and `scripts/copy-tesseract-assets.mjs` wired into `prebuild`. **Not built: the browser module, the capture-form crop, a real-browser CSP check, and the privacy suite's new asset assertions.** The paragraph below is the framing this task carried while the question was open, kept because its cost reasoning is what the measurement answered.

    **The engine question as it stood before the measurement:** Two measurements would inform it and both need an engine: whether the 3 slips whose amount label failed are the same 3 that need a 2× upscale for the QR (D-053) — `lib/slip-scan.ts` already owns that ladder — and digit confusability, which is unmeasured and is about money. What is known: tesseract.js 7.0.0 read Thai on 14 of 14 samples and found the amount label on 11 of 14, and where it found the label the existing policy layer returned a usable amount 11 of 11 (D-066). What it would cost: the CSP already permits it (`script-src` carries `'wasm-unsafe-eval'`, `worker-src` allows `blob:`), but `connect-src` names only `'self'` and the Supabase origin, so **every byte must be self-hosted** in `public/` as `zxing_reader.wasm` (1,065,866 bytes) already is. The Thai traineddata and core are roughly an order of magnitude more, unverified in this repo and worth measuring before any dependency is added.

    **What OCR is for, stated so the scope does not drift:** the **amount**, which is the one field no machine-readable source carries. The date is already exact for 14 of 23 slips from the QR (D-059), and the counterparty is free text in two scripts and stays typed.

    The original framing of this task follows, unchanged except where struck above: OCR pre-fill into the capture form task 20 built, region-targeted per bank layout with a digit whitelist, never auto-confirming. **The sizing step is done, 2026-08-01** — see [docs/SLIP_CONTRACT.md](docs/SLIP_CONTRACT.md), measured against the real slips once the owner widened the read grant to cover them, which is what unblocked it. Its finding changes the design: **region targeting must be label-anchored, not fixed-fraction**, because SCB prints at least three transaction types of different heights and Krungthai's bottom block gains a row whenever a memo exists or a recipient name wraps. Fixed boxes would read the wrong line on a real slip — the same lesson D-024 and D-026 taught on statements. The per-bank label inventory and the value's position relative to each label are recorded there. Also recorded: **every layout prints a Buddhist year**, so a date read from pixels needs the 543-year conversion and must fail closed, which is the exact opposite of a date read from the QR (D-059) — the two sources disagree by 543 years on the same slip. **The date is already done, and without OCR (D-059):** 14 of 23 slips carry a Gregorian `YYYYMMDD` inside the QR reference — SCB 9/9, Krungthai's 21-character variant 5/5 — so it is read exactly, under the QR's CRC, rather than inferred from pixels. What remains for OCR is the **amount, the counterparty and the time** — the time is *not* in the QR, established from a real Krungthai slip whose printed 12:31 appears nowhere in the twelve digits following its embedded date. (A "still unsized" note stood here until 2026-08-05, contradicting the sizing step recorded above in the same item; it predated `docs/SLIP_CONTRACT.md` and was left behind when that landed.)

    **The policy half is built, 2026-08-05** — `lib/slip-ocr.ts` and `tests/slip-ocr.test.ts` (27 tests). It contains **no OCR engine and depends on none**, the same split `lib/slip-scan.ts` uses for the QR ladder (D-053): an engine has only to produce `OcrWord`s — a word and its box — and every rule about what to believe is testable without a browser or a language model. Built: line grouping by vertical *overlap* rather than a shared edge, because a Thai tone mark makes a taller box and two words on one line rarely agree on either; label anchoring that tolerates an engine splitting a Thai label across words and **refuses when a label appears twice** rather than taking the first; the strict money grammar, which **never repairs a doubtful character** — `1,25o.00` is refused, not corrected, because a corrected amount arrives wearing the same confidence as a correct one; and `gregorianFromPrintedYear`, which fails closed on an already-Gregorian year and refuses a two-digit one outright rather than assuming a century. **The load-bearing refusal is the fee**: on Krungthai and KBANK the fee is money on a nearby line, so the amount is found by its own label or not at all, with no fallback to the nearest number. Red-proved — adding that fallback fails the fee test and nothing else.

    **Still open, and it is the one decision here: which engine, and what it costs the phone.** No OCR engine is installed. Anchoring on Thai labels needs a Thai model, and self-hosting one under the strict CSP means roughly 10–15 MB of WebAssembly and traineddata in `public/` — an order of magnitude past the 1.1 MB ZXing reader that D-057 went out of its way to load only on demand. Worth separating before choosing: **the in-app pre-fill and the task 22 measurement are not the same job.** The measurement needs the amounts of 23 images once, which a throwaway local harness can do with nothing shipped and nothing bundled; the pre-fill is a convenience that ships to the phone forever and whose failure mode is a silently wrong amount.

    **It also measured what an OCR engine actually recovers, 2026-08-05** (D-066), which is the thing this task had wanted and never had. tesseract.js 7.0.0 with `tha+eng`, native resolution, default settings: Thai recognised on 14 of 14 images at a mean of 187 words, the amount's label found on **11 of 14 (79%)**, and a usable amount returned on **all 11** where the label was found. So `lib/slip-ocr.ts` works wherever the engine reads the label, and 79% is the figure an in-app pre-fill has to live with — fine for a value the owner reviews anyway, useless for anything automatic. **Untested and the obvious next step:** the 3 failures were not retried at 2× upscale, and D-053 measured exactly 3 of 23 samples needing that upscale for their QR. Whether they are the same 3 images is the first thing to check, and `lib/slip-scan.ts` already owns that ladder.

    ~~**This task now blocks the validation of task 22**~~ — **it does not, and that was wrong** (D-064 said so, D-066 corrected it 2026-08-05). The amount is unreadable *in the app* without OCR, but a local throwaway harness reads it with nothing shipped, nothing bundled and no cost to the phone. The measurement and the pre-fill were never the same job. Task 22's window is now measured; this task is back to being a convenience feature with no one waiting on it. Task 22's rule matches on bank, exact amount and date, and the amount is the one of the three that no machine-readable source carries — not the QR, and not any statement layout. Until OCR reads it, checking a slip against the ledger means reading its image by eye, which caps the evidence for the match rule at the six slips ever checked rather than the 23 samples held. When the amount can be read, the first thing to run is a sweep of all 23 against the live ledger, reporting only the distribution of nearest-candidate date distances, and to re-derive `MATCH_WINDOW_DAYS` from it.
22. ~~**Statement reconciliation** against captured slips, plus manual entry for cash.~~ **Done 2026-08-10** (D-084), against the synthetic project. **Half done, 2026-08-01** (D-063); **both open questions answered 2026-08-05** (D-064); **reconciliation itself complete 2026-08-07** — the rule, the stored override and the way to make one (D-067, D-068); the rest of the title — manual cash entry and the slip correction path — built across two days and recorded in the two paragraphs at the end of this item.

    **The data layer, 2026-08-09.** Migration `202608090013_cash_entries_and_corrections.sql` adds five tables and three RPCs: `public.cash_entries` with `create_cash_entry`, and two correction overlay/revision pairs — `cash_entry_overlays` / `cash_entry_revisions` with `set_cash_entry_correction`, and `slip_correction_overlays` / `slip_correction_revisions` with `set_slip_correction`, which is the omission migration 011 recorded in its own comment. Cash is its own table rather than a `source_transactions` row because that table's `fingerprint`, `source_date`, `account_id` and `post_balance_minor` exist to enforce that a statement row is imported at most once and that printed balances chain; a cash payment has no statement, so admitting it would mean nullable columns on all 1,465 real rows. Two features in one migration deliberately: each new owner-record table bumps the backup schema version and every older version must stay restorable forever, so shipping separately would have meant two bumps. The backup moves **v4 → v5, still restoring 2, 3 and 4**. Migration `202608090014_match_reads_corrected_amount.sql` then fixed a real defect the new pgTAP found: `set_slip_match` compared a slip's *original* amount, so a corrected slip could be accepted against a row its real figure contradicts. **013 was not amended** — an applied artifact is recorded, not editable, the same reason this file supersedes rather than rewrites.

    **The read path, the routes and the UI, 2026-08-10** (D-084), which is what makes any of the above reachable — until it existed, all three RPCs had no caller. Corrections are resolved **once, at the edge of the read path** (`slipsInForce`, `cashInForce`), so the match rule, the candidate list, the totals and the row all read the figure in force; migration 014 is the argument, because a read path applying corrections in some places and not others reproduces exactly the defect 014 fixed, with nothing to raise it. A cash entry is a ledger row that is **never matched** — no bank, so the rule's first fact has nothing to compare — and it is counted apart from `provisional`, because a slip is waiting for a statement and cash never will be. Null in an overlay means *not corrected*, which is forced by 013's storage rather than chosen: a correction can change a counterparty, note or time but cannot remove one, and putting every field back to the original clears the overlay, which is the undo. Built: `lib/corrections.ts`, `lib/cash.ts`, the `cash` row kind in `lib/slip-reconcile.ts`, `app/api/v1/cash/route.ts` (GET and POST), `app/api/v1/cash/[id]/correction/route.ts`, `app/api/v1/slips/[id]/correction/route.ts`, `app/cash-entry.tsx` on the ledger page, and `app/correction-form.tsx` opened from the row it is about. `GET /api/v1/slips` now carries `corrections` on the same response as `slips` and `matches`. **Live is still on migration 012** — 013 and 014 are on the synthetic and recovery projects only, and reaching live needs its own explicit authorization and a fresh backup taken first, exactly as 011 and 012 each did.

    **Built:** `lib/slip-reconcile.ts` matches a slip to a statement row on bank, amount equal to the minor unit, and a date **within one day**, considering only the candidates **nearest in date**, and only when that is mutually unique — the slip has one nearest candidate and that row has one nearest claimant. The nearest-date clause is measured rather than chosen: over the real ledger's 1,465 rows, 16.3% share a bank and exact amount with another row within three days, and restricting to the nearest date returns that to the 6.5% same-day floor, so the window's tolerance costs no ambiguity — which is what makes the window itself cheap to move in either direction. Six real slips were then checked against the live ledger — three matched a single same-day row, two had no candidate, and one found two identical same-day rows, which is the coin toss the rule refuses.

    **The window narrowed from three days to one on 2026-08-05** (D-064), on the owner's judgement that his banks post same-day — which is what the four slips that produced a candidate at all showed. It is tolerance for the clocks rather than the bank: the slip's date is the owner's local date and the row's is the bank's posting date, so an evening payment can straddle midnight between them with no lag. The `Statement only` chip came off the row in the same change: it landed on essentially all 1,465 rows and so carried no information, and now lives as the status cell's accessible name plus a **Status filter** in the ledger controls. The status value itself is unchanged.

    **The window is measured as of 2026-08-05** (D-066), and it holds. Of the 14 samples whose QR carries an exact date, 11 amounts were read by a local throwaway harness and 9 had a ledger candidate: **8 posted on the slip's own day, 1 sits 11 days away**, and no case had more than one row at the nearest distance. The outlier is judged a coincidental same-amount row rather than a late posting — no Thai interbank transfer posts eleven days late, and D-063 measured 27.8% of these 1,465 rows sharing a bank and exact amount with another row within seven days, so a stray hit at that range is what chance looks like when the true row is not imported (two slips had no candidate for exactly that reason). **The useful part: at any window reaching eleven days that slip collapses onto an unrelated row and moves the totals; at one day it correctly stays unmatched.** Same-day posting is confirmed; `MATCH_WINDOW_DAYS = 1` now rests on measurement rather than judgement. Limits: n is 9, and the outlier is inferred rather than resolved. A matched pair collapses onto the statement row, which keeps its printed balance and takes the slip's counterparty; unmatched slips stay their own rows; ambiguous ones are reported as needing review rather than guessed at. Every row carries one of four statuses in words. **The totals now include slips**, which is only safe because a matched payment is one row. A slip is shown against an account when the owner holds exactly one at that bank — no reversal of D-056, since with one account there is no choice to make. Reconciliation runs over the whole ledger before any filter, so a search cannot move the totals.

    The fact that shaped it: **no layout prints the slip's reference** (Krungthai and KBANK none, SCB a short channel code), so a match is a proposal from three facts, never an identifier the two records share.

    **Built 2026-08-05** (migration 012, D-067): the owner can override or undo a match, and the decision is stored, audited, revisioned and carried by the backup. ~~That needs a column on `public.slips`~~ — **it does not, and that sentence was wrong**: migration 011 put `slips_immutable` on that table, so no column on it can ever be updated (`GOTCHAS.md`, which contradicted this line from 2026-08-01 until it was corrected). It is the `transaction_overlays` + `overlay_revisions` shape instead — `public.slip_match_overlays` for the current decision, append-only `public.slip_match_revisions` for its history, `set_slip_match` as the only write path — plus **backup schema v4 keeping v2 and v3 restorable**. A partial unique index makes it impossible for two slips to claim one statement row, which the read-time rule could only ever enforce as a proposal.

    ~~**Still unbuilt in this half:** the RPC has no HTTP route and no UI~~ — **built 2026-08-07** (D-068), which closes this half. `PUT /api/v1/slips/[id]/match` is the RPC's only caller, `GET /api/v1/slips` now returns the stored decisions **on the same response as the slips** so a pairing the owner overruled can never be shown as the rule's, and the ledger row carries the control: `Not this slip` on a matched row, and `Choose a statement row` on a provisional one. The candidate set is deliberately **not** the automatic rule's — it re-checks the bank and the exact amount that `set_slip_match` re-checks server-side and drops the date window entirely, because reaching past that window is what an override is for. No migration; the database is exactly as 012 left it.

    **Choosing is done in the table, not in a dropdown** (D-069, the owner's call after driving it the same day). A dropdown labelled its options with the date and description — the very fields that are identical across candidates on a real statement — while omitting the printed time and running balance, which are the only things that tell them apart. `Choose a statement row` now filters the table to the slip and its candidates, each carrying `This is it` on its own row, with the other filters and Reload suspended so nothing can hide the answer, and the totals strip hidden because a slip plus two candidates has no total worth printing. The 25-row cap went with the dropdown: table rows have no length problem, and a cap that hides the right row is worse than a long list.

    **A `/code-review` pass over the uncommitted work then found five defects in this UI, all fixed the same day** (D-070). Two were about money: a candidate row already paired with *another* slip was offered with its status chip replaced by the button, so taking it returned that slip to provisional with only a green edge to say so; and two decisions could be in flight at once, which produced a spurious "changed in another session" for an owner with one tab open. One could have stored a mis-attributed match — the per-account layout hides which account a candidate belongs to, while candidates are chosen by bank. Two were about being usable at all: focus was dropped to the top of the document on entering the mode, and the mode could be left half-on with no way out but a reload. **Four of the five are invisible to axe and to any assertion that only reads text**, which is what the new focus assertion and the "already another slip's" spec exist for. A sixth finding is open and is the owner's: `playwright.config.ts` reuses a server it finds on port 3000 instead of starting its own pinned one, so a stale server aimed at `.env.local` would put the browser suite's writes into the real ledger.

    **The fee question is settled, 2026-08-07** (D-071, D-072) — the last open design question about the match rule. Fees are posted as their own statement rows and every one of the 1,465 real rows carries a single component, so no transfer row hides a fee inside its amount; and a fee-bearing transaction never produces a slip, which `docs/SLIP_CONTRACT.md` corroborates by recording `Amount` and `Fee` as separately labelled fields on the slip itself. **Amount equality to the minor unit therefore stays, with no residual attached.** Nothing is pending here; D-071 records what loosening the manual path *would* have looked like, for a case that does not arise.

    **Both read paths the owner asked for landed 2026-08-09** (D-075), after he captured his first real slip into the live ledger and asked where the confirmation was. The slips route now **lists what it has captured** — refreshing itself after each capture once shown — and a verified ledger row can be **expanded to show the slip beside it**, including the slip's own amount next to the row's movement, which is what makes "verified by slip" checkable rather than something to take on trust. Third time this shape of gap has been found by using the app rather than by a test.

    ~~Still unbuilt beyond that: manual entry for cash, and the **correction path** the capture form has never had~~ — **both built 2026-08-09/10** by migration 013 and D-084, using the overlay pattern this line pointed at. Struck rather than deleted because it is what the two paragraphs above were written against; the line survived D-084's edit and contradicted this item's own header until 2026-08-10.

25. **Automatic categorisation and description, by local LLM.** Proposed by the owner 2026-08-10 and scoped the same day. Nothing is built and nothing is measured.

    **Its storage needs no migration**, which is what sizes the task: `public.transaction_overlays` has carried `category_id`, `description`, `counterparty`, `note` and `effective_date` since migration 001, with `mutate_overlay` as the single write path, append-only `overlay_revisions` for history, and both routes already serving — `GET /api/v1/categories` and `app/api/v1/transactions/[id]/overlay/route.ts`. The overlay layer is built and has never had a user: live holds **1 category against 1,465 transactions**. What is missing is a UI and the model. Applying it *after* the row is posted is not a compromise but the pattern the schema was built on — the imported row stays immutable and the typed layer sits beside it.

    **The owner chose automatic over approve-first, 2026-08-10**, on the row count: 1,465 rows is not a list anyone clicks through. That choice is what adds the one migration it needs — a proposal applied without being read has to stay **distinguishable from the owner's own typing**, or a machine guess and a decision become the same record and neither can be re-reviewed later. ~~Provenance belongs on the overlay rather than in a new table, which is the cheaper of the two shapes~~ — **reversed on 2026-08-12 by D-097, which settled the open question this sentence recorded.** Provenance gets **its own table**, because a new column on an existing table is no longer permitted: the export serialises whole rows, every row schema is `.strict()`, and all four backup versions share one row schema per table — so a column breaks the *current* export immediately and the obvious repair silently breaks v2, v3 and v4. A new table costs one join and one more table in the backup; a new column costs per-version row shapes across three layers. Two tests in `tests/backup.test.ts` now fail if anyone tries. The version question this sentence called unresolved is therefore answered: **adding the provenance table moves the backup version, exactly like every other new table has.**

    **The precedent that shapes it, and where that precedent stops.** D-087 refused to let a machine-read digit reach the amount field, because a wrong amount arrives wearing the same confidence as a right one. That refusal does **not** extend here: a category is not money, a wrong one moves no total, and it is recoverable. What carries over is the record-keeping, not the refusal.

    **The CSP decides where the model runs, and hosting decides where it can keep running.** `connect-src` names `'self'` and the configured Supabase origin and nothing else (`lib/security-headers.ts`, whose stated rule is that widening the policy is never how a deployment problem gets solved), so a model on a loopback port is a third origin: call it from the app's own server, never from the browser. **That does not survive task 19** — hosting puts the server in the cloud, where it cannot reach a model on this desktop. So categorisation is a desk job run locally against the same project the hosted app reads. Decide that deliberately rather than discovering it after hosting.

    **Measure before building**, the way D-066 and D-087 each settled their own engine question: run the installed model over the real descriptions, score a sample by hand, report counts only, ship nothing. The unknown is not the model's size but the data — Thai counterparty names and heavily abbreviated bank descriptors.

    **Before the model, look at Firefly III's rules engine** — raised by the owner 2026-08-12 and recorded here so it is not re-derived. Firefly III is a self-hosted personal finance manager, and the one thing worth taking from it is its deterministic rule shape: *if the description matches this, set that category*. **It is the baseline this task should have to beat.** A rule is cheaper than a model, predictable, reviewable, and needs no provenance table to explain itself, so if it categorises most of 1,465 rows the model's job shrinks to the remainder — which is a different and much smaller task than the one scoped above. Measure the rule baseline first and the model against it, rather than the model against nothing. Bound the look to that one question: **integrating Firefly III itself was considered and rejected the same day** — it is a separate PHP application with its own database, its strongest feature is bank-aggregation importing that this project deliberately refuses (the owner screenshots a LINE card instead), and adopting it would mean discarding the append-only, audited, versioned-backup architecture this repository is built on.

26. **Item-level records — what was bought, not only what was paid.** Proposed 2026-08-10 and **deferred the same day by the owner**, until the app is usable in its intended form. Recorded so its shape is not re-derived when it comes back.

    **It is the largest item on this list and, unlike 25, almost all of it is new.** Item rows are new owner-record tables, so the backup goes **v5 → v6 with 2, 3, 4 and 5 staying restorable forever** — a migration, new pgTAP contracts, and a new restore version pair to prove, exactly what 013 cost. It also opens a document class nothing here handles: task 13 already established that the statement reader cannot help, because every diagnostic in it works on a pdf.js text layer and a receipt photo has none. Reading items off an image needs a vision model, which is a different and far less reliable thing than the text model 25 needs.

    **The owner's own observation is what should shape it: the item vocabulary saturates.** He expects under roughly 30 days of ordinary use to cover most of what he repeatedly buys. If that holds, the model's job stops being "extract arbitrary items from an image" and becomes "match against a list of things already known", which is easier and far more checkable. That splits the task — **build the item store and manual entry first, use it for a month, and add the model only once there is a list to match against.** The cheap half is worth having alone, and the expensive half gets easier the longer it waits.

27. **Capture a bank's LINE push notification as a payment record.** ~~Not done.~~ **DONE 2026-08-13** (D-102), in seven parts across two days. **The ledger view and the reconciliation landed last** — `lib/notification-card-reconcile.ts` matches a card on the account it was bound to, the exact amount, a one-day window and **the printed balance**, which breaks the tie the amount cannot and refuses to pair when it disagrees with every row that otherwise fits. `app/transactions-view.tsx` collapses a matched card onto its statement row, shows an unmatched one as its own row carrying its printed balance, and counts cards apart from provisional slips and cash. Two owner browser specs drive it end to end. **What the task deliberately does not include**: a stored match decision and a correction overlay for a card, which need their own table and are designed together for one migration. Earlier parts, built 2026-08-12: the per-layout year and account-digit rules in `lib/notification-card.ts`, which know nothing about the database; **the migration** — `public.notification_cards`, its computed-fingerprint identity, `capture_notification_card`, and the backup at **v6** (D-098, migration 016); **the printed grammar** — where every field sits, looked up per layout *and per direction*, with direction itself read from two signals that must agree (D-099, `docs/NOTIFICATION_CARD_CONTRACT.md`); **the reader** — `lib/notification-card-ocr.ts`, which splits a screenshot into its cards and returns a **box per field** for the owner to read from, storing no machine-read digit (D-100); and **the capture form and its route** — `app/notification-card-capture.tsx` on the slips page and `POST /api/v1/notification-cards`, which is also the only layer that checks the printed digits actually resolve to the chosen account (D-101).

    **The route carries a check the database does not, and that is deliberate on both sides** (D-101). `capture_notification_card` checks the channel against the account's bank and stops; it stores `account_id` and `printed_account_digits` side by side so a row records what was read as well as what it was mapped to, which is exactly what makes a wrong mapping storable. The route refuses one, per layout, and the table is append-only so it is the last place that can. The distinguishing test is the KBank pair — the naive last-four value refused, the offset value accepted, against one account — so a global rule fails the suite rather than failing silently forever. **The form keeps D-099's two direction signals independent**: the words come from the image and the sign from a control that starts empty, so the cross-check compares two readings rather than a value with itself.

    **The reader was measured over the real screenshots on 2026-08-12 and the measurement changed its shape twice** (D-100). Counts only, through a throwaway harness under `.runtime/` since deleted. **A screenshot is not a card** — the six that carry cards carry two each, so reading an image as one card meets every label twice and refuses nearly every field. And a Krungthai `ประเภท` row carries a free-text phrase **containing the direction word**, which split one real card into two and pointed the survivor's amount crop at the wrong row; anchoring every label and direction word at the **start** of its row closes that class. This is D-099's nested-word defect for the third time and the first to arrive from data rather than from another layout's label. Result after both: **two screenshots yield zero cards and both are the in-app lists this task excludes**, the other six yield exactly two cards each for twelve in total, and **every field the layout prints was located on all eight cards whose channel only one grammar accepts**. The other four sit on the two images both SCB Connect and KBank Live accept — the identical-incoming-title limit, showing up as predicted — where some labels were not recognised, which is D-087's ceiling rather than a policy failure. **What the reader does not do is read a digit**: the amount, the balance, the timestamp and the account digits are all typed by the owner, which is D-087's rule binding wider here than on a slip because a card stores four digit-bearing fields rather than one.

    **The 2026-08-12 structural read took two passes over twelve cards**, and the second one — three screenshots asked for by name — overturned two conclusions the first had drawn. What the first pass found: direction is printed **in words** on every layout as well as signed on the amount, so it can be cross-checked; a screenshot carries **two clocks** and only the one inside the card is the transaction; the `จากบัญชี` label names the owner's own account outgoing and the *counterparty's* incoming; and field order, labels and row count differ per layout *and* per direction, so a reader must key on labels and never on position. What the second pass corrected: the collision is **not Krungthai's alone** — SCB Connect does the same, invisible while only outgoing SCB cards had been seen; and SCB Connect **does name a counterparty on incoming cards**, which retired a per-layout flag saying it never does. One direction of Krungthai also turned out to have **two label variants**, a transfer to a bank account and one to a wallet.

    **What is still unmeasured**: a card captured while a hold was outstanding, so *available* and *remaining* have never been seen to diverge — asked for, and the owner is unsure such a card exists for these accounts, which is a useful answer in itself and is why the balance stays a tie-breaker rather than identity. Two further limits are structural rather than fillable: **nothing in a card's wording distinguishes SCB Connect from KBank Live** in the incoming direction, since the titles are identical, so the channel comes from the LINE conversation; and the label variants above are certainly not all of them, so an unrecognised row must refuse rather than be guessed at. **Also deliberately absent**: a card has no correction overlay and no stored match decision, and each of those needs its own table and therefore its own version bump — design them together rather than one at a time. Proposed and scoped 2026-08-11, and it was **sequenced after task 19** because it needs a new table and therefore moves the backup schema version — the thing 19 was ordered last to stop doing. **Task 19 closed on 2026-08-12, so that condition is met and this is buildable.** **Its one load-bearing unknown is now measured** (2026-08-12): the card's balance equals the statement row's exactly, 6 of 6 across all three layouts — see the balance bullet below, which also records what that measurement does *not* license. Everything else here is still design rather than evidence.

    **Why it exists.** Not every payment produces the QR-bearing e-slip task 20 reads. When one does not, the bank's own LINE channel still pushes a card carrying the transaction, and the owner screenshots it. **He reports this happens several times a week**, which is what moved it from "type it in as cash" to a built path — the cost of the manual alternative is paid weekly, not occasionally.

    **Three card layouts, one per bank, and they are not interchangeable.** Read from `receipts_sample/line/` on 2026-08-11 and re-read on 2026-08-12 under separate read grants; only shapes and counts are recorded here, never values, per `docs/FIXTURE_POLICY.md`. **The sample is smaller than the file count suggests**: five screenshots, of which **two are the in-app lists this task already excludes** — one SCB EASY deposit inquiry, one Krungthai Connext account detail — and both re-confirmed the exclusion by carrying no per-row balance, with the Krungthai one printing no year on its rows at all. The remaining **three screenshots carry two cards each, so six cards, two per layout**, and both cards in a screenshot share an account and a day.

    | Source | Printed date shape | Balance | Account digits shown | Counterparty |
    | --- | --- | --- | --- | --- |
    | SCB Connect | `dd/mm/yyyy hh:mm` — Gregorian, **four digits** | yes | **last four** | no |
    | KBank Live | `d <thai-month-abbrev> yy hh:mm` — **two-digit Buddhist** | yes | **digits 6–9 of ten, final digit masked — NOT the last four** | no |
    | Krungthai Connext | `dd/mm/yy hh:mm` — **two-digit Buddhist** | yes | **last four** | **yes** — type, source account and sender, or destination |

    **The bank's in-app transaction lists are deliberately out of scope**, and that is a decision rather than an omission. Two more screenshot kinds exist — SCB EASY's deposit inquiry and Krungthai Connext's account detail — and both are month-long lists rather than single transactions. Neither prints a per-row balance and neither carries a totals block, so there is **nothing to cross-check a misread row against**: the two guarantees that make a statement importable (`confirm_import`'s balance walk and D-033's printed-totals check) both have nothing to work with, and a dropped or misread row would fail *open*. Krungthai's list is worse still — its rows carry no year at all, only the month tab does. Those transactions arrive as a real statement anyway, which is already imported.

    **Four problems, and three of them already have their answer in this repository.**

    - **No reference number on any card**, which is what `public.slips` is built around: `slip_reference text not null` with `unique (owner_id, bank_code, slip_reference)` is what makes re-sharing a slip a no-op, and the QR's CRC is what makes that key trustworthy. A card has neither. **The precedent is the statement importer**, which faces exactly this — a statement row carries no bank-issued id either — and answers it with `private.row_fingerprint` over `private.normalize_source_text`. A computed fingerprint over bank, account last four, amount, timestamp and balance is the same shape. **Record what it is not**: a fingerprint is idempotent, not tamper-evident, which is strictly weaker than what a CRC-protected reference gives a slip.
    - **Two of the three print a two-digit Buddhist year**, and `readPrintedDate` refuses that today. **The machinery to resolve it already exists and is already proven**: `resolveStatementEra` in `lib/dates.ts` reads the year in both calendars and keeps whichever falls inside a plausible window, and because the two readings are always 543 years apart a window narrower than that admits exactly one — a test walks all 100 two-digit years and shows the ambiguous branch unreachable. A card is days old, so its window is far tighter than a statement's 20-years-back. What `readPrintedDate` lacks is not arithmetic but an anchor: a slip has no period-end frame, and a card's anchor is its capture time. **The era rule must be per-layout, never global** — SCB's cards are four-digit Gregorian while the other two are two-digit Buddhist, and applying one rule to both reproduces D-031, where `2500 + 26 − 543 = 1983` dated a whole statement 43 years early and parsed cleanly the entire way.
    - **The balance is the ledger balance — measured 2026-08-12, and it is exact.** This was the owner's own answer on 2026-08-11 and it is now evidence rather than a claim. **All six cards matched, 6 of 6, across all three layouts**: for each one a transaction exists in the hosted ledger at the same account, the same date **and the same time**, carrying a balance equal to the card's to the satang. Not close — equal. That makes it the most valuable field on the card and it should be stored and used, for three separate jobs: it **breaks the tie the amount cannot**, since D-063 measured 16.3% of rows sharing a bank and exact amount with another within three days and the rule currently refuses that coin toss outright, while two same-amount rows never share a running balance; it is a **fail-closed cross-check**, because a card matching on amount and date whose balance contradicts the row should refuse rather than pair; and it strengthens the fingerprint above. **A second finding came free and strengthens the fingerprint further:** the card's printed timestamp equals the statement row's on all six, so account plus date plus time already locates the row and the balance then confirms it. **Limits worth keeping**: n is 6 across 2 cards per layout, both cards in each screenshot share an account and a day, and none was captured while a hold was outstanding — two layouts label the field *available* balance and the third *remaining* balance, which diverge at many banks exactly when a hold exists. So the balance is proven for the ordinary case, which is enough to make it a tie-breaker and a cross-check and **not** enough to make it identity.
    - **Account matching must be per-layout, and assuming otherwise fails silently.** Found on 2026-08-12 by the owner, after a first pass of this measurement wrongly reported the two KBank cards as unmatchable. SCB Connect and Krungthai Connext both print the **last four** digits, which is what `public.accounts.last_four` stores, so those match directly. **KBank Live does not**: it formats the account as `xxx-x-xxxxx-x` and reveals digits **6–9** while masking the final one, so its visible group overlaps the stored last four by three digits and sits one digit off. Matching a KBank card on `last_four` therefore hits nothing, on every card, forever — and it fails as *no such account* rather than as an error, which is the worst available shape. Once matched on the correct account both KBank cards agreed on timestamp and balance like the other four. **This is the same lesson as the era rule below**: one layout's convention applied globally reproduces D-031, where a single wrong assumption dated an entire statement 43 years early and parsed cleanly the whole way.
    - ~~**It needs its own table**, so the backup goes **v5 → v6 with 2, 3, 4 and 5 staying restorable forever**, with new pgTAP contracts and a new restore version pair to prove.~~ **Done 2026-08-12** (D-098, migration 016): the table, 26 pgTAP contracts, and the v5-into-v6 pair run end to end into a separately bound project. A card is not a slip (no reference, no QR payload, and `slips_immutable` forbids retrofitting one) and not a cash entry (cash is never matched, and a card certainly will be). **The fingerprint carries the balance, and that is the load-bearing part** — without it two payments of the same amount at the same minute are one row, proven by removing it.

    **What carries over unchanged**, which is most of the surface: the capture form and its confirm-the-amount posture (task 20), the OCR amount finder and D-087's rule that the owner reads the figure rather than the machine storing it (task 21), the abbreviated Thai month table measured across all three layouts (D-086), and the whole reconciliation path that collapses a captured payment onto its statement row (task 22). **OCR should also work better here than it does on slips** — these are screenshots of rendered text at native resolution rather than photographs of paper — but that changes the odds, not the rule: D-087's argument was about consequence, since a wrong figure arrives wearing the same confidence as a right one.

28. **Make the app comfortable on a phone, and review the desktop layout with it.** Raised by the owner on 2026-08-12, immediately after the first real phone use, and **unscoped on purpose** — the observation is that the surfaces work but do not yet read well at that width, not a list of defects. **Measured and first pass applied 2026-08-21** (D-136); more remains, listed at the end.

    **The measurement is what unblocked it**, and it needed writing because the committed owner config is **desktop-only** — so every signed-in surface, including both slip forms, had never once been rendered at phone width by anything. `.runtime/mobile-audit.spec.ts` signs in, walks the four routes at 390 CSS px, screenshots each, and reports two objective things: any element wider than the viewport, and any control under 44×44. Throwaway and re-runnable.

    **Nothing was broken, and that is the finding that scoped the rest.** No route pans sideways; the single over-wide element is `.stage-nav ol`, which sits inside its own `overflow-x: auto` and is meant to. What was wrong is that the page *read* badly, which is precisely the observation the task was opened on and precisely why it could not be actioned until something looked.

    **What the numbers said, and what changed.** The `h1` floor was `12vw` — **47px at this width, a quarter of the viewport before a word of content** — set against a desktop clamp where the floor never binds; now `clamp(29px, 8.4vw, 40px)`, with the ceiling still governing every larger screen. The first control on `/ledger` sat roughly **1,200px down** on an 844px-tall viewport, behind an intro and a bench paragraph; the prose now tightens rather than disappears, because it is worth reading once and deleting it would remove the explanation from the one screen a first-time reader most needs it on. **Every nav link was 36px tall and "Sign out" was 49×20** against the 44×44 both platform guidelines settle on — the two links inside sentences grew by `padding-block` cancelled with a negative `margin-block`, so the hit area grows without the text moving. And the active-route marker **rendered as a detached rounded box**: `box-shadow: inset 0 -2px` is clipped by the link's `border-radius`, so the underline curved up at both ends; it is a bottom border now. `/ledger` came out ~13% shorter and every control clears 44px.

    **That gap was named here and closed a day later, the hard way** (D-138). This paragraph used to say the audit loads no data, so the ledger table had never been seen at phone width with rows in it — and **the owner hit exactly that on his own device within a day of the deployment**: `.ledger-table { min-width: 1160px }` survived a phone override written as `table { min-width: 0 }`, because an element selector cannot reset what a class set, and `.table-scroll { overflow: visible }` in the same block had removed the container holding that width in. The page rendered zoomed to about a third. A second overflow sat behind it — `.ledger-controls` at `1fr 1fr`, whose `auto` floor is min-content, so a select holding an account label refused to shrink and the grid came to 591px inside a 358px `main`. Both fixed; the audit now **seeds rows, loads them, and asserts the table is on the page** before believing anything it measures, so it can no longer report a clean page it never saw.

    **What remains.** The captured-slips list and the batch worklist are still unmeasured with rows in them — the audit loads what a button offers, and those two need records created first. The header still stacks to four rows before any content. And the standing lesson is in `GOTCHAS.md` rather than here: **naming a gap is not closing one**, and the interval between naming this one and being caught by it was under a day.

    **Why it could not have been raised earlier**, which is the part worth keeping: until task 19 the app could only be reached over loopback, so every layout judgement anyone made about it was made on a desktop browser. The mobile Playwright projects exercise behaviour at a mobile viewport, and have since the isolated suite existed — but a passing axe check and a working control are not the same claim as a layout being pleasant to use, and no test can make the second one. This task exists because the owner used it, which is the only way that finding arrives.

    **Sequencing is open.** It moves no schema and needs no migration, so unlike 25, 26 and 27 it carries none of the backup-version cost that put those after task 19 — it can be taken whenever the owner wants it. Scope it before building: decide which surfaces are actually uncomfortable rather than restyling all four routes on principle.

29. **A card's correction overlay, its stored match decision, and a way to retire one.** Scoped 2026-08-13 (D-103) and **DONE 2026-08-14** (D-104, migration 017). Held back from task 27 on purpose: each piece needs its own table and therefore its own backup version bump, so they were designed together and landed in **one migration (017), moving the backup v6 → v7** with v2 … v6 staying restorable. This is D-098's argument one version later, and D-089's: the v6-into-v7 restore pair was proven end to end *before* anything depends on it. The scope below is what was built, with two things worth reading as outcomes rather than plans — the acknowledgement is **stored consent** rather than a computed flag, and the ledger grew a **retired-cards list** so that reversibility is real on screen and not only in the database.

    **The third piece was not in the original scope and the ledger view is what surfaced it.** Nothing in any migration can retire any record — no slip, no cash entry and no card can be withdrawn once captured, so every one counts in the totals forever. That rarely bites a slip, whose identity comes from a CRC-checked QR. A card is typed from a screenshot, so a wrong account binding or a second capture with a typo is a likelier accident, and until now it would sit in the ledger permanently.

    **Four tables, following the slip precedents rather than inventing a shape.** `notification_card_corrections` and its append-only revisions table copy `slip_correction_overlays` (migration 013): one nullable column per correctable field, where null means *not corrected*. `notification_card_decisions` and its revisions table copy `slip_match_overlays` (migration 012): a decision, an optimistic `revision`, and a **partial unique index of its own** so one transaction may be claimed by at most one card — deliberately not shared with the slip index, because a row may legitimately carry both records (D-102).

    **What is correctable and what is not.** Correctable: `kind`, `amount_minor`, `balance_minor`, `occurred_on`, `occurred_at_time`, `counterparty`, `category_id`, `note` — every figure the owner typed. **Not correctable: `account_id`, `channel` and `printed_account_digits`.** The binding was checked against the printed digits under that layout's mask at capture and cannot be re-made (D-101 says so in a message the form already shows); the channel and the digits are what that check was made against, so all three travel together. The remedy for a wrong binding is retirement, below. `balance_minor` carries no sign check for the reason `lib/notification-cards.ts` already gives — a balance is a position, zero is ordinary and negative is an overdraft.

    **The decision has three values, not two.** `matched` names a statement row; `unmatched` says this card is on none of them; **`not-a-payment` retires it** — the card leaves the ledger rows and the totals while staying in its append-only table, because nothing here is ever deleted. Folding retirement into the decision rather than giving it a fourth table keeps one place answering "what is this card's relationship to the ledger". **Retirement is reversible and the card row is not**: a decision is an overlay with a revision, so a card retired by mistake is un-retired by changing the decision.

    **A balance disagreement may be overruled, and the overrule is recorded** (owner's decision, D-103). The RPC compares the card's **corrected** balance with the row's printed balance and **refuses by default**, which is the fail-closed posture D-102 built. The refusal lifts only when the request carries an explicit acknowledgement, and that acknowledgement is what gets stored — `accepted_balance_mismatch`. Storing the consent rather than a computed flag is what keeps it from going stale when the balance is later corrected. The RPC still re-checks the account and the exact amount server-side, exactly as `set_slip_match` re-checks bank and amount.

    **Two read-side rules that are requirements rather than choices.** Corrections are applied **before** the rule, by a `cardsInForce` mirroring `slipsInForce` — migration 014 exists because `set_slip_match` compared a slip's *original* amount, and a card has two figures with that trap instead of one, since it reconciles on the balance as well. And decisions are applied **before** the rule, never layered over its result, so an automatic pairing can never consume a row the owner already assigned (`proposeSlipMatches` records why).

    **Re-capturing a retired card needs no new mechanism, and this is worth not re-deriving.** The fingerprint covers the account, so capturing the same screenshot against the *right* account yields a different fingerprint and therefore a new row. Capturing the identical thing again hits the retired row and returns `captured: false` as it does today; the route says it is already held and retired, and the owner un-retires it by changing the decision.

    **What this does not include.** No new route beyond the two the decisions and corrections need, and no change to `capture_notification_card`. Task 27 is complete and this does not re-open it.

30. ~~**Widen the pgTAP privilege assertions to every table migration 017 added.**~~ Raised by the security review on 2026-08-15 (D-105) and **DONE the same day**. `supabase/tests/008_notification_card_decisions.sql` gains three assertions and its plan moves 33 → 36: `authenticated` may read both revisions tables, `authenticated` holds no insert, update or delete on either, and `anon` holds no read or write privilege on any of the four (a sixteen-check cross join). **pgTAP is now 250 across 8 files.**

    **Both new refusals were red-proved, one at a time.** Granting `insert` on `notification_card_decision_revisions` to `authenticated` failed **exactly one** test — the one that names it — and nothing else; granting `select` on a correction overlay to `anon` failed exactly the `anon` assertion. Both grants were revoked and the final privilege state was read back from the database rather than assumed.

    **It turned up more than it was scoped for**, and that is why task 31 exists: the `anon` assertion cannot include `truncate`, because `anon` genuinely holds it. See D-106.

31. ~~**Revoke the default privileges every table added since migration 002 still carries.**~~ Found on 2026-08-15 while red-proving task 30 (D-106) and **DONE the same day** (D-107, migration 018). `revoke all` plus a `select` re-grant, mirroring migration 002's pair, and an `alter default privileges` revoke so the next table does not inherit it again. **`revoke all` rather than a named privilege list on purpose**: `maintain` does not exist before PostgreSQL 17, and this migration must apply to a hosted project whose version is not verified from here. **No backup version bump** — it adds no table and no column, so the contract stays at v7. **The attack was re-run rather than the catalog re-read**: `truncate` as `anon` succeeded before and is refused after. Gate after a full reset: pgTAP **252 across 8 files**, Vitest **521 / 7 skipped across 28** — unchanged, which is what a grants-only migration should do. **The hosted project is now three migrations behind.** The original scope follows.

    **Original scope, kept for the reasoning.** Found while red-proving task 30 (D-106) and it needed its own migration. `anon` and `authenticated` hold `truncate`, `references`, `trigger` and `maintain` on **thirteen** tables: every `slips`, `slip_match_*`, `slip_correction_*`, `cash_entr*` and `notification_card*` table. Supabase's own bootstrap grants that set on every new table in `public`; migration 002's `revoke all on all tables in schema public from anon, authenticated` cleaned up only what existed when it ran, and no migration since has repeated it.

    **`truncate` bypasses RLS**, so the privilege would empty a ledger table outright. It is **not reachable from the internet today** — PostgREST maps no HTTP verb to it, nothing here runs caller-influenced SQL, and the publishable key is not a database credential — so this is a latent defense-in-depth gap rather than a live hole. Confirmed by doing it: `set local role anon; truncate ...` succeeded inside a rolled-back transaction on the synthetic project.

    **The owner chose to write it before the hosted push**, so 016, 017 and 018 now travel together. **Still not verified on the hosted project**: the standing read grant there covers counts and migration versions only, and a privilege audit is neither — so whether the hosted project carries the same gap is inferred from having the same bootstrap and the same migrations, not measured. Migration 018 closes it there when it is pushed either way, since it revokes unconditionally.

32. ~~**`findCards` finds only the first card on a multi-card image.**~~ **Closed the same day it was raised, 2026-08-16 — it was never a defect.** `findCards` found 2 of 2 on the image first reported and 5 of 5 on a LINE Capture carrying five; the cards it "missed" were sitting in the `Which card on this screenshot` dropdown, which the owner had not noticed. **The entry stays struck rather than deleted because the mistake is the lesson**: a bug was written into `PLAN.md` from a first report, and two hypotheses were investigated in the source, before anyone checked whether the reported symptom was real. **Reproduce before diagnosing.** What remains genuinely open is narrower and belongs to task 33: **field labels** inside a card are missed on a dense multi-card image — `จากบัญชี` was found on one of five cards — which is the known label-miss ceiling meeting smaller glyphs, and the cheap mitigation is capturing two or three cards at a time rather than five.

    **The two hypotheses ruled out, kept because they are true and worth not re-deriving**, are below. The direction-word match is correct: SCB Connect's `directionWord` is the full title phrase `รายการเงินออก`, so `text.startsWith(...)` in `findCards` matches a real title line — an earlier worry that it required a bare `เงินออก` against a title beginning `รายการ` was wrong. And nothing downscales the image before OCR: `app/notification-card-capture.tsx` passes the raw `File` to `readSlipWords`, and the only scaling in that file is `CROP_TARGET_WIDTH`, which applies to the crops shown to the owner *after* the read.

    **What is left is the engine**, and it needs a measurement rather than more reading: run the OCR over the image in a throwaway harness under `.runtime/` and count how many lines are recognised as starting with the direction word, printing **counts and line positions only** — never text, as D-100's harness did. If the lower titles are simply not recognised, that is the known label-miss rate meeting a tall image, and the fix is about how the image is fed to the engine rather than about `findCards` at all.

33. ~~**Decide whether OCR may pre-fill a card's figures, and measure the accuracy nobody has measured.**~~ **Closed 2026-08-16, superseded by task 34.** The measurement it asked for is D-112 and D-113; the decision it asked for was taken as D-114 — trial it rather than decide it — and built as task 34. The crop beside its field, which is the other half of what was proposed here, shipped as `dd6f7e9`.

    **What he proposes**: fill every field from OCR, require him to press a confirm button, and put each field's cropped image **beside** its input so the read value and the picture can be compared at a glance.

    **The second half needs no decision at all and should happen either way.** D-087 already says the design is "a cropped enlargement beside the field"; the form actually renders the crops in a block **above** the inputs, so the current layout does not deliver what that decision described. Moving each crop next to its own field implements the existing decision rather than changing it.

    **The first half changes D-087 and needs the measurement that decision names as missing.** Two premises previously offered for revisiting it are wrong and are recorded so they are not offered again: slips are **not** camera photographs, they are machine-rendered like the cards, so there is no "different input class" argument; and D-087's 1-in-15 figure is **stability between two engine configurations, not accuracy** — the entry says in its own words that establishing accuracy needs every amount read by eye, that this was not done, and that no accuracy rate should be quoted from it. **So the safety of pre-filling has never been measured in either direction.** Two further facts make the gap wider: the numbers were taken on tesseract.js **6.0.1** while `^7.0.0` ships, and they were taken on **slips**, never on cards.

    **The measurement is DONE, 2026-08-16** (D-112), and it inverts D-087's premise. 10 images, **19 cards**; location at amount 19/19, balance 15/19, own account 13/19, date and time 12/19. Of 19 amounts, **13 clean and correctly signed, 3 with no digits located, 3 with the minus read as `=`** — and **none wrong-but-plausible**. What breaks is punctuation, never the number. **So a pre-fill running the existing strict grammar and blanking on a parse failure would have filled 13 of 19 correctly and refused the rest, never offering a wrong figure.** Limits: n is 19 and ground truth is independently verified for only 5 cards. **The residual risk is a lenient parser, not the engine** — anything that "fixes" `=15.00` manufactures the silent error this measurement did not find.

    **A better instrument raised every figure, 2026-08-16** (D-113). The first harness left nineteen judgements to the eye and called the date separators cosmetic; running the app's own `parseThb` instead showed the date would have filled **0 of 19**, because `/` reads as `|` or `!` on essentially every card. Repairing characters that carry **no value** — separators, a misread sign, a detached thousands comma — and **discarding the repair whenever the digit sequence changes** lifts amount to **17/19 (89%)** and date and time to **10/19**, with balance at 15/19 and no refusals among those located, and own account at 11/19. No layout was ambiguous on any image. Every surviving refusal is honest and named. **This is not the lenient parsing D-112 warns against**: a separator is not a digit, `parseThb` is unchanged, and a repaired sign is cross-checked because direction is read twice and `readDirection` refuses a disagreement.

    **The layout half is DONE, 2026-08-16** — each of the five read fields with an input now shows its crop inside that field's label (`dd6f7e9`). **What remains is the owner's decision** on pre-filling, which now has evidence under it: repair, then the strict grammar, then **blank on failure**, never a guess.

    **One argument for pre-filling that D-087 could not have weighed**, worth putting in front of whoever decides: a card reconciles on the **exact** amount *and* the printed balance, so a misread figure does not pair with its statement row — it surfaces as `AWAITING STATEMENT` rather than as a wrong pairing. The statement stays the authority, which is a fail-closed net that did not exist for slips when D-087 was written. It is not a complete answer: an unpaired card still moves the totals until corrected, and correcting it requires noticing.

    **One argument against, which is the real one**: typing forces the figure to be read. A confirm button does not, and a plausible-looking pre-filled value at the end of a long day is exactly what gets confirmed unread.

    **This got more important on 2026-08-16** (D-111). LINE Capture is a better OCR input than a hand-taken screenshot and naturally produces images carrying several cards, so multi-card is becoming the normal case rather than the awkward one — and more cards per session is more typing, which is what makes the question worth settling rather than deferring.

34. ~~**Trial OCR pre-fill on a card, with the statistics that will decide whether it stays.**~~ **DONE 2026-08-16**, all four parts (D-114 the decision, D-115 parts 1 and 2, D-116 parts 3 and 4). Every project including the hosted one is on migration 019.

    **Read this before reading the statistics, because it corrects the plan below.** The comparison this task was scoped around — the pairing rate of pre-filled cards against typed ones — **has almost no group to compare against**: the ledger held **1** card when the trial began, and every card captured from now on is pre-filled. At a few cards a week that comparison will not mean anything for months, and it may never. What still works, and works at n=1, is the **mechanism** rather than the rate: a wrongly pre-filled figure cannot match a real statement row, so the card surfaces as `AWAITING STATEMENT` on the ledger by itself. **A single card that will not pair after its statement has arrived is the signal** — no rate, no second group, nothing to compute. The audit rows are cheap, append-only and there if a pattern ever emerges, but they are not what will answer the question.

    **Four parts, in this order.**

    1. ~~**A new pre-fill module under `lib/`**~~ **Done 2026-08-16** (D-115): `lib/notification-card-prefill.ts` with 35 tests. Repairs characters carrying no value, **discards the repair whenever the digit sequence changes**, runs the unchanged `parseThb`, returns nothing rather than a guess. The digit guard has its own test, including one asserting a repair that would alter a digit is thrown away — proved against `O`→`0`, since no shipped substitution can touch a digit. **Two things came out of building it**: the harness it was ported from preferred a token fragment over the rejoined figure and would have offered a tenth of a printed balance, now fixed and tested; and a month-name date reader for KBank Live was built, measured at **0 of 2 real cards** and removed, because the month token does not survive recognition at all.
    2. ~~**The form fills the four digit-bearing fields**~~ **Done 2026-08-16** (D-115): `offerPrefill` in `app/notification-card-capture.tsx` fills amount, balance, date and time, and account digits, leaves blank whatever refuses, **names on screen which boxes the card filled**, and holds two lists of field names — offered, and changed. The direction control is still never filled from the image. `tests/privacy.test.ts` was reversed to guard the new invariants rather than the old prohibition.
    3. ~~**Migration 019**~~ **Written, applied locally and tested 2026-08-16** (D-116), after the owner reported a fresh backup and authorized it. Two arrays of **field names** join the audit detail `capture_notification_card` already writes. **Structure, never values.** **No new table and no new column, so the backup stays at v7** — verified rather than argued, since portable recovery passes on all four named lines against a 019 ledger. It adds **two optional payload keys rather than a parameter**: the signature was already a single `jsonb`, so absent means an empty list and the app in production keeps working unchanged, which is what lets 019 land on the hosted project before part 4 ships. The closed set is enforced in the database as well as at the route, including that `changed` must be a subset of `offered`. **Not on the hosted project**; that push needs its own authorization.
    4. ~~**The route passes them through**~~ **Done 2026-08-16** (D-116). `notificationCardCaptureSchema` carries `prefillOffered` and `prefillChanged` as `z.enum` over the same constant the pre-fill module fills from, so a figure cannot travel in either by construction rather than by review; both optional, both refusing a repeat and a `changed` that is not a subset of `offered`. The form sends the two derived name lists. **The ordering rule was honoured**: 019 reached the hosted project first, verified against the remote, and only then did this reach `main`.

    **Task 34 is complete.** What remains is not code: the trial now records what it needs, and the decision it feeds is the owner's to take on the evidence — **the pairing rate of pre-filled cards against typed ones**, never the change rate, for the reason D-114 gives.

    **What the statistics can and cannot say, and this is the part not to re-derive.** The obvious measure is how often a pre-filled value gets edited, and **a low edit rate is consistent with two opposite worlds** — the engine is right, or the owner stopped looking. It cannot distinguish them. **The independent check is the statement**: a card pairs on the exact amount, a one-day window and the printed balance, so a wrongly pre-filled figure fails to pair and surfaces as `AWAITING STATEMENT` without anyone auditing anything. **Compare the pairing rate of pre-filled cards against typed ones** — that measure does not depend on the owner having looked.

    **Fill rates, measured over 12 real screenshots and 25 cards after the enlargement** (D-117): **70 of 100** digit-bearing fields, against 62 at native size — amount **80%**, balance **80%**, date and time **60%**, own account **60%**. **KBank Live is the open problem**: 0 of 5 on balance, timestamp and account digits under every segmentation mode and every preprocessing variant tried, with its amount reading on 4 of 5. Unexplained, and those three fields are typed. Earlier, on the 19 cards available before the owner added two screenshots (D-115): amount 89%, balance 79%, date and time 63%, own account 58%. A field that refuses is left blank and typed as it is today, so the trial can only reduce typing, never add it. **The date is up from D-113's 53%**, and not for the reason anyone expected: the gain came from repairing a garbled separator in the **reader's label matching**, since SCB's timestamp label carries a slash and a misread one made the field unfindable. **7 of the 19 date refusals were the reader declining to locate the field, against 2 that reached the pre-fill** — so this field's remaining constraint is upstream of the pre-fill module, and five SCB labels are still garbled some other way.

Task 13 (receipts as originally scoped) is superseded by 20 and 21 for bank slips; paper receipts from cash purchases are entered by hand rather than read.

13. **Receipts, as a separate build** — see tasks 20–22, which supersede the OCR-first framing below. The CSP question it was blocked on is answered: `next.config.ts` already allows `'wasm-unsafe-eval'` and `worker-src blob:`, which is what tesseract.js needs, provided its worker, core and language files are bundled locally rather than fetched from a CDN.

16. ~~**Pin the Supabase target in `playwright.isolated.config.ts` and `playwright.config.ts`.**~~ **Closed 2026-07-28.** Found while re-running the gate after the D-048 migration: `NEXT_PUBLIC_*` are inlined at build time and both configs ran `pnpm build` with no `webServer.env`, so once `.env.local` aimed at `private-ledger-live` either one built a browser suite against real financial records. Only `playwright.owner.config.ts` pinned — its comment about removing "the one path by which a configuration change could aim the suite at real data" was true of itself alone.

    Both now carry `webServer.env`. The isolated config pins all four variables, with `NEXT_PUBLIC_ALLOW_DEV_OWNER_SESSION` at `"0"` because `ledger.spec.ts` asserts the flag is absent. The default config pins only the three database variables: it has no `testIgnore`, so it collects both that spec and `owner-session.spec.ts`, which requires the flag — no single value satisfies both, and it remains the convenient port-3000 config for driving the app by hand rather than a green-run config.

    Verified: the isolated suite passes 14/14 in a shell with **no pins set**, where it read 12/14 before. Live's fingerprint digest was unchanged by that run.

    **Residual gap, deliberate:** `playwright.config.ts` is one of the intentionally uncommitted local files, so its pin exists on this machine only. A fresh clone gets the unpinned version. Committing it would mean adopting a file the owner keeps local; raise it with them rather than deciding unilaterally.

    **A second hole in the same file, found by review and closed on 2026-08-07** (D-070): it also carried `reuseExistingServer: !process.env.CI`, and pins only apply to a server the config starts itself. A server already listening on 3000 — one left running from `.env.local`, which aims at the real ledger — would have been reused, `env` never applied, and this config has no `testIgnore`, so it would then have driven `owner-session.spec.ts`, which writes through the app, into real records. Now `false`, matching both sibling configs. **On this machine only**, for the same reason as the pins above.

13. **Receipts, as a separate build.** Deferred 2026-07-25 (D-037) and explicitly not part of task 11. The receipts are JPGs, so the entire reader — column bands, the frame/grid boundary, every masked diagnostic, and the masking harness itself — does not apply, because all of it operates on a pdf.js text layer and an image has none. Reading them needs OCR under mandatory per-field review against the source image. Nothing here starts before one masked receipt dump exists and a CSP spike says tesseract.js actually runs under this policy.

12. ~~**Persist whether an import was cross-checked.**~~ **Closed 2026-07-27 by deciding it need not be persisted** (D-043). The owner chose refusal over labelling: a statement whose printed totals never confirmed its rows is refused at assembly with `NOT_CROSS_CHECKED`, so nothing unverified can reach the ledger to be labelled and no migration or payload-contract change is needed.

    What decided it: the cross-check is not the main defence against a dropped row — the balance chain already catches a dropped *middle* row on all three layouts. It earns its keep exactly where the printed balances run out, which is a dropped *first* row on Krungthai (its opening is derived, D-026) and a dropped *last* row on Krungthai and SCB (neither prints a closing balance). And refusing costs nothing today, because all three known layouts print a summary block.

    Left for the day a real statement is ever refused: an explicit recorded override. Designing it now would mean designing for a document nobody has seen. If it is built, D-043 records the shape it should take — the printed totals travel in the payload and `confirm_import` re-verifies them, so the flag is a fact the database established rather than a caller's attestation.

14. ~~**A recovery surface a person can drive.**~~ **Done 2026-07-27** (D-046). Opened the same day by the portable-recovery rehearsal (D-044), which proved the contract portable and in doing so showed what was missing around it: no restore surface at all, and an export button producing the non-restorable `.pldemo` preview, so `GET /api/v1/backups/export` had no caller and a genuine recovery was driven from code.

    The recovery surface in the app — § Recovery / 04 then, the `/recovery` route since D-061 — now exports a real encrypted `.plbak` — snapshot validated, encrypted in the browser, custody acknowledged only after the file is written and only if the ledger has not moved — and restores one, decrypting locally and posting stage, eleven chunks and commit through `app/api/v1/backups/restores/[action]/route.ts`, which is therefore no longer covered by the unit suite alone. The empty-ledger requirement is surfaced as the point rather than as an error.

    Proven by destroying the ledger between the two halves: `tests/e2e/owner-session.spec.ts` imports four rows, exports, wipes every ledger table, restores, and asserts the rows, the batch and the bound account all return.

15. ~~**Let the owner create a ledger account.**~~ **Done 2026-07-27** (D-045, migration 010). Not on this list until the day it was built, which is itself the finding: the recovery gate looked like the last thing standing between this project and a real import, and it was not. Every account came from the seed, `public.accounts` grants `authenticated` select only, and both real statements printed suffixes no seeded account carried — so the app could read a statement it had no way to bind.

    `public.mutate_account` is the single write path (strong-access gate, ledger-mutation lock, audit row, sequence bump), `POST /api/v1/accounts` calls it, and the bind stage offers the form exactly when no loaded account matches the statement's bank and last four, prefilled from the statement because binding checks both. Create and relabel only — `bank_code` and `last_four` are identity, and deletion has no story while `source_transactions.account_id` references the account.

    Left deliberately: **relabel is reachable through the RPC and proven by pgTAP, but has no route and no UI.** Creation was the blocker; renaming is a convenience, and a route with no caller is the same smell as the export endpoint in task 14. Build it when something needs it.

41. ~~**The hosted Sync button — fetch statements from the app instead of from a script.**~~ **Shipped 2026-08-24 as `0a4bf9a`** (D-145), deployed, and **complete on 2026-08-25** once the owner set the three variables in Vercel and redeployed. Asked for by the owner immediately after auto-import v1 landed (D-144). **The first real sync worked on the first attempt** — five statements out of the dedicated mailbox, decrypted on the device — which is the first time any of this code had spoken to a real mail server. **What it immediately exposed is D-147**: the press that takes a statement off the resulting worklist appeared to do nothing, because the scroll bringing the answer into view was keyed on a stage automatic binding skips.

    **The shape came from D-141 and was not re-derived.** The server proxies the still-encrypted bytes and the browser decrypts them: `lib/server/statement-mailbox-session.ts` signs in and finds the PDFs, `GET /api/v1/imports/mailbox` lists them, `GET /api/v1/imports/mailbox/attachment` streams one, and `app/statement-sync.tsx` spends both calls on one press and hands `File` objects to the batch worklist that already existed. `lib/server/statement-mailbox.ts` is reused **byte-for-byte unchanged** — this is a second caller of that seam, not a second IMAP implementation, which is exactly what its header said it was for.

    **The two questions this task left open, and the answers.** *Lists first, fetches on demand*: one call returning everything holds every attachment in one server process and returns them in one body, which is a memory bound nobody set and a response size the hosting platform caps independently of this repository — it would surface as a truncated PDF rather than an error. Listing also makes a failure specific: one statement that will not download is one row saying so, and the four that did still reach the batch. *It never claims a statement is already present*: a server has no folder to compare against, the alternative was inventing server-side state to hold a watermark, and the batch already blocks a repeat on the PDF's own SHA-256 — a stronger check, in the place where the bytes actually are. A weaker check that sounded authoritative would be worse than none.

    **What did not change**: the review table, the confirmation, and the fact that the document password never leaves the device. It is not a parameter of either route, the deployment does not hold it, and the sync component has no prop, state or field that could carry one. Auto-binding (D-144) applies unchanged.

    **The uid and part the page sends back are checked twice.** `lib/statement-sync.ts` refuses anything that is not a positive integer and a dotted-digit part path — but a well-formed pair still names an arbitrary part of an arbitrary message, so the mailbox is asked a second question before any download: is this uid from a configured sender, and is this part one of its PDF attachments? Either no is the same 404. Without that, an owner-gated download route would also be a way to read any mail in the mailbox.

    **Proved in a browser** by the throwaway `.runtime/mailbox-sync.spec.ts` (4 tests, 390px, behind a real sign-in): the unconfigured path through the **real** route, the download path with both routes intercepted and synthetic bytes, a failed download landing as one line rather than a failed sync, and the band clean at 390px with every tap target at 44px or more. `import_batches` is asserted to stay at zero.

    **Not proved, and it cannot be from here: the mailbox.** No IMAP connection was made. The seam this calls *is* proven against the real mailbox by the fetcher (D-144).

    **Two of the three gates are discharged and one remains.** `/code-review` ran on 2026-08-24 and found eleven, ten real and fixed. **`/security-review` ran the same day and found nothing** — it verified the owner gate on both routes, that a validated uid/part pair cannot reach unauthorized bytes, TLS certificate validation, that the app password cannot leak into a response body or a log, and that the mail-supplied filename is neither a header-injection nor an XSS vector. The commit and push are spent. **What remains is the app password in Vercel**, a hosted-resource change only the owner can make: `STATEMENT_MAILBOX_USER`, `STATEMENT_MAILBOX_SENDERS` and `STATEMENT_MAILBOX_APP_PASSWORD`, Production scope, the password marked Sensitive and **none of them prefixed `NEXT_PUBLIC_`**, which would inline the credential into the client bundle. They are read at request time, so a redeploy suffices. They are deliberately absent from `.env.example`, which is inside the never-read boundary and is the owner's to edit.

    **The owner was advised to use the local script for a month first and chose to proceed.** That is recorded and is not re-litigated; the script still works and is unchanged.

42. ~~**The Stardew visual direction.**~~ **COMPLETE 2026-08-26** (D-153, D-156, D-157). Asked for by the owner 2026-08-26. He wants the app to read as Stardew Valley — its palette and its pixel type — and gave three critiques of the ledger page alongside it: the standing copy reads like an advertisement for the product rather than the product, the transactions table should be the dominant thing on the page, and *Record a cash payment* should contract to a small control instead of owning a titled section with a paragraph.

    **Three things were settled before any code, and they bound the work.** *The font he found cannot ship*: [TA Stardew Valley Color](https://www.f0nt.com/release/ta-stardew-valley-color/) is Thai-only in its free form, ฿459 for the full one with no licence text published, and a colour font whose SVG flavour is effectively Firefox-only — and `font-src 'self'` means any face is **committed to a public repository**, which is redistribution. The Latin face will be an OFL pixel font instead; **Thai stays on `IBM Plex Sans Thai`**, which costs nothing, because `app/` contains **no Thai at all** — every Thai string in this repo is a parser constant under `lib/`, and Thai reaches the screen only as statement data in table cells. *The theme goes everywhere for a first pass*, the owner's call, on the grounds that type and colour revert cheaply. *The table auto-loads* — see task 43.

    **A canvas of four artboards was published for the choice**: today's look beside the same page in Pixelify Sans, Press Start 2P and Silkscreen, identical in every respect but the type, plus the disclosed `(i)` panel. Every figure, date, description and reference on it is **invented** (`docs/FIXTURE_POLICY.md`). The panel treatment is drawn from **this app's own tokens** rather than from the game's art — Cornsilk stays the ground (D-137), a darkened Copper becomes the frame, Light Caramel the inner rule — so nothing of ConcernedApe's is reproduced.

    **What the choice actually turns on, and it is not taste.** Pixel faces are drawn for legibility at display sizes, and this app's whole pre-fill discipline (D-114, D-115, D-123) is the owner reading a figure off a cropped enlargement and typing it into a box. `8`/`B`, `0`/`O` and `1`/`7` must be unmistakable at the 12px the Movement and Balance columns use. **If the pixel face wins those columns too, that is a knowing trade and it belongs in a decision entry**, not in a stylesheet diff.

    Superseding is owed either way: the palette is **D-136 and D-137**, and D-137 is the entry that made Cornsilk the ground. A new entry supersedes them; `DESIGN.md` must be rewritten regardless, since it still documents the cool-mist/navy palette this app stopped using on 2026-08-21 and `check:docs --strict` cannot see that because it reads structure rather than meaning.

    **The plumbing and the picker shipped 2026-08-26**, as `1d2ca59` carried out by `f46ee64`'s push. `lib/ui-font.ts` is the closed set and the only thing that decides a face; `tests/ui-font.test.ts` is **17 tests** over it, most of them about untrusted input, because the value lands in an attribute on `<html>` and a cookie is client-supplied. `POST /api/v1/ui/font` writes an **httpOnly** cookie and answers with what it stored; `app/layout.tsx` reads it server-side and writes `data-font`, so the face is correct on **first paint** rather than swapped after one. `app/font-picker.tsx` sits in the shell header and holds no optimistic value — a control showing a choice the server has not stored is the D-147 shape. **The route is deliberately not owner-bound**: it touches no records, reads a four-value enum and writes it straight back, so there is no boundary to defend and requiring aal2 would only mean the signed-out page could not be made legible.

    All three faces are **Fontsource packages at OFL-1.1**, matching the two IBM Plex packages already imported in `app/layout.tsx` — **not** hand-downloaded files, which was the first attempt and was wrong. `font-src 'self'` admits no external host and the CSP is unchanged. **Every switched stack keeps `IBM Plex Sans Thai` behind the pixel face**, which is load-bearing rather than tidy: all three cover Latin only.

    **Default is `system`, not the owner's leading candidate**, and that is deliberate — the pixel faces are on trial, and the way back to something legible must not depend on the trial going well. Flipping `DEFAULT_FONT` is the whole of that change when he decides.

    **Proved in a real browser, because the cookie is httpOnly and no unit test can see it work**: a throwaway spec under `.runtime/` watches `data-font` change, survive a reload, and lead the computed `font-family` with Thai still behind it, and watches an unknown face refused **422** with the stored one unchanged. Gate: Vitest **807 passed / 7 skipped across 38 files**, Playwright owner **31/31**, isolated **18/18** including the axe pass on every route, production build clean at **twenty-one** `/api/v1/` routes.

    **The restructure shipped 2026-08-26 as `f46ee64`** (D-156). The distinction that decided every sentence is **principle behind the `(i)`, warning-about-a-write kept and moved closer to its control** — so three paragraphs folded and *"It is written once and never edited"* moved **down** from the cash heading to sit beside the submit button. One sentence was deleted rather than folded, and only because D-155 had made it false: *"Nothing loads until asked"*. `app/ledger-note.tsx` is a real `<button aria-expanded>` whose panel is **rendered only when open**, not hidden with CSS, so collapsed copy is out of the accessibility tree; every dimension in it is absolute, because one sits inside an `h1` at `clamp(40px, 6.2vw, 86px)` and an `em` would make the badge track the heading — D-153's `ch` cap again. The cash bench is one line where it was a titled section, and **stays above the table**: moving it below was tried and rejected, because recording a payment reloads the rows and the row just written would land in a table the owner had scrolled past. Heading levels did not move, so axe's `heading-order` still passes on this route.

    **What the restructure could not fix, and did not try to.** On a phone the **shell header** — brand, privacy chip, typeface picker and its note, two sign-in buttons, the route row, the session line — occupies most of the first screen before this page's own heading begins. The table cannot be the most visible thing on that device while that is true, and nothing trimmed below it changes that. Measured at iPhone 13 width and **left alone**, because it is the shell every route shares and the owner's critiques were about the ledger page. **This is the next thing to raise with him on task 42.**

    ~~**Still owed on this task**~~ **COMPLETE 2026-08-26** (D-157). The owner ran the deployed app, asked why Press Start 2P was so much bigger than the other faces, and the answer was a measurement: it draws its capitals on a **full em** against IBM Plex's 0.7, so everything came out **1.43x** the designed size while the previous per-selector step-downs reached only what somebody had remembered to name. Each face is now declared locally with a measured `size-adjust`, so one CSS size means one visual size in all four. **Every route opens with a title rather than a headline** — Ledger, Import, Slips, Recovery — the two-column intro band went with the paragraphs it held, the totals block's matching-rule prose folded behind its counts, **the phone header folded behind one Settings control** (`display: contents` above 700px, so the desktop header is unchanged rather than re-derived), panel chrome landed, and `DESIGN.md` was rewritten from the stylesheet rather than from memory. The picker note no longer wraps badly, because the header no longer competes for that row.

43. ~~**The ledger loads on arrival.**~~ **Shipped 2026-08-26 as `f46ee64`** (D-155). Task 17 recorded *"Nothing loads until asked"* — with the reason that every other read surface here is driven by an explicit action, so a section fetching the ledger on page load would be the one place that stopped being deliberate about it. **That was a consistency argument and not an invariant**: no money, privacy or append-only property rested on it, which is what made it reversible where D-114's pre-fill guarantees are not.

    The owner's own reasoning is the stronger half and is recorded because it is the sort a later review will re-derive badly: **the page reads like an advertisement partly because it is empty**, and the standing copy was filling the hole the table should occupy. Filling the hole resolved much of task 42's copy problem without a word being cut.

    **The open question was answered before any code, and the answer changed the shape of the task.** `list_account_transactions` **bounds nothing** — no `limit`, no `offset`, one `jsonb_agg` of every row for the account. It is nonetheless **deliberately still unpaged**: the balances here are derived over *whole accounts* and reconciliation runs over the *whole* ledger before any filter (D-063), so a first page would silently change both, and the all-accounts figure would become the running total of whatever happened to be fetched. **Paging it properly means computing balances in SQL, which means a migration** — that is the next reader's decision and it needs an authorization this session did not have.

    **What was bounded instead is the width of a row.** `import_batch_rows` was parsed and read by nothing — **241 of 848 bytes, 28.4%** of the object, about **290 KB of a 1,020 KB** response at the ledger's present size. The route drops it; `ledgerTransactionSchema` stays `.strict()` without it, so a regression fails by name. **No SQL, no new route, no contract version change.**

    **Two things it exposed, both found by the suite rather than by reasoning**, are in D-155: a 401 on the load the page performs by itself is the route working and must not be reported as a failure, and signing in does not navigate — so `lib/owner-ready.ts` announces an aal2 session to anything already refused for want of one. It has two producers and is a counter rather than a flag, and each of those is a bug the owner suite failed by name on first.

44. ~~**Statistics and analysis.**~~ **Scoped and then BUILT on 2026-08-27** (D-160 scoped it, D-161 built it; migration 023). **Deployed 2026-08-27 as `9ce1f06`** (`6a8399b..9ce1f06`), database first — the owner ran the migration and hosted was read back at head `202608270023`, 23 applied, the function executable by `authenticated` and **not** by `anon`, sequence 37 and 1,604 rows unchanged. Every project is on 023 except `private-ledger-live`, frozen on 012. **Nobody has looked at it on the real ledger yet.** What follows is the scoping the build was done from, kept because its reasoning is what the code rests on — and because **every real defect in it was found by rendering the page or by review, and none by the gate** — which was green throughout. The three questions this task was parked on are answered below. Two of them the repo had already settled without anyone writing it down; the third is a rule this app has never needed and now does.

    **The finding that reorders the task: the obvious statistics page cannot be built, and it would fail in exactly the way task 45's all-accounts column failed.** "Where does my money go" is a breakdown by category, and **this ledger has no categories** — `public.categories` and `transaction_overlays.category_id` have existed since migration 001 with both routes serving, and live holds **1 category against 1,465 transactions** (task 25). A category chart drawn over that is a figure that is right and empty, which is D-159's lesson arriving a second time — this time before the code is written rather than after the deploy. **The category breakdown is therefore task 25's work, not this task's**, and what follows is shaped so that it drops in as one more grouping key when task 25 lands.

    **Question 1 — what is being asked of the data. Answer: what happened this period, and how it compares with the periods before it.** That is the largest question this ledger can answer *today* — with no new capture surface, no LLM, and no division.

    Everything that is populated on every confirmed row right now: **time** (`source_date`, `source_time`, `effective_date`), **account** (three hold rows), **direction** (`source_components.kind`), and **the bank's own `transaction_label`** — free text from the statement's transaction column, and the only category-like field that is present on every row. `counterparty` and `category_id` on the overlay are populated on almost nothing. **`cash_entries` is the exception worth knowing**: it carries `category_id` and `counterparty` *at capture time*, so cash is the only population in this ledger that is categorised today.

    Three figures follow, in ascending cost:

    1. **Position over time** — the combined balance series. **It already exists.** `private.combined_balances(uuid)` returns precisely this and was built as a reusable helper *for this task* (D-159). No new derivation and no division.
    2. **Money in and money out, per month** — `sum(amount_minor) filter (where kind = ...)` with a `group by`. **This is the shipped totals strip plus a grouping key**, and that strip is already a whole-ledger aggregate computed in SQL.
    3. **Where it went** — needs a dimension. `transaction_label` today; categories after task 25.

    **Recommendation: build 1 and 2, defer 3.** They answer a real question, they are exact, and neither waits on anything.

    **Two population questions decide every total on the page, and the owner settled both** (below):

    - **Do cash entries count? — no, not in v1.** `cash_entries` is real money that never appears on a statement. Excluded, "money out" is understated by whatever was spent in cash. Included, the statistics page reads two tables where the balance series reads one, and two figures on one screen stop being derived from the same population. **The balance series cannot include cash by construction** — a cash entry has no `post_balance_minor` and never entered a printed balance chain.
    - **Slips and cards must not be summed at all.** A paired slip *is* the statement row (D-063), so summing `source_components` counts each payment exactly once and adding slips would double it. An **unmatched** slip is money that moved with no statement row yet — a real gap, and the honest treatment is to report how many there are beside the totals rather than fold their amounts in.

    **`include_in_reporting` exists and nothing has ever read it.** The column is on `transaction_overlays` from migration 001, `mutate_overlay` writes it, `PUT /api/v1/transactions/[id]/overlay` accepts it, `lib/backup-contract.ts` and `lib/transactions.ts` both carry it — and **no query in this repository filters on it**. This task would be its first consumer, which makes it a decision rather than a detail: if the statistics page honours the flag and the ledger's totals strip does not, two totals over one ledger will disagree on the same screen and both will be right. **The owner chose to honour it in both places** — see below for the reason that decided it.

    **Question 2 — PostgreSQL or on-device. Answer: PostgreSQL, and the repo settled this the day before the question was asked.** A statistic is whole-ledger by definition, and **the client no longer holds the whole ledger** — it holds a page (D-158). Computing on the device means fetching everything again, which is the ~785 KB problem task 45 just removed. D-159 is the direct precedent and its reasoning transfers unchanged: a figure the client cannot compute correctly once the ledger pages belongs where the facts are.

    **This is not D-120's refusal, and that should not be re-litigated.** D-120 keeps the *matching rule* in TypeScript because it is a judgement about a specific row carrying ~85 tested cases; re-implementing it in PL/pgSQL would mean one rule in two languages. An aggregate decides nothing — it sums. That is the same line D-159 drew.

    **The shape, following `list_account_transactions_page` exactly**: one `security definer` function with `set search_path`, gated on `private.has_strong_owner_access(auth.uid())` and returning an empty shape rather than raising when the gate fails, `revoke ... from public, anon`, `grant execute ... to authenticated`. `private.combined_balances` stays granted to nobody and is called from inside that definer function. One route under `/api/v1/`. **SQL computes, the client formats**: every money figure crosses the wire as a canonical minor-unit string via `::text` and is parsed by `minorUnitStringSchema`, exactly as every other money value in this app already does.

    **Question 3 — how it stays inside the exact-money rule. Answer: division never produces money.** It produces a ratio, and a ratio is not money. Three tiers, of which only the third is genuinely hard:

    1. **Totals, nets and subtotals — no division at all.** `bigint` minor units summed, exactly as the totals strip does. These are money and they stay money.
    2. **Shares and percentages — the numerator and the denominator travel; the percentage does not.** A share crosses the wire as two exact minor-unit strings and becomes a percentage in the presentation layer, for display only. **A percentage is a label**: nothing is ever derived from one and none is ever stored. Two consequences to accept rather than paper over — shares printed to one decimal **will not sum to exactly 100.0**, and the honest remedy is to show the exact parts beside them rather than fudge the last slice; and **a zero denominator is undefined, not zero**, so a period with no spending prints an absence.
    3. **Averages, which are the trap, because an average of money *is* money.** Two forms are permitted and no third. Either **integer division that keeps its remainder** — `sum / n` and `sum % n` together, so `quotient * n + remainder = sum` holds exactly and nothing is lost — or, preferably, **do not average at all**: twelve exact monthly totals answer the question better than one average of them, and a chart reads them better than a number does. **Prefer the series to the average** wherever the owner is looking at a screen rather than exporting a figure.

    **The structural guard, and the specific way this will be got wrong: `avg()` over `bigint` returns `numeric`, and so does `sum() over ()`.** D-159 already had to cast one back explicitly for that reason. The first person who writes the obvious `avg(amount_minor)` puts a `numeric` into a money path with nothing complaining. **So the return types are a contract asserted in pgTAP** — every money column of the new function declared `bigint` or `text`, with a test that fails by name the moment a `numeric` or a `double precision` appears there. That test is what makes this rule enforced rather than merely remembered.

    **The owner answered all three on 2026-08-27, the same day, before any of this was committed** (recorded in D-160). **Cash is out of v1** — `cash_entries` is not read, so every figure on the page is a fact about statement rows alone and the page must say so rather than let a total quietly stand for all spending. **`include_in_reporting` is honoured, and the ledger's totals strip is retrofitted in the same change.** And the figures are settled:

    | Figure | Period | Shown as | Divides? |
    | --- | --- | --- | --- |
    | Incoming, spending, net | per month, as a series | chart + table | no |
    | Incoming, spending | average per day | stat tile | yes |
    | Incoming, spending | average per week | stat tile | yes |
    | Balance | daily closing, as a series | chart | no |
    | Largest transactions | top N in the window | table | no |
    | Transaction count | per month | table column | no |

    **The monthly series is the base and everything else is derived from it or sits beside it.** Nothing here needs a category, which is why this task does not wait on task 25 — when categories arrive they are one more `group by` on a page that already exists.

    **The page is chart-led, and it is two charts rather than a gallery** (the owner asked for graphs on 2026-08-27):

    1. **Balance over time** — a line, one point per day, from the daily closing balance below. This is the headline: it is the owner's actual position and it is the only figure on the page that needs no explanation.
    2. **Incoming against spending, per month** — paired bars, one pair per month, with net readable as the gap between them. This is the comparison the page exists to make, and it is the chart the averages are summarising.

    The averages are **stat tiles, not charts** — a single number reads worse as a bar than as a figure — and the largest transactions are a **table**, because the interesting part is which ones they were.

    **The strict CSP decides how a chart is drawn, and it rules out the obvious answer.** `lib/security-headers.ts` names `'self'` and the Supabase origin alone, and its stated rule is that widening the policy is never how a problem gets solved — so **no charting library loaded from a CDN**, at all. Two routes remain: an installed npm dependency bundled by Next, or **inline SVG drawn by the app itself**. **Inline SVG is the recommendation**: the data is small (roughly a dozen monthly pairs and a few hundred daily points), it adds no dependency, it inherits the existing palette and both themes for free, and it raises no CSP question whatsoever. A charting library would be a large dependency earning very little against six chart marks. **Load `/dataviz` before drawing either chart** — the shapes above are what to draw, not how it should look.

    **The denominator is the whole difficulty of an average, and it is a decision rather than an implementation detail.** *Average daily spending* over a month can mean the total over **calendar days in the period** or over **days that had a transaction**, and the two differ by a large factor. **Calendar days elapsed is the answer** — it is the burn rate, which is what the figure is for; days-with-a-transaction answers "how big is a busy day", which nobody asked. Two boundaries follow and both silently distort the figure if they are missed:

    - **The current period is partial.** Dividing this month's spend by 31 on the 10th understates it by three times. **A closed period divides by its full length; the current one divides by the days elapsed so far**, and the page says which it did.
    - **The first period is partial too**, because the statement history starts wherever the first import starts, not on the 1st. Same treatment.

    **An average of an average is not an average.** `avg_week` is the window's total divided by its weeks, never `avg_day × 7` — the partial weeks at either end make those two different numbers. Each average is computed from the total against its own denominator. Weeks are ISO, starting Monday, which is what `date_trunc('week', ...)` already does.

    **Every average is integer division that keeps its remainder** — `sum / n` with `sum % n`, so `quotient * n + remainder = sum` holds exactly (D-160). The unit is satang, so the remainder is under one baht by construction and the displayed figure is exact to the minor unit rather than rounded toward one.

    **The balance chart wants daily closing balance, not the per-transaction series.** `private.combined_balances(uuid)` returns one row per transaction — 1,604 of them — where a chart can draw at most one point per day. Narrowing to the chronologically last row of each day is a `distinct on` over the existing helper: no new derivation, no division, and a payload that is a function of the history's length rather than its row count.

    **Internal transfers double-count, and this is what `include_in_reporting` is for.** Money moved between the owner's own accounts leaves one as a withdrawal and arrives at the other as a deposit, and **both are real statement rows**. Summed naively, incoming and spending are each inflated by the transfer while net stays correct — so the two figures the owner actually asked for are the two the flag protects. **Honouring the flag is one predicate and it is cheap. What is missing is a way to set it**: nothing in the app toggles it today, so it defaults `true` everywhere and the filter is inert until a control exists. **That control is the smallest piece of follow-on work this task creates**, and until it ships the page carries the limitation on its face rather than in a document. Detecting transfers automatically — equal amount, opposite direction, adjacent date, two of the owner's own accounts — is a matching rule and belongs beside task 25, not here.

    **Task 44 goes first and task 25 second.** Nothing on this page needs a category; task 44 is one function, one route, one page, while task 25 is a migration, a provenance table, a backup-contract version and a measurement of a model against a rules baseline. Building 44 first also gives 25 somewhere to land — a surface that already shows totals gains a category breakdown as a grouping key, where the reverse order categorises 1,465 rows with nothing yet able to show what it bought.

    **What the build added to the scoping, all of it found by doing the work:**

    - **The averages needed a formula, not just a rule.** `avg_week` is `total * 7 / days` — one division on a scaled numerator — because `avg_day * 7` compounds the daily truncation. The pgTAP fixture is chosen so the two give **different** answers (17219 against 17213), since a suite that only checked the remainder identity would pass against either.
    - **A signed month-over-month delta inverts for spending** and was removed from the wire. Withdrawals are stored negative, so spending more yields a more negative delta, which prints as a fall. The comparison works on magnitudes now.
    - **The largest movements are two lists, not one ranking.** A combined top ten ordered by absolute size is all deposits, because income moves in bigger lumps than spending does.
    - **Every `<td>` needs a `data-label`.** The phone stacked-table mode renders `attr(data-label)`; without it the tables became screens of unlabelled figures at 390px.
    - **The charts are inline SVG and the two series colours were validated**, not chosen — the app's own celadon and copper failed the dataviz chroma and normal-vision checks; `#5c8a1a` against `#9b2c2c` clears all five.

    **Follow-on work this created and did not close** — the state as of D-161; the first of the three has since shipped as task 48: **nothing in the app could set `include_in_reporting`**, so the filter was inert until a control shipped, which it did on 2026-08-27; automatic transfer detection is a matching rule and belongs beside task 25; and there is no account filter and no window picker, the surface being whole-ledger and all-time.

    **A run-rate projection (“at this pace you will spend X by month end”) was offered and declined by the owner on 2026-08-27**, on the grounds that it is a prediction rather than a fact. Recorded as a far-future maybe that may never be built, so nobody re-proposes it as an oversight.

    **`/code-review high` found six more before the commit, and one of them the audit could not have seen.** `.stats-section table { min-width: 560px }` outranked the phone block's `min-width: 0` reset and escaped the viewport — D-138 on a new surface — while the audit reported it clean, **because it measured against `document.documentElement.clientWidth`, which grows to contain the overflow**. A check that expands to fit the defect is not a check; against `window.innerWidth` it reproduced the fault at once. The other five: the balance line interpolated across gaps where the balance was constant and is now a step; the balance chart had no keyboard path and no table twin and now carries a `<desc>` of its opening, closing and extremes; the largest-movement lists named a *leg* amount `net`, which is false for the interest/tax pairing, and it is `amount` now; the monthly axis drew every label; and **two of the new tests were wrong** — a share case that asserted the opposite of its own name, and a docblock promising one decimal where the scale gives two.

    **Still open on this task: nothing.**

45. ~~**Page the ledger — migration 021.**~~ **Done 2026-08-27** (D-158). Built in full and validated against the local synthetic project only. What follows is kept because it records the reasoning the task was scoped on, and **two of its three predictions were wrong** — which is worth reading before trusting a scoping document again.

    **The measurement, which is what makes this real rather than tidy.** The ledger is past **1,604 rows** and `list_account_transactions` bounds nothing — no `limit`, no `offset`, one `jsonb_agg` of every row for the account. After D-155's width trim a row is about **584 bytes**, so a visit fetches roughly **940 KB**, on arrival, on a phone, and it grows with every import. **D-155 deliberately did not page it**, and the reason is the whole of this task's difficulty: three things on this page are derived over the *whole* ledger and a first page silently changes all three.

    **What breaks, each needing its own answer.**

    1. **The combined balance.** `combinedBalanceByTransaction` seeds each account from its *oldest* row — `post_balance − movement` — then walks forward accumulating. Handed the newest 100 rows it would seed from the wrong row and every combined figure on screen would be wrong. **The fix is small and does not need the rule to move**: the server returns, per account, the balance carried *into* the page, and the existing function seeds from that instead. The rule stays in TypeScript where its tests are.
    2. **The totals strip** — Rows, Deposits, Withdrawals, Net. These are whole-ledger aggregates and must be computed in SQL. **Safe against the exact-money invariant**: they are sums of `bigint` minor units, no division anywhere, which is the thing that makes this different from task 44.
    3. **Reconciliation, and this is the hard one** (D-063). `reconcileLedger` matches every slip and card against every transaction, before any filter, precisely so that choosing an account or typing in the search box cannot silently unmatch a pair. With a page, any provisional record whose transaction is off-page reads as *awaiting a statement* — a wrong answer about money, not a slow one.

        **The rule must not move to SQL.** It is ~85 tested cases across `tests/slip-reconcile.test.ts` and `tests/notification-card-reconcile.test.ts`, and re-implementing it in PL/pgSQL means maintaining one rule in two languages, one of which has no tests for it. That is the two-engines mistake D-120 already refused.

        **The shape that keeps it in TypeScript**: a second, narrow query returning only the transactions that *could* match a provisional record — same bank, exact amount to the satang, within a day. Slips and cards are few and the predicate is indexable, so this is a small result whatever the ledger's size. `matchCandidates` and `cardMatchCandidates` already want exactly this set for the manual chooser.

    4. **The filters follow the rows.** The account, status and text filters currently narrow a set the client already holds. Paged, each either moves server-side or silently means "…within this page", which is the worse half of every paging bug. **Status is the awkward one**, because it is derived from reconciliation rather than stored — so either the candidate query above is enough to compute it for the page, or status filtering has to be reconsidered. **This is the open question in the design, and it should be answered before any SQL is written**, the way D-155's payload question was.

    **What was actually true, added 2026-08-27 after building it** (D-158). Hazard **1 was aimed at the wrong half**: per account the rule is exact at any depth, because `post_balance − movement` is the balance immediately *before* whatever row it is applied to — so no carried balance from the server was needed. But the **merged** figure does break, and a review caught it: the walk sums rows older than a shallow account's window against that account's mid-history balance. The remedy is a floor below which the column shows nothing, not a figure. Hazards **2 and 3 were right**, and 3 is what the second function exists for. Hazard **4's open question resolved without SQL**: the statuses partition by population, so only `statement-only` pages at all.

    **Proposed surface**, for the owner to accept or change: `list_account_transactions_page(p_account_id, p_before, p_limit)` returning `{rows, carriedBalance, totals}`, plus `list_match_candidates(...)` for the reconciliation set. Two RPCs rather than one because they answer to different callers and different sizes. Migration 021 on every project including hosted; backup contract **unchanged at v7**, since no table gains a column.

    **A cheaper step exists and is worth taking first, with no migration at all**: `fingerprint` is 64 hex characters on every row and the ledger view **never reads it** — about **14%** of a row, verified the same way `import_batch_rows` was. That is a route-side deletion and a schema line, an afternoon rather than a week, and it does not foreclose anything here.

    **The recommendation is to take that first and then decide.** Roughly 800 KB on arrival is unpleasant and not dangerous, and this task is a migration, two RPCs, a route, a real change to the client's data flow, and new tests for all of it — worth doing deliberately rather than because the number is large. **It is also the natural moment to reconsider it**, because task 44 (statistics) would want whole-ledger aggregates in SQL too, and building those twice would be the waste.

## Later authorization gates

Only after every local task above passes:

1. ~~Ask for renewed permission to run local real-PDF smoke tests without logging or retaining values.~~ **Granted 2026-07-25 and discharged for all three layouts.** Twelve runs to date: ten for Krungthai (2026-07-25) and one each for SCB and KBANK (2026-07-27). The password is entered interactively and never logged; no value, screenshot, or derived fixture may be retained or committed. Requires the owner present, so it cannot run unattended. Purpose: the fixture geometry is invented (D-015), so this was the only check of parser-vs-reality — and it now says all three readers parse real documents.
2. ~~Ask separately before creating hosted Supabase, OAuth, Vercel, or deployment resources.~~ **Each was asked for and granted separately, and all four are now spent** — the Google OAuth client and the hosted Supabase project on 2026-08-11 (D-092, D-094), the Vercel project and its deployment on 2026-08-12 (D-096). **The rule is not discharged by having been used.** It governs the *next* hosted resource as much as it governed these, and none of these grants is standing: a further project, a custom domain, a second region or any new external service needs its own ask at the time.
3. ~~**Test portable recovery into an empty separately bound project before importing real data.**~~ **Done locally 2026-07-27** (D-044). `tests/recovery-portability.test.ts` restores a ledger into `private-ledger-recovery` — a second project bound to a different owner, which has never seen the source's auth users — over HTTP under real aal2 sessions on both sides, and asserts every table arrived intact, that the source owner survives nowhere including inside jsonb, and that a second recovery into the now-populated destination is refused. `lib/restore-plan.ts` makes that sequence buildable outside a test.

    **Superseded 2026-07-28.** The owner made the decision this gate deferred to: one real KBANK statement was imported (D-047), and `SPEC.md`'s "committed data remains invented only" line is corrected there. Two of the three absences this entry recorded are also closed — the app now has a restore surface a person can drive (D-046, task 14), and it was used for a real migration rather than a rehearsal (D-048). **Nothing hosted has been rehearsed**, and that remains the open one. The rehearsal still drives PostgREST directly, so the Next.js restore route is exercised by the browser suite rather than by `tests/recovery-portability.test.ts`.

## Working constraints

- **The migration is done and verified (2026-07-28).** The real ledger lives in `private-ledger-live` (5434x); `private-ledger-local` (5432x) was reset to migrations + seed and is purely synthetic again, which is what every suite targets. `.env.local` points at `http://127.0.0.1:54341`. Verified after the move: all eleven table counts match the backup (175 rows), and the account-id, transaction-id and fingerprint digests are byte-identical across both projects, so every row returned under its original id.
- **`.env.local` now aims the app at real financial records, and that leaks into any build.** `NEXT_PUBLIC_*` are inlined at build time, so a browser config that runs `pnpm build` without pinning its target builds against the live ledger. Only `playwright.owner.config.ts` pins; `playwright.isolated.config.ts` and `playwright.config.ts` do not. Pin `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `OWNER_GOOGLE_EMAIL` and `NEXT_PUBLIC_ALLOW_DEV_OWNER_SESSION` in the shell before running either. See GOTCHAS.
- `pnpm supabase:reset` still cannot be guarded and still destroys whatever project the working directory names — the repo root names `private-ledger-local`, `live/` names the real one. Back up through the `/recovery` route before anything destructive, and never silence `assertOnlyDisposableLedgerData` with `ALLOW_DESTRUCTIVE_TESTS=1` to get a green run.
- Do not inspect `private-statements/`. Real statements are readable only as the re-passworded copies in `shared-statements/`, produced by `scripts/repassword-pdfs.py` (D-049). Try `scripts/mask-statement.mjs` dumps first. Reading a real value never permits writing one into a fixture, a doc or a commit.
- Do not commit, push, deploy, or create hosted resources without explicit authorization.
- Do not request the Windows PostgreSQL password until a separately approved backup/recovery task actually needs it.
- Preserve unrelated and uncommitted files.

35. **The card reader calls Google Cloud Vision, behind this app's own route, with no fallback.** Decided by the owner and **built 2026-08-17** (D-120). Task complete.

    **What shipped.** `lib/notification-card-vision.ts` is the engine seam — request body, response mapping, transport, no key and no logging — and `app/api/v1/notification-cards/read/route.ts` is the only thing that holds the key. The card form posts the screenshot there and hands the words to the same `findCards` / `locateCardFields` / `prefillCardFields` chain as before. `lib/slip-ocr-engine.ts` is untouched: **slips still read on the device.**

    **Three things in the framing this task inherited were wrong, and each narrowed the decision** (all three in D-120, found by reading the code):

    1. **Deleting the local engine was never available** — it is shared with the slip amount finder, and Vision has never been measured on a slip.
    2. **"It works offline today" is nearly empty** — there is no offline app shell, so the hosted card form neither loads nor saves without a network.
    3. **The no-image-leaves-the-device promise is narrower than "the app"** — its two on-screen strings are on the statement path, and the tests holding it all name `lib/slip-ocr-engine.ts`, so they pass unchanged.

    **The 2× enlargement is gone, measured rather than assumed.** Through the **shipped** path at native size over the same 12 screenshots: **25 of 25 cards, 99 of 100 fields**, amount / balance / account digits 25/25 each. D-117's `cardReadingScale` was a tesseract remedy; at native size the coordinate spaces of the reading and the crops are identical by construction.

    **The key is set in Vercel and the project was redeployed, on the owner's report, 2026-08-17 — and no agent can verify that from here.** `GOOGLE_VISION_KEY` is server-only and never `NEXT_PUBLIC_`; without it the reader refuses with a message and the owner types the card, which is exactly the pre-2026-08-16 behaviour. **The reader is untested in production until a card has actually been captured there** — that single act distinguishes a working key from an absent one, and nothing else does.

    **The last refused field is diagnosed and now repaired** (D-121 diagnosed it, D-127 fixed it; the reader offers **112 of 112** fields with no refusals). `S__18636806_0.jpg` card 2: the label `วันที่ทำรายการ` normalises to a form holding **U+0E4D** (nikhahit) and Vision reads **U+0E48** (mai ek) there — one superscript mark misread as another that looks like it. The line matches 7 of the label's 15 characters and diverges on the 8th, so the row was read and the *label* is unfindable. It is D-117's slash problem in a different character class. **A repair is not obviously safe** — a tone mark carries meaning where a slash does not — and it would fire on 1 card in 25, which is thinner evidence than D-115's month-name reader had.

36. ~~**Should slip capture use Vision too?**~~ **Measured 2026-08-18 (D-128), adopted the same day (D-129). Complete.** Asked by the owner on 2026-08-17, answered *no for now* on an estimate, then **measured on his authorization — and the estimate was wrong about the size of the prize.** He chose adoption **in the same shape as the card reader, not the two-engine variant**, which D-120 had already refused for cards: two engines behind one grammar means every grammar change needs measuring twice and one side rots quietly.

    **The measurement**, over all 23 real slips at native size, through `locateAmount` and `proposeAmount` themselves: the amount is **located on 23 of 23** against tesseract's 16, and **parses as money on 23 of 23**, with no read failures. The seven slips that gave the owner nothing at all now all resolve, which was the actual pain. **It was not re-run for the adoption** — repeating it would mean sending 23 real slips to a third party a second time for a number already in hand.

    **What shipped, in four parts.** The reader route moved to `/api/v1/ocr/read` and names no record type, with the encoding and the POST in `lib/browser/ocr-reader.ts` so both capture forms make one call rather than two copies of it. `app/slip-capture.tsx`'s **Enlarge the amount** became **Read the amount**: it fills the box from `proposeAmount` and keeps the crop as the owner's check on the figure. The local engine is deleted — `lib/slip-ocr-engine.ts`, `scripts/copy-tesseract-assets.mjs` and its `prebuild` step, `public/tesseract/`, the `tesseract.js` dependency, three privacy tests and the browser spec that was **the only check anywhere of the strict CSP against a real OCR engine in the page**. That spec is replaced by one driving the same-origin POST that now happens, and D-129 says in as many words that re-adding a bundled engine means re-earning the old proof rather than inheriting this one. And two documents that had become false — `SPEC.md` line 11 and task 21 above — are corrected.

    **Not measured, and unchanged by adoption**: whether a proposed figure is the *right* figure. No agent can produce that by scoring its own OCR against its own OCR (D-112). The independent check is the statement, exactly as for cards — a wrong amount fails to pair and surfaces as unmatched (D-063, D-102). **Slip identity stays the QR's** and is never machine-read.

    **Every slip image now leaves the device when the read button is pressed**, where before none did, and both the slip form and the slips page say so on screen. **Twenty-three real slips had already been sent once** under an explicit authorization; that is a wider boundary than the cards and **does not generalise to statements**, which still read entirely on the device — and which are now the only path in this app that does (D-128, D-129).

37. **Migration 020 — `array_length` of an empty array is NULL, so migration 019 refused `[]`.** Found in production 2026-08-17 (D-122), **applied everywhere on 2026-08-18 including the hosted project** (D-126). Complete.

    **The fix is `coalesce(array_length(v_names, 1), 0)`** in `private.assert_prefill_field_names`. No table, no column, no signature change — the backup contract stays at **v7**.

    **Red-proved before it was fixed**: the new pgTAP test died with the exact production error on the pre-020 schema and passes after. pgTAP **263 → 266 across 8**. Both spellings of "nothing" are now asserted — an absent key and an explicit `[]` — because only the first was ever exercised and the second is where the defect lived.

    **The route test flipped from 422 to 201** rather than being deleted, which is what it was written for.

    **The owner's backup was verified from the database before the push and is still current after it**: sequence **33**, last-exported **33**, equal both times, because DDL mutates no owner data. Verified on the remote by *running* the function there, not by reading the migration text. All twenty migrations match local and remote. The hosted ledger holds **7 cards**.

38. **Capture flow: the next card, the direction, and a result you can see.** Asked for by the owner after his first three real cards, built 2026-08-17 (D-123). Complete.

    **The next card is selected automatically after a capture** and re-cropped from the held image. A screenshot carries two cards more often than one (D-100), so re-picking from the chooser was the second half of every capture. `selectCard` is now the single path both the chooser and the advance go through.

    **The direction is filled from the printed sign, never from the direction word.** Filling it from the word would hand `readDirection` back the signal it already holds and the cross-check would agree with itself on every card while still looking like a check (D-099). Measured over 28 real cards: **26 print both a word and a sign and all 26 agree**; the two that do not are KBank Live incoming, where the control stays blank and the owner sets it.

    **The result is a coloured banner** — green captured, amber already held, red failed — with the meaning in the words as well as the colour. A banner rather than a modal dialog: a dialog needs a focus trap, Escape handling, focus restoration and `aria-modal`, each a way to fail the axe pass this route holds.

    **One thing this does not do**: the direction is absent from the pre-fill audit lists, because migration 019's closed set is the four digit-bearing fields and widening it is a migration. The trial's offered and changed rates therefore describe those four and say nothing about the direction.

    **Amended 2026-08-17 after the owner used it** (D-124): the result moved from under the submit button to the **top of the form**, above the channel and the screenshot, and the page scrolls there. Under the button it was correct and nearly useless — the next card was already loaded in the fields *above* the message, off-screen behind it.

39. **Bulk slip upload — many slips at once, with no second look at each.** Asked for by the owner; **built 2026-08-21** (D-135). `app/slip-batch.tsx` and `lib/slip-batch.ts`, mounted under the single-slip form on `/slips`. **No SQL, no new route**: it reuses `POST /api/v1/ocr/read` and `POST /api/v1/slips`, so the build still emits eighteen `/api/v1/` routes.

    **The rule it turns on: a slip is filed unseen only when its date is exact.** The amount already had a safe path — `proposeAmount`'s strict grammar, no lenient fallback, and a wrong-but-parsing figure fails to pair and surfaces as unmatched, which is the independent check. **The date did not.** The single form falls back to today, which is right for a payment just made and fatal for a backlog: `MATCH_WINDOW_DAYS` is 1, so a slip from last month dated today can never pair with anything, ever. So this form has no clock in it at all — the date comes from the QR reference (D-059) or from the printed slip, or the slip goes to a review list.

    **What made that affordable was already in the repository and called by nothing.** `readPrintedDate` shipped on 2026-08-10 with 40 tests (D-086) and had no caller in `app/` or `lib/`. The batch already sends every image to Vision for the amount, so the same words yield the date at no extra request. **That is why the un-dateable residue is KBANK alone** rather than every layout whose reference carries no date — and it is why the shape the handoff proposed was scoped around a limitation that had already been removed. `GOTCHAS.md` now carries that as its own trap: shipped, tested, uncalled code is invisible to both greps anyone actually runs.

    **Direction is asked once per batch and is never read from the image.** The handoff proposed measuring it across the 23 real samples before defaulting it. **That measurement was declined as unanswerable, not skipped for cost**: a slip prints who paid whom, and which side is the owner is neither on the image nor in this app. A batch holding both directions is two batches, and the form says so. The magnitude is what gets classified; the sign is applied at submit, so changing the control re-signs every row without re-reading an image.

    **Also refused rather than picked**: when the QR's date and the printed date disagree, neither is used. Nothing available can say which is right and a wrong date never heals.

    **Bounds, and why each is there.** Fifty files per batch, because every slip is a paid third-party read and a mistaken drop of a camera roll should cost a sentence rather than a bill. One read and one write at a time, because an unbounded burst at a metered service and a queue of captures contending for one owner's advisory lock are both worse than a slower pass. Counterparty and category are not captured in bulk — neither is readable from a slip, and both are correctable afterwards (migration 013).

    **What is not built**: a per-row direction override, and any way to re-read a slip whose QR would not decode without going back to the single form. Both were considered and left out; a slip with no readable QR has no identity, and identity is the one thing that cannot be typed.

    **Reviewed 2026-08-23 and eight defects fixed** (D-142), discharging the review debt D-125 records against this commit. Four were one defect in four costumes — **a failure path that discarded information on a form whose failures are billed**: a transient reader 503 left rows the read button would never process again, a failed capture POST was excluded from capture forever, an unparsable 201 body reported an already-written slip as refused, and the reader being unreachable threw away the QR's own date even though `slipDateFromReference` never touches a recognised word. Every recovery path this form offered was "discard and re-read all fifty", at a paid read each. It now pre-fills whatever resolved, re-reads failed rows, re-sends refused ones (safe because `capture_slip` writes nothing for a slip already in the ledger), and claims its busy phase **before** awaiting the 1.1 MB WASM detector rather than after — that window let a second press send every image to the metered reader twice.

48. **`include_in_reporting` needs a control, and the real ledger has already shown why.** **Asked for by the owner 2026-08-27, approved, BUILT, committed, pushed and verified in the running app the same day** (D-165). The column has existed since migration 001 and both statistics and the ledger's totals strip have read it since migration 023 (D-161); until this task **nothing in the app could set it**, so the filter was inert and every figure stood as if it were absent. **It is no longer hypothetical**: the deployed page shows a `Transfer Withdrawal` and a `Transfer in` of the same amount on the same date in the two largest-movement lists, one internal transfer inflating money-in and money-out alike while net stays correct.

    **The hazard that shapes it, and it is a data-loss one.** `PUT /api/v1/transactions/[id]/overlay` takes the **whole** overlay and its schema is `.strict()`: description, counterparty, effective date, category, note and the flag, plus `expectedRevision`. A control that sent only the flag would be rejected; **one that sent the rest as null would erase whatever the owner had typed**, silently, on a row he was only trying to mark. The control must round-trip the overlay it already has, and a test must prove a toggle preserves a populated overlay rather than only that the flag changed.

    **Open**: whether the control lives on the row or in its detail panel — marking transfers is a scanning task, which argues for the row, while the ledger table is already at a 1160px minimum width, which argues against another column.

    **Answered — the row, and specifically the Status cell.** A column was refused on the width argument above. **A detail panel was refused because on these rows there is none**: `pair-detail` renders only where a slip or a card matched, and the internal transfers this exists for are statement-only — so "put it in the panel" meant building a second panel *and* charging two presses for a scanning task. The Status cell is a single em dash on exactly those rows, so the control costs no width: measured **148px of content in a 148px cell in all four typefaces**, included and excluded, and the check was proved able to fail (457px and 523px under a deliberate break). It is hidden while a slip or card is being picked, because that mode is deliberately one question at a time.

    **Verified on the real deployment 2026-08-27, and it corrected a number this plan had wrong.** Excluding a genuine internal transfer moved money-out and net by exactly the row's amount and left deposits, the row count and every balance column alone; `/statistics` agreed to the satang and printed *1 row excluded from reporting*; the round trip returned every figure to its exact starting value, with the database holding revision 2, the flag restored, every other overlay field still null, and two entries each in `overlay_revisions` and `audit_events`. It also held **under an active text filter**, which is the branch `/code-review` had caught double-counting the same afternoon.

    **The excludable set is about eighteen rows, not the 248 that carry a transfer label.** A label match was mistaken for an internal transfer: of the transfer rows on screen, most name **other people** as the destination and are ordinary spending. Only the ones whose counterparty is one of the owner's own three accounts are internal, and filtering on his own name returns **18**. **This matters because it was briefly used to argue that automatic transfer detection was urgent** — at eighteen rows it is a convenience, not a necessity, and the argument was built on a count nobody had checked the direction of. Detection stays where D-161 put it, beside task 25.

    **The explanation folded, one change later.** *"Not counted as income or spending…"* first rendered on **every** excluded row — standing copy repeated per row, which is exactly what D-156 says to fold, written by the same change that had just applied that rule to the typeface note. On the real ledger it roughly doubled a row's height. The chip is the state now; what the state means is said once behind the Transactions `(i)`, and the Include button's accessible name carries it for a screen reader.

    **Closed on 2026-08-29, by measurement** (D-168): both surfaces read **1604** and agree to the satang, and no *N rows excluded* line renders at all because nothing is excluded. The 1603 was a correct reading taken *during* the exclude/include round trip and was carried forward as though it were standing. The mechanism described here is right; the discrepancy was never a live one. **Still owed**: neither page has been seen on a real phone. `/statistics` has now been measured at 390px under iPhone 13 emulation for the first time and reads correctly as cards, but D-138 remains the standing proof that a measured width is not a phone.

49. **Every typeface should occupy the same space, and the honest version of that is that nothing reflows.** **Asked for by the owner 2026-08-27, approved, and BUILT the same day** (D-166), committed, pushed and deployed the same day in `17a93ca`. He asked that switching the face not move the page, and offered Pixelify Sans as the reference to pin the others to.

    **Identical positions are not reachable and chasing them would waste the effort.** `size-adjust` — applied to all three pixel faces in D-157 — normalises apparent size by pinning cap height; **it does not touch advance widths**, and `app/globals.css` already says so in as many words. Press Start 2P is far wider per character than IBM Plex Sans Thai, so text wraps differently whatever descriptor is set.

    **What is reachable is most of what "the page moves" means.** Pin the vertical box with `ascent-override`, `descent-override` and `line-gap-override` so line heights stop drifting between faces, and give the containers that matter — table rows, stat tiles, the header — fixed heights so reflow inside a box cannot move anything outside it. **State the goal as "nothing reflows", not "positions are identical."**

    **Measure before building**, on D-066 and D-128's precedent: `.runtime/font-metrics.spec.ts` already exists and is the harness. Report the per-face deltas for all four faces first, then decide which are worth pinning. **One part is already done**: figures no longer render in a pixel face at all (D-163), so the numeric columns — the widest offenders — no longer change width with the typeface.

    **Measured, and the remedy above was not built — see D-166.** The three overrides would have pinned nothing. Font boxes per 100px are IBM Plex **165**, Press Start 2P **70** with zero descent, Pixelify Sans **133**, Silkscreen **142** — a spread that should reflow everything — and **nothing reflowed**, because every line height in this stylesheet is an explicit ratio and a font's own metrics never reach layout. Three of the four faces were identical to the pixel on both routes.

    **The one box that moved was the header, and the cause was width.** `.header-side` measured 71px in IBM Plex against 88px in Press Start 2P, pushing every landmark below it down 16-17px. Text capped by *width* occupies a face-dependent number of lines — the picker's standing note, now folded behind the `(i)` on D-156's rule, and the phone's privacy chip at its 130px cap. The header is **148px in every face** now, 23px shorter than before. `tests/e2e/font-picker.spec.ts` asserts every shell-header box keeps its height in every face on both projects; the document's own height is deliberately not asserted, because body prose re-wrapping is the reachable boundary rather than a defect.

46. **The statistics filters — a window picker and an account filter.** **MIGRATION 024 IS WRITTEN AND THE SECOND HALF IS STILL UNBUILT ABOVE IT, 2026-08-29** (D-174). The SQL exists — `ledger_statistics` takes `p_account_id`, and the balance series has **two sources**: the derived combined position for all accounts, that account's own **printed** `post_balance_minor` for one, because per account the bank has already done the arithmetic and its figure is authoritative (022). Applied to `private-ledger-local` only; pgTAP `012` proves the partition reconciles and that the two sources genuinely differ. **It needs a backup verified from the database and the owner's own `db push` before the control can be built**, so no UI exists yet and that is deliberate — the database goes first. The same migration gives `list_account_transactions_page` the date bounds the ledger date filter needs (see task 47). **Named as follow-on by D-161 and confirmed wanted by the owner on 2026-08-27. Nothing built.** `public.ledger_statistics` already takes `p_from` and `p_to` and `app/api/v1/statistics/route.ts` already parses them, so **the window picker is a control and its wiring and nothing else** — and it stays honest by construction, because the page already prints the window and the day count above every figure derived from them. Do that one first; it is most of the value for a fraction of the work. **One thing measured on 2026-08-29 belongs to this task**: under iPhone 13 emulation with a single month of data, a bar in the money-in-against-money-out chart overflowed the viewport by 3px and clipped. On the real fourteen-month ledger the bars are narrow and it does not occur — but a window picker exists precisely to let someone select one month, so this task creates the data shape that shows the defect. Fix the bar sizing with the picker, not after it. **AUTHORIZED BY THE OWNER 2026-08-29, both halves; THE WINDOW PICKER IS BUILT, COMMITTED, PUSHED AND DEPLOYED the same day** (D-170, `0b88ea2`) — presets plus a Custom tick, no SQL; the account filter is what remains, and two of its three open questions answered with him. **The window picker offers presets plus a Custom toggle** — a tick that reveals free date entry — rather than one or the other, so the common questions are one press and an exact range is still reachable. **The account filter narrows every figure to one account and the balance chart follows it**, which was his own reading of what the filter is for: it is the specific account's balance, drawn from post_balance_minor, and a chart that stayed combined while every figure beside it narrowed would be the only thing on the page still answering a different question. Sequencing note, not a decision: the picker needs no SQL and can ship alone, while the filter needs migration 024 for the two-source balance, and therefore a verified backup and the owner running db push himself (D-152). **Migration 024 reached hosted 2026-08-30 (D-176) and the account filter's control is built, reviewed and committed as of 2026-08-31 (D-177)** — a `<select>` beside the window picker, shared with `/ledger` as `app/account-select.tsx` after `/code-review high` found the first draft was a near-copy of the ledger's own control, an "Unknown account" fallback for an id the owner does not hold (also review-found: the first draft could show "All accounts" selected while the page was genuinely narrowed), and one sentence stating which of the two balance sources the chart is currently drawn from. `tsc`, `eslint`, `check:docs --strict`, `pnpm build` and Vitest (892/7/41) are clean against the isolated commit; confirmed in a real browser against `next build && next start` with a dev-signed-in owner, on both `/statistics` and `/ledger` — the select populates from the three seeded accounts, choosing one rewrites the address bar and the outgoing statistics request to carry `account=<uuid>`, no sideways pan at 375px, and `/ledger`'s own filtering still fires the same per-account requests it did before the extraction. **Committed as `4f51a7e`, pushed and deployed** — confirmed live against the real hosted ledger: the select listed the owner's three real accounts and narrowing to one correctly changed every figure and the balance-source sentence. **Task 46 is done.**

    **The account filter is the one with a real design question in it, and the question is the balance chart.** Every other figure on the surface narrows by `account_id` with one predicate. The balance line does not: `private.combined_balances` is the *combined* position across every account **by construction** (D-159), and there is no sensible way to filter it — an account's share of a combined running total is not a quantity.

    **What makes it tractable is that the per-account answer is the easier one, not the harder one.** A single account's balance at a row is `post_balance_minor`, printed on the statement, already exact and already stored — no derivation at all. So the filter selects between **two** balance sources rather than filtering one: `private.combined_balances` when the filter is *all accounts*, and `distinct on (source_date) post_balance_minor` for that account otherwise. That is a small function and it is worth stating plainly in the migration, because the instinct will be to filter the combined helper and the result would be a number that no statement agrees with.

    **The averages need their denominators re-read, not re-used.** A window change moves every divisor, which is the point. An *account* change does not move the day count — the window is the same window — so per-day and per-week figures for one account are that account's rate over the same period, which is correct and is what the label should say.

47. **The day view — a calendar, and what a single day actually held.** **THE OWNER CHOSE THE LEDGER'S DATE FILTER FIRST, 2026-08-29**, and said he would then see whether the calendar heatmap was still wanted here. That was the recommendation and the reason stands: the calendar is new, the row list is not, and a day should open `/ledger` filtered — which makes the ledger's missing date filter the real unit of work. **Migration 024 carries the SQL half**: `list_account_transactions_page` now takes `p_from`/`p_to` **bounds**, which are not the `p_before_*` **cursor** it already had — the cursor walks inside the bounds, so a window narrower than a page still pages (D-174). **The plain date filter is built, reviewed and locally verified, 2026-08-31** (D-178) — see the checkpoint above for the gate and browser evidence. **Unlike every other ledger control it cannot filter client-side**: the fetch itself has to be bounded, so the two date inputs take effect only on the next Reload rather than filtering live, and `loadMore` walks its cursor under a separately-tracked `appliedRange` rather than whatever is currently typed, so an edited-but-unreloaded date field cannot reach a deeper page ahead of Reload. `totals.rows` needed no change in `lib/ledger-window.ts` to become bounded — it already read the RPC's own totals as a whole-scope fact rather than deriving them. **The calendar heatmap remains unauthorized, unscoped and unbuilt** beyond the paragraph below — this task closes only the filter. Pick a date and see that day's transactions with its total in and total out. Every field it needs exists today: `source_date` is on every confirmed row, the totals are sums of `bigint` minor units, and **nothing here divides** — so it is inside the exact-money rule without an argument (D-160).

    **The idea is worth more than a date picker, and the upgrade is nearly free.** A calendar is a grid of days, and a grid of days with a figure attached is a heatmap — so the month view can carry each day's spending as a **sequential ramp, one hue, light to dark**, and the owner sees which days were expensive *before* choosing one to open. That converts a lookup into a finding. It is also the one place on this surface where a sequential scale is the right encoding rather than a categorical one.

    **The trap that decides the shape: a day's rows are not `source_transactions` filtered by date.** The ledger reconciles slips and cards against statement rows and renders a matched pair as **one** row, the statement's (D-063) — which is what makes its totals count each payment exactly once. A day view that re-listed `source_transactions` would quietly disagree with the ledger about the same day, and would show a paired slip as a second transaction. **So the day's row list must go through the same reconciliation the ledger uses**, or it must link to `/ledger` filtered to that date and let the existing surface answer. Building a second row renderer is the waste D-158 named and the two-answers mistake D-120 refused.

    **The recommendation is therefore: the calendar is new, the row list is not.** Put the heatmap on the statistics surface, and let a day open the ledger already filtered to it. That gives the finding without duplicating the rule, and it makes the ledger's date filter — which does not exist yet — the actual unit of work.

    **Open, and none of it blocks a start**: whether the heatmap colours spending, income or net; whether `include_in_reporting` applies to the cell colour as it does to every other total here (it should, for the same reason); and whether an empty day is drawn as an empty cell or as a zero-value step of the ramp — they look different and mean the same thing, and the first is the honest one.

    **Adjacent and cheap once the day grouping exists**: the biggest single day in the window, and a count of **no-spend days**, which is the one figure on this whole surface that a person acts on directly.

40. **Bulk statement import — many PDFs unlocked in one pass, bound and confirmed one at a time.** Asked for by the owner on 2026-08-23 as the first step of a larger auto-import idea; **built the same day** (D-141). `app/statement-batch.tsx` and `lib/statement-batch.ts`, mounted under the single import on `/import`. **No SQL, no new route, and no new network call of any kind** — the build still emits eighteen `/api/v1/` routes, and the only request is the same `POST /api/v1/imports/confirm` the single import already made, once per statement.

    **The split is at the authentication boundary, and that is the whole design.** Every write route needs `aal2` plus a verified TOTP factor (`lib/server/supabase.ts`, `REQUIRED_FACTORS = 1` since migration 015), and there is no service-role escape — `SUPABASE_SERVICE_ROLE_KEY` is deliberately absent from the deployment. So the tedious half is automated and the accountable half is not: many PDFs are unlocked and read in one pass, then each is bound and confirmed by hand. Nothing holds a credential that can write to the ledger unattended, because nothing needs to.

    **Confirming stays per statement for a reason that is not caution.** `assembleImportPayload` returns reconciliation warnings *alongside a valid payload*, and the sharpest of them says rows were **reordered** to make the balance chain close (D-055). Re-reconciling the finished payload cannot reproduce that — its rows are already in applied order — so a single button confirming a whole batch would file every one of those unseen. That is the one failure mode that writes to an append-only ledger while hiding what it did, and there is no un-importing a row.

    **Binding is still never inferred.** A batch makes D-017 far more tempting to break than a single import does: a statement prints a bank code and four digits, `public.accounts` is unique on `(owner_id, bank_code, last_four)`, so a lookup would resolve unambiguously every time and would save a choice per statement. It is still the ledger's routing decision. The policy module takes no account list at all, and `tests/privacy.test.ts` now asserts that structurally over both new files.

    **What a batch can see that a single import cannot**, which is the only genuinely new policy here: the same file picked twice (blocked on its artifact digest), a statement whose printed totals never confirmed its rows (blocked before the owner works down a list choosing accounts for it), and **two chosen statements whose periods intersect for one account** — a warning and never a refusal, because the exact guard is `unique (owner_id, account_id, fingerprint)` and an overlap only *predicts* that collision. Without it the collision surfaces at confirm as a unique violation the route flattens into a generic "could not be confirmed atomically", which reads identically to a real database fault. Ready statements are ordered oldest-first within an account so the earlier period is confirmed first.

    **Bounds, and why they differ from task 39's.** Forty files, and the reason is *not* the reason bulk slips cap at fifty: a slip is a billed Vision call, a statement is parsed entirely on this device, so nothing here is metered and the cap bounds memory and wall time only. One file at a time through a fresh worker pair, matching the path proven against real documents. One password for the pass, cleared the moment it ends, with a retry for the files that refused it — a bank can change its scheme between periods, and the masking harness has re-asked per unopened file since it was written.

    **Statement import remains the only path in this app that reads entirely on the device** (D-128, D-129), and opening many at once is exactly where that would erode quietly, so it is asserted rather than intended: neither new file constructs a request of any kind.

    **The mailbox half is built too, as a local fetcher rather than the hosted button this task proposed** (D-144). `scripts/fetch-statements.mjs` pulls statement PDFs over IMAP into `private-statements/inbox/` and the owner imports them with the form above; the IMAP work sits in `lib/server/statement-mailbox.ts` so a hosted Sync button stays a second *caller* rather than a second implementation. **The design got cheaper once this queue existed**: a local script needs no route, no server-side credential, no CSP question and no security review, and it is strictly more private, because the app password never leaves the machine and the PDFs never touch a server. **Binding is now automatic where the printed bank and last four resolve to exactly one account**, superseding D-017 on the owner's decision — the review table and the confirmation are untouched, which is what D-055 needs. Run against the real mailbox: 3 messages, 5 PDFs, four statements imported.

50. ~~**Three synthetic accounts are offered in the production account picker.**~~ **DONE 2026-08-30** (D-175). Deleted by the owner after a backup verified from the database at sequence 39/39; the picker now offers three and the header reads `IMPORTED ACCOUNTS: 3 OF 3`. **The explanation this task was investigated under was wrong**: not a `seed.sql` leak — hosted never held the synthetic owner. The accounts carried the seed's primary keys but were owned by the real owner and predated the real accounts, which is what `restore_backup` produces (D-013). Historical, not recurring. What follows is the finding as first recorded.

    **Found on 2026-08-29 during the first browser reading of the deployed `/ledger`. Nothing built, nothing decided.** The picker lists **six** accounts and the header reads `IMPORTED ACCOUNTS: 3 OF 6`: the three real ones, plus `Synthetic current account ···· 4242`, `Synthetic SCB savings account ···· 4242` and `Synthetic KBANK savings account ···· 4242`. All three hold **zero** transactions on hosted, so no figure moves and no total is wrong — the ledger's 1,604 rows across 3 accounts is unaffected.

    **What is wrong is that a real filter offers choices that cannot ever match anything**, on the owner's own ledger, and selecting one shows an empty ledger that looks like a fault. They are almost certainly the seed's accounts, created when hosted was first stood up and never removed. No continuity document has ever mentioned them, which is why this is written down rather than fixed in passing.

    **The decision is the owner's and it is not obviously "delete".** Removing rows from hosted is a destructive operation on the real project and needs its own authorization and a verified backup (D-152); hiding accounts with no rows is a product change that would also hide a *real* account that happens to be empty, which is worse. A third option is to leave them and say so. **Nothing here is urgent** — it costs a confusing menu entry, not a wrong number.

51. ~~**Whether the phone audit stops being a throwaway.**~~ **DECIDED BY THE OWNER, DONE AND DEPLOYED 2026-08-29** (D-173, `676a8ea`). It is now `tests/e2e/owner-phone-audit.spec.ts`, collected by `playwright.owner.config.ts`, and it **asserts** the 44px standard and no sideways pan rather than printing a list. Three things changed in the port: it seeds **120 rows** so `Load older rows` renders — the control D-168 could only see on the real ledger; it measures **every route twice**, disclosure shut and open, plus `/statistics` a third time with the Custom tick on, because both are surfaces that exist only after an action (D-139); and **all three Playwright configs now key off an `owner-` prefix** instead of a list of filenames. **Its first committed run found `/ledger` panning sideways at 390px** — the account filter `<select>` at 404px in a 390px viewport, from a rule older than the audit that had been missing it. What follows is the scoping this was decided from.

    `.runtime/mobile-audit.spec.ts` is the only instrument in this repository that measures tap targets and horizontal overflow at 390px, and it is gitignored — so it dies with this machine, has no history, and is invisible to `/code-review`.

    **The argument for committing it is what D-168 found**: it was silently broken by D-157 for three days, across exactly the window in which new undersized controls shipped, and nothing could have noticed because a throwaway only fails when a person asks it to run. A committed spec in `playwright.isolated.config.ts`'s `mobile` project would fail in the gate on the day a header change hides its sign-in.

    **The argument against is that it reports rather than asserts**, deliberately (PLAN task 28 is unscoped), and a spec that cannot fail is not a gate — it would need a threshold nobody has agreed to. **The cheap middle is to commit it asserting only what is already true and stable**: no route pans sideways at 390px, and every control clears 44px. Both hold today, both are red-provable, and neither requires agreeing what the page should look like. It would be the first `.runtime/` spec to become permanent, which is why it is a decision rather than a chore.
